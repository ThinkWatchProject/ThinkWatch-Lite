{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的能力。**只给网页真的会调的那几样**：托盘、通知、开机启动都由 Rust 这一侧去做，网页不需要直接碰这些插件；应用自己的命令不受这份清单约束。Tauri v2 的 capability 漏配是静默失败 —— 网页里新用一个 window/插件接口，就要回来确认这里有它。start-dragging 是同一个坑的实例：标题栏是 Overlay，没有一条真的标题栏可以抓，窗口全靠 data-tauri-drag-region 移动，而那条权限不在 core:window 的默认集合里。少了它拖拽区毫无反应，也不报错。",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
